# 4b.8 — PUBLISHER ADDITIONS — QA Proof Review

**Mode:** filesystem-MD
**Verdict:** **PASS**
**Reviewer:** go-qa-proof-agent (opus)
**Date:** 2026-05-04
**Round:** 1

---

## 1. Per-Acceptance Verdict Table

| # | Acceptance Item | Verdict | Evidence (file:line) |
| - | --------------- | ------- | -------------------- |
| 1 | `RestoreActionItem` publishes after repo write | **PROVEN** | `internal/app/service.go:1163` (after `s.enqueueActionItemEmbedding` + after `s.repo.UpdateActionItem` at line 1157) |
| 2 | `RenameActionItem` publishes after repo write | **PROVEN** | `internal/app/service.go:1197` (after `s.enqueueThreadContextEmbedding` + after `s.repo.UpdateActionItem` at line 1184) |
| 3a | `DeleteActionItem(DeleteModeArchive)` publishes | **PROVEN** | `internal/app/service.go:1414` (after `s.repo.UpdateActionItem` at line 1411 inside the `case DeleteModeArchive:` branch) |
| 3b | `DeleteActionItem(DeleteModeHard)` publishes | **PROVEN** | `internal/app/service.go:1461` (after `s.repo.DeleteActionItem` at line 1428 + embedding cleanup, inside `case DeleteModeHard:` branch) |
| 4 | `ReparentActionItem` publishes after repo write | **PROVEN** | `internal/app/service.go:1739` (after `s.repo.UpdateActionItem` at line 1736) |
| 5 | `ImportSnapshot` SINGLE bulk publish per project_id | **PROVEN** | `internal/app/snapshot.go:357-372` — dedup `map[string]struct{}` over `snap.Projects[*].ID` AND `snap.ActionItems[*].ProjectID`; fires `s.publishActionItemChanged(projectID)` ONCE per distinct id at line 371. Located AFTER all error-returning calls (last error-returner is `importSnapshotHandoffs` at line 345-347), so partial-write failures short-circuit before the publish loop. |
| 6 | 5 named tests + 1 ImportSnapshot regression | **PROVEN** | All 6 tests located, named, and verified-green under `-race`. See §3 below. |
| 7 | R1 + R2 closed | **PROVEN** | R1: 5 publish call-sites resolve walker-droplet route. R2: snapshot bulk publish resolves daemon-mode route. Both refinements mechanically resolved by this droplet. |

---

## 2. Per-Surface Code Evidence

### 2.1 `Service.RestoreActionItem` — `service.go:1130-1165`

Publish at line 1163 fires AFTER:
- Capability guard at line 1141.
- `s.repo.UpdateActionItem(ctx, actionItem)` at line 1157 (the canonical "successful repo write" anchor).
- `s.enqueueActionItemEmbedding(...)` at line 1160 (post-write embedding hygiene).

Order matches the canonical pattern at `MoveActionItem` (line 1125 publishes after `s.repo.UpdateActionItem` at line 1106). **PROVEN**.

### 2.2 `Service.RenameActionItem` — `service.go:1167-1199`

Publish at line 1197 fires AFTER:
- Capability guard at line 1177.
- `s.repo.UpdateActionItem(ctx, actionItem)` at line 1184.
- Embedding enqueue + thread-context embedding at lines 1187, 1190 (the latter wraps `ErrNotFound` non-fatally, so the publish runs even when no thread-context-subject pre-exists).

**PROVEN**.

### 2.3 `Service.DeleteActionItem(DeleteModeArchive)` — `service.go:1397-1415`

Publish at line 1414 fires AFTER:
- Capability guard at line 1406.
- `actionItem.Archive(s.clock())` at line 1409 (domain mutation).
- `s.repo.UpdateActionItem(ctx, actionItem)` at line 1411 (the archive write).

Worklog §3 correctly identifies that `ArchiveActionItem` does NOT exist as a standalone method — archiving is a `DeleteMode` branch. PLAN row 4b.8 listed `ArchiveActionItem` but the spec language explicitly admits "plus any other write paths missing publish," and the worklog rationale is consistent with PLAN row 7's accepted shape. **PROVEN**.

### 2.4 `Service.DeleteActionItem(DeleteModeHard)` — `service.go:1416-1462`

Publish at line 1461 fires AFTER:
- Capability guard at line 1425.
- `s.repo.DeleteActionItem(ctx, actionItemID)` at line 1428.
- `s.searchIndex.DeleteEmbeddingDocument` cleanup loop at lines 1431-1445 (work-item + thread-context embedding documents).
- `s.embeddingLifecycle.DeleteEmbeddingSubject` cleanup loop at lines 1446-1460.

Critically, the publish is placed AFTER the embedding cleanup, NOT inline between the repo delete and the cleanup. This is correct because all five cleanup steps are error-returning paths — placing the publish at the very end means a partial-cleanup failure short-circuits before publish, matching the "publish only on full success" pattern. **PROVEN**.

### 2.5 `Service.ReparentActionItem` — `service.go:1690-1741`

Publish at line 1739 fires AFTER:
- Capability guard on the action item (line 1700) AND the new parent (line 1718).
- Cycle-prevention check at line 1725 via `wouldCreateParentCycle`.
- Kind-validation re-check on the post-reparent shape at line 1729.
- `actionItem.Reparent(parentID, ...)` domain mutation at line 1732.
- `s.repo.UpdateActionItem(ctx, actionItem)` at line 1736.

**PROVEN**.

### 2.6 `Service.ImportSnapshot` — `snapshot.go:276-375`

Publish loop at lines 370-372 fires AFTER all error-returning import phases:

```
Phase 1: upsertProject loop      (282-286)  — error-returning
Phase 2: upsertKindDefinition    (287-291)  — error-returning
Phase 3: SetProjectAllowedKinds  (292-296)  — error-returning
Phase 4: column upsert loop      (311-323)  — error-returning
Phase 5: action-item upsert loop (325-338)  — error-returning
Phase 6: importSnapshotComments  (339-341)  — error-returning
Phase 7: importSnapshotCapabilityLeases (342-344) — error-returning
Phase 8: importSnapshotHandoffs  (345-347)  — error-returning
[Publish loop here — 357-372 — only reached on full success]
```

**Dedup correctness:** the `projectIDs` set is built from BOTH `snap.Projects[*].ID` AND `snap.ActionItems[*].ProjectID`. Builder worklog §4 calls out the two coverage cases:
- Imports touching only existing projects (no new project rows but some action items): action-items branch covers it.
- Cold-bootstrap snapshots with new projects but no action items: projects branch covers it.

Both branches use `strings.TrimSpace` defensive guard before insertion (lines 359, 365), matching the inner guard in `publishActionItemChanged` itself at `coordination_live_wait.go:69-72`. Whitespace-only IDs are silently skipped — defensive but harmless.

**N-amplification regression gate:** with 2 projects × 2 action items, per-item publish would fire 4 events; the bulk loop fires exactly 2 (one per distinct project). The test pins `Sequence == 1` per project (§3.6 below) — Sequence is keyed per `(eventType, key)` per `live_wait.go:151-160`'s `nextSequenceLocked`, so a per-item bug for 2 items per project would land at Sequence==2. The pin at 1 is the correct regression gate.

**PROVEN**.

---

## 3. Per-Test Verification

All 6 tests located, named-correctly, and run **green under `-race`** via `mage test-func ./internal/app <name>`. Not name-only assertions — each test has real behavior coverage:

| Test | File:Line | Asserts | Run Status |
| ---- | --------- | ------- | ---------- |
| `TestRestoreActionItemPublishesActionItemChanged` | `service_test.go:5214` | Pre-archive item, capture cursor, call `RestoreActionItem`, assert event arrives within 100ms with `Type=LiveWaitEventActionItemChanged`, `Key=projectID`, `Sequence > cursor` | **GREEN** |
| `TestRenameActionItemPublishesActionItemChanged` | `service_test.go:5249` | Same shape on `RenameActionItem` | **GREEN** |
| `TestDeleteActionItemArchivePublishesActionItemChanged` | `service_test.go:5284` | Same shape on `DeleteActionItem(DeleteModeArchive)` | **GREEN** |
| `TestDeleteActionItemHardPublishesActionItemChanged` | `service_test.go:5316` | Same shape on `DeleteActionItem(DeleteModeHard)` | **GREEN** |
| `TestReparentActionItemPublishesActionItemChanged` | `service_test.go:5345` | Same shape on `ReparentActionItem` (parent + child fixture) | **GREEN** |
| `TestImportSnapshotPublishesActionItemChangedPerProject` | `snapshot_test.go:5354` | 2 projects × 2 items each → assert `broker.Latest(p1).Sequence == 1` AND `broker.Latest(p2).Sequence == 1` (the N-amplification gate) AND `Wait(p1, 0)` + `Wait(p2, 0)` complete without timeout | **GREEN** |

**Helper functions** at `service_test.go:5162-5206`:
- `waitForActionItemChangedSinceCursor(t, broker, projectID, cursor)` — wraps `broker.Wait(...)` with 100ms timeout; asserts on `Type`, `Key`, `Sequence > cursor`.
- `captureActionItemChangedCursor(t, broker, projectID)` — wraps `broker.Latest(...)`; returns 0 for fresh brokers.

Pattern is race-free because in-process broker's `Wait(...)` short-circuits when `latest.Sequence > afterSequence` (`live_wait.go:137`), avoiding the goroutine-sleep race in `dispatcher/broker_sub_test.go`. Worklog §5.1 calls this out correctly.

---

## 4. ImportSnapshot Single-Publish Verification

The N-amplification gate is the most-load-bearing claim of this droplet. Three independent pieces of evidence:

1. **Code shape:** `snapshot.go:357-372` builds a `map[string]struct{}` then iterates `for projectID := range projectIDs { s.publishActionItemChanged(projectID) }`. Map-iteration is exactly once per distinct key. Verified by reading.

2. **Test pin:** `snapshot_test.go:1395-1397` asserts `p1Latest.Sequence == 1` (NOT >= 1). With the in-process broker's per-(type,key) counter (`live_wait.go:151-160`), 2 items per project publishing per-item would land at Sequence==2. The pin at 1 catches the regression directly.

3. **Wait-after-import sanity:** `snapshot_test.go:1413-1420` runs `broker.Wait(ctx, ..., "p1", 0)` and `broker.Wait(ctx, ..., "p2", 0)` with 100ms timeout. Both resolve immediately (Latest > 0). Catches a separate failure mode where Latest is stamped but the waiter pool never wakes.

**PROVEN — single bulk publish per project, no N-amplification.**

---

## 5. Edge-Case Coverage

| Edge | Handled at | Verification |
| ---- | ---------- | ------------ |
| Empty `projectID` (whitespace) | `publishActionItemChanged` `coordination_live_wait.go:69-72` | Trim + skip if empty |
| Whitespace-only `project.ID` in snapshot | `snapshot.go:359` | `strings.TrimSpace` + `if id != ""` |
| Whitespace-only `actionItem.ProjectID` in snapshot | `snapshot.go:365` | Same shape |
| Empty snapshot | `snapshot.go:370-372` | `len(projectIDs)==0` → loop is no-op |
| Failed import (partial write) | `snapshot.go:282-347` | Every phase returns on error BEFORE the publish loop at line 357 |
| `liveWait == nil` (test contexts without broker) | `coordination_live_wait.go:65-68` | Early-return guard |
| `Service` nil receiver | `coordination_live_wait.go:65-68` | Early-return guard |

All edge cases handled defensively.

---

## 6. Mage Verification

| Run | Result |
| --- | ------ |
| `mage test-func ./internal/app TestRestoreActionItemPublishesActionItemChanged` | GREEN (1 test, -race, 1.46s) |
| `mage test-func ./internal/app TestRenameActionItemPublishesActionItemChanged` | GREEN |
| `mage test-func ./internal/app TestDeleteActionItemArchivePublishesActionItemChanged` | GREEN |
| `mage test-func ./internal/app TestDeleteActionItemHardPublishesActionItemChanged` | GREEN |
| `mage test-func ./internal/app TestReparentActionItemPublishesActionItemChanged` | GREEN |
| `mage test-func ./internal/app TestImportSnapshotPublishesActionItemChangedPerProject` | GREEN |

Per-test verified, not just builder's `mage ci` claim. All 6 pass under `-race -count=1`.

---

## 7. Refinement Closure

- **R1** ("publish gap on Restore/Rename/Archive/Reparent action-item write paths") — 5 publish call sites added at the service-layer write surfaces. Walker droplet (4a.18) was the originally-routed fix; closing earlier in the pipeline at 4b.8 directly resolves the gap. **MECHANICALLY RESOLVED.**
- **R2** ("ImportSnapshot bypasses publishers") — single bulk publish per project_id at end of successful import. Closes the daemon-mode planning route. **MECHANICALLY RESOLVED.**

Both refinements should be struck from `project_drop_4a_refinements_raised.md` once 4b.8 lands.

---

## 8. Findings

**None.** All acceptance items proven by direct code reading + per-test mage runs. No gaps. No drift between worklog claims and code.

---

## 9. Missing Evidence

**None.** Every acceptance item has file:line citations + test verification.

---

## 10. Hylla Feedback

| Query | Missed because | Worked via | Suggestion |
| ----- | -------------- | ---------- | ---------- |
| (would have been: `hylla_search_keyword(query="publishActionItemChanged")` to locate publish call sites) | Builder worklog §8 already documented the same Hylla miss with the same root cause: stale ingest snapshot=5 predates Drop 4a's `publishActionItemChanged` additions. I reused the worklog's evidence rather than re-querying. | `Read` + `/usr/bin/grep` on `internal/app/service.go`, `internal/app/snapshot.go`, `internal/app/coordination_live_wait.go`, `internal/app/live_wait.go` | Re-ingest `tillsyn@main` so Drop 4a publish wiring lands in the index. Surface ingest-staleness as a one-line warning so callers know to fall back rather than treat zero-results as authoritative. |

Net: same stale-ingest issue as builder's §8 — no new Hylla-feedback entries from QA.

---

## 11. Summary

**PASS.** All 7 acceptance items proven, all 6 tests verified-green under `-race`, ImportSnapshot single-bulk-per-project semantic verified by code shape AND test pin AND wait-after-import sanity. R1 + R2 mechanically resolved. No findings, no missing evidence.

**Hand-off:** safe to commit `internal/app/service.go`, `internal/app/service_test.go`, `internal/app/snapshot.go`, `internal/app/snapshot_test.go`, `workflow/drop_4b/4b.8_BUILDER_WORKLOG.md`, `workflow/drop_4b/4b.8_BUILDER_QA_PROOF.md` once falsification sibling also passes.

---

## TL;DR

- **T1.** Per-acceptance verdict: 5 service-layer publish surfaces verified at exact line cites (1163, 1197, 1414, 1461, 1739); ImportSnapshot bulk publish at snapshot.go:357-372.
- **T2.** Per-surface code evidence: every publish lands AFTER the canonical "successful repo write" anchor + applicable embedding/cleanup hygiene.
- **T3.** All 6 tests run GREEN under `-race` via `mage test-func`; ImportSnapshot test pins Sequence==1 per project as N-amplification gate.
- **T4.** Single-publish-per-project verified three ways: code shape, Sequence==1 pin, post-publish Wait sanity.
- **T5.** Edge cases (empty ID, whitespace, nil broker, partial-fail import) all guarded defensively.
- **T6.** Mage verification: 6 individual `mage test-func` runs, all green.
- **T7.** R1 + R2 mechanically resolved — strike from running list.
- **T8.** No findings.
- **T9.** No missing evidence.
- **T10.** Hylla feedback inherits builder's stale-ingest miss; no new entries.
- **T11.** Verdict: **PASS**.
