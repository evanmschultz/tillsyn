# 4b.5 BUILDER QA PROOF — AUTH AUTO-REVOKE WIRING

**Verdict:** PASS

**Mode:** filesystem-MD only. Round 1 (no prior round).

**Spec sources:**
- `workflow/drop_4b/PLAN.md` row 4b.5 (lines 124–138).
- `workflow/drop_4b/WAVE_C_PLAN.md` §2 (lines 39–118).
- `workflow/drop_4b/4b.5_BUILDER_WORKLOG.md`.

**Mage runs (this review):**
- `mage test-pkg ./internal/app` — 378/378 pass, 0 fail, 0 skip.
- `mage test-pkg ./internal/app/dispatcher` — 141/141 pass, 0 fail, 0 skip.

---

## Acceptance Item Verdicts

### AI-1 — `Service.RevokeSessionForActionItem(ctx, actionItemID) error`

**PROVEN.** Method declared at `internal/app/auth_requests.go:912`. Signature matches spec: `func (s *Service) RevokeSessionForActionItem(ctx context.Context, actionItemID string) error`. Doc-comment at lines 873–911 documents the multi-session + lease-cascade + idempotency contract.

### AI-2 — Multi-session iteration (WC-A2)

**PROVEN.** `auth_requests.go:921` calls `s.authBackend.ListAuthSessions(ctx, AuthSessionFilter{})` (empty filter → all sessions). Loop at lines 928–953 iterates over every returned session and invokes revoke on each match (no early `return` on first hit; only `continue` on skip cases at 930, 933, 939). Pinned by test `TestRevokeSessionForActionItemRevokesAllSessions` (`auth_revoke_for_action_item_test.go:145–183`) which seeds 3 matching + 1 noise sibling and asserts exactly 3 revoke calls hitting the matching set, none on the sibling. Test verified GREEN in mage run (counted in the 378 pass).

### AI-3 — Lease cascade (WC-A1)

**PROVEN.** Per matching session, both surfaces are invoked:
- Session revoke: `auth_requests.go:941` — `s.authBackend.RevokeAuthSession(ctx, session.SessionID, terminalStateCleanupRevokeReason)`.
- Lease revoke: `auth_requests.go:950` — `s.repo.RevokeCapabilityLeasesByScope(ctx, projectID, capScope, path.ScopeID, now, terminalStateCleanupRevokeReason)`.

Scope conversion at `auth_requests.go:948` uses `path.ScopeType.ToCapabilityScopeType()`. `Repository.RevokeCapabilityLeasesByScope` confirmed in interface (`internal/app/ports.go:67`) and implemented at `internal/adapters/storage/sqlite/repo.go:2541`. Pinned by `TestRevokeSessionForActionItemRevokesLeaseAndSession` (`auth_revoke_for_action_item_test.go:190–244`) which seeds a session+lease pair and asserts both surfaces revoke. Test verified GREEN.

### AI-4 — `errors.Join` aggregation across all per-session failures

**PROVEN.** `auth_requests.go:926` declares `var errs []error`. Each per-session failure path (line 942 session-revoke error, line 951 lease-revoke error) appends rather than returns. Final return at line 954: `return errors.Join(errs...)`. Pinned by `TestRevokeSessionForActionItemIteratesPastSessionRevokeError` (`auth_revoke_for_action_item_test.go:279–318`) which seeds two sessions with the first erroring; asserts both calls fire AND `errors.Is(err, revokeErr)` succeeds. Test verified GREEN.

### AI-5 — Returns nil on no-matching-sessions (idempotent)

**PROVEN.** Three documented degenerate paths return nil:
- Empty/whitespace ID: `auth_requests.go:914-916`.
- Nil `authBackend`: `auth_requests.go:917-919`.
- Zero matching sessions: loop body `continue`s on every session; `errs` stays empty; `errors.Join()` returns nil.

Pinned by:
- `TestRevokeSessionForActionItemNoMatchingSessions` (lines 251–271) — project-scoped + sibling-action-item sessions; target has zero matches; asserts nil error + zero revoke calls.
- `TestRevokeSessionForActionItemEmptyIDIsNoop` (lines 324–337) — empty + whitespace IDs; zero backend calls.
- `TestRevokeSessionForActionItemNilAuthBackendIsNoop` (lines 343–351).
- `TestRevokeSessionForActionItemSkipsRevokedSessions` (lines 356–380) — skips already-revoked sessions.
- `TestRevokeSessionForActionItemSkipsMalformedApprovedPath` (lines 386–407) — malformed ApprovedPath skipped silently.

All five tests verified GREEN.

### AI-6 — `cleanup.go:154` constructor binding updated

**PROVEN (with line-number drift, harmless).** The spec referenced `cleanup.go:154` for the constructor binding, but post-edit the constructor body lands at `cleanup.go:159–189`. Material check:
- Constructor signature at `cleanup.go:159` adds the `authRevoker actionItemAuthRevoker` parameter.
- Nil-validation guard at `cleanup.go:169-171`.
- Production binding at `cleanup.go:181-183`: `revokeAuthBundle: func(ctx context.Context, actionItemID string) error { return authRevoker.RevokeSessionForActionItem(ctx, actionItemID) }`.
- Call site updated at `dispatcher.go:258`: `cleanup, err := newCleanupHook(fileLocks, pkgLocks, monitor, svc)` — `*app.Service` satisfies `actionItemAuthRevoker` via the new method on `Service`.

Pinned by `TestCleanupHookCallsRevokeSessionForActionItem` (`cleanup_test.go:396-421`) which constructs a real `cleanupHook` via `newCleanupHook` and asserts the seam delegates to the supplied `actionItemAuthRevoker`. Test verified GREEN.

### AI-7 — `revokeAuthBundleStub` replaced/deleted

**PROVEN.** Repo-wide search (`rg revokeAuthBundleStub internal/`) returns zero matches. The stub function and the deleted test `TestRevokeAuthBundleStubReturnsNil` (per worklog line 181) are absent from the tree. The function is replaced by the closure at `cleanup.go:181-183`.

### AI-8 — Existing `errors.Join` aggregation (cleanup.go:218-237) unchanged

**PROVEN.** `cleanup.go` `OnTerminalState` at lines 233–272 retains the four-step aggregation pattern: declare `var errs []error` at line 252; append per-step error at lines 254–256, 259–261, 264–266; `errors.Join(errs...)` at line 271. The spec referenced lines 218-237; the doc-comment shifted slightly but the semantic content is intact (lines 220–225 of doc-comment + 252–271 of body). Aggregation pinned by:
- `TestCleanupContinuesPastIndividualFailure` (`cleanup_test.go:252-308`) — every step still fires after the first errors; final `errors.Is` for fileErr/pkgErr/authErr all succeed.
- `TestCleanupHookAggregatesAuthRevokeError` (`cleanup_test.go:429-469`) — auth-revoke error propagates; lock release + monitor unsubscribe still fire; sibling can re-acquire the file lock.

Both tests verified GREEN.

### AI-9 — 5 named tests + integration test

**PROVEN with EXCEEDS-SPEC.** The spec called for 5 named tests; the build delivered 8 in `auth_revoke_for_action_item_test.go`:

| # | Test | Status |
| - | ---- | ------ |
| 1 | `TestRevokeSessionForActionItemRevokesAllSessions` | GREEN — multi-session iteration |
| 2 | `TestRevokeSessionForActionItemRevokesLeaseAndSession` | GREEN — lease cascade |
| 3 | `TestRevokeSessionForActionItemNoMatchingSessions` | GREEN — no-match no-op |
| 4 | `TestRevokeSessionForActionItemIteratesPastSessionRevokeError` | GREEN — errors.Join aggregation |
| 5 | `TestRevokeSessionForActionItemEmptyIDIsNoop` | GREEN — empty/whitespace ID |
| 6 | `TestRevokeSessionForActionItemNilAuthBackendIsNoop` | GREEN — fixture-compat |
| 7 | `TestRevokeSessionForActionItemSkipsRevokedSessions` | GREEN — already-revoked skip |
| 8 | `TestRevokeSessionForActionItemSkipsMalformedApprovedPath` | GREEN — malformed-path skip |

Plus integration tests in `cleanup_test.go`:

| # | Test | Status |
| - | ---- | ------ |
| 9 | `TestCleanupHookCallsRevokeSessionForActionItem` | GREEN — newCleanupHook wiring |
| 10 | `TestCleanupHookAggregatesAuthRevokeError` | GREEN — error aggregation through hook |
| 11 | `TestCleanupReleasesFileAndPackageLocks` | GREEN — extended with authRevoker assertion |
| 12 | `TestNewCleanupHookValidatesDependencies` | GREEN — new `nil authRevoker` table case |

All 12 verified GREEN by the mage run, not name-only.

### AI-10 — Multi-package surgery doesn't break existing tests

**PROVEN.** `mage test-pkg ./internal/app` reports 378 tests passing — worklog baseline 370 + 8 new = 378, exact match. `mage test-pkg ./internal/app/dispatcher` reports 141 tests passing — worklog baseline 134 + 7 net (2 new wiring + 1 new validation table case + 4 sub-tests from the table-test split, minus deleted stub-test) = 141, exact match. Zero failures. The seam-signature change (added `ctx` to `revokeAuthBundle`) propagated cleanly into the 4 direct-struct-literal cleanup-hook constructions in `cleanup_test.go` (visible at lines 159, 211, 269, 327) and 1 in `dispatcher_test.go:590`.

---

## Hylla Feedback

None — Hylla answered everything needed for proof. (Build worklog reported its own Hylla misses; this review's queries hit only `rg` for committed-state symbol presence checks, which is the documented MD-non-Go path. Hylla queries on `RevokeSessionForActionItem`, `actionItemAuthRevoker`, and `RevokeCapabilityLeasesByScope` would have worked equivalently but the `rg` path was sufficient given the file targets were specifically named in the spec.)

---

## Summary

All 10 acceptance items PROVEN. 12 tests verified GREEN by mage runs (not name-only). `revokeAuthBundleStub` confirmed deleted. Constructor binding updated at `dispatcher.go:258`; `cleanup.go:159` constructor wires the function-typed seam to `actionItemAuthRevoker.RevokeSessionForActionItem`. Lease cascade and multi-session iteration both implemented in `auth_requests.go:912-955` and pinned by named tests. Existing aggregation behavior preserved.

**Verdict: PASS.**
