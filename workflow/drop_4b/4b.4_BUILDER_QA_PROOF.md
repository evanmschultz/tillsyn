# 4b.4 BUILDER QA-PROOF — `mage_test_pkg` GATE IMPLEMENTATION

**Verdict:** PASS
**Mode:** filesystem-MD only
**Pass:** PROOF — Evidence Completeness (does not attempt to break)
**Reviewed:** 2026-05-04

## Files Reviewed

- `internal/app/dispatcher/gate_mage_test_pkg.go` (174 lines, NEW)
- `internal/app/dispatcher/gate_mage_test_pkg_test.go` (443 lines, NEW)
- `workflow/drop_4b/4b.4_BUILDER_WORKLOG.md`
- `internal/app/dispatcher/gate_mage_ci.go` (cross-checked for `commandRunner` / `combineGateOutput` reuse — 4b.3 baseline)
- `internal/app/dispatcher/gate_mage_ci_test.go` (cross-checked for `withFakeCommandRunner` reuse — 4b.3 baseline)

## Spec Cross-References

- `workflow/drop_4b/PLAN.md` § 7 droplet 4b.4 (lines 114–120) — **revised acceptance**, FAIL-LOUD on empty Packages.
- `workflow/drop_4b/WAVE_A_PLAN.md` § 4b.4 (lines 231–278) — wave-internal acceptance + 8-test scenario list. Note: this file's PQA-4 line 306 documents the original "empty=success" semantic; PLAN.md row 4b.4 supersedes via revision WA-A5.

## Acceptance-Item Verification (11 items)

### AI-1: `gateMageTestPkg(ctx, item, project) GateResult` matches gateFunc signature

**PROVEN.**

- Definition: `gate_mage_test_pkg.go:80` — `func gateMageTestPkg(ctx context.Context, item domain.ActionItem, project domain.Project) GateResult`.
- Sibling 4b.3 signature: `gate_mage_ci.go:141` — `func gateMageCI(ctx context.Context, _ domain.ActionItem, project domain.Project) GateResult`.
- Identical 3-arg + return shape; type imports match (`internal/domain`, `internal/templates`); GateResult fields populated (`GateName`, `Status`, `Err`, `Output`, `Duration`).

### AI-2: Empty `item.Packages` = FAIL-LOUD per WA-A5 (revision flipped from silent-success)

**PROVEN.** Highest-stakes item — explicitly verified per QA-Proof handoff.

- Guard: `gate_mage_test_pkg.go:83-93` returns `GateStatusFailed` with `Err = errors.New("mage_test_pkg: action item declares no packages — planner must populate item.Packages or remove this gate from kind")`.
- Doc-comment: `gate_mage_test_pkg.go:22-29` cites `WAVE_A_PLAN.md plan-revision WA-A5` and rationale ("silent-pass would mask a real QA gap").
- PLAN.md row 4b.4 (line 118) text: "Empty `item.Packages` is FAIL-LOUD (per plan-QA-falsification WA-A5)" — exact match against shipped behavior.
- Dedicated test: `gate_mage_test_pkg_test.go:234-259` — `TestGateMageTestPkgRejectsEmptyPackages` configures fake to *would-pass* (line 237: `{exitCode: 0}`) and asserts (a) `Status == Failed` (line 244, with "fail-loud per WA-A5" message), (b) `Err` contains "no packages" (line 250), (c) `len(runner.calls) == 0` (line 253), (d) `GateName == GateKindMageTestPkg` (line 256). Test verified-green (per-test mage run, this review).
- Guard order: packages-first BEFORE worktree guard. `gate_mage_test_pkg.go:83` (packages) precedes `:95` (worktree). Worklog § Design Choices documents the rationale (most-actionable error first; planner misconfig vs dispatcher misconfig).

**REVISED-ACCEPTANCE LANDED CORRECTLY** — was the named risk; verified explicit, dedicated, and asserts the runner is NOT invoked.

### AI-3: Empty-RepoPrimaryWorktree guard mirrors 4b.3

**PROVEN.**

- 4b.4 guard: `gate_mage_test_pkg.go:95-102` — `strings.TrimSpace(project.RepoPrimaryWorktree) == ""` → `GateStatusFailed` with `errors.New("project: RepoPrimaryWorktree is empty")`.
- 4b.3 guard: `gate_mage_ci.go:144-151` — identical pattern: `strings.TrimSpace(project.RepoPrimaryWorktree) == ""` → same error literal `"project: RepoPrimaryWorktree is empty"`.
- Mirror is exact: same condition, same error, same `Duration: time.Since(start)` post-guard return.
- Test: `gate_mage_test_pkg_test.go:263-286` — `TestGateMageTestPkgRejectsEmptyWorktree` asserts Failed status, error substring "RepoPrimaryWorktree is empty", and zero runner calls. Verified-green individually.

### AI-4: Per-package iteration via reused `commandRunner` (4b.3 verbatim — no copy-paste)

**PROVEN.**

- Reuse-by-reference (not duplication): `gate_mage_test_pkg.go:109` calls `defaultCommandRunner.Run(ctx, project.RepoPrimaryWorktree, "mage", "test-pkg", pkg)`. The `defaultCommandRunner` package-level var is declared once in `gate_mage_ci.go:96` (`var defaultCommandRunner commandRunner = execCommandRunner{}`).
- The `commandRunner` interface (`gate_mage_ci.go:45-47`) and `execCommandRunner` impl (`gate_mage_ci.go:66-91`) are NOT redefined — they are referenced by name from the same package. No new types, no copy-paste.
- Iteration: `gate_mage_test_pkg.go:108` `for _, pkg := range item.Packages` invokes the runner once per declared package.
- Test seam reuse: `withFakeCommandRunner` (`gate_mage_ci_test.go:44`) is reused unchanged by all 4b.4 tests — verified by `rg` cross-check (same file unmodified, just additional callers in `gate_mage_test_pkg_test.go`).

### AI-5: Aggregated output — ordered concat across packages-run-so-far → tailOutput

**PROVEN.**

- Aggregation: `gate_mage_test_pkg.go:106` `var aggregated []byte` accumulator declared outside loop.
- Per-iteration fold: `gate_mage_test_pkg.go:120` `aggregated = combineGateOutput(aggregated, combineGateOutput(stdout, stderr))`. Reuses `combineGateOutput` from `gate_mage_ci.go:210-224` (same package, package-private — no copy).
- Net ordering: declared `pkg1_stdout, pkg1_stderr, pkg2_stdout, pkg2_stderr, …` because `combineGateOutput` deterministically appends second arg after first (with newline alignment between).
- Tail-once-at-end: `tailOutput(aggregated, MaxGateOutputLines, MaxGateOutputBytes)` called only on the failure paths (lines 133, 145, 157), NOT per-iteration. Important — per-iteration would lose pkg1 lines when pkg2 fails after them.
- Test: `gate_mage_test_pkg_test.go:372-442` — `TestGateMageTestPkgAggregatesOutputAcrossPackages` asserts (a) pkg1 marker idx < pkg2 marker idx (line 411), (b) pkg2 stdout idx < pkg2 stderr idx (line 414), (c) PKG1_LINE_00 absent (line 424 — line bound dropped earliest), (d) PKG1_LINE_01 present (line 428), (e) PKG2_LINE_49 present (line 431 — failure tail retained), (f) `len(Output) <= MaxGateOutputBytes` (line 434), (g) newlines `<= MaxGateOutputLines` (line 438). Verified-green individually.

### AI-6: Exit-code routing — 0 → continue; !=0 → halt + Failed naming the package

**PROVEN.**

- Continue path: `gate_mage_test_pkg.go:160` (implicit fall-through after `if exitCode != 0`) — comment lines 161-164 confirm "exitCode == 0: continue to next package."
- Halt path: `gate_mage_test_pkg.go:150-160` — `if exitCode != 0` → return Failed with `Err = fmt.Errorf("mage test-pkg %s failed: exit code %d", pkg, exitCode)`. The package name is interpolated into the error.
- Halt is `return` (not `break`) so subsequent iterations are NOT invoked.
- Tests:
  - `gate_mage_test_pkg_test.go:159-192` `TestGateMageTestPkgFailsOnFirstPackageNonZero` — script has only pkg1 entry; `runner.calls == 1` (line 183); Err contains "mage test-pkg pkg1" (line 177) + "exit code 1" (line 180). The script overflow-panics if pkg2/pkg3 reached.
  - `gate_mage_test_pkg_test.go:197-227` `TestGateMageTestPkgFailsOnSecondPackageNonZero` — pkg1 passes, pkg2 fails, pkg3 absent; `runner.calls == 2` (line 219); last call `args[1] == "pkg2"` (line 223).
- Both verified-green individually.

### AI-7: Process-start failure mid-iteration — halt + Failed naming package + start-error wrapped

**PROVEN.**

- Code path: `gate_mage_test_pkg.go:138-148` — `if runErr != nil` → return Failed with `Err = fmt.Errorf("mage test-pkg %s start failed: %w", pkg, runErr)`. `%w` wraps so `errors.Is` / `errors.Unwrap` chains preserve the underlying error.
- Halt: `return` halts iteration immediately; no further packages invoked.
- Test: `gate_mage_test_pkg_test.go:292-321` — `TestGateMageTestPkgFailsOnStartError`:
  - pkg1 passes, pkg2 returns `startErr = errors.New("exec: ...not found in $PATH")`, pkg3 absent (overflow panics if reached).
  - Asserts `errors.Is(result.Err, startErr)` (line 309 — proves `%w` wrap is intact).
  - Asserts `Err` contains "mage test-pkg pkg2" (line 312 — failed package named).
  - Asserts `Err` contains "start failed" (line 315 — distinguishes from exit-code path).
  - Asserts `runner.calls == 2` (line 318 — pkg3 NOT invoked).
- Verified-green individually.

### AI-8: Ctx cancellation propagates per 4b.3's pattern

**PROVEN.**

- 4b.4 ctx-check: `gate_mage_test_pkg.go:126-136` — `if ctxErr := ctx.Err(); ctxErr != nil` BEFORE checking `runErr`. Returns Failed with `Err = fmt.Errorf("mage test-pkg %s context cancelled: %w", pkg, ctxErr)`.
- 4b.3 ctx-check pattern: `gate_mage_ci.go:163-170` — same `ctx.Err()`-before-`runErr` ordering, same `%w` wrap, same "context cancelled" prefix.
- Pattern mirror is exact down to the prefix/wrap convention.
- Test: `gate_mage_test_pkg_test.go:333-363` — `TestGateMageTestPkgHonorsContextCancel`:
  - Pre-cancelled `ctx`, scripted runner returns `ctx.Err()` as runErr to simulate `exec.CommandContext` behavior.
  - Asserts `errors.Is(result.Err, context.Canceled)` (line 350 — wrap chain intact).
  - Asserts `Err` contains "context cancelled" (line 353).
  - Asserts `Err` does NOT contain "start failed" (line 357 — proves ctx-branch routes BEFORE start-failure branch).
  - Asserts `Err` contains "pkg1" (line 360 — failed package named).
- Verified-green individually.

### AI-9: Duration measurement = total wall-clock

**PROVEN.**

- `start := time.Now()` declared once at function entry: `gate_mage_test_pkg.go:81`.
- `Duration: time.Since(start)` populated on every return path:
  - Empty-packages guard: line 91.
  - Empty-worktree guard: line 100.
  - Ctx-cancel: line 134.
  - Start-error: line 146.
  - Exit-code-failure: line 158.
  - All-pass: line 171.
- 6 of 6 return paths populate `Duration` — covers every branch.
- Test: `gate_mage_test_pkg_test.go:130-132` (`TestGateMageTestPkgPassesAllPackages`) asserts `Duration > 0` on the multi-package pass path. Other tests don't explicitly probe duration but the field is populated regardless of status — confirmed by inspection.

### AI-10: 8 named tests

**PROVEN.** All 8 verified-green individually via `mage test-func ./internal/app/dispatcher <TestName>`:

| # | Test name (verified line) | Mage run verdict |
| - | ------------------------- | ----------------- |
| 1 | `TestGateMageTestPkgPassesAllPackages` (line 101) | passed: 1 |
| 2 | `TestGateMageTestPkgFailsOnFirstPackageNonZero` (line 159) | passed: 1 |
| 3 | `TestGateMageTestPkgFailsOnSecondPackageNonZero` (line 197) | passed: 1 |
| 4 | `TestGateMageTestPkgRejectsEmptyPackages` (line 234) | passed: 1 |
| 5 | `TestGateMageTestPkgRejectsEmptyWorktree` (line 263) | passed: 1 |
| 6 | `TestGateMageTestPkgFailsOnStartError` (line 292) | passed: 1 |
| 7 | `TestGateMageTestPkgHonorsContextCancel` (line 333) | passed: 1 |
| 8 | `TestGateMageTestPkgAggregatesOutputAcrossPackages` (line 372) | passed: 1 |

Aggregate package run: `mage test-pkg ./internal/app/dispatcher` → 139 tests / 0 fails (worklog claimed 131 + 8 = 139; matches exactly). NONE are name-only — every test has been individually executed green this review.

### AI-11: NO mutation of dispatcher.go / gates.go / gate_mage_ci.go

**PROVEN.**

- `git status internal/app/dispatcher/` shows only two **untracked** files:
  - `internal/app/dispatcher/gate_mage_test_pkg.go`
  - `internal/app/dispatcher/gate_mage_test_pkg_test.go`
- Zero modified files in the dispatcher package. `dispatcher.go`, `gates.go`, `gate_mage_ci.go`, `gate_mage_ci_test.go` are all untouched.
- HARD CONSTRAINT honored.

## Cross-Cut Spot Checks

- **`scriptedCommandRunner` is a sibling, not a generalization** of 4b.3's `fakeCommandRunner` — `gate_mage_test_pkg_test.go:41-75`. Existing 4b.3 fake stays unchanged (HARD CONSTRAINT). Overflow-panic on script exhaustion (line 67) gives loud failure on halt-on-first-failure violations.
- **`scriptedInvocation` records every call** (`gate_mage_test_pkg_test.go:47-51`) including a defensive `argsCopy` (line 60) so post-call mutation of the gate's args slice cannot poison assertions.
- **No imports of new external packages** — the gate file imports only stdlib + `internal/domain` + `internal/templates`. Same import set as `gate_mage_ci.go` plus `strings` (already in 4b.3) and `errors`/`fmt` (already in 4b.3).
- **Gate-name attribution** (`templates.GateKindMageTestPkg`) populated on every return path: lines 85, 97, 128, 140, 152, 169 — 6 of 6 returns. Same enum constant introduced in 4b.1.
- **Production wiring deferred to 4b.7** — gate function exists; no `gateRunner.Register(GateKindMageTestPkg, gateMageTestPkg)` call lands here. Worklog § "Production wiring NOT in this droplet" confirms this is intentional (matches PLAN.md 4b.7's role).
- **Mage discipline:** verification done exclusively via `mage test-pkg` and `mage test-func`. Zero raw `go test`. Zero `mage install`.

## Hylla Feedback

None — proof review used `Read` directly per the agent's evidence-order rule (start from declared `paths`, query files by name); no Hylla queries were needed for committed-code lookup since the spec named the files explicitly. The two new files are uncommitted, so Hylla would be stale for them anyway (`git status` is the right tool there).

## Definition of Done

| Item | Status | Notes |
| ---- | ------ | ----- |
| Per-acceptance-item PROVEN/GAP/WRONG with file:line cites | DONE | 11 of 11 PROVEN; 0 GAP; 0 WRONG |
| Per-test verified-green vs name-only | DONE | All 8 verified-green individually |
| Empty-Packages FAIL-LOUD landing verified (vs original silent-success) | DONE | AI-2 explicitly proven, dedicated test verified |
| `git status` confirms no forbidden-file mutation | DONE | Only the 2 NEW files untracked |
| `mage test-pkg ./internal/app/dispatcher` clean | DONE | 139/0/0/0 |

## Verdict

**PASS.** All 11 acceptance items proven against the shipped code with file:line evidence. All 8 tests verified-green via individual `mage test-func` runs. The named-risk revision (empty-Packages → FAIL-LOUD per WA-A5, supersedes original silent-success spec) landed explicitly and correctly with a dedicated test asserting Failed status, "no packages" Err substring, and zero runner invocations. HARD CONSTRAINT (no mutation of `dispatcher.go` / `gates.go` / `gate_mage_ci.go` / `gate_mage_ci_test.go`) honored — only the two NEW files were created.
