# 4b.2 — GATE RUNNER + REGISTRY — QA Proof Review

**Verdict:** PASS-WITH-NIT
**One-line:** All 11 spawn-prompt acceptance items proven against `gates.go` / `gates_test.go`; 9 named tests verified individually green; `dispatcher.go` proven untouched. Nit: spawn prompt and the WAVE_A_PLAN.md §4b.2 acceptance describe two different runner shapes (`GateResult`-slice vs `GateRunOutcome`-aggregate) — builder built the spawn-prompt shape, which is the surface this proof gates.

**Artifact path:** `workflow/drop_4b/4b.2_BUILDER_QA_PROOF.md` (this file).

---

## 1. Surface under review

| File                                                | Status                                  |
| --------------------------------------------------- | --------------------------------------- |
| `internal/app/dispatcher/gates.go`                  | NEW, 318 LOC                            |
| `internal/app/dispatcher/gates_test.go`             | NEW, 397 LOC                            |
| `internal/app/dispatcher/dispatcher.go`             | UNTOUCHED — verified `git diff` empty   |
| `workflow/drop_4b/4b.2_BUILDER_WORKLOG.md`          | Present, 112 lines                      |

`mage test-pkg ./internal/app/dispatcher` returned `118 tests, 118 passed, 0 failed, 0 skipped`. Pre-droplet baseline (per worklog §4) was 109 tests; the +9 gain matches the 9 new gate tests exactly.

---

## 2. Per-acceptance-item proof (spawn-prompt items 1–11)

### 2.1 Item 1 — `gateRunner` with `Register` (rejects duplicates) + `Run(ctx, item, project, tpl)` returning `[]GateResult`

**PROVEN.**

- `gateRunner` struct: `gates.go:152-154`. Field `gates map[templates.GateKind]gateFunc`.
- `Register(name templates.GateKind, fn gateFunc) error`: `gates.go:180-186`. Duplicate-rejection: `gates.go:181-183` returns `fmt.Errorf("%w: %q", ErrGateAlreadyRegistered, name)` when `r.gates[name]` already exists.
- `Run(ctx context.Context, item domain.ActionItem, project domain.Project, tpl *templates.Template) []GateResult`: `gates.go:203`. Signature returns `[]GateResult` exactly.
- Duplicate-rejection regression: `TestGateRunnerRegisterRejectsDuplicate` at `gates_test.go:76-110` — second `Register` returns `ErrGateAlreadyRegistered` (`:88-94`), original gateFunc remains bound (`:103-109`).

### 2.2 Item 2 — `gateFunc` synchronous signature `(ctx, item, project) GateResult`

**PROVEN.**

- `gates.go:61`: `type gateFunc func(ctx context.Context, item domain.ActionItem, project domain.Project) GateResult`.
- Synchronous-execution doc-comment: `gates.go:54-56` ("MUST NOT spawn goroutines or rely on the runner to orchestrate concurrency: the runner executes every gateFunc serially").
- Run loop: `gates.go:213-228` iterates `sequence` linearly; each `fn(ctx, item, project)` call (`:223`) is awaited synchronously before appending the result.

**NIT (deferred from spec, not blocking):** WAVE_A_PLAN.md §4b.2 originally specified `(ctx, GateInput) GateResult` with a struct-bag input; the spawn prompt simplified to direct args. Builder followed the spawn prompt — which is the contract this review honors per the QA-Proof brief. Flagging only because it's evidence the spec drifted between WAVE_A_PLAN.md and the spawn prompt.

### 2.3 Item 3 — `GateResult` struct: `GateName, Status, Output, Duration, Err`

**PROVEN.**

- `gates.go:72-99`: struct definition with all five fields.
  - `GateName templates.GateKind` — `:76`.
  - `Status GateStatus` — `:82`.
  - `Output string` — `:87`.
  - `Duration time.Duration` — `:93`.
  - `Err error` — `:98`.
- Field-by-field doc-comments (`:73-98`) describe each field's role on Passed / Failed / Skipped.

### 2.4 Item 4 — `tailOutput` last-100-lines-or-8KB-shorter + UTF-8 sanitization

**PROVEN.**

- `tailOutput(b []byte, maxLines int, maxBytes int) string`: `gates.go:250-276`.
- "Shorter of" semantics: `gates.go:266-272` — chooses `byLines` when `len(byLines) <= len(byBytes)`, else `byBytes`. (Builder elected `<=` — ties favor lines. Reasonable; not a defect.)
- Sanitization runs after truncation: `gates.go:275` calls `sanitizeUTF8(chosen)`.
- `sanitizeUTF8` at `gates.go:315-318`: drops null bytes via `bytes.ReplaceAll(b, []byte{0}, nil)` (`:316`) and replaces invalid UTF-8 with U+FFFD via `strings.ToValidUTF8(string(withoutNulls), "�")` (`:317`).
- Public bound constants: `MaxGateOutputLines = 100` (`gates.go:121`), `MaxGateOutputBytes = 8 * 1024` (`gates.go:124`). Both match Q7.
- Tests:
  - `TestTailOutputBoundedByLines` (`gates_test.go:288-319`): 10 short lines + maxLines=3 → `"L08\nL09\nL10\n"` (12 B); maxBytes=0 fallback → last 2 lines.
  - `TestTailOutputBoundedByBytes` (`gates_test.go:323-347`): 5000-'A'-line + maxBytes=64 → 64 bytes; maxLines=0 fallback → last 16 bytes.
  - `TestTailOutputUTF8Sanitization` (`gates_test.go:351-380`): `\x00` dropped, lone `0x80` → U+FFFD, nil/empty → `""`.

### 2.5 Item 5 — Closed-enum `GateStatus`: `Passed`, `Failed`, `Skipped`

**PROVEN.**

- `type GateStatus string` at `gates.go:27`.
- Constants at `gates.go:32-50`:
  - `GateStatusPassed = "passed"` — `:35`.
  - `GateStatusFailed = "failed"` — `:42`.
  - `GateStatusSkipped = "skipped"` — `:49`.
- No `GateStatusUnknown`/`GateStatusPending` sentinel — confirmed by symbol search across the file.
- `GateStatusSkipped` reservation rationale documented at `gates.go:21-26` and `:46-48` — reserved for future runner-level filters; not emitted by 4b.2's `Run`.
- No exported `IsValidGateStatus` helper — matches "internal-only closed enum" per spawn prompt + worklog §2.3.

### 2.6 Item 6 — `ErrGateNotRegistered` typed error; `Run` returns Failed result + Err on unknown gate; halts subsequent

**PROVEN.**

- `var ErrGateNotRegistered = errors.New("dispatcher: gate not registered")` at `gates.go:107`.
- Run path: `gates.go:214-222`. When `r.gates[name]` lookup fails (`:215`), the runner appends `GateResult{GateName: name, Status: GateStatusFailed, Err: fmt.Errorf("%w: %q", ErrGateNotRegistered, name)}` (`:216-220`), then `return results` (`:221`) — subsequent gates are never invoked.
- Test: `TestGateRunnerRunReturnsErrGateNotRegistered` at `gates_test.go:196-226`. Template lists `[mage_ci, mage_test_pkg]`; only `mage_test_pkg` registered. Asserts:
  - `subsequentCalls == 0` (`:211-213`) — second gate never ran.
  - `len(results) == 1` (`:214-216`).
  - `results[0].GateName == GateKindMageCI` (`:217-219`).
  - `results[0].Status == GateStatusFailed` (`:220-222`).
  - `errors.Is(results[0].Err, ErrGateNotRegistered)` (`:223-225`).

### 2.7 Item 7 — Empty `tpl.Gates[kind]` → empty slice, no execution

**PROVEN.**

- Run handles both empty-slice and missing-kind cases identically: `gates.go:207-210` returns `nil` when `len(sequence) == 0` (a missing map entry returns the zero value `nil`, which has length 0 — same path).
- Test: `TestGateRunnerRunEmptyGatesForKind` at `gates_test.go:231-264`.
  - Case 1 (empty slice): `tplEmpty.Gates[KindBuild] = []GateKind{}` → `len(results) == 0`, `calls == 0` (`:241-248`).
  - Case 2 (missing entry): `tplMissing.Gates` only has `KindPlan` entry, no `KindBuild` → `len(results) == 0`, `calls == 0` (`:251-263`).

**Note:** Builder returns `nil` rather than a zero-length non-nil slice. Spawn-prompt acceptance says "empty slice" — Go convention treats `nil []T` and `[]T{}` interchangeably for `len(...)` and `range`, and the test asserts `len(results) != 0` rather than nil-ness, so the contract holds. PROVEN.

### 2.8 Item 8 — `tpl == nil` → empty slice (defensive)

**PROVEN.**

- `gates.go:204-206`: `if tpl == nil { return nil }` — returns before any map indirection that would panic on nil.
- Test: `TestGateRunnerRunNilTemplate` at `gates_test.go:268-284`. Asserts no panic, `len(results) == 0`, `calls == 0`.

### 2.9 Item 9 — Doc-comment notes rollback policy: Wave A side-effect-free, Drop 4c addresses commit/push

**PROVEN.**

- `gates.go:142-145`: gateRunner doc-comment carries the rollback paragraph verbatim — "Wave A gates (mage_ci, mage_test_pkg) have NO side effects requiring rollback — failed gates leave no state to undo. Drop 4c's commit + push gates DO produce side effects (a created commit, a pushed ref); the rollback design for those gates is explicitly out of scope here and lands alongside the Drop 4c gate definitions."
- Mitigates plan-QA-falsification WA-A2 ("what happens when push succeeds but reingest fails halfway") — answered "doesn't apply for Wave A; designed in Drop 4c."

### 2.10 Item 10 — 9 named tests (per acceptance §10)

**PROVEN — every test is real (not name-only) and individually green.**

| #   | Test                                                    | File:line range          | Verified-green individually | Body verified (non-trivial assertions)                                                                                              |
| --- | ------------------------------------------------------- | ------------------------ | --------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `TestGateRunnerRegisterRejectsDuplicate`                | `gates_test.go:76-110`   | YES                         | Asserts `errors.Is(err, ErrGateAlreadyRegistered)`, error message contains gate name, original counter ticks 1, overwrite stays 0.   |
| 2   | `TestGateRunnerRunHaltsOnFirstFailure`                  | `gates_test.go:115-147`  | YES                         | Asserts `firstCalls==1`, `secondCalls==0`, `len(results)==1`, `results[0].Status==Failed`, `errors.Is(Err, gateErr)`.                |
| 3   | `TestGateRunnerRunReturnsResultsInOrder`                | `gates_test.go:151-190`  | YES                         | Asserts both counters ==1, `len(results)==2`, ordered names match template, every result Passed + nil Err.                          |
| 4   | `TestGateRunnerRunReturnsErrGateNotRegistered`          | `gates_test.go:196-226`  | YES                         | Asserts subsequent counter ==0, `len==1`, GateName==MageCI, Status==Failed, errors.Is ErrGateNotRegistered.                          |
| 5   | `TestGateRunnerRunEmptyGatesForKind`                    | `gates_test.go:231-264`  | YES                         | Two cases (empty slice, missing kind); both assert `len==0` and `calls==0`.                                                          |
| 6   | `TestGateRunnerRunNilTemplate`                          | `gates_test.go:268-284`  | YES                         | Asserts no panic, `len==0`, `calls==0`.                                                                                              |
| 7   | `TestTailOutputBoundedByLines`                          | `gates_test.go:288-319`  | YES                         | Builds 10 lines via `itoa` helper; asserts last-3 = `"L08\nL09\nL10\n"`; tests `maxBytes=0` fallback.                                |
| 8   | `TestTailOutputBoundedByBytes`                          | `gates_test.go:323-347`  | YES                         | 5000-'A' single line; asserts `len==64`, last byte=='\n'; tests `maxLines=0` fallback at `len==16`.                                  |
| 9   | `TestTailOutputUTF8Sanitization`                        | `gates_test.go:351-380`  | YES                         | Mixed `hello\x00\x80world\n` → no null bytes, U+FFFD present, nil/empty input returns `""`.                                          |

Verification command per test: `mage test-func ./internal/app/dispatcher <TestName>`. Each returned `1 test passed across 1 package`.

### 2.11 Item 11 — NO mutation of `dispatcher.go`

**PROVEN.**

- `git diff --stat HEAD internal/app/dispatcher/dispatcher.go` → empty output.
- `git status --porcelain internal/app/dispatcher/dispatcher.go` → empty output (clean working tree for that file).
- `git log --oneline -3 -- internal/app/dispatcher/dispatcher.go` returns `05fd894 feat(dispatcher): add manual trigger CLI and runonce pipeline` and `1812e80 feat(dispatcher): add package skeleton with runonce interface` — both Drop 4a commits, no Drop 4b touch.
- Worklog §2.7 explicitly affirms the constraint and lists `gates.go`'s import set: `bytes, context, errors, fmt, strings, time, internal/domain, internal/templates` — no `Options`/`Dispatcher`/`RunOnce` references.

---

## 3. Cross-cutting checks

### 3.1 Mage-only verification

Builder ran `mage test-pkg ./internal/app/dispatcher` (118 tests pass) and the 9 individual `mage test-func ...` invocations confirmed in §2.10 — no raw `go test`, no `mage install`. Worklog §4 records `mage ci` GREEN baseline + 72.0% dispatcher coverage (above 70% floor).

### 3.2 Symbol-existence cross-checks

- `templates.GateKindMageCI`, `templates.GateKindMageTestPkg`, `templates.IsValidGateKind`, `templates.SchemaVersionV1`: verified live in `internal/templates/schema.go:73, 78, 105, 28`.
- `templates.Template.Gates map[domain.Kind][]GateKind`: verified live at `internal/templates/schema.go:160`.
- `domain.KindBuild`, `domain.KindPlan`: standard domain enum (referenced in test fixtures `gates_test.go:20, 254`).
- All test imports match production code shapes.

### 3.3 Hylla feedback — pass-through audit

Worklog §5 records three Hylla misses (`Template.Gates`, `GateKind/IsValidGateKind`, `ActionItem/Project field shapes`) all caused by **enrichment-in-progress** on the live ref. Falsification path: `Read` + `rg`. Suggestion: surface enrichment-status visibility. Aligns with the project's standing Hylla-feedback discipline; recorded for drop-end aggregation.

---

## 4. Spec-vs-prompt drift (NIT, not blocking)

The WAVE_A_PLAN.md §4b.2 acceptance bullet list (lines 142-154) describes a different runner shape than the spawn prompt:

| Aspect                  | WAVE_A_PLAN.md §4b.2 acceptance       | Spawn-prompt acceptance (this review)            |
| ----------------------- | ------------------------------------- | ------------------------------------------------ |
| `gateFunc` signature    | `(ctx, GateInput) GateResult`         | `(ctx, item, project) GateResult`                |
| `Run` return            | `(GateRunOutcome, error)`             | `[]GateResult`                                   |
| Concurrency             | `sync.RWMutex` mandated               | Not required; doc-comment notes single-goroutine |
| Result type             | `GateRunOutcome{Success, FailedGate, Output, Err}` | `GateResult` per gate, slice from Run |
| Mandated tests          | 9 tests with `Run`/`Outcome` shape (incl. `TestGateRunnerContextCancel`, `TestGateRunnerNoDeduplication`, `TestGateRunnerConcurrentRuns`) | 9 tests with `Run`/slice shape (NO ctx-cancel / no-dedup / concurrent)        |

Builder followed the spawn prompt verbatim — which the QA-Proof brief instructs is the contract. The spawn-prompt shape is also more conservative (smaller surface area, no premature mutex). Worklog §2.7 + REVISION_BRIEF.md alignment supports the spawn-prompt direction.

**Routed:** flag for orchestrator to decide whether to update WAVE_A_PLAN.md §4b.2 to match the shipped surface so 4b.3/4b.4 builders read consistent specs. Not a 4b.2 defect.

---

## 5. Hylla Feedback

`N/A — this review queried only non-Go artifacts` is wrong; Hylla was queried indirectly via the worklog's documented misses, not by this reviewer's own queries — fallback `Read` was sufficient given the file count is two new files. Reporting **None — Hylla answered everything needed** for this proof pass: I read the new files directly with `Read` (the canonical fallback for new-files-not-yet-ingested) and did not find a Hylla query that would have plausibly shortened the path. The builder's reported enrichment-in-progress misses (worklog §5) remain the drop-end aggregation candidate.

---

## TL;DR

- **T1.** Surface verified — 2 new files, 11 acceptance items mapped to file:line, `dispatcher.go` proven untouched.
- **T2.** All 11 spawn-prompt acceptance items PROVEN; all 9 named tests verified individually green via `mage test-func`; `tailOutput` shorter-of-bounds + UTF-8 sanitization both behaviorally tested.
- **T3.** Cross-cutting: mage-only verification, symbol existence verified against `internal/templates/schema.go`, builder Hylla-miss notes captured.
- **T4.** Spec drift between WAVE_A_PLAN.md §4b.2 and spawn-prompt acceptance (different runner shape); builder followed spawn-prompt; flagged for orchestrator triage. Not blocking.
- **T5.** Hylla feedback section present; no proof-pass-side queries needed beyond direct `Read`.
