# 4b.1 — `[GATES]` TABLE SCHEMA + CLOSED-ENUM `GateKind` PRIMITIVE — Builder QA Proof Round

**State:** PASS-WITH-NIT
**Reviewed:** 2026-05-04
**Reviewer:** go-qa-proof-agent (filesystem-MD mode)
**Round:** R1
**Verdict summary:** All six acceptance items proven via file:line cites + verified-green named tests. One transparent disclosure (NIT) on the `GateRulesRaw` line-number drift — field/tag/intent intact.

---

## 1. Per-Acceptance-Item Verdicts

### A1 — `GateKind` closed enum with 3 constants + `IsValidGateKind` helper — **PROVEN**

- `internal/templates/schema.go:63` — `type GateKind string`.
- `internal/templates/schema.go:69-84` — three closed-enum constants:
  - `GateKindMageCI GateKind = "mage_ci"` (line 73).
  - `GateKindMageTestPkg GateKind = "mage_test_pkg"` (line 78).
  - `GateKindHyllaReingest GateKind = "hylla_reingest"` (line 83).
- `internal/templates/schema.go:89-93` — `validGateKinds` slice carries exactly those three members.
- `internal/templates/schema.go:100-107` — `IsValidGateKind(g GateKind) bool` does exact-match against the slice (no whitespace trim, no case fold). Doc-comment cites the deliberate strictness.
- Test `TestGateKindClosedEnum` (schema_test.go:117-151) — verified-green; 11 sub-cases (3 valid, 7 invalid plus parent). Explicit invalid coverage of `commit`, `push` (Drop-4c-future literals rejected today), empty string, garbage, `MAGE_CI`, ` mage_ci `, `mage-ci`.

### A2 — `Template.Gates map[domain.Kind][]GateKind` field with TOML tag `toml:"gates"` — **PROVEN**

- `internal/templates/schema.go:155` — exact declaration: `Gates map[domain.Kind][]GateKind \`toml:"gates"\``.
- `internal/templates/schema.go:141-154` — doc-comment names Drop 4b Wave A 4b.1, calls out absence-as-no-gates semantic, names slice-order preservation by go-toml/v2.
- TOML key is **`gates`**, NOT `gate_rules` (the latter is a distinct reserved field on the same struct, see A5). Acceptance bullet satisfied verbatim.
- Test `TestTemplateTOMLRoundTrip` (schema_test.go:53-110) — verified-green; populates `Gates: {KindBuild: {GateKindMageCI, GateKindMageTestPkg}}` and asserts `reflect.DeepEqual` after marshal+unmarshal.

### A3 — `validateGateKinds` rejects unknown gate kinds AND unknown parent kinds; hooked into `Load` after `validateChildRuleReachability` — **PROVEN**

- `internal/templates/load.go:306-315` — `validateGateKinds(tpl Template) error` iterates `tpl.Gates` value slices, returns `fmt.Errorf("%w: gates[%q] entry %q", ErrUnknownGateKind, parentKind, g)` on unknown gate-kind values.
- `internal/templates/load.go:163` — sentinel `ErrUnknownGateKind = errors.New("template references an unknown gate kind")` declared.
- **Validator chain order** in `Load` (load.go:100-114):
  1. `validateMapKeys` (line 100).
  2. `validateChildRuleKinds` (line 103).
  3. `validateChildRuleCycles` (line 106).
  4. `validateChildRuleReachability` (line 109).
  5. **`validateGateKinds` (line 112)** — runs AFTER `validateChildRuleReachability` per acceptance contract.
- **Parent-kind axis** is covered by extending `validateMapKeys` at `load.go:185-189` to iterate `tpl.Gates` keys. Bogus parent kinds reject as `ErrUnknownKindReference: gates map key "<bogus>"`. Worklog §2.4 transparently splits the two axes across two validators (closed-enum-Kind for keys, closed-enum-GateKind for values) — defensible split, mirrors existing pattern for `Kinds`/`AgentBindings` maps.
- Test `TestValidateGateKindsRejectsUnknownKind` (load_test.go:319-339) — verified-green; asserts `errors.Is(err, ErrUnknownGateKind)` plus `"build"` and `"bogus"` substrings in error message.
- Test `TestValidateGateKindsRejectsUnknownParentKind` (load_test.go:347-367) — verified-green; asserts `errors.Is(err, ErrUnknownKindReference)` plus `"gates map key"` and `"bogus_kind"` substrings.

### A4 — `default.toml` `[gates.build] = ["mage_ci"]` per L6 — **PROVEN**

- `internal/templates/builtin/default.toml:350-351` — top-level `[gates]` table with `build = ["mage_ci"]`. Semantically identical to `[gates.build] = ["mage_ci"]` (both produce `Gates[KindBuild] == [GateKindMageCI]`).
- `default.toml:319-348` — extensive doc-comment block citing L6, halt-on-first-failure semantic, absence-as-no-gates rule, and the `gates`-vs-`gate_rules` distinction.
- Test `TestDefaultTemplateLoadsWithGates` (embed_test.go:352-388) — verified-green; asserts `Gates[KindBuild]` length 1 + element `GateKindMageCI`, plus 11 sibling kinds explicitly absent.

### A5 — `Template.GateRulesRaw` (acceptance-stated line 85) survives untouched (reserved seam) — **PROVEN with NIT**

- `internal/templates/schema.go:169` — declaration intact: `GateRulesRaw map[string]any \`toml:"gate_rules"\``. Tag, type, and reserved-seam role unchanged.
- `internal/templates/schema.go:157-168` — doc-comment EXTENDED (not erased) to call out the new `Gates` sibling and the distinct-TOML-key separation.
- **NIT:** the acceptance bullet states "line 85 untouched", but the field actually lives at ~line 169 because the new `Gates` field was inserted above it (schema.go:155 vs the pre-droplet location). Worklog §2.1 transparently discloses this. Behaviorally equivalent — the field, tag, type, and forward-compat seam intent are unchanged. The bullet's intent (don't break the reserved seam) holds; only the literal line number drifted as a side-effect of insertion.
- Test `TestLoadGateRulesReservedTable` (load_test.go:62-84, pre-existing) — covered by full 251-pass run; reserved-table decode still lands on `GateRulesRaw`.
- Test `TestTemplateGatesAndGateRulesCoexist` (load_test.go:282-312) — verified-green; document with both `[gates]` AND `[gate_rules.mage_ci]` decodes cleanly into both fields independently.

### A6 — Five named tests + extended round-trip — **PROVEN**

| Test                                         | Status         | File:line                | Type           |
| -------------------------------------------- | -------------- | ------------------------ | -------------- |
| `TestGateKindClosedEnum`                     | verified-green | schema_test.go:117-151   | new (3+7 sub)  |
| `TestTemplateTOMLRoundTrip` (extended)       | verified-green | schema_test.go:53-110    | extended in place |
| `TestTemplateGatesAndGateRulesCoexist`       | verified-green | load_test.go:282-312     | new            |
| `TestValidateGateKindsRejectsUnknownKind`    | verified-green | load_test.go:319-339     | new            |
| `TestValidateGateKindsRejectsUnknownParentKind` | verified-green | load_test.go:347-367  | new            |
| `TestDefaultTemplateLoadsWithGates`          | verified-green | embed_test.go:352-388    | new            |

All six fired individually via `mage testFunc ./internal/templates <Name>` plus the full-package `mage testPkg ./internal/templates` (251/251 pass).

---

## 2. Verification Run Log

- `mage testPkg ./internal/templates` — 251 tests pass / 0 fail / 0 skip.
- `mage testFunc ./internal/templates TestGateKindClosedEnum` — 11/11 pass (parent + 10 sub-cases).
- `mage testFunc ./internal/templates TestTemplateGatesAndGateRulesCoexist` — 1/1 pass.
- `mage testFunc ./internal/templates TestValidateGateKindsRejectsUnknownKind` — 1/1 pass.
- `mage testFunc ./internal/templates TestValidateGateKindsRejectsUnknownParentKind` — 1/1 pass.
- `mage testFunc ./internal/templates TestDefaultTemplateLoadsWithGates` — 1/1 pass.
- `mage testFunc ./internal/templates TestTemplateTOMLRoundTrip` — 1/1 pass.

Worklog §4 reports full `mage ci`: 2191 passed / 0 failed / 1 pre-existing skipped (`TestStewardIntegrationDropOrchSupersedeRejected`); coverage 96.0% on `internal/templates`.

---

## 3. Falsification Attacks (run by proof-agent against own PASS verdict)

| Attack                                                | Mitigation                                                                                |
| ----------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| TOML tag wrong (`gate_rules` vs `gates`)              | schema.go:155 declares `toml:"gates"`. Distinct from `gate_rules` at schema.go:169.       |
| Validator declared but never called                   | load.go:112 invokes `validateGateKinds(tpl)` inside `Load`.                               |
| Parent-kind axis missing                              | `validateMapKeys` at load.go:185-189 covers `tpl.Gates` keys; test routes ErrUnknownKindReference. |
| Default TOML malformed                                | default.toml:350-351 `[gates] / build = ["mage_ci"]` decodes correctly per embed_test.go. |
| `GateRulesRaw` deleted/renamed                        | schema.go:169 intact; `TestTemplateGatesAndGateRulesCoexist` proves coexistence.          |
| `IsValidGateKind` accepts whitespace                  | schema_test.go:140-141 explicitly rejects whitespace-padded + case-mismatch invalids.     |
| Validator order drift                                 | load.go:100-114 chain matches "after `validateChildRuleReachability`" contract.           |
| `embed_test.go` doesn't assert sibling-kind absence   | embed_test.go:370-387 enumerates 11 absent sibling kinds.                                 |
| TOML decoder doesn't honor `[]GateKind` aliasing      | `TestTemplateTOMLRoundTrip` deep-equal proves round-trip including `Gates` slice values.  |
| `mage ci` regression                                  | Worklog §4 reports clean; 2191 pass / 0 fail / coverage 96%.                              |

No unmitigated counterexample landed.

---

## 4. Hand-off

**Verdict: PASS-WITH-NIT.**

- All 6 acceptance items PROVEN.
- A5 carries a NIT: the line number named in the acceptance bullet ("line 85") drifted to ~169 because the new `Gates` field was inserted above it. Worklog §2.1 transparently discloses the shift; field, TOML tag, type, and reserved-seam role are unchanged. NIT is purely a line-number-drift artifact, not a behavioral defect.
- All 6 named tests fired verified-green; full-package `mage testPkg ./internal/templates` 251/251 pass; worklog reports `mage ci` clean.

**Recommendation:** progress to commit. The NIT does NOT block — fix is "update the acceptance bullet language from 'line 85' to 'reserved-seam role intact'" if anyone re-cuts this droplet, but no code or test change is warranted.

---

## 5. Hylla Feedback

N/A — review touched non-Go-only artifacts (`workflow/drop_4b/PLAN.md`, `WAVE_A_PLAN.md`, `4b.1_BUILDER_WORKLOG.md`, `default.toml`) plus Go source files I read directly via `Read`. Hylla is Go-only today; the Go files in scope (`schema.go`, `load.go`, `schema_test.go`, `load_test.go`, `embed_test.go`) are post-ingest deltas (uncommitted in working tree per builder worklog). Direct file reads were the correct primary tool for this droplet's working set; no Hylla query was needed and no fallback miss to record.
