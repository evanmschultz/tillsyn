# 4b.7 BUILDER QA PROOF — AUTO-PROMOTION SUBSCRIBER

**Verdict: PASS**

Per-acceptance verification with file:line cites + per-test verified-green vs name-only.

---

## Acceptance 1 — `Dispatcher.Start(ctx)` body replaces `ErrNotImplemented`

**PROVEN.** `internal/app/dispatcher/subscriber.go:50-91` carries the real `Start` body. It validates `broker` / `walker` / `projectsLister` non-nil (lines 54-62), calls `d.projectsLister.ListProjects(ctx, false)` once (line 70), derives a `context.WithCancel(context.Background())` child ctx (line 76), spawns one `runSubscriberLoop` goroutine per project ID (lines 80-87). The pre-Drop-4b.7 `return ErrNotImplemented` stub is gone — `dispatcher.go:851-852` only carries the comment pointing at `subscriber.go`. Cross-file confirmation: `dispatcher.go:114-122` keeps `ErrNotImplemented` as a deprecated sentinel alias (acceptance #4 / spec #8 leeway).

---

## Acceptance 2 — Per-event walk + RunOnce promotion

**PROVEN.** `subscriber.go:149-164` implements `runSubscriberLoop`: it calls `d.subscribeBroker(ctx, projectID)` (line 152) and on every received event invokes `d.handleSubscriberEvent(ctx, projectID)` (line 161). `subscriber.go:175-193` implements `handleSubscriberEvent`: walks via `d.walker.EligibleForPromotion(ctx, projectID)` (line 179) and calls `d.RunOnce(ctx, item.ID, "")` for each eligible item (line 191). The empty `projectIDOverride` is correct — RunOnce resolves project from item per `dispatcher.go:403-406` (`overrideID == "" && overrideID != strings.TrimSpace(item.ProjectID)` short-circuits). Walker signature confirmed at `walker.go:130` (`EligibleForPromotion(ctx, projectID) ([]domain.ActionItem, error)`).

---

## Acceptance 3 — `Dispatcher.Stop(ctx)` cancels + waits via WaitGroup with ctx-deadline

**PROVEN.** `subscriber.go:103-139`:
- Locks `subMu`, idempotent no-op when `!started || stopped` (lines 108-112).
- Sets `d.stopped = true`, captures `cancel` reference, unlocks (lines 113-115).
- Calls `cancel()` outside lock (lines 117-119) — avoids deadlock with concurrent Start.
- Spawns drain goroutine that closes `done` after `d.subWG.Wait()` (lines 123-127).
- `select` on `done` vs `ctx.Done()` (lines 133-138) honors caller-supplied deadline; returns `ctx.Err()` on deadline-exceeded.
- `ctx == nil` path (lines 129-132) waits unbounded — defensive for callers that pass `nil`.

Concrete drain test pinned at `subscriber_test.go:205-241` (`TestDispatcherStopCancelsAllSubscribers`).

---

## Acceptance 4 — Idempotent Start (`ErrAlreadyStarted`); idempotent Stop

**PROVEN — Start.** `subscriber.go:65-68` rejects with `ErrAlreadyStarted` when `started || stopped`. The `|| stopped` clause means a re-start of a stopped dispatcher is also rejected, matching worklog "stopped is terminal" design choice. Sentinel defined at `dispatcher.go:123-129`. Pinned by `TestDispatcherStartIdempotent` (`subscriber_test.go:246-274`) — second `Start` returns wrapped sentinel + `ListProjects calls = 1`.

**PROVEN — Stop.** `subscriber.go:108-112` returns nil when `!started || stopped`. Pinned by `TestDispatcherStopIdempotent` (`subscriber_test.go:278-319`) — second Stop within 50ms, never-started Stop also nil.

---

## Acceptance 5 — Race-safe Start/Stop concurrent

**PROVEN.** Lifecycle state guarded by `sync.Mutex subMu` (`dispatcher.go:260`). Critical insight verified: mutex is released BEFORE invoking `cancel()` or `wg.Wait()` (subscriber.go:115 unlocks, line 117 cancels) — eliminates the canonical "lock-across-external-call" deadlock. `TestDispatcherStartStopConcurrent` (`subscriber_test.go:325-378`) drives concurrent goroutines with 500ms deadlines; both return cleanly; final state `stopped` (re-Start → `ErrAlreadyStarted`).

---

## Acceptance 6 — `hylla_reingest` gate stub DROPPED

**PROVEN.** Directory listing of `internal/app/dispatcher/` shows no `gate_hylla_reingest.go` or `hylla_reingest.go` file. Worklog §"`hylla_reingest` gate stub DROPPED per WC-A10" documents the rationale: silent-skip violates `feedback_orchestrator_runs_ingest.md`; fail-loud `ErrGateNotRegistered` (4b.2) is the better signal. Spec WC-A10 explicitly authorized this drop — and the prompt's acceptance #6 confirms it.

---

## Acceptance 7 — `cmd/till runServe` wires dispatcher.Start + deferred Stop

**PROVEN.** `cmd/till/main.go:2616-2630` (`runServe` body):
- Constructs dispatcher via `dispatcherFactoryFunc(svc, broker)` (line 2619).
- Calls `disp.Start(ctx)` with wrapped error (lines 2623-2625).
- `defer` block (lines 2626-2630) runs `disp.Stop(stopCtx)` with a fresh 5s `context.WithTimeout` — survives caller-ctx cancellation. Order: Start fails → fail-loud return BEFORE deferred Stop registers, so Stop does NOT fire on Start failure (pinned at `main_test.go:2554-2556`).

`runServe` invoked at `cmd/till/main.go:2354` with `liveWaitBroker` threaded through. Factory at `main.go:66-74` returns `dispatcher.NewDispatcher(svc, broker, dispatcher.Options{})`.

---

## Acceptance 8 — 7 named tests + integration test in cmd/till

**PROVEN — exceeds bar.** Builder shipped 8 named tests in `subscriber_test.go` (1 more than required) + 2 integration tests in `cmd/till/main_test.go` (1 more than required). All verified-green via `mage test-pkg ./internal/app/dispatcher` (150/150 pass) and `mage test-pkg ./cmd/till` (229/229 pass).

`subscriber_test.go` — verified-green:
- `TestDispatcherStartSpawnsPerProjectSubscribers` (line 97) — 3 projects, 3 events, asserts `counter.count >= 3` + `ListProjects calls = 1`.
- `TestDispatcherStartTriggersRunOnceOnEvent` (line 157) — 1 project, 1 event, asserts RunOnce within 150ms.
- `TestDispatcherStopCancelsAllSubscribers` (line 205) — 2 projects, asserts WaitGroup drains within 50ms.
- `TestDispatcherStartIdempotent` (line 246) — second Start → `errors.Is(err, ErrAlreadyStarted)`.
- `TestDispatcherStopIdempotent` (line 278) — second Stop nil + never-started Stop nil.
- `TestDispatcherStartStopConcurrent` (line 325) — concurrent Start+Stop, no deadlock, terminal state stopped.
- `TestDispatcherStartRejectsListProjectsError` (line 383) — wrapped `ListProjects` error, `started == false`.
- `TestDispatcherStartRejectsNilProjectsLister` (line 408) — `ErrInvalidDispatcherConfig`.
- `TestHandleSubscriberEventInvokesRunOnceForEachEligibleItem` (line 432) — explicit per-event drain pin: 2 eligible items → 2 RunOnce invocations.

`cmd/till/main_test.go` — verified-green:
- `TestRunServeIntegrationStartsAndStopsDispatcher` (line 2478) — stub factory + stub serveCommandRunner; ctx cancelled after Start; asserts `startCalls == 1 && stopCalls == 1`.
- `TestRunServeIntegrationFailsOnDispatcherStartError` (line 2522) — synthetic Start error; asserts `errors.Is(err, startErr)`, `serveCommandRunner` NOT invoked, `stopCalls == 0`.

Replaced tests in `dispatcher_test.go`:
- `TestStartRequiresWalker` (line 345) — replaces `TestStartReturnsNotImplemented`; asserts nil-walker rejection.
- `TestStopOnUnstartedDispatcherReturnsNil` (line 362) — replaces `TestStopReturnsNotImplemented`; asserts idempotent Stop.

None are name-only stubs; each carries assertions and a fail message.

---

## Acceptance 9 — Doc-comment notes 4a.19 spawn-stub dependency (Drop 4c F.7 replaces)

**PROVEN.** `subscriber.go:22-25`: "Spawn invocation today goes through the existing 4a.19 spawn stub (BuildSpawnCommand). Drop 4c F.7 lands the real Claude-Code spawn pipeline; the subscriber loop survives that swap unchanged because it only consumes RunOnce's DispatchOutcome contract." Cross-doc'd at `subscriber.go:174` (handleSubscriberEvent doc) and `cmd/till/main.go:2610-2611` (`runServe` doc).

---

## Mage Verification

- `mage test-pkg ./internal/app/dispatcher` → 150/150 pass (was 142 baseline pre-4b.7 per worklog math; +8 net new + 2 replaced — replacement net = +8).
- `mage test-pkg ./cmd/till` → 229/229 pass (+2 from baseline 227).

Coverage per worklog: dispatcher 73.2%, cmd/till 75.5% — both above 70% gate. Worklog claims `mage ci` 21/21 packages green; not re-run by QA-proof but per-package targets validate the specific 4b.7 surface.

---

## Summary

All 9 acceptance items PROVEN via direct code citation + verified-green tests. No GAPs. No WRONGs. The `hylla_reingest` drop is correctly executed (no file). Lifecycle state machine is race-safe (mutex held only on state transitions, never across external calls). `runServe` wiring is fail-loud with bounded 5s shutdown. Builder shipped one more test than the spec required, all carrying real assertions.

**Verdict: PASS.**
