# Drop 3 Droplet 3.15 — Builder QA Proof Review

**Droplet:** 3.15 — Rewrite/delete old `KindTemplate` + `AllowedParentScopes` + `AllowsParentScope` API + delete `till.kind upsert` MCP wire + CLI
**Reviewed:** 2026-05-02
**Net diff:** ~663 line deletions across 18 files (`git diff --stat HEAD`).
**Verdict:** PASS

## 1. Required Proof Checks

- 1.1 **`KindTemplate`, `KindTemplateChildSpec`, `AllowedParentScopes` field, `AllowsParentScope` method deleted from `internal/domain/kind.go`** — verified. `kind.go:101-110` `KindDefinition` struct now carries only `ID`, `DisplayName`, `DescriptionMarkdown`, `AppliesTo`, `PayloadSchemaJSON`, timestamps. `KindDefinitionInput` (`:113-119`) carries the same minimal surface. No `KindTemplate*` types defined; no `AllowsParentScope` method; no `normalizeKindTemplate` / `normalizeKindParentScopes`. `bytes` import is also gone (file imports only `encoding/json`, `fmt`, `slices`, `strings`, `time`).
- 1.2 **`ErrInvalidKindTemplate` deleted from `internal/domain/errors.go`** — verified. `errors.go:20-26` lists `ErrInvalidKind`, `ErrInvalidKindID`, `ErrInvalidKindAppliesTo`, `ErrInvalidKindPayload`, `ErrInvalidKindPayloadSchema`, `ErrKindNotAllowed` — no `ErrInvalidKindTemplate`. Consumer site `internal/adapters/server/common/app_service_adapter.go:647` `mapAppError` removed the corresponding `errors.Is` line.
- 1.3 **`KindCatalog.AllowsNesting` wrapper added to `internal/templates/catalog.go`** — verified. `catalog.go:106-108` defines `func (c KindCatalog) AllowsNesting(parent, child domain.Kind) (allowed bool, reason string)` and delegates to `Template{Kinds: c.Kinds}.AllowsNesting(parent, child)`. The canonical resolver lives in `internal/templates/nesting.go:35`.
- 1.4 **Resolver in `internal/app/kind_capability.go` uses catalog-first lookup with `domain.IsValidKind` fallback** — verified. `kind_capability.go:571-623` `resolveActionItemKindDefinition` calls `lookupKindDefinitionFromCatalog` first; on miss falls back to `s.repo.GetKindDefinition`; on `ErrNotFound` synthesizes from `domain.IsValidKind` (`:598-601`). Parent-nesting gate at `:607-614` runs `catalog.AllowsNesting(parent.Kind, kindID)` only on catalog-hits — pre-Drop-3 fallback rows recorded universal-allow already, so the missing gate is functionally equivalent (documented at `:563-566`).
- 1.5 **`internal/app/snapshot.go` `SnapshotKindDefinition` no longer carries `AllowedParentScopes` / `Template`** — verified. `snapshot.go:95-104` struct dropped both JSON tags. `snapshotKindDefinitionFromDomain` (`:1099-1114`) and `SnapshotKindDefinition.toDomain` (`:1353-1369`) no longer set those fields. Pre-MVP rule honored — pre-Drop-3 snapshots silently drop those fields on import (no migration logic).
- 1.6 **`internal/adapters/server/common/mcp_surface.go` `UpsertKindDefinitionRequest` no longer carries the deleted fields** — verified. `mcp_surface.go:259-265` struct exposes `ID`, `DisplayName`, `DescriptionMarkdown`, `AppliesTo []string`, `PayloadSchemaJSON`. Doc comment (`:252-258`) records the wire-surface deletion.
- 1.7 **`extended_tools.go` `till.kind` tool exposes `operation=list` only; `till.upsert_kind_definition` legacy alias deleted** — verified. `extended_tools.go:1657-1681` registers `till.kind` with `mcp.Enum("list")` only and a `default` branch returning `invalid_request`. The `till.list_kind_definitions` legacy alias remains (`:1683-1701`), but `till.upsert_kind_definition` is fully gone (`:1703-1707` doc comment confirms intent). `extended_tools_test.go:1516-1519, :1535-1537` assert it is absent from BOTH default and legacy surfaces.
- 1.8 **`repo.go` `kind_catalog` columns reduced; boot-seed deleted; FK dropped** — verified. `repo.go:307-316` `CREATE TABLE kind_catalog` carries only `id, display_name, description_markdown, applies_to_json, payload_schema_json, created_at, updated_at, archived_at` — no `allowed_parent_scopes_json` and no `template_json`. `repo.go:317-332` `project_allowed_kinds` no longer has `FOREIGN KEY(kind_id) REFERENCES kind_catalog(id)`. No `seedKindCatalog` / `builtinKindDefinitions` / `defaultKindCatalogSeed` function exists in the file.
- 1.9 **`repo_test.go` `TestRepositoryFreshOpenKindCatalog*` tests deleted** — verified. `repo_test.go:2598-2604` is a tombstone comment in place of the deleted test bodies. `git grep TestRepositoryFreshOpenKindCatalog -- '*.go'` returns only that comment. Equivalent assertions live in `internal/templates/embed_test.go` per droplet 3.14 (95.7 % coverage on `internal/templates`).
- 1.10 **`cmd/till/main.go` `till kind upsert` subcommand deleted; read-only retained** — verified. `main.go:887-972` registers `kindCmd` with only `kindListCmd` + `kindAllowlistCmd` (`list/set`). No `kindUpsertCmd` definition. `main.go:883-886` doc comment records the deletion. `main_test.go:612-619, :620-639` golden help-string tests assert `list` + `allowlist` are present (no `upsert`).
- 1.11 **LSP `findReferences` on all deleted symbols returns 0 hits** — verified via `grep -rEn "^(type|func) .*(KindTemplateChildSpec|KindTemplate\b|AllowsParentScope|normalizeKindTemplate|normalizeKindParentScopes|validateKindTemplateExpansion|ErrInvalidKindTemplate)"`: only `mergeActionItemMetadataWithKindTemplate` survives (intentional pass-through stub at `kind_capability.go:869`, plan §droplet 3.15 line 364 calls this out — `mergeActionItemMetadataWithKindTemplate` is rewritten to return `base, nil` with the legacy body deleted; the function name stays so call sites compile). Field-level `AllowedParentScopes` and method `AllowsParentScope` references appear only in doc comments.
- 1.12 **`mage ci` GREEN** — verified locally. 1788/1788 tests across 20 packages, all packages ≥ 70 % coverage (`internal/templates` 95.7 %, `internal/app` 71.8 %, `internal/domain` 79.9 %, `internal/adapters/storage/sqlite` 75.6 %, `cmd/till` 75.9 %, `internal/adapters/server/common` 73.1 %, `internal/adapters/server/mcpapi` 73.0 %, `internal/tui` 71.0 %). Build target green.
- 1.13 **No `tc := tc` introduced by this droplet** — verified. `git diff HEAD | grep -E "^\+.*tc := tc"` returns empty. Pre-existing `tc := tc` lines in `cmd/till/main_test.go:739, :2070, :3191`, `internal/app/schema_validator_test.go:87, :139` are not part of droplet 3.15's diff.
- 1.14 **Pre-MVP rule honored** — verified. Vestigial column drops (`allowed_parent_scopes_json`, `template_json`) are real schema changes; doc comment in `repo.go:289-306` instructs dev to fresh-DB. PLAN.md droplet 3.15 acceptance line 382 explicitly calls for `dev DELETEs ~/.tillsyn/tillsyn.db BEFORE mage ci`. No migration code introduced (no `till migrate`, no SQL backfill).

## 2. Verification Gates

- 2.1 **`mage format .`** — clean. Re-running the formatter produced no diff.
- 2.2 **`mage ci`** — GREEN. Full output captured locally; 1788/1788 tests pass, build target succeeds, coverage threshold met.
- 2.3 **LSP-equivalent `findReferences`** — 0 hits on every deleted symbol. Only intentional surviving symbol is `mergeActionItemMetadataWithKindTemplate` (now a pass-through, name retained per plan).
- 2.4 **No `tc := tc`** — droplet diff did not introduce any.
- 2.5 **Schema column drops** — `kind_catalog.allowed_parent_scopes_json` + `kind_catalog.template_json` + FK from `project_allowed_kinds.kind_id` to `kind_catalog(id)` all gone from `repo.go`.
- 2.6 **Wire/CLI deletions** — `till.kind operation=upsert` rejected with `invalid_request`; `till.upsert_kind_definition` not registered; `till kind upsert` cobra command absent. Tests assert all three.

## 3. Findings

- 3.1 None blocking.
- 3.2 Trace-completeness note (informational, not a finding): builder did not touch `internal/tui/model.go` or `internal/tui/model_test.go` despite PLAN.md droplet 3.15 line 375 listing them as starting-point lower-bound paths. Verification: `grep -rn "AllowedParentScopes\|AllowsParentScope\|KindTemplate\|template_json" internal/tui/` returns empty, so the LSP-driven sweep correctly determined nothing in `internal/tui` actually referenced the deleted symbols (drop-1.75 collapse `8d624c7` and the unused-symbols pass `43b5b10` had already cleaned them out). The lower-bound-not-closed-enumeration disclaimer in PLAN.md line 377 explicitly authorizes this.
- 3.3 Trace-completeness note (informational): `internal/app/service.go:623` still calls `mergeActionItemMetadataWithKindTemplate` — this is correct per plan line 364 (the function is intentionally retained as a pass-through during the transition). A future drop will fold it into the caller; no action required here.

## 4. Verdict Summary

**PASS.** All 14 required proof checks satisfied. All six verification gates green. The diff cleanly deletes the legacy `KindTemplate` / `AllowedParentScopes` / `AllowsParentScope` API across domain, app, adapters (server/common, server/mcpapi, storage/sqlite), and CLI; rewrites the kind-capability resolver to catalog-first with `domain.IsValidKind` fallback; deletes the `till.kind operation=upsert` wire surface, the `till.upsert_kind_definition` legacy alias, and the `till kind upsert` CLI subcommand; drops the vestigial schema columns; and retires the boot-seed regression tests in favor of `internal/templates/embed_test.go`. `mage ci` GREEN locally, 1788/1788 tests, all packages ≥ 70 %.

## 5. Hylla Feedback

Reviewed via `Read`, `Grep`, and `Bash` (git diff/grep). Did not invoke Hylla MCP — the verification surface is dominated by raw deletion enumeration and `git diff` shape-checks where structured grep is the lowest-friction tool. No Hylla queries attempted, no Hylla misses to report.
