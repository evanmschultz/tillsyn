# Droplet 3.8 — Builder QA Proof

- **Reviewer:** go-qa-proof-agent
- **Droplet:** 3.8 — TOML schema structs (NEW `internal/templates` package)
- **Verdict:** PASS
- **Date:** 2026-05-02

## 1. Required Proof Checks

### 1.1 `Template` struct — 4 fields, snake_case TOML tags

`internal/templates/schema.go` lines 38–56 declare:

- `SchemaVersion string` — tag `toml:"schema_version"`.
- `Kinds map[domain.Kind]KindRule` — tag `toml:"kinds"`.
- `ChildRules []ChildRule` — tag `toml:"child_rules"`.
- `AgentBindings map[domain.Kind]AgentBinding` — tag `toml:"agent_bindings"`.

All four match the PLAN.md droplet-3.8 acceptance contract. All TOML tags are `snake_case`. Status: PASS.

### 1.2 `KindRule` struct — 4 fields per L7

Lines 61–82 declare:

- `Owner string` (line 67) — doc explicitly notes per L7 the value `"STEWARD"` marks STEWARD-owned kinds; other identifiers accepted verbatim.
- `AllowedParentKinds []domain.Kind` (line 72).
- `AllowedChildKinds []domain.Kind` (line 76).
- `StructuralType domain.StructuralType` (line 81).

All TOML tags `snake_case`. Status: PASS.

### 1.3 `ChildRule` struct — 5 fields

Lines 88–110 declare:

- `WhenParentKind domain.Kind` (line 91).
- `CreateChildKind domain.Kind` (line 95).
- `Title string` (line 98).
- `BlockedByParent bool` (line 103).
- `WhenParentStructuralType domain.StructuralType` (line 109).

All TOML tags `snake_case`. Status: PASS.

### 1.4 `AgentBinding` struct — all 11 fields per PLAN.md § 19.3 lines 1653–1656

Lines 120–165 declare exactly the 11 fields enumerated in PLAN.md:

| # | Field | Type | TOML tag | Line |
|---|-------|------|----------|------|
| 1 | `AgentName` | `string` | `agent_name` | 123 |
| 2 | `Model` | `string` | `model` | 127 |
| 3 | `Effort` | `string` | `effort` | 132 |
| 4 | `Tools` | `[]string` | `tools` | 137 |
| 5 | `MaxTries` | `int` | `max_tries` | 141 |
| 6 | `MaxBudgetUSD` | `float64` | `max_budget_usd` | 145 |
| 7 | `MaxTurns` | `int` | `max_turns` | 148 |
| 8 | `AutoPush` | `bool` | `auto_push` | 152 |
| 9 | `CommitAgent` | `string` | `commit_agent` | 156 |
| 10 | `BlockedRetries` | `int` | `blocked_retries` | 160 |
| 11 | `BlockedRetryCooldown` | `time.Duration` | `blocked_retry_cooldown` | 164 |

`BlockedRetryCooldown` is correctly typed `time.Duration` (not `string`). The package-level `import "time"` (line 17) supports it. Status: PASS.

### 1.5 `SchemaVersionV1 = "v1"` constant exported

Line 26: `const SchemaVersionV1 = "v1"`. Doc comment lines 22–25 documents the gate role + future-bump policy. Status: PASS.

### 1.6 No `GateRule` Go struct (per L6 defer)

`rg -n GateRule internal/templates/` returns exactly one match — line 32, a doc-comment on `Template` explaining the deliberate absence:

> *"The struct intentionally has no GateRules field: per Drop 3 fix L6 and finding 5.B.11, the [gate_rules] TOML table is reserved for forward-compat and consumed by Drop 4's dispatcher."*

No struct definition, no `Template.GateRules` field, no behavior. Status: PASS.

### 1.7 No `GateRules` field on `Template`

Confirmed by 1.6 above and by direct read of `Template` (lines 38–56) — only the four contracted fields appear. Status: PASS.

### 1.8 Doc comments on package + every type + every field + the constant

- Package doc: lines 1–14 (canonical-source pointers included).
- `SchemaVersionV1` constant: lines 22–25.
- `Template` type + every field: lines 28–37 + per-field comments at 39, 44, 49, 53.
- `KindRule` type + every field: lines 58–60 + per-field comments at 62, 70, 74, 78.
- `ChildRule` type + every field: lines 84–87 + per-field comments at 89, 93, 97, 100, 105.
- `AgentBinding` type + every field: lines 112–119 + per-field comments at 121, 125, 129, 134, 139, 143, 147, 150, 154, 158, 162.

Every top-level declaration is documented, including the per-field comments PLAN.md required. Status: PASS.

### 1.9 All TOML tags use `toml:"snake_case_name"`

Every declared `toml:"..."` tag inspected: all in `lower_snake_case` form, no camelCase, no kebab-case. Status: PASS.

### 1.10 `TestSchemaVersionV1Constant` asserts string equality

`schema_test.go` lines 17–21: `if SchemaVersionV1 != "v1" { t.Fatalf(...) }`. Direct string-equality assertion. Status: PASS.

### 1.11 `TestZeroValuesConstructible` covers all 4 struct types

`schema_test.go` lines 27–38 declares zero-value variables for every type (`Template`, `KindRule`, `ChildRule`, `AgentBinding`) and references one field on each so the compiler does not strip the declaration. Status: PASS.

### 1.12 `TestTemplateTOMLRoundTrip` populates all 4 sub-types + uses `pelletier/go-toml/v2` + asserts `reflect.DeepEqual`

`schema_test.go` lines 49–99:

- Imports `toml "github.com/pelletier/go-toml/v2"` (line 8) and `reflect` (line 4).
- Populates `Template.SchemaVersion`, `Template.Kinds[domain.KindBuild]` (every `KindRule` field non-empty including `AllowedChildKinds` slice with 2 elements), `Template.ChildRules` (one element with every field set), `Template.AgentBindings[domain.KindBuild]` (every one of the 11 `AgentBinding` fields populated, including `BlockedRetryCooldown: 30 * time.Second`).
- Marshals via `toml.Marshal`, unmarshals via `toml.Unmarshal`, asserts via `reflect.DeepEqual(original, decoded)`.
- Failure messages dump both values + the encoded TOML bytes — diagnosable.

`go.mod` line 66 confirms `github.com/pelletier/go-toml/v2 v2.2.4` as a direct dependency. Status: PASS.

### 1.13 No `tc := tc`

`rg -n "tc := tc" internal/templates/` returns no matches. Go 1.26.1 (go.mod line 3) makes the loop-variable copy unnecessary. Status: PASS.

## 2. Verification Gates

### 2.1 `mage format ./internal/templates`

```
[RUNNING] Formatting Go sources
[SUCCESS] Formatted Go sources
```

Status: GREEN.

### 2.2 `mage testPkg ./internal/templates`

```
[RUNNING] Running go test ./internal/templates
[PKG PASS] github.com/evanmschultz/tillsyn/internal/templates (0.00s)

Test summary
  tests: 3
  passed: 3
  failed: 0
  skipped: 0
  packages: 1
  pkg passed: 1
  pkg failed: 0
  pkg skipped: 0
```

3/3 tests pass: `TestSchemaVersionV1Constant`, `TestZeroValuesConstructible`, `TestTemplateTOMLRoundTrip`. Status: GREEN.

### 2.3 `rg -n GateRule internal/templates/`

Returns exactly one match — `schema.go:32` — which is the Doc-comment explaining the deliberate absence. No struct definition. Status: GREEN.

### 2.4 `rg -n "tc := tc" internal/templates/`

Returns zero matches. Status: GREEN.

## 3. Findings

No blocking findings. No nits raised — the deliverable is tight and matches the PLAN.md droplet-3.8 contract exactly.

The skeletal `AgentBinding` is correctly flagged in its own doc-comment block (lines 112–119) referencing finding 5.B.17 and the N4 commit, so a future reader of the file knows why no validators ship in this droplet — this is exactly the post-droplet-3.8 hand-off contract the plan asked for.

## 4. Verdict Summary

**PASS.**

Every droplet-3.8 acceptance criterion is supported by direct evidence in `internal/templates/schema.go` and `internal/templates/schema_test.go`:

- All four struct types declared with the exact field counts and types specified.
- All 11 `AgentBinding` fields present (with `BlockedRetryCooldown` correctly typed `time.Duration`).
- `SchemaVersionV1` constant exported with the correct value `"v1"`.
- L6 `GateRule` defer honored — only a doc-comment mentions the term.
- Every top-level decl + every field documented with a Go doc comment.
- Three tests cover compile-time construction, zero-value smoke, and full TOML round-trip via `pelletier/go-toml/v2 v2.2.4`.
- `mage format` clean; `mage testPkg ./internal/templates` 3/3 green.

No blockers. No nits. Cleared to proceed to droplet 3.9.
