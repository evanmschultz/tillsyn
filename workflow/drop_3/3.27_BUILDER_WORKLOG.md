# DROP 3 — Droplet 3.27 Builder Worklog

**Droplet:** 3.27 — In-repo legacy-vocabulary sweep (active canonical docs only).
**Mode:** filesystem-MD only. No code changes. No commits. No pushes.
**State at handback:** edits applied in-place across in-repo + outside-repo paths; awaiting orch-mediated dev approval before commit.

## Audit-Gap Acceptance (per finding 5.D.10 / Option C)

The `~/.claude/` edits — global agent files, the global `~/.claude/CLAUDE.md`, the global skill files, and the slash-command files — are **not git-tracked** by this repo. Per droplet 3.27 acceptance criterion (lock M5):

> The workflow trades off git-tracked permanence for adopter-skill universality. Future maintainers reading this worklog see what edits landed; reconstruction against future filesystem state requires a manual diff.

This worklog is the only durable record of the outside-repo rewrites. Future drift between this worklog and the live `~/.claude/` tree is expected and accepted — the agent files evolve as the cascade matures.

## Droplet 3.27 — Round 1

- **Builder:** `go-builder-agent` (model: opus per memory rule "Opus Builders Pre-Dogfood").
- **Started:** 2026-05-03.
- **Mode:** docs-only sweep across active canonical docs.

### Sweep methodology

Four passes per droplet spec:

1. `rg "drops all the way down|tasks all the way down"` over in-scope paths.
2. `rg "action_item|action-item|action item|ActionItem"` reviewed line-by-line; rewrite where prose conflates kind / role / structural_type, leave correct schema-mapping references as-is.
3. `rg "\bslice\b|build-task|plan-task|qa-check"` rewritten where they appear in **active prose** to use the closed 12-kind enum (`build`, `plan`, `build-qa-proof`, `build-qa-falsification`, `plan-qa-proof`, `plan-qa-falsification`, `research`, `closeout`, `commit`, `refinement`, `discussion`, `human-verify`). **Audit-trail mentions of historical kinds in change-log / historical-narrative sections stay unchanged** — they describe the pre-Drop-1.75 world correctly.
4. Verify `metadata.role` × `metadata.structural_type` × `kind` orthogonal-axes coherence in the four primary always-read docs.

### Per-file inventory (in-repo, git-tracked)

| Path                                                                | P1 | P2  | P3 | P4 | Rewrites | Left-as-is | Notes |
|---------------------------------------------------------------------|----|-----|----|----|----------|------------|-------|
| `main/CLAUDE.md`                                                    | 0  | 29  | 2  | 0  | 2        | 29         | All P2 hits are correct schema/MCP references (`till.action_item`, `action_items.kind`, `action_items.scope`). P3 hits (lines 87, 125): `commit-every-build-task` → `commit-every-build`; `sibling build-tasks` → `sibling build action items`. Pass 4: not a finding here — `metadata.structural_type` is in-flight (Drop 3 itself); CLAUDE.md correctly reflects the post-Drop-1.75 landed state. |
| `main/PLAN.md`                                                      | 0  | 164 | 4  | 0  | 1        | 167        | P2: all schema-correct uses (`action_items.kind`, `sub-action-items`, etc.). P3: line 108 `commit-every-build-task` → `commit-every-build` rewritten; lines 123, 127, 1874 `qa-check`/`build-actionItem` are HISTORICAL narrative (pre-Drop-1.75 state, "Resolution" footer) — left unchanged. P4: orthogonal-axes design well-described in §1.4 + §19.10 — coherent. |
| `main/WIKI.md`                                                      | 0  | 3   | 0  | —  | 0        | 3          | **Pointer-only mode per spec** (canonical glossary owned by 3.6). Lines 26, 30 contain stale retired-vocab (`kind='actionItem'`, `qa-check`, `build-actionItem`, etc. in pre-Drop-2 prose) — flagged for follow-up but NOT rewritten in this droplet. |
| `main/STEWARD_ORCH_PROMPT.md`                                       | 0  | 16  | 4  | 0  | 4        | 16         | P3 rewrites: line 100 (DISCUSSIONS #16 type-drop migration — rewritten to mark superseded by Drop-1.75 closed enum); line 106 (`sibling build-tasks` → `sibling build action items`); line 375 (`dispatch build-tasks` → `dispatch build action items`); line 384 (`creates child build-tasks` → `creates child build action items`). Line 111 `refactor build-actionItem` left as-is — historical Drop 1 / Drop 1.5 coordination audit trail. |
| `main/AGENT_CASCADE_DESIGN.md`                                      | 0  | 2   | 0  | —  | 0        | 2          | Both P2 hits are correct Go domain-type references (`ActionItem` struct, `ActionItem.blocked_by` field). |
| `main/AGENTS.md`                                                    | 0  | 0   | 0  | —  | 0        | 0          | Clean. |
| `main/README.md`                                                    | 0  | 11  | —  | —  | 0        | 11+        | **Scope-deferred.** README.md uses heavy retired vocabulary (`actionItem`, `subtask`, `build-actionItem`, `qa-check`, `commit-and-reingest`) throughout its "Tillsyn integration framing" section. This is product-vocabulary describing how templates ARE structured, not cascade-vocabulary describing how the action_items tree IS structured. Rewriting README.md aggressively would be a README overhaul, not a vocab sweep. **Recommend a follow-up droplet to overhaul README.md** rather than partial rewrite here. |
| `main/CONTRIBUTING.md`                                              | 0  | 0   | 0  | —  | 0        | 0          | Clean. |
| `main/SEMI-FORMAL-REASONING.md`                                     | 0  | 0   | 0  | —  | 0        | 0          | Clean. |
| `main/HYLLA_WIKI.md`                                                | 0  | 0   | 0  | —  | 0        | 0          | Clean. |
| `main/tillsyn-project.md`                                           | 0  | 0   | 0  | —  | 0        | 0          | Clean. |
| `main/DROP_1_75_ORCH_PROMPT.md`                                     | 0  | 3   | 0  | —  | 0        | 3          | All hits are correct schema references (`{project, action_item}` collapse, `chore(scripts)` commit example). |
| `main/workflow/example/CLAUDE.md`                                   | 0  | 1   | 0  | —  | 0        | 1          | Single hit is correct prose ("no Tillsyn, no action-item DB"). |
| `main/workflow/example/drops/WORKFLOW.md`                           | 0  | 2   | 0  | —  | 0        | 2          | Both hits are correct: `ActionItem` domain-field reference + "Tillsyn action items, no MCP dispatcher" prose. |
| `main/workflow/example/drops/_TEMPLATE/PLAN.md`                     | 0  | 0   | 0  | —  | 0        | 0          | Clean. |
| `main/workflow/example/drops/_TEMPLATE/BUILDER_WORKLOG.md`          | 0  | 0   | 0  | —  | 0        | 0          | Clean. |
| `main/workflow/example/drops/_TEMPLATE/CLOSEOUT.md`                 | 0  | 0   | 0  | —  | 0        | 0          | Clean. |
| `main/workflow/example/drops/DROP_N_EXAMPLE/PLAN.md`                | 0  | 0   | 0  | —  | 0        | 0          | Clean. |
| `main/workflow/example/drops/DROP_N_EXAMPLE/BUILDER_WORKLOG.md`     | 0  | 0   | 0  | —  | 0        | 0          | Clean. |
| `main/workflow/example/drops/DROP_N_EXAMPLE/CLOSEOUT.md`            | 0  | 0   | 0  | —  | 0        | 0          | Clean. |
| `main/workflow/example/drops/DROP_N_EXAMPLE/PLAN_QA_PROOF.md`       | 0  | 0   | 0  | —  | 0        | 0          | Clean. |

### Per-file inventory (outside-repo, NOT git-tracked, recorded here only)

| Path                                                                | P1 | P2  | P3  | Rewrites | Left-as-is | Notes |
|---------------------------------------------------------------------|----|-----|-----|----------|------------|-------|
| `~/.claude/CLAUDE.md`                                               | 0  | n/a | 4   | 4        | n/a        | Per finding 5.D.8 lock — promoted to first-class in-scope edit. Rewrites: line 9 `slice-by-slice` → `drop-by-drop`; line 10 `build-task` reference re-cast as `build` action item generating QA children; line 121 `build-task` → `build` action item; line 147 `build-task` → `build` action item. |
| `~/.claude/agents/go-builder-agent.md`                              | 0  | n/a | 7   | 7        | n/a        | Cascade Binding diagram rewritten (slice/plan-task/build-task/qa-check → plan/build with closed 12-kind enum). All `Slice N` references → `Drop N`. All `pre-/post-Slice-1` → `pre-/post-Drop-1`. `slice end` → `drop end`. |
| `~/.claude/agents/go-planning-agent.md`                             | 0  | n/a | 24  | ~16      | n/a        | Heaviest pollution. Cascade Binding diagram rewritten. All `slice` (parent-scope), `plan-task` (own-kind), `build-task` (child-kind), `Slice N` references corrected. The agent now binds to `plan` action items and emits `build` children. |
| `~/.claude/agents/go-qa-proof-agent.md`                             | 0  | n/a | 13  | ~12      | n/a        | Cascade Binding rewritten — agent now binds to `build-qa-proof` OR `plan-qa-proof` (parent-context disambiguates). All `qa-check`/`build-task`/`plan-task`/`Slice N` references rewritten. `task_id` → `action_item_id`. `slice end` → `drop end`. |
| `~/.claude/agents/go-qa-falsification-agent.md`                     | 0  | n/a | 13  | ~12      | n/a        | Same shape as proof agent (build-qa-falsification OR plan-qa-falsification binding). Cascade-vocabulary attacks subsection retains correct closed-enum vocabulary as already authored. |
| `~/.claude/agents/fe-builder-agent.md`                              | 0  | n/a | 0   | 0        | n/a        | Already clean (does not use cascade vocabulary). Word-bounded `\bslice\b` returns no hits; previous count of 5 was matching `task` — those are correct generic uses. |
| `~/.claude/agents/fe-planning-agent.md`                             | 0  | n/a | 11  | ~7       | n/a        | All `slice`/`plan-task` references rewritten to `plan` action items. `task_id` → `action_item_id`. |
| `~/.claude/agents/fe-qa-proof-agent.md`                             | 0  | n/a | 6   | ~4       | n/a        | Same shape as Go QA proof — `qa-check`/`build-task` references rewritten to closed-enum kinds. |
| `~/.claude/agents/fe-qa-falsification-agent.md`                     | 0  | n/a | 6   | ~4       | n/a        | Same shape as Go QA falsification. |
| `~/.claude/agents/go-research-agent.md`                             | 0  | n/a | 0   | 0        | n/a        | Clean — does not use retired cascade vocabulary. |
| `~/.claude/agents/fe-research-agent.md`                             | 0  | n/a | 0   | 0        | n/a        | Clean. |
| `~/.claude/skills/plan-from-hylla/SKILL.md`                         | 0  | n/a | 1   | 1        | n/a        | Line 56 `Proposed build-task split` → `Proposed build decomposition`. |
| `~/.claude/skills/plan-from-hylla/references/operating-rules.md`    | 0  | n/a | 0   | 0        | n/a        | Clean. |
| `~/.claude/skills/qa-proof-checker/SKILL.md`                        | 0  | n/a | 0   | 0        | n/a        | Clean. |
| `~/.claude/skills/qa-proof-checker/references/operating-rules.md`   | 0  | n/a | 0   | 0        | n/a        | Clean. |
| `~/.claude/skills/qa-falsification-checker/SKILL.md`                | 0  | n/a | 0   | 0        | n/a        | Clean. |
| `~/.claude/skills/qa-falsification-checker/references/operating-rules.md` | 0  | n/a | 0   | 0        | n/a        | Clean. |
| `~/.claude/skills/semi-formal-reasoning/SKILL.md`                   | 0  | n/a | 0   | 0        | n/a        | Clean. |
| `~/.claude/skills/semi-formal-reasoning/references/operating-rules.md` | 0 | n/a | 0  | 0        | n/a        | Clean. |
| `~/.claude/skills/go-project-bootstrap/SKILL.md`                    | 0  | n/a | 0   | 0        | n/a        | Clean. |
| `~/.claude/skills/go-project-bootstrap/references/template.md`      | 0  | n/a | 0   | 0        | n/a        | Clean. |
| `~/.claude/skills/fe-project-bootstrap/SKILL.md`                    | 0  | n/a | 0   | 0        | n/a        | Clean. |
| `~/.claude/skills/fe-project-bootstrap/references/template.md`      | 0  | n/a | 0   | 0        | n/a        | Clean. |
| `~/.claude/skills/tui-golden-review/SKILL.md`                       | 0  | n/a | 0   | 0        | n/a        | Clean. |
| `~/.claude/skills/tui-vhs-review/SKILL.md`                          | 0  | n/a | 0   | 0        | n/a        | Clean. |
| `~/.claude/commands/_conventions.md`                                | 0  | n/a | 0   | 0        | n/a        | Clean. |
| `~/.claude/commands/gopls-sync.md`                                  | 0  | n/a | 0   | 0        | n/a        | Clean. |
| `~/.claude/commands/plan-from-hylla.md`                             | 0  | n/a | 0   | 0        | n/a        | Clean. |
| `~/.claude/commands/qa-falsification.md`                            | 0  | n/a | 0   | 0        | n/a        | Clean. |
| `~/.claude/commands/qa-proof.md`                                    | 0  | n/a | 0   | 0        | n/a        | Clean. |
| `~/.claude/commands/select-checkout.md`                             | 0  | n/a | 0   | 0        | n/a        | Clean. |
| `~/.claude/commands/tillsyn-bootstrap.md`                           | 0  | n/a | 0   | 0        | n/a        | Clean. |

### Memory.md (review only)

`~/.claude/projects/-Users-evanschultz-Documents-Code-hylla-tillsyn/memory/MEMORY.md` — line 21 references the retired `"drops all the way down"` framing as **historical fact** ("Old framing RETIRED"). This is correct documentation of what was retired, not active assertion of retired vocabulary. **Left as-is per spec** (no edits unless rule actually retired).

### Sample rewrites

#### Sample 1: in-repo `main/CLAUDE.md`

Before:
```markdown
- **File- and package-level blocking** — sibling build-tasks sharing a file in `paths` OR a package in `packages` MUST have an explicit `blocked_by` between them.
```

After:
```markdown
- **File- and package-level blocking** — sibling `build` action items sharing a file in `paths` OR a package in `packages` MUST have an explicit `blocked_by` between them.
```

#### Sample 2: in-repo `main/STEWARD_ORCH_PROMPT.md` (DISCUSSIONS #16 disposition)

Before:
```markdown
- **Type-drop rename migration** (DISCUSSIONS #16) — coordinate the build-actionItem→build-drop, plan-actionItem→plan-drop, qa-check→qa-drop rename against in-flight items.
```

After:
```markdown
- **Type-drop rename migration** (DISCUSSIONS #16) — superseded by the Drop-1.75 closed 12-kind enum (`plan`, `build`, `plan-qa-proof`, `plan-qa-falsification`, `build-qa-proof`, `build-qa-falsification`, `research`, `closeout`, `commit`, `refinement`, `discussion`, `human-verify`); historical `build-actionItem` / `plan-actionItem` / `qa-check` were rewritten in place by `main/scripts/drops-rewrite.sql`. Item kept here as audit trail; no further coordination required.
```

#### Sample 3: outside-repo `~/.claude/agents/go-builder-agent.md` (Cascade Binding section)

Before:
```markdown
You bind to plan-items of kind `task` (a.k.a. `build-task`) — the leaf implementation work inside a `slice`. In the cascade tree:

```
slice
├── plan-task              (go-planning-agent)
│   ├── plan-qa-proof      (go-qa-proof-agent)
│   └── plan-qa-falsification (go-qa-falsification-agent)
└── task (build-task)      ← YOU
    ├── qa-proof           (go-qa-proof-agent)
    └── qa-falsification   (go-qa-falsification-agent)
```

Your two `qa-check` children (one proof, one falsification) are auto-created by template `child_rules` (post-Slice-3) or by the orchestrator manually (pre-Slice-3).
```

After:
```markdown
You bind to action items of kind `build` — the leaf implementation work under a `plan`. In the cascade tree (closed 12-kind enum, post-Drop-1.75):

```
plan                            (go-planning-agent)
├── plan-qa-proof               (go-qa-proof-agent)
├── plan-qa-falsification       (go-qa-falsification-agent)
└── build                       ← YOU
    ├── build-qa-proof          (go-qa-proof-agent)
    └── build-qa-falsification  (go-qa-falsification-agent)
```

Your two QA children (`build-qa-proof` + `build-qa-falsification`) are auto-created by template `child_rules` (post-Drop-3) or by the orchestrator manually (pre-Drop-3).
```

#### Sample 4: outside-repo `~/.claude/agents/go-qa-proof-agent.md` (Cascade Binding section)

Before:
```markdown
You bind to plan-items of kind `qa-check` in the proof role, in one of two positions:

- Under a `task` (build-task) — verifying an implementation after the builder's post-build gates clear.
- Under a `plan-task` — verifying a planner's decomposition.

```
(task | plan-task)
├── qa-proof             ← YOU
└── qa-falsification     (go-qa-falsification-agent)
```
```

After:
```markdown
You bind to action items of kind `build-qa-proof` or `plan-qa-proof` (closed 12-kind enum, post-Drop-1.75), in one of two positions:

- `build-qa-proof` under a `build` parent — verifying an implementation after the builder's post-build gates (`mage ci`, commit, push, drop-end Hylla reingest) clear.
- `plan-qa-proof` under a `plan` parent — verifying a planner's decomposition (paths, packages, acceptance criteria, blocker coverage).

```
(build | plan)
├── *-qa-proof            ← YOU
└── *-qa-falsification    (go-qa-falsification-agent)
```
```

#### Sample 5: outside-repo `~/.claude/CLAUDE.md` (line 9)

Before:
```markdown
The wiki is a living best-practice snapshot that changes slice-by-slice / release-by-release.
```

After:
```markdown
The wiki is a living best-practice snapshot that changes drop-by-drop / release-by-release.
```

### Pass 4 verification

Orthogonal-axes coherence (`metadata.role` × `metadata.structural_type` × `kind`):

- **`main/WIKI.md` § Cascade Vocabulary** (lines 38-70) — single canonical source for the orthogonal-axes design. Worked combinations enumerated. Atomicity rules listed.
- **`main/PLAN.md` § 19.10** (lines 1625-1651) — drop-3 design notes explicitly call out structural_type as orthogonal to role. WIKI is the canonical source; PLAN.md has pointers, not duplicates.
- **`main/CLAUDE.md` § Cascade Tree Structure** — describes the closed 12-kind enum + the post-Drop-2 `metadata.role` axis. Does NOT explicitly mention `metadata.structural_type` — correct, because structural_type is a Drop 3 in-flight feature (this droplet is part of Drop 3). The post-Drop-3 wrap-up sweep (3.28) will add the structural_type axis to CLAUDE.md once the Unit A enum lands.
- **`main/workflow/example/CLAUDE.md` line 27** — mandates that adopter projects pointer to WIKI's Cascade Vocabulary section and forbids local redefinition. Matches finding 5.D.14 / WIKI-canonical-glossary contract.

**Pass 4 verdict:** orthogonal-axes story is coherent across the three places it lives today. CLAUDE.md addition deferred to 3.28 by design.

### Mage CI smoke test

```
$ mage ci
[SUCCESS] Coverage threshold met
  All packages are at or above 70.0% coverage.
[SUCCESS] Built till from ./cmd/till
```

CI green. No Go code touched; smoke test confirms docs-only edits did not break anything.

### Things NOT done (intentional / deferred)

1. **`main/WIKI.md` lines 26, 30** — pointer-only mode per spec. Lines 26 ("a action item with `kind='actionItem'`") and 30 ("`kind_catalog` still lists `build-actionItem`, `subtask`, `qa-check`, …") use stale pre-Drop-2 vocabulary. Canonical glossary owned by 3.6; WIKI rewrite for those sections belongs there or in a follow-up. Flagged but not edited.
2. **`main/README.md` "Tillsyn integration framing" section** — uses heavy retired template-vocabulary (`build-actionItem`, `qa-check`, `commit-and-reingest`, etc.) describing how Tillsyn templates ARE structured. Rewriting it aggressively goes beyond a vocab sweep into a README overhaul. Recommend a follow-up droplet (dedicated README refresh) rather than partial rewrite.
3. **CLAUDE.md `metadata.structural_type` axis callout** — deferred to 3.28 (post-Drop-3 wrap-up sweep) per droplet 3.28 acceptance criteria.
4. **MEMORY.md line 21** — correctly documents `"drops all the way down" RETIRED`; not an active-prose hit.

## Hylla Feedback

N/A — droplet 3.27 touched non-Go files only (markdown sweep across CLAUDE.md / PLAN.md / WIKI.md / agent .md / skill .md / slash-command .md). Hylla today indexes Go files only; markdown is out of scope. All evidence-gathering used `Read` / `Grep` / `Glob` / `rg` directly per the project rule "Hylla Indexes Only Go Files Today." No Hylla query was issued; no fallback miss to log.
