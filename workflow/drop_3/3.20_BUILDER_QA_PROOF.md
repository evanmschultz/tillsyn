# Droplet 3.20 — Builder QA Proof

**Reviewer:** go-qa-proof-agent (opus)
**Date:** 2026-05-02
**Scope:** Template auto-gen consumer + STEWARD persistent parent seeds.
**Verdict:** **PASS**.

Per project rule (Hylla indexes only Go files; uncommitted Go files are not yet ingested), evidence below is gathered via `Read`, `rg`, and `mage` against the live working tree at `/Users/evanschultz/Documents/Code/hylla/tillsyn/main/`.

---

## 1. Required Proof Checks

### 1.1 `StewardSeed` struct + `Template.StewardSeeds` field exist with correct TOML tags

`internal/templates/schema.go`:

- Lines 109: `StewardSeeds []StewardSeed `toml:"steward_seeds"`` declared on `Template`.
- Lines 123–132: `StewardSeed` struct with `Title string `toml:"title"`` (line 127) and `Description string `toml:"description"`` (line 131).
- Doc comment (lines 87–109) explicitly references fix L3, droplet 3.14's option (a) deferral, and the open-ended-at-schema policy.

PASS.

### 1.2 `default.toml` has 6 `[[steward_seeds]]` entries with correct titles + descriptions

`internal/templates/builtin/default.toml` lines 295–317. Six entries in declaration order: `DISCUSSIONS`, `HYLLA_FINDINGS`, `LEDGER`, `WIKI_CHANGELOG`, `REFINEMENTS`, `HYLLA_REFINEMENTS`. Each has a non-empty `description`. The leading block comment (lines 267–293) documents materialization invariants (Owner=STEWARD, Persistent=true, DevGated=false, Kind=discussion, StructuralType=droplet, DropNumber=0) and the option-(a) encoding decision.

PASS.

### 1.3 Repository interface declares both methods; SQLite implements both using the 3.18 indexes

`internal/app/ports.go`:

- Line 39: `FindActionItemByOwnerAndTitle(ctx, projectID, owner, title) (domain.ActionItem, error)`. Doc cites `idx_action_items_owner_title (project_id, owner, title)` (3.18) and `ErrNotFound` semantics.
- Line 47: `ListActionItemsByDropNumber(ctx, projectID, dropNumber) ([]domain.ActionItem, error)`. Doc cites `idx_action_items_drop_number (project_id, drop_number, owner)` (3.18) and deterministic `created_at ASC, id ASC` ordering.

`internal/adapters/storage/sqlite/repo.go`:

- Lines 1428–1439: `FindActionItemByOwnerAndTitle` — `WHERE project_id = ? AND owner = ? AND title = ? ORDER BY id ASC LIMIT 1`. Selects index prefix `(project_id, owner, title)`.
- Lines 1449–1472: `ListActionItemsByDropNumber` — `WHERE project_id = ? AND drop_number = ? ORDER BY created_at ASC, id ASC`. Selects index prefix `(project_id, drop_number)`.

Both index references match the 3.18 schema (verified previously through commit `54437fa feat(sqlite): add owner dropnumber persistent devgated columns`).

PASS.

### 1.4 `seedStewardAnchors` creates 6 anchor nodes with `Owner="STEWARD"`, `Persistent=true`

`internal/app/auto_generate_steward.go`:

- Lines 88–134. Loads `tpl.StewardSeeds` via the `loadStewardSeedTemplate` seam (default = `templates.LoadDefaultTemplate()` → 6 seeds). Iterates seeds; for each:
  - Lines 105–112: idempotency check via `FindActionItemByOwnerAndTitle(projectID, "STEWARD", title)` — skips if exists, returns on non-`ErrNotFound`.
  - Lines 113–131: `CreateActionItem` with `Kind=KindDiscussion`, `StructuralType=StructuralTypeDroplet`, `Owner=stewardOwner` ("STEWARD"), `Persistent=true`, `DevGated=false`, `Title=trim(seed.Title)`, `Description=trim(seed.Description)`.
- Empty-title guard at line 102–104 returns explicit error.

Test `TestAutoGenSeeds6StewardPersistentParents` (auto_generate_steward_test.go lines 68–126) asserts exactly 6 STEWARD-owned items with the canonical 6 titles and verifies Owner / Persistent / DevGated / DropNumber / Kind / StructuralType / ParentID for each.

PASS.

### 1.5 `seedDropFindingsAndGate` creates 5 level_2 findings + refinements-gate confluence with correct `blocked_by`

`internal/app/auto_generate_steward.go`:

- Lines 152–158: `canonicalDropFindings` slice — 5 entries pairing anchor parent title with finding suffix (HYLLA_FINDINGS / LEDGER → LEDGER_ENTRY / WIKI_CHANGELOG → WIKI_CHANGELOG_ENTRY / REFINEMENTS → REFINEMENTS_RAISED / HYLLA_REFINEMENTS → HYLLA_REFINEMENTS_RAISED).
- Lines 207–294 `seedDropFindingsAndGate`:
  - Line 208: `if drop.DropNumber <= 0 { return nil }` — non-numbered short-circuit.
  - Lines 217–255: per-finding loop. For each plan: lookup anchor by title (returns `errStewardParentNotSeeded` on missing — line 220–222), idempotency check on finding title `DROP_<N>_<SUFFIX>`, then `CreateActionItem` with `ParentID=anchor.ID`, `Kind=KindDiscussion`, `StructuralType=StructuralTypeDroplet`, `Owner=stewardOwner`, `DropNumber=drop.DropNumber`, `Persistent=false`, `DevGated=false`. Collects created-finding IDs.
  - Lines 256–292: gate creation. Title `DROP_<N>_REFINEMENTS_GATE_BEFORE_DROP_<N+1>`. Idempotency check at line 257. `assembleRefinementsGateBlockedBy` (lines 306–324) lists every `drop_number=N` action item via `ListActionItemsByDropNumber`, merges with finding IDs through a map (de-dups), sorts deterministically. Gate created with `ParentID=drop.ID`, `Kind=KindPlan`, `StructuralType=StructuralTypeConfluence`, `Owner=stewardOwner`, `DropNumber=drop.DropNumber`, `Persistent=false`, `DevGated=true`, `Metadata.BlockedBy=blockedBy`.

Test `TestAutoGenSeedsLevel2FindingsOnNumberedDropCreation` (lines 169–317) asserts:
- All 5 expected suffix titles exist with the right anchor parents (lines 205–266).
- Gate exists with Kind=KindPlan, StructuralType=Confluence, Owner=STEWARD, DropNumber=N, DevGated=true, ParentID=drop.ID (lines 269–298).
- `gate.Metadata.BlockedBy` includes the drop's own ID + each finding ID, and excludes the gate's own ID (lines 302–316).

PASS.

### 1.6 Auto-gen wired into project creation + level_1 numbered drop creation

`internal/app/service.go`:

- Line 246–250: `EnsureDefaultProject` invokes `seedStewardAnchors` after column auto-creation, gated by `s.autoSeedSteward`.
- Lines 305–309: `CreateProjectWithMetadata` invokes `seedStewardAnchors` after `createDefaultColumns`, gated by both `s.autoProjectCols` AND `s.autoSeedSteward`.
- Lines 787–791: `CreateActionItem` invokes `seedDropFindingsAndGate` when `s.autoSeedSteward && actionItem.ParentID == "" && actionItem.DropNumber > 0` — i.e. level_1 numbered drops only. The level_2 findings created inside the helper carry `parent_id != ""` (under STEWARD anchors), so the recursion guard (parent_id == "" required) prevents infinite descent. Doc comment lines 779–786 explains this.

PASS.

### 1.7 `AutoSeedStewardAnchors` flag default false; production opts in

- `internal/app/service.go` lines 77–82: `ServiceConfig.AutoSeedStewardAnchors bool` (zero value false). Doc explicitly notes "Defaults false so existing tests that pre-allocate a fixed ID slice are not retro-broken".
- `internal/app/service.go` line 190: plumbed to `s.autoSeedSteward` field.
- `cmd/till/main.go` line 2247: `AutoSeedStewardAnchors: true` in production `app.NewService` call.
- All three auto-gen call sites guard on `s.autoSeedSteward` (verified above in 1.6).

PASS.

### 1.8 5 tests pass: 6 anchors, idempotent, level_2 findings, skip non-numbered, reject missing

`internal/app/auto_generate_steward_test.go`:

| # | Test | Lines | Coverage |
|---|------|-------|----------|
| 1 | `TestAutoGenSeeds6StewardPersistentParents` | 68–126 | 6 anchors with full domain shape |
| 2 | `TestAutoGenSeedsIdempotentOnReseed` | 132–162 | Re-running seed → still 6 rows |
| 3 | `TestAutoGenSeedsLevel2FindingsOnNumberedDropCreation` | 169–317 | 5 findings + gate + blocked_by |
| 4 | `TestAutoGenSeedsSkipsNonNumberedDrop` | 323–372 | drop_number=0 → no findings, no gate |
| 5 | `TestAutoGenSeedsRejectsMissingAnchor` | 378–413 | Missing anchor → `errStewardParentNotSeeded` |

All 5 pass individually under `mage testFunc`. Full suite (`mage testPkg internal/app`) reports 307 tests passed. The `withSeedTemplateFixture` helper substitutes a hand-built `Template` so tests do not depend on default.toml content drift.

PASS.

### 1.9 `mage ci` green; no `tc := tc`

`mage ci` output (verbatim summary):

- 1826 tests passed across 20 packages (0 failed, 0 skipped).
- Coverage threshold (70%) met on every package. Lowest is `internal/app` at 71.8%.
- `internal/templates` at 95.7%.
- Build of `./cmd/till` succeeds.

`rg "tc := tc"` against all 8 files this droplet touches returns zero hits.

PASS.

---

## 2. Verification Gates

| Gate | Command | Result |
|------|---------|--------|
| Full CI | `mage ci` | 1826/1826 pass; coverage threshold met |
| App pkg | `mage testPkg github.com/evanmschultz/tillsyn/internal/app` | 307 tests pass |
| Auto-gen test 1 | `mage testFunc … TestAutoGenSeeds6StewardPersistentParents` | 1 pass |
| Auto-gen test 2 | `mage testFunc … TestAutoGenSeedsIdempotentOnReseed` | 1 pass |
| Auto-gen test 3 | `mage testFunc … TestAutoGenSeedsLevel2FindingsOnNumberedDropCreation` | 1 pass |
| Auto-gen test 4 | `mage testFunc … TestAutoGenSeedsSkipsNonNumberedDrop` | 1 pass |
| Auto-gen test 5 | `mage testFunc … TestAutoGenSeedsRejectsMissingAnchor` | 1 pass |
| Format | inside `mage ci` | clean |
| Build | inside `mage ci` | binary built |

Note on `mage testFunc TestAutoGen` (no specific suffix): produced "0 tests" because the runner appends `^…$` anchors and there is no test literally named `TestAutoGen`. Each individual test name resolves correctly. Not a bug, just regex anchoring behavior of the focused runner.

---

## 3. Findings

None. The implementation matches every required check, including the doc-comment intent on idempotency, the explicit `errStewardParentNotSeeded` safety net, deterministic sorted blocked_by assembly, and recursion-prevention via the `ParentID == ""` call-site guard.

Two observations worth noting (not findings):

- **3.1 Defensive de-dup in `assembleRefinementsGateBlockedBy`.** The findings created above all carry `drop_number=N`, so `ListActionItemsByDropNumber` already returns them. The second loop merging `findingIDs` is belt-and-suspenders, and the doc comment at line 299–301 acknowledges this explicitly. Correct as written.
- **3.2 Empty-description tolerance.** `seedStewardAnchors` rejects empty title but only `TrimSpace`-passes empty description. The struct doc (schema.go lines 119–122) says description is "recommended but not strictly rejected at the schema layer." Consistent with stated policy.

---

## 4. Verdict Summary

**PASS** on all 9 required checks. mage ci 1826/1826 green. The droplet's implementation matches its claim end-to-end: the schema declares the seed type and field with correct TOML tags; default.toml carries the canonical 6 entries; the SQLite repo implements the two new lookup methods on top of the 3.18 indexes; the auto-generator seeds anchors at project creation and per-drop findings + a refinements-gate confluence at level_1 numbered-drop creation, with proper idempotency and a missing-anchor diagnostic. The `AutoSeedStewardAnchors` flag defaults false to preserve existing test contracts; production opts in.

No fixes required. The droplet may proceed to the falsification round and then to commit.
