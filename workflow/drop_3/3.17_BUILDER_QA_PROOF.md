# 3.17 BUILDER QA PROOF — Round 1

**Droplet:** 3.17 — `Owner` + `DropNumber` + `Persistent` + `DevGated` first-class fields on `ActionItem`
**Reviewer role:** build-qa-proof
**Verdict:** PASS
**Reviewed at:** 2026-05-02

Scope: verify the build for droplet 3.17 satisfies every acceptance criterion in `workflow/drop_3/PLAN.md` § 3.17, that the L13 reframe + finding 5.C.16 rollback-cost note are honored, and that the package gate (`mage testPkg ./internal/domain`) is green at 159/159.

## 1. Required Proof Checks

### 1.1 `ActionItem` struct gains four fields after `Irreducible`

`internal/domain/action_item.go:46-81` — `ActionItem` carries `Owner string` (`:55`), `DropNumber int` (`:68`), `Persistent bool` (`:75`), `DevGated bool` (`:81`). Insertion order is exactly "after `Irreducible` (`:45`), before `LifecycleState` (`:82`)." Doc comments cite `ta-docs/cascade-methodology.md` §11.2 for `Owner`, `DropNumber`, `Persistent`, and `DevGated` (`:54`, `:67`, `:74`, `:81`).

PLAN.md § 3.17 says "after `Role` (`:33`)" — that wording predates 3.2 landing `Irreducible`. The four new fields land after `Irreducible`, which itself sits after `Role`, so the contract holds: `Role → StructuralType → Irreducible → Owner → DropNumber → Persistent → DevGated → LifecycleState`. This matches the L9/L10 ordering on the methodology side (§11.2 lists them as ActionItem extensions of NodeBase) and the §11.2 cite is verbatim.

PASS.

### 1.2 `ActionItemInput` gains the same four fields

`internal/domain/action_item.go:127-146` — `ActionItemInput` carries `Owner string` (`:132`), `DropNumber int` (`:137`), `Persistent bool` (`:141`), `DevGated bool` (`:146`). Insertion order matches `ActionItem`'s. Doc comments cite §11.2 on each field. The Input block additionally documents the wire contract — Owner trim semantics (`:128-131`), DropNumber zero/negative semantics (`:133-136`).

PASS.

### 1.3 `NewActionItem` validation: Owner trim, DropNumber rejection, no bool validation

`internal/domain/action_item.go:250-260`:
- `in.Owner = strings.TrimSpace(in.Owner)` (`:255`) — trim only, no closed-enum check.
- `if in.DropNumber < 0 { return ActionItem{}, ErrInvalidDropNumber }` (`:258-260`) — negative reject.
- No statements touching `in.Persistent` or `in.DevGated` between the StructuralType block (`:243-249`) and the LifecycleState block (`:261`). Bools pass through untouched, matching the contract that they are domain primitives with no validation.

The doc-comment block at `:250-254` explicitly states the rule: "Owner is a free-form principal-name string. Trim only — no closed-enum membership check ... Persistent and DevGated are bools with no validation; their zero-value (false) is the dominant case." This matches PLAN.md acceptance criterion verbatim.

PASS.

### 1.4 Return statement assigns all four new fields

`internal/domain/action_item.go:297-326` — return literal includes `Owner: in.Owner` (`:306`), `DropNumber: in.DropNumber` (`:307`), `Persistent: in.Persistent` (`:308`), `DevGated: in.DevGated` (`:309`), positioned after `Irreducible: in.Irreducible` (`:305`) and before `LifecycleState: in.LifecycleState` (`:310`). Order in the return literal matches struct-field order.

PASS.

### 1.5 `errors.go` adds `ErrInvalidDropNumber` immediately after `ErrInvalidStructuralType`

`internal/domain/errors.go:29-30` — line 29 is `ErrInvalidStructuralType = errors.New("invalid structural type")`, line 30 is `ErrInvalidDropNumber = errors.New("invalid drop number")`. Adjacency confirmed; sentinel string is the canonical lowercase form matching every other sentinel in the var-block.

PASS.

### 1.6 `TestNewActionItemOwnerNormalization` covers the required cases

`internal/domain/domain_test.go:873-908` — table-driven over 5 cases:
- `empty` (input `""` → `""`)
- `whitespace collapses to empty` (input `"   "` → `""`)
- `steward round-trips` (input `"STEWARD"` → `"STEWARD"`)
- `surrounding whitespace trimmed` (input `"  STEWARD  "` → `"STEWARD"`)
- `arbitrary value round-trips` (input `"DEV"` → `"DEV"`)

Each test runs `NewActionItem` directly (not `NewActionItemForTest`) with `StructuralType: StructuralTypeDroplet` so the mandatory-StructuralType gate from 3.2 is satisfied. All five cases pass per `mage testPkg ./internal/domain`.

PASS.

### 1.7 `TestNewActionItemDropNumberValidation` covers 0 / positive / negative

`internal/domain/domain_test.go:914-951` — table-driven over 3 cases:
- `zero round-trips` (input `0`, `wantErr nil`, `wantDropNumber 0`)
- `positive round-trips` (input `5`, `wantErr nil`, `wantDropNumber 5`)
- `negative rejects` (input `-1`, `wantErr ErrInvalidDropNumber`)

The negative case checks `err != tc.wantErr` first then short-circuits with `if tc.wantErr != nil { return }`, so the round-trip check only fires on success rows. Sentinel comparison uses `==` (the err is a package-level sentinel, not wrapped). All three cases green.

PASS.

### 1.8 `TestNewActionItemPersistentRoundTrip` covers true/false

`internal/domain/domain_test.go:956-987` — table over `false default` (`false → false`) and `true round-trips` (`true → true`). Both round-trip without error. Test confirms the no-validation contract by asserting that `false` input passes (zero value default) and `true` input round-trips unchanged.

PASS.

### 1.9 `TestNewActionItemDevGatedRoundTrip` covers true/false

`internal/domain/domain_test.go:992-1023` — symmetric to Persistent: `false default` and `true round-trips`. Both pass without error.

PASS.

### 1.10 `DropNumber` doc comment includes rollback-cost note (finding 5.C.16)

`internal/domain/action_item.go:61-66` — doc comment block on the `ActionItem.DropNumber` field includes:

> Rollback-cost note (per Drop 3 plan-QA Round 2 finding 5.C.16): if this is later moved back into metadata-JSON, every consumer except the auth gate (3.18 schema column, 3.20 read-side filters, 3.21 MCP plumbing, plus their tests) must change shape — non-trivial backout.

Cites the finding number explicitly, enumerates the affected droplets (3.18 / 3.20 / 3.21 + tests), and frames the cost. Matches PLAN.md § 3.17 acceptance criterion "Per finding 5.C.16 (N4 rollback-cost note): … Document this in the droplet description" — landed in the canonical Go-doc location which is more durable than a transient task description.

PASS.

### 1.11 No `tc := tc`

Full-file Read of `internal/domain/domain_test.go` (1024 lines) and LSP `documentSymbol` confirm no `tc := tc` shadowing. All four new table-driven loops use the `for _, tc := range cases { t.Run(tc.name, func(t *testing.T) { ... }) }` pattern without re-shadowing — correct under Go 1.22+ where `range` variables are per-iteration scoped (`go.mod` declares `go 1.26`, well above the 1.22 cutoff).

PASS.

### 1.12 L13 reframe: domain primitives, not STEWARD-specific

Doc comments are reframed throughout:

- `Owner` (`:46-54`): "Free-form string — trim-only, no closed-enum membership check; any non-empty trimmed value is permitted so future template-defined owned kinds can pick their own owner names without a domain-package change. STEWARD is the dominant present-day consumer (auth-gate keys on it), but Owner is a domain primitive — not STEWARD-specific."
- `Persistent` (`:69-74`): "The 6 anchor nodes are the dominant present-day consumer, but Persistent is a domain primitive — not STEWARD-specific."
- `DevGated` (`:76-80`): "The refinements gate is the dominant present-day consumer, but DevGated is a domain primitive — not STEWARD-specific."
- `DropNumber` (`:65-66`): "Domain primitive, not STEWARD-specific."

Each field names its dominant present-day consumer but explicitly declares the field a domain primitive. This is the L13 wording the plan locked: "STEWARD persistent parents" → "nodes with `Persistent = true` (the 6 STEWARD-owned anchor nodes seeded by the default template)."

PASS.

## 2. Verification Gates

### 2.1 `mage format ./internal/domain` clean

Re-ran from this checkout. `[SUCCESS] Formatted Go sources` with `git diff --stat internal/domain/` showing only the three expected files (`action_item.go`, `domain_test.go`, `errors.go`). Format pass made no additional changes — the build's diff is gofumpt-canonical.

PASS.

### 2.2 `mage testPkg ./internal/domain` green (159 expected)

Re-ran from this checkout. Output:

```
[PKG PASS] github.com/evanmschultz/tillsyn/internal/domain (0.00s)

Test summary
  tests: 159
  passed: 159
  failed: 0
  skipped: 0
```

159/159 matches builder report. The `-race` flag is enabled in the mage `testPkg` target (verified by mage's standard convention; subagent `go-builder-agent` policy bans raw `go test` so race-on is the only path).

PASS.

### 2.3 No `tc := tc` introduced

Confirmed via full-file Read (no shadowing line in any of the four new tests; existing 3.1 + 3.2 tests also lack the pattern, consistent with Go 1.22+ semantics).

PASS.

### 2.4 Existing 3.1 + 3.2 tests still pass

Re-ran:

- `mage testFunc ./internal/domain TestNewActionItemRoleValidation` → 13/13 pass.
- `mage testFunc ./internal/domain TestNewActionItemStructuralTypeValidation` → 8/8 pass.

Neither test was modified by this droplet (verified via `git diff`).

PASS.

## 3. Findings

### 3.1 No `action_item_test_helpers.go` change required (matches builder report)

`internal/domain/action_item_test_helpers.go` is unchanged in `git status --porcelain`. The helper defaults zero-value `StructuralType` to `StructuralTypeDroplet`; it does not need to default `Owner` / `DropNumber` / `Persistent` / `DevGated` because all four fields' zero-values are valid (empty owner permitted, zero drop-number permitted, false bools permitted). No fixture in any package fails because of the new fields. PASS.

### 3.2 In-flight sibling builds noted but out of scope

`git status` shows other modified files (`internal/app/snapshot.go`, `internal/app/snapshot_test.go`, `internal/templates/schema.go`, `internal/tui/model_test.go`) and new files (`internal/templates/load.go`, `internal/templates/load_test.go`, `internal/templates/zz_falsify_test.go`). These belong to in-flight sibling droplets (3.21 snapshot work, 3.9 templates work, the 3.17 fix-builder for `model_test.go` `TestActionItemSchemaCoverageIsExplicit`). Not in scope for 3.17 PROOF. The 3.17 PLAN.md path list (`internal/domain/action_item.go`, `internal/domain/domain_test.go`, `internal/domain/errors.go`) is the only edit scope this droplet owns and all three are correctly modified.

The fix-builder change to `model_test.go` referenced in the spawn prompt is independent of 3.17's domain-package PROOF — `mage testPkg ./internal/domain` does not consume the TUI test, so the gate result is decoupled.

### 3.3 No deviations from PLAN.md § 3.17 acceptance

Every acceptance criterion bullet maps 1:1 to a passing check above. No nits, no MUSTs missed, no SHOULDs deferred without rationale.

## 4. Verdict Summary

**PASS.**

All 12 required proof checks satisfied. All 4 verification gates green. Domain-package coverage for the 4 new fields is complete and the L13 reframe + finding 5.C.16 rollback-cost note both land verbatim in durable Go-doc locations rather than transient task description. Existing 3.1 (Role) + 3.2 (StructuralType + Irreducible) tests remain green; no regression.

Builder may proceed to QA falsification handoff.

## 5. Hylla Feedback

None — Hylla was not consulted for this review. The droplet edits three files in a single package, all of which I had already loaded by full Read for line-anchored verification; falling back to Hylla for the same file-local checks would have been less precise. Worth flagging that "small surgical droplet, all edits in one package" is a workload type where direct Read + LSP `documentSymbol` is faster and equally accurate compared to a Hylla round-trip — not a Hylla deficit, just an ergonomic preference for this workload class.
