# Droplet 3.21 — BUILDER QA PROOF REVIEW

**Drop**: 3
**Droplet**: 3.21 — MCP/snapshot plumbing for Owner + DropNumber + Persistent + DevGated
**Reviewer role**: go-qa-proof-agent (proof-completeness pass)
**Verdict**: PASS

## 1. Required Proof Checks

### 1.1 Service Input Structs Carry Four Fields With Correct Types

`CreateActionItemInput` and `UpdateActionItemInput` both carry the four fields with the correct shape per droplet contract.

`CreateActionItemInput` (`internal/app/service.go:491-537`) — plain (value-typed) fields:
- `Owner string` (line 512)
- `DropNumber int` (line 517)
- `Persistent bool` (line 520)
- `DevGated bool` (line 524)

Each field carries a multi-line doc comment that pins zero-value semantics ("permitted", "default false") and asserts L13 domain-primitive scope ("not STEWARD-specific") so future readers do not collapse the field's domain identity into a STEWARD subtype.

`UpdateActionItemInput` (`internal/app/service.go:540-582`) — pointer-sentinel fields:
- `Owner *string` (line 563)
- `DropNumber *int` (line 568)
- `Persistent *bool` (line 574)
- `DevGated *bool` (line 577)

The sentinel rationale ("nil preserves existing; non-nil applies dereferenced") is documented inline (`service.go:993-998`) with the load-bearing motivating example: a description-only update must not silently clobber a STEWARD-seeded `Persistent=true` anchor.

PASS.

### 1.2 Service Methods Thread The Values

`Service.CreateActionItem` threads all four into `domain.NewActionItem` via the `domain.ActionItemInput` literal at `internal/app/service.go:742-768`:
- `Owner: in.Owner` (line 750)
- `DropNumber: in.DropNumber` (line 751)
- `Persistent: in.Persistent` (line 752)
- `DevGated: in.DevGated` (line 753)

`Service.UpdateActionItem` (`internal/app/service.go:939-1048`) applies the four pointer-sentinels at lines 999-1017 with the documented "nil preserves; non-nil applies" semantics:

```go
if in.Owner != nil {
    actionItem.Owner = strings.TrimSpace(*in.Owner)
    actionItem.UpdatedAt = s.clock().UTC()
}
if in.DropNumber != nil {
    if *in.DropNumber < 0 {
        return domain.ActionItem{}, domain.ErrInvalidDropNumber
    }
    actionItem.DropNumber = *in.DropNumber
    actionItem.UpdatedAt = s.clock().UTC()
}
if in.Persistent != nil { … }
if in.DevGated != nil { … }
```

Notable additions beyond raw value-application:
- DropNumber re-validates the negative-value invariant at the update boundary (mirrors create-side `domain.NewActionItem`).
- Owner `TrimSpace` matches the create-side trim policy (`app_service_adapter_mcp.go:657`).
- Each non-nil branch updates `UpdatedAt` so audit timestamps reflect the mutation.

PASS.

### 1.3 MCP Request Structs Carry Four Fields With Correct Types

`CreateActionItemRequest` (`internal/adapters/server/common/mcp_surface.go:64-107`) — plain types:
- `Owner string` (line 86)
- `DropNumber int` (line 91)
- `Persistent bool` (line 94)
- `DevGated bool` (line 98)

`UpdateActionItemRequest` (`internal/adapters/server/common/mcp_surface.go:109-158`) — pointer-sentinels:
- `Owner *string` (line 137) — pre-existing from droplet 3.19
- `DropNumber *int` (line 143) — pre-existing from droplet 3.19
- `Persistent *bool` (line 152) — added in 3.21
- `DevGated *bool` (line 155) — added in 3.21

Doc comments on the four Update-side fields specifically call out the STEWARD anchor clobber-protection rationale.

PASS.

### 1.4 Adapter Literals Thread Fields

`AppServiceAdapter.CreateActionItem` (`internal/adapters/server/common/app_service_adapter_mcp.go:634-678`) builds the `app.CreateActionItemInput` literal at lines 650-673 with all four fields:
- `Owner: strings.TrimSpace(in.Owner)` (line 657) — explicit whitespace-trim before threading
- `DropNumber: in.DropNumber` (line 658)
- `Persistent: in.Persistent` (line 659)
- `DevGated: in.DevGated` (line 660)

`AppServiceAdapter.UpdateActionItem` (`internal/adapters/server/common/app_service_adapter_mcp.go:692-739`) builds the `app.UpdateActionItemInput` literal at lines 717-734 passing the pointer-sentinels through verbatim:
- `Owner: in.Owner` (line 726)
- `DropNumber: in.DropNumber` (line 727)
- `Persistent: in.Persistent` (line 728)
- `DevGated: in.DevGated` (line 729)

The pre-fetch + STEWARD owner-state-gate at lines 707-715 is preserved (only triggers when `in.Owner != nil || in.DropNumber != nil`, so description-only updates skip the extra read).

PASS.

### 1.5 MCP Tool Schema Declares All Four Fields With Correct Types

The unified `till.action_item` tool at `internal/adapters/server/mcpapi/extended_tools.go:1394-1443` declares all four field types via the mcp library:

- Line 1411: `mcp.WithString("owner", mcp.Description(...))`
- Line 1412: `mcp.WithNumber("drop_number", mcp.Description(...))`
- Line 1413: `mcp.WithBoolean("persistent", mcp.Description(...))`
- Line 1414: `mcp.WithBoolean("dev_gated", mcp.Description(...))`

The bound args struct on the handler (`extended_tools.go:864-905`) types these as pointer-sentinels (`*string`, `*int`, `*bool`, `*bool`) at lines 872-875 so JSON-omitted fields decode to `nil`, distinguishing "absent" from "explicit zero" at the wire boundary.

The create branch (lines 1064-1075) collapses pointer-sentinel inputs to value-typed fields on the Create request, which is the correct shape for create semantics ("nil = not supplied = leave the request's zero value"). The update branch (lines 1135-1138) passes pointer-sentinels through verbatim to the Update request.

The legacy `till.create_task` / `till.update_task` aliases at lines 1458-1508 do NOT declare these new schema fields — but they share the same `handleActionItemOperation` handler, so callers pinned to legacy aliases lose schema discoverability of the four fields without losing functional support. Acceptance criteria targets the unified `till.action_item` tool; legacy schemas are deprecated and out of scope. No nit.

PASS.

### 1.6 MCP Round-Trip Test Covers Create + Update With Presence + Nil-Pointer Preservation

`TestActionItemMCPOwnerDropNumberPersistentDevGatedRoundTrip` (`internal/adapters/server/mcpapi/extended_tools_test.go:3683-3820`) ships exactly four subtests:

1. **`create plumbs owner/drop_number/persistent/dev_gated`** (lines 3703-3733) — sends all four fields, asserts the value-typed Create-request fields land at the stub.
2. **`create without the four fields round-trips zero-values`** (lines 3735-3761) — omits all four, asserts `Owner=""`, `DropNumber=0`, `Persistent=false`, `DevGated=false` on the captured request.
3. **`update plumbs all four fields as non-nil pointer-sentinels`** (lines 3763-3792) — sends all four, asserts each pointer is non-nil AND dereferences to the supplied value.
4. **`update without the four fields preserves prior via nil pointer-sentinels`** (lines 3794-3819) — omits all four, asserts each pointer is nil. This is the load-bearing nil-preservation case; it is precisely what would have masked a regression on STEWARD-anchor `Persistent=true` clobbering.

The test runs at the JSON-RPC boundary (`postJSONRPC` over `httptest.NewServer`), proving the schema declarations + bind-args + literal threading work end-to-end at the MCP wire level — not just at the Go struct boundary.

PASS.

### 1.7 SnapshotActionItem Carries Four Fields With Omitempty JSON Tags

`SnapshotActionItem` (`internal/app/snapshot.go:57-90`):

```go
Owner          string                    `json:"owner,omitempty"`        // line 66
DropNumber     int                       `json:"drop_number,omitempty"`  // line 67
Persistent     bool                      `json:"persistent,omitempty"`   // line 68
DevGated       bool                      `json:"dev_gated,omitempty"`    // line 69
```

All four carry `omitempty`, matching the legacy-format compatibility contract (legacy snapshots without the field deserialize to zero with no SnapshotVersion bump).

Both directions thread cleanly:
- `snapshotActionItemFromDomain` at `snapshot.go:1080-1083` copies `t.Owner`, `t.DropNumber`, `t.Persistent`, `t.DevGated` into the snapshot literal.
- `(SnapshotActionItem).toDomain` at `snapshot.go:1342-1345` copies them back, with an explanatory comment (lines 1336-1341) justifying the no-fallback policy: "Pre-droplet-3.21 snapshots without these fields deserialize to zero values, which are the legitimate domain defaults; legacy-format compatibility is covered by the json:\",omitempty\" tags. No SnapshotVersion bump required."

PASS.

### 1.8 Snapshot Round-Trip Test Covers All Four Fields

`TestSnapshotActionItemOwnerAndDropNumberRoundTrip` (`internal/app/snapshot_test.go:671-733`) is a six-case table covering:

- `all-zero-values` — proves the zero-default round-trip
- `steward-anchor` — the realistic STEWARD shape (Owner=STEWARD, DropNumber=5, Persistent=true, DevGated=true)
- `owner-only`, `drop-number-only`, `persistent-only`, `dev-gated-only` — each field in isolation, proving no field interferes with the others on round-trip

Each case asserts the value survives both `snapshotActionItemFromDomain` (domain → snapshot) and `snap.toDomain()` (snapshot → domain).

A bonus seventh test, `TestSnapshotActionItemOwnerLegacyFormatCompatibility` (lines 743-784), proves the JSON wire-level legacy compatibility: a snapshot whose JSON literally omits the four fields deserializes cleanly with all four set to zero, with no error. This catches a class of bug the in-memory round-trip cannot — anything that goes wrong at the JSON tag level (typo in tag, wrong tag option, missing field).

The test name only mentions "OwnerAndDropNumber" but the cases and assertions cover all four fields. Minor naming nit but does not affect coverage.

PASS.

### 1.9 3.19 Steward Gate Test Tightened To Verify Owner Mutation Persists

`TestAssertOwnerStateGateUpdateActionItemOwnerMutationAgentRejected` (`internal/adapters/server/common/app_service_adapter_steward_gate_test.go:172-224`) — the steward-CAN-change-Owner branch (lines 208-223) now does:

```go
stewardActor := stewardGatedActor("steward")
if _, err := fixture.adapter.UpdateActionItem(ctx, UpdateActionItemRequest{
    ActionItemID: stewardGated.ID,
    Title:        stewardGated.Title,
    Owner:        &mutatedOwner,
    Actor:        stewardActor,
}); err != nil {
    t.Fatalf(…)
}
after, getErr := fixture.adapter.GetActionItem(ctx, stewardGated.ID)
if getErr != nil { … }
if after.Owner != mutatedOwner {
    t.Fatalf("UpdateActionItem(steward) did not persist Owner: got %q, want %q", after.Owner, mutatedOwner)
}
```

The test calls `GetActionItem` after the steward mutation and asserts `after.Owner == mutatedOwner`. Pre-3.21 (per the inline comment at lines 200-207), this assertion was impossible because Owner was not yet wired through the service layer; the test could only assert the gate-permission decision. Post-3.21, the field is wired, and the test correctly proves the mutation persists. This catches a class of regression where the gate would correctly say "yes" but the field would silently fail to mutate.

PASS.

### 1.10 No SnapshotVersion Bump

`SnapshotVersion` at `internal/app/snapshot.go:16` remains `"tillsyn.snapshot.v5"` — unchanged from prior droplets. This is the correct call: `omitempty` JSON tags keep legacy snapshots forward-compatible without a version bump, and the bonus legacy-compat test at `snapshot_test.go:743-784` proves that contract empirically.

PASS.

### 1.11 mage ci Green

`mage ci` run from this checkout (`/Users/evanschultz/Documents/Code/hylla/tillsyn/main/`) reports:

- `tests: 1826` / `passed: 1826` / `failed: 0` / `skipped: 0`
- `packages: 20` / `pkg passed: 20` / `pkg failed: 0`
- All 16 covered packages at or above the 70.0% threshold; lowest is `internal/app` at 71.8%, highest is `internal/buildinfo` at 100.0%.
- `[SUCCESS] All tests passed` and `[SUCCESS] Coverage threshold met`
- Build step succeeded: "Built till from ./cmd/till"

Matches the builder's claim of 1826/1826 green exactly.

PASS.

### 1.12 No `tc := tc` Loop Captures

`rg "tc := tc"` against the three modified test files (`snapshot_test.go`, `extended_tools_test.go`, `app_service_adapter_steward_gate_test.go`) returns zero matches. Go 1.22+ loop semantics make the explicit shadow unnecessary; absence is correct.

PASS.

## 2. Verification Gates

| Gate                         | Status | Evidence                                          |
| ---------------------------- | ------ | ------------------------------------------------- |
| `mage ci` green              | PASS   | 1826/1826 tests, 20/20 packages, coverage met     |
| Coverage ≥ 70.0% per package | PASS   | Lowest `internal/app` at 71.8%                    |
| No `mage install` invoked    | PASS   | Reviewer used `mage ci` only                      |
| No raw `go` toolchain        | PASS   | All build/test verification via mage              |
| No `tc := tc` shadows        | PASS   | rg confirms zero matches in modified test files   |

## 3. Findings

None. Every required check has direct file-cited evidence and the build is green. The work also delivers two unsolicited but valuable additions:

- **Bonus legacy-format JSON test** (`TestSnapshotActionItemOwnerLegacyFormatCompatibility`) explicitly exercises the wire-level legacy-snapshot path that the in-memory round-trip cannot reach.
- **Pointer-sentinel doc comments** (`mcp_surface.go:127-155`, `service.go:557-577`) carry the STEWARD-anchor clobber-protection rationale inline, making future readers' lives easier without external references.

### 3.1 Minor Naming Nit (Non-Blocking)

`TestSnapshotActionItemOwnerAndDropNumberRoundTrip` (`snapshot_test.go:671`) covers all four fields (Owner, DropNumber, Persistent, DevGated) but the name only mentions two. The implementation matches the droplet contract; the name is just slightly stale relative to the case coverage. Leave as-is unless the closeout-orch wants tidiness — does not affect verdict.

## 4. Verdict Summary

**PASS**

All 12 required proof checks have direct file-cited evidence. The Owner / DropNumber / Persistent / DevGated plumbing threads cleanly from the MCP wire (schema + bound args struct), through the request/input boundary (pointer-sentinels preserved on Update, value-typed on Create), through the service layer (Update applies the four pointer-sentinels with proper invariant re-validation), through the snapshot layer (omitempty JSON tags, both-direction round-trip, legacy compat). The 3.19 steward gate test is tightened to prove field-level persistence post-mutation, closing the pre-3.21 gap explicitly noted in the inline comment. No SnapshotVersion bump (correctly). `mage ci` reports 1826/1826 green, 20/20 packages, coverage met, build succeeded — matching the builder's claim exactly.
