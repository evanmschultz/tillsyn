# 3.5 Builder QA Proof

**Droplet:** 3.5 — Snapshot serialization round-trip for `StructuralType` (+ `Irreducible` per orchestrator scope-extension)
**Reviewer:** go-qa-proof-agent (proof pass)
**Files reviewed:**
- `internal/app/snapshot.go` (struct + `snapshotActionItemFromDomain` + `(SnapshotActionItem).toDomain`)
- `internal/app/snapshot_test.go` (two new round-trip tests)
- `internal/domain/action_item.go` (3.2 contract verified)
- `workflow/drop_3/PLAN.md` §3.5 (acceptance contract)

**Spec note:** orchestrator extended droplet 3.5 to also cover `Irreducible` (3.21 covers Owner/DropNumber/Persistent/DevGated but not Irreducible). PLAN.md as written names only `StructuralType`. The `Irreducible` work is reviewed against the orchestrator-stated extension contract.

## 1. Required Proof Checks

### 1.1 `SnapshotActionItem.StructuralType` field with `omitempty`

PASS. `internal/app/snapshot.go:64`:

```
StructuralType domain.StructuralType     `json:"structural_type,omitempty"`
```

Inserted immediately after `Role` (line 63) per PLAN.md acceptance bullet "after `Role` (`:63`)". Type is `domain.StructuralType` — the closed enum from droplet 3.1. JSON tag is exactly `structural_type,omitempty` matching SQLite column convention from droplet 3.3.

### 1.2 `SnapshotActionItem.Irreducible` field with `omitempty`

PASS. `internal/app/snapshot.go:65`:

```
Irreducible    bool                      `json:"irreducible,omitempty"`
```

Sits immediately after `StructuralType`. Bool zero-value is `false`, which `omitempty` will drop on serialize — matching the convention used elsewhere in the struct (e.g., `AllowEqualScopeDelegation` in `SnapshotCapabilityLease`).

### 1.3 `snapshotActionItemFromDomain` assigns both fields

PASS. `internal/app/snapshot.go:1070-1071`:

```
StructuralType: t.StructuralType,
Irreducible:    t.Irreducible,
```

Both new lines sit between `Role` and `LifecycleState`, mirroring struct-field order. Pure value copy — no normalization or coercion (correct, since the domain layer already normalizes via `NewActionItem`).

### 1.4 `(SnapshotActionItem).toDomain` assigns both fields with NO empty-string fallback for `StructuralType`

PASS. `internal/app/snapshot.go:1319-1325`:

```go
// StructuralType intentionally has no empty-string fallback (unlike
// Kind above): legacy snapshots without a structural_type field
// surface as "" so the next mutation through NewActionItem catches it
// with ErrInvalidStructuralType per droplet 3.2's required-field
// contract. Inventing a default here would mask schema drift.
StructuralType: t.StructuralType,
Irreducible:    t.Irreducible,
```

Direct assignment, no `if t.StructuralType == ""` guard, no default. Comment explicitly contrasts with the `Kind`-fallback pattern at lines 1277-1282 and explains the design rationale exactly as PLAN.md acceptance bullet 3 prescribes (`empty intentionally surfaces so 3.2's validation catches it on next mutation`).

### 1.5 `TestSnapshotActionItemStructuralTypeRoundTripPreservesAllValues` covers all 4 enum values

PASS. `internal/app/snapshot_test.go:579-615`. Cases array covers every member of the closed `StructuralType` enum verified against `internal/domain/structural_type.go:18-21`:

| Case name    | Value                              |
| ------------ | ---------------------------------- |
| `drop`       | `domain.StructuralTypeDrop`        |
| `segment`    | `domain.StructuralTypeSegment`     |
| `confluence` | `domain.StructuralTypeConfluence`  |
| `droplet`    | `domain.StructuralTypeDroplet`     |

Each subtest constructs via `domain.NewActionItemForTest(... StructuralType: tc.structuralType ...)`, runs `snapshotActionItemFromDomain` then `(SnapshotActionItem).toDomain`, and asserts equality at both boundaries. `NewActionItemForTest` (`internal/domain/action_item_test_helpers.go:30-35`) preserves a non-empty `StructuralType` input verbatim — the `if empty -> StructuralTypeDroplet` defaulting only fires when the input is empty, so every case still pins the enum value under test. Mage `testFunc` run under `-race` returned 5/5 green (1 outer + 4 subtests).

### 1.6 `TestSnapshotActionItemIrreducibleRoundTripPreservesBothStates` covers both bool states

PASS. `internal/app/snapshot_test.go:624-658`. Cases:

| Case name           | Value   |
| ------------------- | ------- |
| `irreducible-true`  | `true`  |
| `irreducible-false` | `false` |

Each subtest constructs via `domain.NewActionItemForTest(... Irreducible: tc.irreducible ...)` and asserts both directions of the round-trip. Mage `testFunc` with `-race` returned 3/3 green (1 outer + 2 subtests).

The test docstring transparently flags the `false` ↔ `omitempty` interplay and notes the in-memory path under test does NOT pass through JSON, so a copy bug in either direction surfaces regardless. That observation is correct: `snapshotActionItemFromDomain` and `toDomain` are struct-to-struct, not JSON. A separate JSON-shape regression for `Irreducible` is not a 3.5 acceptance criterion.

### 1.7 No `tc := tc`

PASS. `rg "tc := tc" internal/app/snapshot_test.go` returns nothing. Project Go version is 1.26.1 (`go.mod:3`), which carries the per-iteration loop-variable scope from Go 1.22+ — `tc := tc` is unnecessary and would be a stale-pattern lint nit.

### 1.8 No `SnapshotVersion` bump

PASS. `git diff internal/app/snapshot.go` shows no `SnapshotVersion` line touched. Constant remains `tillsyn.snapshot.v5` (`internal/app/snapshot.go:16`). `omitempty` on both new fields keeps pre-3.5 snapshots backward-compatible: missing `structural_type` deserializes to empty (which the no-fallback path 1.4 deliberately surfaces), and missing `irreducible` deserializes to `false`.

## 2. Verification Gates (Re-Run)

### 2.1 `mage format ./internal/app`

PASS. `[SUCCESS] Formatted Go sources`. `git diff --stat` after format showed identical line counts to before — no formatting noise.

### 2.2 `mage testPkg ./internal/app`

PASS. 287 tests, 287 passed, 0 failed. Matches builder's reported `287/287` exactly.

### 2.3 `mage testFunc ./internal/app TestSnapshotActionItemStructuralTypeRoundTripPreservesAllValues`

PASS under `-race`. 5/5 (1 outer + 4 enum subtests).

### 2.4 `mage testFunc ./internal/app TestSnapshotActionItemIrreducibleRoundTripPreservesBothStates`

PASS under `-race`. 3/3 (1 outer + 2 bool subtests).

## 3. Findings

None. Build matches both PLAN.md §3.5 acceptance and the orchestrator-stated `Irreducible` extension. The `toDomain` comment is unusually load-bearing — it documents an intentional asymmetry with `Kind`'s fallback that future readers might otherwise be tempted to "fix" — and deserves to stay.

Process note (not a finding against this droplet, but worth flagging upstream): PLAN.md §3.5 as written does not name `Irreducible`. The orchestrator's scope extension is sound on the merits — `Irreducible` is part of the domain contract from 3.2 and 3.21 covers other 3.2-added fields but not this one — but the canonical PLAN.md text is now narrower than the actual landed work. Consider amending the §3.5 acceptance line in a follow-up doc commit so the audit trail matches reality.

## 4. Verdict Summary

**PASS.**

All 8 required proof checks satisfied. All 4 verification gates re-ran green. The struct, the from-domain literal, and the to-domain literal each carry both new fields. Tests cover the full closed enum (4/4) and both bool states (2/2). No `tc := tc` regression, no `SnapshotVersion` bump, no formatting drift. Backward compatibility for legacy snapshots is preserved by `omitempty` and the deliberate no-fallback design surfaces schema drift through `NewActionItem`'s `ErrInvalidStructuralType` rather than masking it.
