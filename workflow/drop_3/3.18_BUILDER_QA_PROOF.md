# Drop 3 droplet 3.18 — Builder QA Proof Review

- **Droplet:** 3.18 — SQLite `owner` + `drop_number` + `persistent` + `dev_gated` columns + indexes
- **Reviewer:** go-qa-proof-agent
- **Reviewed paths:**
  - `internal/adapters/storage/sqlite/repo.go`
  - `internal/adapters/storage/sqlite/repo_test.go`
  - `internal/adapters/storage/sqlite/embedding_lifecycle_adapter_test.go`
- **Verdict:** PASS

## 1. Required Proof Checks

- 1.1 **CREATE TABLE schema correctness — PASS.** `repo.go:168-203` defines the canonical `action_items` schema. The four new columns appear at lines 177-180, immediately after `irreducible INTEGER NOT NULL DEFAULT 0` (line 176) and before `lifecycle_state` (line 181):
  - `owner TEXT NOT NULL DEFAULT ''`
  - `drop_number INTEGER NOT NULL DEFAULT 0`
  - `persistent INTEGER NOT NULL DEFAULT 0`
  - `dev_gated INTEGER NOT NULL DEFAULT 0`

  Types and defaults match the contract: TEXT default `''` for `owner`, INTEGER default `0` for the integer/bool columns. Booleans persisted as INTEGER 0/1.

- 1.2 **INSERT, UPDATE, all 3 SELECTs share consistent column ordinal — PASS.** Verified by reading each site:
  - **INSERT** (`repo.go:1186-1190`): column list starts `id, project_id, parent_id, kind, scope, role, structural_type, irreducible, owner, drop_number, persistent, dev_gated, lifecycle_state, ...` (positions 9-12 = the four new columns). 32 columns / 32 placeholders / 32 bind args (`repo.go:1192-1223`). Bind args at positions 9-12: `t.Owner`, `t.DropNumber`, `boolToInt(t.Persistent)`, `boolToInt(t.DevGated)`.
  - **UPDATE** (`repo.go:1285-1289`): `SET parent_id = ?, kind = ?, scope = ?, role = ?, structural_type = ?, irreducible = ?, owner = ?, drop_number = ?, persistent = ?, dev_gated = ?, lifecycle_state = ?, ...`. 27 SET clauses + 27 bind args + 1 WHERE-id arg. SET bind args at the same relative positions: `t.Owner`, `t.DropNumber`, `boolToInt(t.Persistent)`, `boolToInt(t.DevGated)` (`repo.go:1297-1300`).
  - **SELECT 1: `ListActionItems`** (`repo.go:1357-1363`): same 32-column projection with the new four at positions 9-12.
  - **SELECT 2: `ListActionItemsByParent`** (`repo.go:1395-1402`): identical 32-column projection.
  - **SELECT 3: `getActionItemByID`** (`repo.go:2438-2444`): identical 32-column projection. (Verified by re-reading at the correct offset; this site was the spot I first misread — the SELECT does include the four new columns.)

- 1.3 **`scanActionItem` matches projection ordinal — PASS.** `scanActionItem` (`repo.go:2731-2851`) declares 4 raw vars (`ownerRaw`, `dropNumberRaw`, `persistentRaw`, `devGatedRaw` at `:2749-2752`), passes them to `Scan` at positions 9-12 (`:2765-2768`, after `&irreducibleRaw` and before `&state`), and assigns into the struct at `:2801-2804` (`t.Owner`, `t.DropNumber`, `t.Persistent = persistentRaw != 0`, `t.DevGated = devGatedRaw != 0`). Position-aligned with all three SELECTs.

- 1.4 **`boolToInt` reused, no new helper — PASS.** Single definition at `repo.go:3155-3156`. Six new use sites (3 in INSERT, 3 in UPDATE — `Irreducible` was reused, `Persistent` and `DevGated` are the new ones at lines 1202, 1203, 1299, 1300). No second helper introduced.

- 1.5 **Two new indexes added per finding 5.C.5 + 5.C.17 — PASS.** `repo.go:442-443`:
  - `CREATE INDEX IF NOT EXISTS idx_action_items_drop_number ON action_items(project_id, drop_number, owner);` — covers "find every drop N item" (and "find every drop N item with owner X" via the third column).
  - `CREATE INDEX IF NOT EXISTS idx_action_items_owner_title ON action_items(project_id, owner, title);` — covers "find every level_2 finding under STEWARD persistent parents" via owner + title prefix.

  Both are appended after the existing `idx_action_items_project_column_position` and `idx_action_items_project_parent`. Builder picked variant (a) from the contract — `(project_id, drop_number, owner)` column order — which is acceptable per the contract's "either is fine" note.

- 1.6 **Round-trip test cell coverage — PASS.** `TestRepository_PersistsActionItemOwnerAndDropNumber` (`repo_test.go:2448-2607`) defines four cases (`:2477-2480`):
  - `{Owner:"", DropNumber:0, Persistent:false, DevGated:false}`
  - `{Owner:"STEWARD", DropNumber:5, Persistent:true, DevGated:true}`
  - `{Owner:"", DropNumber:5, Persistent:true, DevGated:false}`
  - `{Owner:"STEWARD", DropNumber:0, Persistent:false, DevGated:true}`

  All four values for `Owner` × `DropNumber` × `Persistent` × `DevGated` are exercised at least once (every column writes a non-default value at least once and a default value at least once, so the SQLite INTEGER 0/1 round-trip is verified for both directions on both bool columns). The contract only required the single `Owner="STEWARD", DropNumber=5, Persistent=true, DevGated=true` case; the four-cell coverage is more thorough than the spec.

- 1.7 **Round-trip test exercises all 5 paths — PASS.** Trace:
  - Create (×4) — `repo.CreateActionItem` at `:2501`.
  - Get (×4 + 1) — `repo.GetActionItem` at `:2504`, `:2579`, `:2591`.
  - ListActionItems — `:2523-2551` (length + per-row Owner/DropNumber/Persistent/DevGated assertions).
  - ListActionItemsByParent — `:2554-2575` (length + struct assertions on every cell).
  - UpdateActionItem — `:2588` (flips all four columns, then re-reads via Get to confirm UPDATE wrote all four through).

- 1.8 **Index-coverage test queries the right shape — PASS.** `TestRepository_IndexCoversDropNumberQuery` (`repo_test.go:2614-2693`) seeds 6 rows across drop_number values {3, 3, 3, 4, 4, 0}, then runs `SELECT id FROM action_items WHERE project_id = ? AND drop_number = ? ORDER BY id ASC` (`:2668`) for `drop_number = 3` and asserts exactly the three drop-3 rows return in deterministic order. Test docstring at `:2611-2613` explicitly notes this is correctness-rather-than-EXPLAIN-plan validation because SQLite EXPLAIN output is brittle across versions — acceptable trade-off.

- 1.9 **Legacy DB fixture update preserves migration test — PASS.** `embedding_lifecycle_adapter_test.go:301-310` adds `owner TEXT NOT NULL DEFAULT ''` + `drop_number INTEGER NOT NULL DEFAULT 0` to the legacy fixture's `CREATE TABLE action_items` block. The fixture-update rationale is correct: the new indexes `idx_action_items_drop_number(project_id, drop_number, owner)` and `idx_action_items_owner_title(project_id, owner, title)` reference `owner` + `drop_number` (and `title`, which already existed), so the legacy fixture must declare both for index creation to succeed. `persistent` + `dev_gated` are NOT referenced by any index, so the fixture omits them — that is the minimal correct fixture-shape, not an oversight. Test is purely fixture maintenance, not a migration assertion (the test name `…SchemaMigratesLegacyDatabase` refers to the embedding-lifecycle schema, not action_items columns; that test continues to pass at 73/73).

- 1.10 **No `ALTER TABLE` migration code — PASS.** `rg -n "ALTER TABLE"` over `internal/adapters/storage/sqlite/` returns only pre-existing legacy ALTER blocks (`repo.go:472-484` for action_items, `:501-503` for auth_requests, comments, etc.). None of the matched lines reference `owner`, `drop_number`, `persistent`, or `dev_gated`. Pre-MVP "no migration logic" rule honored — dev fresh-DBs to pick up the new columns.

- 1.11 **No `tc := tc` — PASS.** `rg -n "tc := tc" internal/adapters/storage/sqlite/repo_test.go` returns no matches. The test loop at `:2483` uses `for i, tc := range cases` directly without the (Go ≤1.21) loop-variable shadow workaround, which is correct on Go 1.22+ (per project tech stack `Go 1.26+`).

## 2. Verification Gates

- 2.1 **`mage format internal/adapters/storage/sqlite` — PASS clean.** Hashed all three files pre-format, ran `mage format internal/adapters/storage/sqlite`, re-hashed: identical MD5s for `repo.go`, `repo_test.go`, `embedding_lifecycle_adapter_test.go`. No formatting drift.

- 2.2 **`mage test-pkg ./internal/adapters/storage/sqlite` — PASS green.** Re-ran. Result: `tests: 73, passed: 73, failed: 0, skipped: 0`. Matches builder report.

- 2.3 **No `tc := tc`** — see § 1.11.

- 2.4 **No `ALTER TABLE` migration code added** — see § 1.10.

## 3. Findings

- 3.1 No findings. Implementation is correct, ordinal-consistent across all six SQL sites, idiomatic, and the test coverage exceeds the contract minimum (4 cells vs the 1 the contract required).

- 3.2 **Self-correction note.** Initial read of `getActionItemByID` landed at the wrong offset (line 2410-range read of an earlier file revision picked up `getAttentionItemByID`'s body and I misattributed it to `getActionItemByID`). Re-read at the correct offset (`:2437-2446`) confirmed the SELECT is correct. Re-running `mage test-pkg` immediately disambiguated — a 32→28 column-shift would have produced a hard scan error, which it did not.

## 4. Verdict Summary

**PASS.** All 11 required proof checks confirmed. Both verification gates green. No nits.

The four new columns (`owner`, `drop_number`, `persistent`, `dev_gated`) round-trip correctly through Create + Get + ListActionItems + ListActionItemsByParent + UpdateActionItem. INSERT, UPDATE, and all three SELECTs maintain ordinal-position consistency at column positions 9-12 (immediately after `irreducible`). `scanActionItem` aligns. Two new indexes (`idx_action_items_drop_number` over `(project_id, drop_number, owner)`, `idx_action_items_owner_title` over `(project_id, owner, title)`) are present per finding 5.C.5 + 5.C.17. `boolToInt` reused. Pre-MVP "no migration logic" rule honored — no ALTER TABLE for the new columns; dev fresh-DBs. Legacy embedding-lifecycle migration test fixture updated minimally (only `owner` + `drop_number`, since those are the columns the new indexes reference).

Cleared for falsification round.
