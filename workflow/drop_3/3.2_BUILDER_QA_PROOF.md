# DROPLET 3.2 — BUILDER QA PROOF

**Reviewer:** go-qa-proof-agent
**Droplet:** 3.2 — `StructuralType` + `Irreducible` fields on `ActionItem` + `NewActionItemForTest` helper + cross-package call-site sweep
**Date:** 2026-05-02
**Verdict:** PASS

## 1. Required Proof Checks

### 1.1 `ActionItem` struct extension — TWO fields after `Role`

PASS. `internal/domain/action_item.go:34-45` declares exactly two new fields immediately after `Role` (line 33): `StructuralType StructuralType` (line 40) and `Irreducible bool` (line 45). No `Persistent` / `DevGated` introduced — those are deferred to droplet 3.17 per L9 + L10 + the spawn-prompt's R2.B1 carve-out. Doc comments cite `ta-docs/cascade-methodology.md` §11.3 for both fields.

### 1.2 `ActionItemInput` struct extension — same TWO fields with cited doc comments

PASS. `action_item.go:80-90` adds `StructuralType` (lines 80-85) with the comment "MUST be a member of the closed StructuralType enum. Empty is rejected — `NewActionItem` returns `ErrInvalidStructuralType`. This diverges from Role's permissive empty…" matching the PLAN's prescribed text. `Irreducible` (lines 86-90) cites `ta-docs/cascade-methodology.md` §2.3 + §11.3 as required.

### 1.3 `NewActionItem` validation block placement and ordering

PASS. `action_item.go:182-193` lands the StructuralType validation block immediately AFTER the existing Role block (lines 173-181) and BEFORE the `LifecycleState` initialization (lines 194-199). Block:

- Line 187 normalizes via `NormalizeStructuralType` first (whitespace collapses to empty before the empty check).
- Lines 188-190 reject empty with `ErrInvalidStructuralType`.
- Lines 191-193 reject non-enum values with `ErrInvalidStructuralType`.

Ordering matches PLAN's "after Role, matching declaration order" mandate. Empty-rejection diverges from Role's permissive empty per spec (3.2 acceptance criterion: "Empty rejects with `ErrInvalidStructuralType` (diverges from Role's permissive empty).").

### 1.4 `NewActionItem` return-statement assigns both new fields

PASS. `action_item.go:230-256` return literal includes `StructuralType: in.StructuralType` (line 237) and `Irreducible: in.Irreducible` (line 238) directly after `Role: in.Role` (line 236). Field order in struct declaration is mirrored in the return literal.

### 1.5 `NewActionItemForTest` helper — exported, in regular `.go` file, doc-commented

PASS. `internal/domain/action_item_test_helpers.go` (NEW) declares an EXPORTED `NewActionItemForTest(in ActionItemInput, now time.Time) (ActionItem, error)` (line 30) in a regular `.go` file (not `_test.go`) per PLAN's recommended location. Doc comment (lines 8-29):

- Names it as TEST-ONLY explicitly: "TEST-ONLY fixture constructor."
- Warns production code: "Production code MUST use NewActionItem directly so the ErrInvalidStructuralType enforcement fires."
- Documents the defaulting rule: empty `StructuralType` (post-`NormalizeStructuralType`) → `StructuralTypeDroplet`.
- Cites the Drop 1.75 Kind-default precedent at `internal/tui/model_test.go` `newActionItemForTest`.
- Notes a refinement path (move to `domaintest` sub-package) so the helper can become unimportable from production callers.

Body (lines 30-35) defaults empty `StructuralType` to `StructuralTypeDroplet`, then delegates to `NewActionItem`. Other fields pass through unchanged — Role's permissive empty preserved per spec.

### 1.6 `TestNewActionItemStructuralTypeValidation` — table-driven, parallel, 7 cases

PASS. `internal/domain/domain_test.go:265-305` matches `TestNewActionItemRoleValidation` shape line-for-line: 7 cases (4 valid enum values accepted; empty rejected; whitespace-only rejected; unknown rejected). Each case asserts `err == tc.wantErr` AND on success `actionItem.StructuralType == tc.wantST`. Test does NOT call `t.Parallel()` — neither does the sibling `TestNewActionItemRoleValidation` (lines 212-258). Mirror is line-for-line as the PLAN required ("mirrors `TestNewActionItemRoleValidation` shape line-for-line"). Acceptable: PLAN's word "table-driven" + "mirrors role_test.go shape" took precedence over a strict "must be parallel" directive — the role precedent is non-parallel and the spec required mirror parity.

### 1.7 Existing `TestNewActionItemRoleValidation` rows updated to supply valid `StructuralType`

PASS. `domain_test.go:237-246`: every Role row supplies `StructuralType: StructuralTypeDroplet` in the `NewActionItem` call. The `unknown role rejects` row (line 232) still fires on `ErrInvalidRole` because Role validation (line 179 in `action_item.go`) fires BEFORE StructuralType validation (line 187). Verified by trace: Role normalize+check returns `ErrInvalidRole` and short-circuits before the StructuralType block.

### 1.8 Cross-package call-site migration

PASS. `git grep -n "domain.NewActionItem(" -- "*.go"` returns exactly 2 hits OUTSIDE `internal/domain/`:

- `internal/app/service.go:574` — production caller (item 1.9 below).
- `internal/tui/model_test.go:14692` — wrapper's delegated call inside `newActionItemForTest` (item 1.10 below).

Every other cross-package call site was rewritten to `domain.NewActionItemForTest(`. Sweep tally per `git grep -c`: `internal/app/service_test.go` (~57 sites), `internal/adapters/storage/sqlite/repo_test.go` (~12 sites), `internal/app/snapshot_test.go` (5 sites), plus the remaining 6 files each carrying 1-3 sites. The builder's claim of ~84 total sites + 77 cross-package migrations is consistent with the surviving call distribution.

### 1.9 `internal/app/service.go` production caller defaults `StructuralType`

PASS. `service.go:574-601` `domain.NewActionItem(...)` call passes `StructuralType: domain.StructuralTypeDroplet` (line 586) with a 5-line inline comment (lines 581-585) naming the default as "the dominant cascade-leaf shape" and flagging that `CreateActionItemInput` will grow a real `StructuralType` field in a follow-up droplet. The comment explicitly references `ErrInvalidStructuralType` to make the load-bearing nature of the default obvious to future readers.

### 1.10 `internal/tui/model_test.go` wrapper extended for `StructuralType` defaulting

PASS. `model_test.go:14674-14692` extends the existing `newActionItemForTest` wrapper with a `StructuralType` defaulting block (lines 14689-14691) that mirrors the existing `Kind` (lines 14683-14685) and `Scope` (lines 14686-14688) defaulting blocks. Doc comment (lines 14674-14681) updated to describe the new behaviour and cite "droplet 3.2."

`TestActionItemSchemaCoverageIsExplicit` (lines 14850-14885) read-only set extended with two new keys: `"StructuralType": {}` (line 14867) and `"Irreducible": {}` (line 14868). Both new fields reflected in the explicit schema-coverage closure — the test would fail at `assertExplicitFieldCoverage` if either were omitted.

### 1.11 No `tc := tc` introduced in 3.2's edit scope

PASS. `git grep -n "tc := tc" -- "*.go"` returns 9 hits, all in files OUTSIDE 3.2's edit scope:

- `cmd/till/main_test.go:741, 2099, 3220` — pre-existing.
- `internal/adapters/server/common/app_service_adapter_helpers_test.go:34, 262` — pre-existing.
- `internal/adapters/server/common/app_service_adapter_mcp_guard_test.go:95` — pre-existing.
- `internal/app/schema_validator_test.go:87, 139` — pre-existing.
- `internal/domain/auth_request_test.go:65` — pre-existing.

3.2 modified `internal/domain/domain_test.go`, `internal/domain/action_item.go`, `internal/domain/action_item_test_helpers.go` (NEW), `internal/app/service.go`, `internal/tui/model_test.go`, plus 8 cross-package test files. None contain a fresh `tc := tc`. The new `TestNewActionItemStructuralTypeValidation` (lines 283-304) uses Go 1.22+ implicit per-iteration `tc` scoping correctly without the legacy `tc := tc` rebind.

### 1.12 Doc comments on every NEW top-level declaration

PASS. The two NEW top-level surfaces:

- `NewActionItemForTest` (helper) — full doc block at lines 8-29 of `action_item_test_helpers.go`.
- `TestNewActionItemStructuralTypeValidation` — doc block at lines 260-264 of `domain_test.go`.

Both new struct fields (`ActionItem.StructuralType` / `ActionItem.Irreducible` and the input mirror) carry contextual doc comments citing `ta-docs/cascade-methodology.md` §11.3 / §2.3 (action_item.go:34-44, 80-89).

## 2. Verification Gates

### 2.1 `mage format`

PASS. Mage exposes `format` as a single-arg target (`format <path>`); the canonical formatting check is `go tool gofumpt -l .` which is the underlying tool the target invokes. Ran `go tool gofumpt -l .` from the worktree root: zero output (zero formatting drift).

### 2.2 `mage ci`

PASS. Re-ran `mage ci` from `/Users/evanschultz/Documents/Code/hylla/tillsyn/main/`. Result:

- `tests: 1525 / passed: 1525 / failed: 0 / skipped: 0`
- `packages: 19 / pkg passed: 19 / pkg failed: 0`
- Coverage threshold (70.0%) met across all 19 packages. Lowest-covered packages are `internal/tui` (70.0%) and `internal/app` (71.8%) — the latter rose into spec range; tui is at floor.
- Build target `till` produced from `./cmd/till`.

Test count matches the builder's reported 1525/1525.

### 2.3 `git grep "domain.NewActionItem("` outside `internal/domain/`

PASS. Returns exactly 2 legitimate hits: production caller in `internal/app/service.go:574` (supplies field directly) and wrapper-internal call at `internal/tui/model_test.go:14692` (inside the wrapper that defaults the field). Every other cross-package test caller goes through `NewActionItemForTest`.

### 2.4 No NEW `tc := tc`

PASS. See § 1.11 — 9 pre-existing occurrences, none in 3.2's edit scope.

## 3. Pre-Existing Diagnostics Audit

PASS. `mage ci` returned green with all 1525 tests passing and coverage thresholds met. The pre-existing TUI / SQLite / service.go / main.go gopls warnings flagged in the spawn prompt (tracked in `project_drop_2_refinements_raised.md` R1 / R5 / R6 / R7) are not regressions:

- 3.2's edit scope is two struct fields + a validation block + a test helper + ~84 call-site migrations. No control-flow changes. No new exported APIs in production code other than the test helper. No package boundary moves.
- Test count delta is consistent with one new test function (`TestNewActionItemStructuralTypeValidation` adds 7 sub-cases inside one `t.Run` table). Coverage thresholds did not regress — `internal/domain` at 79.7%, `internal/app` at 71.8%, `internal/tui` at floor 70.0%.
- Cross-package sweep is mechanical (rename `domain.NewActionItem` → `domain.NewActionItemForTest`); none of the test files gained new test logic, so existing pre-existing diagnostics in those files (e.g. the 9 `tc := tc` spots) are untouched.

No new regressions detectable in the gate output.

## 4. Findings

### 4.1 BLOCKING

None.

### 4.2 NIT

- 4.2.1 (NIT) `TestNewActionItemStructuralTypeValidation` does not call `t.Parallel()`. The PLAN's domain-tests precedent (`role_test.go` shape and `TestNewActionItemRoleValidation` itself) is non-parallel, so this is consistent with the "mirrors role_test.go shape line-for-line" mandate. No action required this droplet; could be raised as a refinement to add `t.Parallel()` to BOTH `TestNewActionItemRoleValidation` and `TestNewActionItemStructuralTypeValidation` together for consistency.

- 4.2.2 (NIT) The `NewActionItemForTest` helper lives in the production package `internal/domain` (in a `_helpers.go` file rather than `_test.go`), which makes it importable from production code. The doc comment warns against this and proposes moving it to a `domaintest` sub-package as a refinement. This is a known-acceptable trade-off documented in the helper's doc comment (lines 26-29). No action required this droplet.

## 5. Verdict Summary

PASS. Every droplet 3.2 acceptance criterion is supported by code-level evidence:

- Two struct fields (`StructuralType`, `Irreducible`) added to `ActionItem` and `ActionItemInput` after `Role` with cited doc comments.
- Validation block placed correctly between Role and lifecycle init, rejects empty + non-enum with `ErrInvalidStructuralType`.
- Return statement assigns both new fields.
- Exported `NewActionItemForTest` helper with TEST-ONLY semantics + StructuralTypeDroplet defaulting + production-rejection delegation.
- New `TestNewActionItemStructuralTypeValidation` (7 cases) + updated `TestNewActionItemRoleValidation` rows + 4-case `TestNewActionItemValidation` carve-out preserved with valid pre-StructuralType-block error rationale.
- 8 cross-package test files swept to `NewActionItemForTest`; production caller (`service.go`) defaults `StructuralType` with explanatory comment; TUI wrapper extended; schema-coverage test extended.
- `mage ci` green: 1525/1525 tests, 19/19 packages, coverage thresholds met.
- `gofumpt -l .` clean.
- No NEW `tc := tc` in 3.2's edit scope.
- Pre-existing diagnostics audited as non-regressions.

Two NITs raised (parallelism mirror parity + helper location refinement) are non-blocking and tracked.
