# DROP 3 — Droplet 3.28 Builder Worklog

**Droplet:** 3.28 — Per-drop final wrap-up sweep (post-Drop-3 vocabulary).
**Mode:** filesystem-MD only. No code changes. No commits. No pushes.
**State at handback:** edits applied in-place; awaiting orch-mediated dev approval before commit.

## Audit-Gap Acceptance (per finding 5.D.10 / Option C)

3.28's in-scope set is the same as 3.27 (see `3.27_BUILDER_WORKLOG.md` for the full list). The same audit-gap acceptance applies: outside-repo `~/.claude/` files are not git-tracked by this repo; this worklog is the only durable record of any outside-repo changes. **For 3.28 specifically, no `~/.claude/` edits were required** — every gap surfaced was inside `main/`, so this worklog records only in-repo edits. The audit-gap clause stays in force for any future re-sweep that does need outside-repo edits.

## Droplet 3.28 — Round 1

- **Builder:** `go-builder-agent` (model: opus per memory rule "Opus Builders Pre-Dogfood").
- **Started:** 2026-05-03.
- **Mode:** docs-only sweep across active canonical docs (same path scope as 3.27).

### Sweep methodology

Four passes per droplet 3.28 spec:

1. **`rg "drops all the way down|tasks all the way down"`** over in-scope paths. Re-confirm 3.27's pass-1 result still holds — zero hits in active prose.
2. **Unit A search terms** — `structural_type` + 4 enum values (`drop`, `segment`, `confluence`, `droplet`); atomicity / `Irreducible` rules; 6 plan-QA-falsification attack vectors.
3. **Unit B search terms** — TOML template-system terms (`Template.AllowsNesting`, `Template.ChildRulesFor`, `KindCatalog`, `[child_rules]`, `[kinds.*]`, `[agent_bindings.*]`, `internal/templates/builtin/default.toml`, `templates.LoadDefaultTemplate`).
4. **Unit C search terms** — `principal_type: steward`, `Owner = "STEWARD"`, `Persistent`, `DevGated`, auth-level state-lock (`assertOwnerStateGate`, `assertOwnerStateGateUpdateFields`), template auto-generation (`seedStewardAnchors`, `seedDropFindingsAndGate`, refinements-gate).

For every hit found in pass 2/3/4: verify it uses canonical vocabulary consistent with the source-of-truth (WIKI.md § Cascade Vocabulary; `internal/templates/`; `STEWARD_ORCH_PROMPT.md` + `internal/app/auto_generate_steward.go`). Where prose invents a parallel name or uses retired pre-Drop-2 vocabulary in active rules, rewrite. Where prose correctly references the canonical name, leave as-is.

### Pass 1 — retired "drops all the way down" framing

Re-ran `rg "drops all the way down|tasks all the way down"` across the same in-scope paths as 3.27 plus the workflow/drop_3/ planning artifacts.

**Result:** zero hits in active in-scope prose (`main/CLAUDE.md`, `main/PLAN.md`, `main/WIKI.md`, `main/STEWARD_ORCH_PROMPT.md`, `main/AGENT_CASCADE_DESIGN.md`, `main/README.md`, `main/CONTRIBUTING.md`, `main/SEMI-FORMAL-REASONING.md`, `main/HYLLA_WIKI.md`, `main/tillsyn-project.md`, `main/DROP_1_75_ORCH_PROMPT.md`, `main/AGENTS.md`, `main/workflow/example/CLAUDE.md`, `main/workflow/example/drops/**`).

The hits that ARE present in `workflow/drop_3/PLAN.md`, `workflow/drop_3/UNIT_D_PLAN.md`, `workflow/drop_3/3.27_BUILDER_WORKLOG.md`, and `workflow/drop_3/PLAN_QA_FALSIFICATION_R2.md` all describe the retired framing as **historical fact** ("RETIRED", "retired vocabulary") — they're correct documentation of what was retired, not active assertion. Same disposition as MEMORY.md line 21 in the 3.27 worklog.

### Pass 2/3/4 — Drop-3 new-vocabulary consistency

#### Per-file inventory (in-repo, git-tracked)

| Path                                                                | Pass 1 | Pass 2/3/4 hits | Rewrites | Notes |
|---------------------------------------------------------------------|----|----|----|-------|
| `main/CLAUDE.md`                                                    | 0  | 0 hits before edit | 1 addition | **Added: "Three Orthogonal Axes" subsection** under "Cascade Tree Structure" (after "Project-level kind restriction" paragraph, before "Kind Hierarchy" diagram). Documents `kind` × `metadata.role` × `metadata.structural_type` as orthogonal axes; lists the closed 4-value structural_type enum + atomicity rules; points back to WIKI.md § Cascade Vocabulary as canonical with explicit "do not redefine here." Closes the deferral from 3.27 worklog (Pass 4 verdict noted "CLAUDE.md addition deferred to 3.28 by design"). |
| `main/WIKI.md`                                                      | 0  | 7 hits in active prose | 5 rewrites | Pre-Drop-2 retired vocabulary (`kind='actionItem'`, `actionItem` action items, `kind=actionItem, scope=actionItem`) carried over from a 3.6-era pre-Drop-2 freeze. 3.27 explicitly deferred these. Rewrites land below. |
| `main/PLAN.md`                                                      | 0  | many hits, all correct | 0 | Every reference uses canonical vocab. Lines 1625–1651 (§19.10) carry the design-time prose where `structural_type` was introduced; lines 74 and 1629–1651 reference the field correctly. Unit A/B/C terms (`Template.AllowsNesting`, `child_rules`, `principal_type: steward`, `metadata.persistent`, `metadata.dev_gated`) all appear with canonical naming and pointers to source-of-truth. |
| `main/STEWARD_ORCH_PROMPT.md`                                       | 0  | many hits, all correct | 0 | `principal_type: steward` (line 198, 248), `principal_type: agent` (line 282), Persistent STEWARD-Owned Drops section (line 18ff) — all canonical. Auth-level state-lock referenced correctly (line 198). |
| `main/AGENT_CASCADE_DESIGN.md`                                      | 0  | 2 hits | 0 | `child_rules` (line 254, 567) — both canonical references. No structural_type / steward-vocab usage. |
| `main/AGENTS.md`                                                    | 0  | 0 | 0 | Clean. |
| `main/README.md`                                                    | 0  | many hits, mixed | 0 | `child_rules` (line 361, 487, 510) — these appear inside template-spec JSON examples that pre-date Drop 1.75 (use `kind_id: build-actionItem`, `qa-check`, etc.). Per 3.27 disposition, README's "Tillsyn integration framing" section is template/product-vocabulary describing how Tillsyn is structured for adopters; needs a dedicated overhaul droplet, not a partial vocab sweep. **Disposition unchanged from 3.27 — recommend follow-up README refresh.** |
| `main/CONTRIBUTING.md`                                              | 0  | 0 | 0 | Clean. |
| `main/SEMI-FORMAL-REASONING.md`                                     | 0  | 0 | 0 | Clean. |
| `main/HYLLA_WIKI.md`                                                | 0  | 0 | 0 | Clean. |
| `main/tillsyn-project.md`                                           | 0  | 1 hit | 0 | "Persistent Daemon" (line 112, 136) — refers to the Tillsyn daemon process, not the cascade `metadata.persistent` flag. Different concept; correct as-is. |
| `main/DROP_1_75_ORCH_PROMPT.md`                                     | 0  | 0 | 0 | Clean (no structural_type / template-system / steward-vocab references — Drop 1.75 was the kind-collapse drop, structural_type lands in Drop 3). |
| `main/workflow/example/CLAUDE.md`                                   | 0  | 1 hit | 0 | Line 27 carries the canonical-pointer rule for adopters. Already cites WIKI's `Cascade Vocabulary` section by name and forbids local redefinition. Coherent. |
| `main/workflow/example/drops/WORKFLOW.md`                           | 0  | 0 | 0 | Clean — no Drop-3-new-vocab usage. References to droplet/build/QA stay in cascade-vocab generic mode (correct). |
| `main/workflow/example/drops/_TEMPLATE/PLAN.md`                     | 0  | 0 | 0 | Clean. |
| `main/workflow/example/drops/_TEMPLATE/BUILDER_WORKLOG.md`          | 0  | 0 | 0 | Clean. |
| `main/workflow/example/drops/_TEMPLATE/CLOSEOUT.md`                 | 0  | 0 | 0 | Clean. |
| `main/workflow/example/drops/DROP_N_EXAMPLE/PLAN.md`                | 0  | 0 | 0 | Clean. |
| `main/workflow/example/drops/DROP_N_EXAMPLE/BUILDER_WORKLOG.md`     | 0  | 0 | 0 | Clean. |
| `main/workflow/example/drops/DROP_N_EXAMPLE/CLOSEOUT.md`            | 0  | 0 | 0 | Clean. |
| `main/workflow/example/drops/DROP_N_EXAMPLE/PLAN_QA_PROOF.md`       | 0  | 0 | 0 | Clean. |

#### WIKI.md rewrites (in detail)

Five WIKI.md sections rewritten to remove pre-Drop-2 retired vocabulary and replace with current Drop-1.75 closed-enum vocabulary + canonical-pointer back to § Cascade Vocabulary.

1. **§ "The Tillsyn Model (Node Types)"** (lines 19–34) — replaced the "Drop is a action item with `kind='actionItem'`" framing + "Do Not Use Other Kinds Today" subsection with a current Drop-1.75 description: two node tables (`projects`, `action_items`), the closed 12-value `kind` enum spelled out, three-orthogonal-axis pointer with `metadata.structural_type`, and an updated "Closed 12-Value `kind` Enum (Post-Drop-1.75)" subsection that lists the active enum + names the retired kinds as historical fact. The "Do Not Use Templates Right Now" subsection retained (still current pre-Drop-3 rule).
2. **Line 137** — atomic-leaf description rewritten from "atomic single-actionItem action items" to "`structural_type=droplet` nodes" (canonical structural_type vocabulary).
3. **Line 206** — "the adopting project's drop-end actionItem has one additional deliverable" → "the adopting project's drop-end closeout has one additional deliverable" (closeout = the canonical drop-end action; `actionItem` was the retired pre-Drop-2 kind).
4. **Line 242** — "files a level_2 actionItem under REFINEMENTS" → "files a level_2 `kind=refinement` node under REFINEMENTS" (canonical post-Drop-1.75 closed-enum vocabulary).
5. **§ "Drop Orch Cross-Subtree Exception"** (lines 250–257) — rewrote the kind-choice rules. Was: "drop orchs may add level_2 actionItem nodes …" + "`kind=actionItem, scope=actionItem` still applies — the pre-Drop-2 rule has no carve-outs." Now: drop orchs add level_2 nodes choosing `kind` from the closed 12-value enum (`refinement` for refinements, `discussion` for discussion topics, `closeout` or `plan` for ledger / wiki-changelog / findings rollups as appropriate); pointer to CLAUDE.md § "Post-Drop-1.75 Creation Rule" for the enum; explicit "no fallback kind, no carve-outs" replaces the retired `kind=actionItem` carve-out.

#### Sample rewrites

##### Sample 1: `main/CLAUDE.md` — new "Three Orthogonal Axes" subsection (added)

After (new section between "Project-level kind restriction" paragraph and "Kind Hierarchy" diagram):

```markdown
### Three Orthogonal Axes — `kind` × `metadata.role` × `metadata.structural_type`

Every non-project node is classified along three independent axes, set explicitly at create time. None of them are inferred from the others. Templates `child_rules`, gate rules, and agent bindings dispatch on combinations of all three.

- **`kind` (what work)** — the closed 12-value enum above (post-Drop-1.75). Names the kind of work the agent does (`build` / `plan` / `build-qa-proof` / …).
- **`metadata.role` (who does it)** — closed enum (post-Drop-2): `builder`, `qa-proof`, `qa-falsification`, `qa-a11y`, `qa-visual`, `design`, `commit`, `planner`, `research`. The dual-axis design earns its keep on QA kinds where parent context disambiguates (`build-qa-proof` vs `plan-qa-proof` both carry `role=qa-proof`); non-QA role values exist for agent-binding-lookup symmetry. Pre-Drop-2 the role lives in description prose (`Role: builder`, `Role: qa-proof`, …); post-Drop-2 it lands on `metadata.role`.
- **`metadata.structural_type` (where it sits in the cascade flow)** — closed 4-value enum landing in Drop 3: `drop | segment | confluence | droplet`. Names the node's cascade shape independent of role / kind. Atomicity rules: `droplet` MUST have zero children; `confluence` MUST have non-empty `blocked_by`; `segment` may recurse; `drop` is the level_1 cascade step.

**Cascade vocabulary canonical: `WIKI.md` § `Cascade Vocabulary` — the worked-combinations table, atomicity rules, and orthogonality with `metadata.role` live there. Do not redefine the structural_type vocabulary in this file or any other doc.** Pre-Drop-3, `metadata.structural_type` is not yet validated at the create/update boundary; the orchestrator chooses values consistent with the WIKI definition and Drop 3 lands the validation. Adopters mirror the same canonical-pointer rule in their own project `CLAUDE.md` (see `workflow/example/CLAUDE.md` line 27).
```

##### Sample 2: `main/WIKI.md` § "The Tillsyn Model (Node Types)"

Before:
```markdown
Tillsyn has exactly **two node types** you should use today:

1. **Project** — the root container. One per repo / product / coordination scope. Never nested inside another project.
2. **Drop** — every node below the project. Drops nest **infinitely**.

A "drop" is the Tillsyn-native word for a unit of work. In current runtime terms it is a action item with `kind='actionItem'` (the pre-Drop-2 creation rule — see `CLAUDE.md` § "Pre-Drop-2 Creation Rule"). Drop 2 of the cascade collapses every non-project kind to literal `kind='drop'`; for now, write `kind='actionItem', scope='actionItem'` and **refer to the node as a drop in prose**.

### Do Not Use Other Kinds Today

The `kind_catalog` still lists `build-actionItem`, `subtask`, `qa-check`, `plan-actionItem`, `commit-and-reingest`, `a11y-check`, `visual-qa`, `design-review`, `phase`, `branch`, `decision`, `note`. **Do not use them.** Drop 2's SQL rewrites every non-project node to `drop`. Pre-Drop-2, stick to plain `actionItem` and keep the runtime writes consistent.
```

After:
```markdown
Tillsyn has exactly **two node tables** in the runtime:

1. **`projects`** — the root container. One per repo / product / coordination scope. Never nested inside another project. No `kind` column post-Drop-1.75.
2. **`action_items`** — every node below the project. Nest **infinitely**. The `kind` column is a closed 12-value enum (see `CLAUDE.md` § "Post-Drop-1.75 Creation Rule" for the full enum + creator-chooses-explicitly rule).

In prose we call any non-project node a **drop**, classified along three orthogonal axes — `kind` (what work), `metadata.role` (who does it), and `metadata.structural_type` (where it sits in the cascade flow: `drop | segment | confluence | droplet`). See § Cascade Vocabulary below for the full structural_type enum and orthogonality table.

### Closed 12-Value `kind` Enum (Post-Drop-1.75)

`action_items.kind` is the closed 12-value enum: `plan`, `research`, `build`, `plan-qa-proof`, `plan-qa-falsification`, `build-qa-proof`, `build-qa-falsification`, `closeout`, `commit`, `refinement`, `discussion`, `human-verify`. There is no inferred default and no fallback kind — the creator picks explicitly at create time. Old kinds (`actionItem`, `build-actionItem`, `subtask`, `qa-check`, `plan-actionItem`, `commit-and-reingest`, `a11y-check`, `visual-qa`, `design-review`, `phase`, `branch`, `decision`, `note`) were rewritten by `main/scripts/drops-rewrite.sql` during Drop 1.75 and are no longer accepted.
```

##### Sample 3: `main/WIKI.md` § "Drop Orch Cross-Subtree Exception"

Before:
```markdown
Drop orchs operate inside their assigned level_1 subtree. The one exception: drop orchs may **add** level_2 actionItem nodes under STEWARD's six persistent level_1 parents — `DISCUSSIONS`, `HYLLA_FINDINGS`, `LEDGER`, `WIKI_CHANGELOG`, `REFINEMENTS`, `HYLLA_REFINEMENTS` — and may nest further actionItem drops under their own additions.

This lets drop orchs route per-drop content through the Tillsyn tree for STEWARD visibility, while the on-disk source of truth lives in `workflow/drop_N/` on the drop branch (see `PLAN.md` §15.9). Findings, refinement candidates, ledger entries, wiki-changelog entries, Hylla feedback, and ad-hoc discussion topics each become a `kind=actionItem, scope=actionItem` level_2 node under the matching persistent parent; …

**Hard restrictions on the exception:**

- **Adds only.** …
- **`kind=actionItem, scope=actionItem`** still applies — the pre-Drop-2 rule has no carve-outs.
- **No state transitions on STEWARD-owned nodes.** …
- **Subagents inherit the exception** …
```

After:
```markdown
Drop orchs operate inside their assigned level_1 subtree. The one exception: drop orchs may **add** level_2 nodes under STEWARD's six persistent level_1 parents — `DISCUSSIONS`, `HYLLA_FINDINGS`, `LEDGER`, `WIKI_CHANGELOG`, `REFINEMENTS`, `HYLLA_REFINEMENTS` — and may nest further descendants under their own additions.

This lets drop orchs route per-drop content through the Tillsyn tree for STEWARD visibility, while the on-disk source of truth lives in `workflow/drop_N/` on the drop branch (see `PLAN.md` §15.9). Findings, refinement candidates, ledger entries, wiki-changelog entries, Hylla feedback, and ad-hoc discussion topics each become a level_2 node (`kind=refinement` for refinements, `kind=discussion` for discussion topics, `kind=closeout` or `kind=plan` for ledger / wiki-changelog / findings rollups as appropriate) under the matching persistent parent; …

**Hard restrictions on the exception:**

- **Adds only.** …
- **`kind` chosen from the closed 12-value enum** (post-Drop-1.75) — see `CLAUDE.md` § "Post-Drop-1.75 Creation Rule" for the enum and creator-chooses-explicitly rule. No fallback kind, no carve-outs.
- **No state transitions on STEWARD-owned nodes.** …
- **Subagents inherit the exception** …
```

### Pass 4 verification — orthogonal-axes coherence across the four always-read docs

Acceptance criterion #3 of droplet 3.28: `metadata.role` × `metadata.structural_type` × `kind` orthogonal-axes design must be documented coherently in **at least four always-read docs**: `main/CLAUDE.md`, `main/WIKI.md`, `main/PLAN.md`, `main/workflow/example/CLAUDE.md`.

| Doc | Coverage | Verdict |
|---|---|---|
| `main/CLAUDE.md` | New "Three Orthogonal Axes" section (added in this droplet) explicitly enumerates `kind`, `metadata.role`, `metadata.structural_type` as independent axes with one-line definitions per axis + canonical-pointer to WIKI § Cascade Vocabulary. | **Coherent.** Closes the 3.27 deferral. |
| `main/WIKI.md` | § "The Tillsyn Model (Node Types)" (rewritten in this droplet) introduces the three orthogonal axes by name. § "Cascade Vocabulary" (lines 36–84, 3.6 landed) is the canonical glossary: structural_type enum + atomicity rules + orthogonality with `metadata.role` table + worked combinations. | **Coherent.** Single canonical source. |
| `main/PLAN.md` | Line 74 introduces `metadata.structural_type` axis with waterfall metaphor + 4-value enum. § 19.10 (lines 1625–1651) carries the design-time bullets where the structural_type axis was decided + bound to `metadata.role` orthogonality. | **Coherent.** PLAN.md is design-history-ground-truth; WIKI.md is the user-facing canonical glossary. |
| `main/workflow/example/CLAUDE.md` | Line 27 carries the canonical-pointer rule for adopters: "Cascade vocabulary canonical: project `WIKI.md` § `Cascade Vocabulary` — every adopter project's CLAUDE.md MUST include this pointer and MUST NOT redefine the structural_type vocabulary locally." | **Coherent.** Single canonical anchor — pointer-only mode for the adopter template. |

**Pass 4 verdict:** all four always-read docs cover the orthogonal-axes story coherently with WIKI.md as canonical source. CLAUDE.md acceptance #3 lock now satisfied.

### Pass 2 — Unit A new vocabulary spot-checks

Verified consistency for: `structural_type`, the 4 enum values, atomicity / `Irreducible` rules, 6 plan-QA-falsification attack vectors.

- `structural_type` field name: 11 hits across in-scope active docs (CLAUDE.md, WIKI.md, PLAN.md, workflow/example/CLAUDE.md). All canonical, all pointer-aware.
- 4 enum values (`drop`, `segment`, `confluence`, `droplet`): used contextually as `structural_type` values in WIKI.md § Cascade Vocabulary (lines 38–70) and PLAN.md §19.10 (lines 1625–1651). The same words also appear in cascade-tree-generic prose ("level_1 drop", "atomic droplet") — those generic uses are correct because the structural_type values are deliberately named after the prose words. No disambiguation rewrite needed; WIKI § Cascade Vocabulary already explains the dual usage.
- Atomicity rules: WIKI.md § Cascade Vocabulary table (lines 44–49) has the canonical atomicity rules per structural_type. PLAN.md §19.10 line 1645 enforces "Droplet-with-children is misclassification." No drift.
- 6 plan-QA-falsification attack vectors: lock referenced as canonical at `~/.claude/agents/go-qa-falsification-agent.md` lines 109–130 (per droplet-3.28 spec note). No in-repo doc redefines them; PLAN.md §19.10 line 1644–1649 lists the 5 cascade-vocab attacks (droplet-with-children, confluence-without-blockers, segment-without-recursion, role/structural_type contradiction, plus implicit "global L1 sweep" added by 3.7). Pointer-coherent.

### Pass 3 — Unit B template-system spot-checks

Verified consistency for: `Template.AllowsNesting`, `Template.ChildRulesFor`, `KindCatalog`, `[child_rules]`, `[kinds.*]`, `[agent_bindings.*]`, `internal/templates/builtin/default.toml`, `templates.LoadDefaultTemplate`.

- Template-system terms appear in design prose in `main/PLAN.md` §19.10 (lines 1612–1623) and TOML config-shape examples (lines 612–656, 824, 1175–1181, 1361). All canonical references — match the symbol vocabulary that landed in `internal/templates/`. No parallel naming invented.
- `child_rules` references in `main/AGENT_CASCADE_DESIGN.md` (lines 254, 567), `main/CLAUDE.md` (line 78 implicit, plus the new Three Orthogonal Axes section's mention of templates dispatching on combinations of all three axes), and `main/WIKI.md` (line 34, 103) — all canonical.
- `README.md` Template-system JSON examples (lines 361, 487, 510) reference `child_rules` correctly but use **retired pre-Drop-1.75 kind values** (`build-actionItem`, `qa-check`). Same disposition as 3.27: README needs a dedicated overhaul droplet.

### Pass 4 — Unit C STEWARD / auth spot-checks

Verified consistency for: `principal_type: steward`, `Owner = "STEWARD"`, `Persistent`, `DevGated`, auth-level state-lock (`assertOwnerStateGate`, `assertOwnerStateGateUpdateFields`), template auto-generation (`seedStewardAnchors`, `seedDropFindingsAndGate`).

- `principal_type: steward` referenced canonically in `STEWARD_ORCH_PROMPT.md` line 198 + `main/PLAN.md` line 1289, 1659. No parallel naming. The auth-level state-lock concept is named in PLAN.md line 1289 ("auth-level state-lock"), §19.3 (the auto-generation seed), and STEWARD_ORCH_PROMPT.md line 198. Coherent.
- `Persistent` / `DevGated` (the two domain-primitive booleans on every cascade node, named in PLAN.md §19.10 + WIKI.md § "Adjacent Domain Primitives" lines 72–79) — WIKI.md exposes them as `metadata.persistent` and `metadata.dev_gated` (snake-case, lowercase). PLAN.md uses `metadata.persistent: true` (line 1637) and `metadata.persistent` (line 1637) — same convention. STEWARD_ORCH_PROMPT.md uses prose form ("Persistent orchestrator", "Persistent Level_1 STEWARD-Owned Drops") — these are about the abstract persistent concept, not the metadata field. No parallel-naming drift.
- `Owner = "STEWARD"` — referenced in PLAN.md §19.10 line 1660 as `metadata.owner = STEWARD`. STEWARD_ORCH_PROMPT.md uses `metadata.owner: "STEWARD"` (TOML/JSON form). Both canonical.
- `seedStewardAnchors`, `seedDropFindingsAndGate`, `assertOwnerStateGate`, `assertOwnerStateGateUpdateFields` — these are Go-symbol names from `internal/app/auto_generate_steward.go` (per the droplet-3.28 spec). MD docs reference the **concept** ("template auto-generation of STEWARD-scope items", PLAN.md line 1660) rather than the symbol names — correct discipline (MDs document behavior, code is the source-of-truth for symbol names). No drift.

### Mage CI smoke test

```
$ mage ci
[SUCCESS] Coverage threshold met
  All packages are at or above 70.0% coverage.
[RUNNING] Building till from ./cmd/till
[SUCCESS] Built till from ./cmd/till
```

CI green. No Go code touched; smoke test confirms docs-only edits did not break anything.

### Things NOT done (intentional / deferred)

1. **`main/README.md` "Tillsyn integration framing" section** — same disposition as 3.27. README's template/product-vocabulary section uses retired pre-Drop-1.75 kind names inside template JSON examples. Recommend a dedicated README-refresh droplet rather than a partial vocab sweep.
2. **MEMORY.md line 21** — correctly documents `"drops all the way down" RETIRED` as historical fact. No edit (3.27 disposition unchanged).
3. **Drop-2 / Drop-1.5 / Drop-1.75 worklog audit-trail prose** — historical narrative correctly references retired vocabulary describing the pre-collapse world. Out of scope for active-docs sweep.
4. **`workflow/drop_3/PLAN.md` § own §19.10 references to retired terms** — these are design-history prose explaining what the drop replaced. Out of scope for active-docs sweep (planning artifacts are audit trail).
5. **`~/.claude/` outside-repo edits** — none required this round; 3.27 already swept the cascade-vocabulary pollution there.

## Hylla Feedback

N/A — droplet 3.28 touched non-Go files only (markdown sweep across CLAUDE.md / WIKI.md / PLAN.md and the `workflow/example/**` adopter templates, plus pass-2/3/4 spot-checks across STEWARD_ORCH_PROMPT.md / AGENT_CASCADE_DESIGN.md / etc.). Hylla today indexes Go files only; markdown is out of scope. All evidence-gathering used `Read` / `Grep` / `Glob` / `rg` directly per the project rule "Hylla Indexes Only Go Files Today." No Hylla query was issued; no fallback miss to log.
