# Drop 3 Droplet 3.14 — Builder QA Falsification

**Target:** `internal/templates/builtin/default.toml` + `internal/templates/embed.go` + `internal/templates/embed_test.go`
**Mode:** Falsification (counterexample-driven attack)
**Verdict:** PASS

## Round 1

Attempted 14 attacks against the builder's claim. None produced a CONFIRMED counterexample. Two scope deferrals (drop-planner rule, STEWARD persistent-parent seeds) verified as honest, fully documented inline, and routed forward to droplet 3.20.

---

## 1. Counterexamples (CONFIRMED)

None.

No counterexample reproduced. All 14 attack vectors REFUTED.

---

## 2. Refuted Attacks

### 2.1 `//go:embed` path attack — REFUTED

The directive in `internal/templates/embed.go:13` reads `//go:embed builtin/default.toml` — a clean relative path with no `..` traversal, matching fix L4. The TOML file exists at `internal/templates/builtin/default.toml`. `mage ci` builds cleanly, which is sufficient proof the embed pipeline links. Per L4 the repo-root `templates/` is NOT recreated; `ls` confirms only `internal/templates/builtin/default.toml` exists.

### 2.2 Reverse-hierarchy completeness — REFUTED

All 5 prohibitions encoded as 11-element `allowed_child_kinds` allow-lists (closed-12 minus the prohibited child):

- `[kinds.closeout]` lines 120–132: 11 entries, omits `closeout`.
- `[kinds.commit]` lines 139–151: 11 entries, omits `plan`.
- `[kinds.human-verify]` lines 169–181: 11 entries, omits `build`.
- `[kinds.build-qa-proof]` lines 81–93: 11 entries, omits `plan`.
- `[kinds.build-qa-falsification]` lines 99–111: 11 entries, omits `plan`.

`TestDefaultTemplateRejectsReverseHierarchyProhibitions` (embed_test.go:64–93) covers all 5 cases via `tpl.AllowsNesting(parent, child)` returning `(false, non-empty)`. `TestDefaultTemplateProhibitionsAreExplicit` (embed_test.go:351–384) additionally asserts `len(AllowedChildKinds) == 11` AND the prohibited child is absent — finding 5.B.16 N3 (explicit-deny, not implicit-by-absence) is encoded structurally.

### 2.3 Auto-create rule completeness — REFUTED

For `(KindBuild, StructuralTypeDroplet)`: 2 resolutions (build-qa-proof + build-qa-falsification, both `BlockedByParent=true`). `TestDefaultTemplateChildRulesForBuild` (embed_test.go:136–163) asserts both kinds + `BlockedByParent`.

For `(KindPlan, StructuralTypeDroplet)`: 2 resolutions (plan-qa-proof + plan-qa-falsification). `TestDefaultTemplateChildRulesForPlan` (embed_test.go:169–196).

For `(KindPlan, StructuralTypeDrop)`: 4 resolutions (universal plan-qa-proof + universal plan-qa-falsification + drop-specific plan-qa-proof + drop-specific plan-qa-falsification). The 4th-not-5th expected; the planner-droplet rule named at PLAN.md § 19.3 line 1635 is DEFERRED. `TestDefaultTemplateChildRulesForDropPlan` (embed_test.go:206–227) asserts `len == 4`, each kind appears twice.

### 2.4 Drop-planner deferral honesty — REFUTED

(a) Inline documentation present at `default.toml:228–245` — explicitly headed `# structural_type=drop planner droplet — DEFERRED.`, naming the cycle-validator failure mode (`plan -> plan` self-loop tripped by `validateChildRuleCycles` in `internal/templates/load.go`).

(b) No `[[child_rules]]` row in default.toml has `create_child_kind = "plan"` — verified by reading every `[[child_rules]]` block (lines 200–261). The only kinds created are `build-qa-proof`, `build-qa-falsification`, `plan-qa-proof`, `plan-qa-falsification`.

(c) `ChildRulesFor(KindPlan, StructuralTypeDrop)` returns 4 NOT 5 children. Confirmed via test pass.

The deferral is technically forced: if the rule were added, `validateChildRuleCycles` would build a graph with `plan → plan` and DFS would re-enter the gray-colored node, returning `ErrTemplateCycle` at load time. The deferral comment correctly names two forward paths for 3.20 (relax cycle validator OR introduce distinct planner kind).

### 2.5 STEWARD seed deferral honesty — REFUTED

(a) Inline documentation present at `default.toml:263–291` — explicitly headed `# STEWARD persistent parent seeds — DEFERRED to droplet 3.20.`

(b) No `[[child_rules]]` row seeds STEWARD's 6 persistent parents (`DISCUSSIONS`, `HYLLA_FINDINGS`, `LEDGER`, `WIKI_CHANGELOG`, `REFINEMENTS`, `HYLLA_REFINEMENTS`).

(c) Three forward-routing options named at lines 279–283: (a) new `[[steward_seeds]]` TOML table, (b) sentinel `WhenParentKind` value, (c) new `ChildRule.AtProjectCreation` bool. All three documented as 3.20's encoding choice.

The deferral rationale at lines 271–278 is technically correct: `ChildRule.WhenParentKind` is a closed-enum `domain.Kind` value, but project rows have no `kind` column post-Drop-1.75, so there is no clean way to express "spawn at project creation" with the current schema.

NIT: PLAN.md droplet 3.14 acceptance line 350 ("Per L3 ... default template's `[child_rules]` seeds the 6 STEWARD persistent parents") explicitly required this in 3.14, but the deferral is documented inline AND surfaced to the orchestrator as a scope finding. The orchestrator's spawn prompt acknowledged the deferral up front. Treating as accepted scope adjustment, not a counterexample.

### 2.6 `agent_bindings` validation pass-through — REFUTED

`TestDefaultTemplateAgentBindingsCoverAllKinds` (embed_test.go:234–250) asserts every closed-12-kind has a populated `[agent_bindings.<kind>]` AND `binding.Validate()` returns nil. Test passes. Probed counterexample: a kind with `max_tries = 0` would fail Validate (line 225–227 in schema.go). Inspected all 12 binding rows — every one carries `max_tries = 3` (or `max_tries = 1` for orchestrator-managed kinds), `max_turns >= 1`, `agent_name` non-empty, `model` non-empty, `max_budget_usd >= 0`, `blocked_retries >= 0`, `blocked_retry_cooldown >= 0`. All pass.

### 2.7 opus enforcement — REFUTED

`TestDefaultTemplateBuildersRunOpus` (embed_test.go:256–287) covers the 7 opus kinds (plan, research, build, plan-qa-proof, plan-qa-falsification, build-qa-proof, build-qa-falsification) AND asserts `commit.Model == "haiku"`. Direct TOML inspection confirms:

- `[agent_bindings.plan]` model = "opus" (line 319)
- `[agent_bindings.research]` model = "opus" (line 332)
- `[agent_bindings.build]` model = "opus" (line 345)
- `[agent_bindings.plan-qa-proof]` model = "opus" (line 358)
- `[agent_bindings.plan-qa-falsification]` model = "opus" (line 371)
- `[agent_bindings.build-qa-proof]` model = "opus" (line 384)
- `[agent_bindings.build-qa-falsification]` model = "opus" (line 397)
- `[agent_bindings.commit]` model = "haiku" (line 425)

Memory rule `feedback_opus_builders_pre_mvp.md` honored.

### 2.8 Closed-12-enum coverage — REFUTED

`TestDefaultTemplateCoversAllTwelveKinds` (embed_test.go:45–57) iterates `allKinds` and asserts each has a `[kinds.<kind>]` section. `allKinds` mirrors `domain.validKinds` exactly (verified: nesting_test.go:13–26 vs domain/kind.go:35–48). All 12 entries present in default.toml at lines 52–182.

### 2.9 `Duration` wire format — REFUTED

Every `agent_bindings.<kind>.blocked_retry_cooldown` in default.toml uses string form `"30s"` (lines 328, 341, 354, 367, 380, 393, 406, 421, 434, 448, 462, 476). The `templates.Duration` wrapper's `UnmarshalText` (schema.go:41–48) parses these via `time.ParseDuration`. The pre-existing `TestAgentBindingDurationStringWireForm` (agent_binding_test.go:192–233) is the regression bar; default.toml's strings round-trip cleanly through `LoadDefaultTemplate`.

### 2.10 Cross-fixture drift — REFUTED

`TestDefaultTemplateMatchesNestingFixture` (embed_test.go:298–319) iterates the cartesian product `allKinds × allKinds`. For every pair the hand-coded fixture rejects, the loaded template must also reject AND emit a non-empty reason. Test passes. Probed counterexample direction: could the loaded template REJECT a pair the fixture ALLOWS? In principle yes (loaded is at least as strict; not equality). But inspection of all 12 `[kinds.X]` rows shows non-prohibition kinds carry only `structural_type = "droplet"` (no `allowed_child_kinds`, no `allowed_parent_kinds`), so they fall through to universal-allow. No spurious extra rejection.

The test description explicitly names the asymmetry ("We do NOT assert literal Template equality") and explains why: agent_bindings + child_rules are loaded-only, not fixture-side. The chosen one-directional invariant — fixture rejection ⇒ loaded rejection — is the correct minimal property to enforce per finding 5.B.12 (CE7 drift mitigation).

### 2.11 STEWARD-owned kinds — REFUTED

`TestDefaultTemplateStewardOwnedKinds` (embed_test.go:326–343) asserts `Kinds[KindCloseout].Owner == "STEWARD"` AND `Kinds[KindRefinement].Owner == "STEWARD"`. Both pass. Direct TOML inspection: `[kinds.closeout]` line 119 sets `owner = "STEWARD"`; `[kinds.refinement]` line 157 sets `owner = "STEWARD"`. Per fix L7 + PLAN.md § 15.7.

### 2.12 Embed file existence — REFUTED

`mage ci` succeeded with `Built till from ./cmd/till`. Go's `//go:embed` directive is compile-time; a missing or misplaced `builtin/default.toml` would have produced a compile-time error. The fact that `mage ci` produced 1793 passing tests across 20 packages — including a green build of `cmd/till` — proves the embed link is healthy.

### 2.13 Documentation completeness — REFUTED

Both deferrals carry inline comment blocks visible to a future reader scrolling the file:

- Drop-planner deferral: lines 228–245, named `# structural_type=drop planner droplet — DEFERRED.`
- STEWARD seeds deferral: lines 263–291, named `# STEWARD persistent parent seeds — DEFERRED to droplet 3.20.`

Each block explains the technical blocker AND points at 3.20 as the resolution drop. Both reference PLAN.md sections by name.

### 2.14 YAGNI — REFUTED

`git status --porcelain` shows the diff is exactly:

- `internal/templates/builtin/default.toml` (new)
- `internal/templates/embed.go` (new)
- `internal/templates/embed_test.go` (new)

Plus an unrelated pre-existing untracked `ta-docs/`. No edits to existing committed code. No spurious new packages, no new modules, no extra files. Scope is exactly TOML + embed pipeline + tests.

---

## 3. Most Damaging Counterexample

None produced. The most carefully-probed angle was attack 2.5 (STEWARD seed deferral) — PLAN.md droplet 3.14 acceptance line 350 explicitly required STEWARD parents seeded NOW, and the builder deferred it. But:

- The deferral is technically forced by the closed-enum `WhenParentKind` schema constraint.
- The deferral is documented inline with three forward-routing options for 3.20.
- The orchestrator's spawn prompt accepts the deferral up front ("Two scope deferrals to 3.20").
- 3.20's blocked-by edge already routes the work forward (PLAN.md line 650).

No counterexample reproducible. Treating as accepted scope adjustment.

---

## 4. Verdict Summary

**PASS.**

236/236 templates package tests green. `mage ci` green (1793/1793 across 20 packages, 96.3% coverage on `internal/templates`). All 14 attack vectors REFUTED. Two scope deferrals (drop-planner rule, STEWARD persistent-parent seeds) verified as:

- Technically forced by current schema constraints (cycle validator + closed-enum `WhenParentKind`).
- Inline-documented in default.toml with forward-routing options.
- Already routed to droplet 3.20 via the existing blocked-by graph.
- Acknowledged in the orchestrator's spawn prompt.

The 12 `[kinds.<kind>]` rows + 12 `[agent_bindings.<kind>]` rows + 6 `[[child_rules]]` rows form a closed, validated, embedded artifact. Reverse-hierarchy prohibitions are explicit (11-element allow-lists, not implicit absence) per finding 5.B.16 N3. Cross-validation against the hand-coded fixture per finding 5.B.12 CE7 is structurally encoded. opus-pre-MVP rule honored. STEWARD ownership encoded on closeout + refinement per fix L7.

No counterexamples found. Verdict: **PASS**.
