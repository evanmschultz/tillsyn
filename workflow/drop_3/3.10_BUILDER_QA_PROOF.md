# Droplet 3.10 — Builder QA Proof

**Droplet:** 3.10 — `Template.AllowsNesting(parent, child Kind)` validation truth
**Drop:** 3 — Template Configuration
**Reviewer:** go-qa-proof-agent
**Verdict:** **PASS**

## 1. Required Proof Checks

- 1.1 **Signature exact match.** `func (t Template) AllowsNesting(parent, child domain.Kind) (allowed bool, reason string)` declared at `internal/templates/nesting.go:35`. gopls hover confirms the merged-parameter form unfolds to `(parent domain.Kind, child domain.Kind)` — semantically identical, no drift from PLAN.md § 3.10 acceptance line.
- 1.2 **Reason format exact.** Two `fmt.Sprintf` callsites (lines 45 and 56) both use the literal format string `"kind %q cannot nest under parent kind %q (rule: %s)"`. Rule substitutions are the two literal strings `"not in allowed_child_kinds"` (parent-side rejection) and `"not in allowed_parent_kinds"` (child-side rejection). Tests assert the entire formatted string verbatim (`nesting_test.go:135` for parent-side, `:233` for child-side).
- 1.3 **Empty-template universal-allow.** `nesting.go:36-39` reads `parentRule := t.Kinds[parent]` (zero-value when parent absent), then `isZeroKindRule(parentRule)` short-circuits to `(true, "")`. `TestAllowsNestingEmptyTemplateUniversalAllow` (`:163-183`) hits this for all 144 pairs against `var empty Template`, asserting both `allow == true` and `reason == ""`.
- 1.4 **AllowedChildKinds non-empty + child not in list rejects with correct reason.** `nesting.go:41-46` — when `len(parentRule.AllowedChildKinds) > 0` and `slices.Contains` returns false, the function returns `(false, ..."not in allowed_child_kinds")`. Cartesian-product test exercises this for the 5 prohibition cells in `fixtureTemplate` (closeout→closeout, commit→plan, human-verify→build, build-qa-proof→plan, build-qa-falsification→plan).
- 1.5 **AllowedParentKinds non-empty + parent not in list rejects with correct reason.** `nesting.go:52-57` — when parent rule has no `AllowedChildKinds` constraint and `len(childRule.AllowedParentKinds) > 0` and `slices.Contains` returns false, returns `(false, ..."not in allowed_parent_kinds")`. `TestAllowsNestingChildSideAllowList` row "child allow-list excludes parent rejects" (`:228-234`) covers this branch directly with build→build-qa-proof.
- 1.6 **Hand-coded fixture (no default.toml load).** `fixtureTemplate()` at `nesting_test.go:55-92` constructs a `Template{}` literal in Go. No `os.Open`, no `embed`, no `loadTemplate`, no reference to `default.toml` anywhere in the test file. Per PLAN.md § 3.10 finding 5.B.12: 3.14's `embed_test.go` will independently round-trip the loaded TOML against this same hand-coded shape.
- 1.7 **144-row cartesian product.** `nesting_test.go:117-158`. Outer loop iterates `allKinds` (12 values, line 13-26) crossed with `allKinds` again, building 144 rows. Line 141-143 explicitly asserts `len(rows) == 144` and fails fast if not. Each pair is run as a `t.Parallel()` subtest. `mage testFunc ./internal/templates TestAllowsNestingCartesianProduct` reports 145 passes (1 parent + 144 subtests).
- 1.8 **Empty-template test covers universal-allow.** `TestAllowsNestingEmptyTemplateUniversalAllow` at `:163-183` — uses `var empty Template` (zero-value, no Kinds map allocated), calls `AllowsNesting` for an explicit primary case (build→build-qa-proof) plus a 144-pair sweep, asserting `(true, "")` on every pair.
- 1.9 **Child-side allow-list test covers non-cartesian branches.** `TestAllowsNestingChildSideAllowList` at `:191-267` constructs a Template where the parent rule sets only `Owner` (skipping the AllowedChildKinds branch on `nesting.go:41`), forcing flow into the child-side block on `:48-57`. Four test rows cover: (a) child reject via parent not in `AllowedParentKinds`, (b) child allow via parent in `AllowedParentKinds`, (c) child rule absent → final fallback returns `(true, "")`, (d) child rule present but with no allow-lists → final fallback returns `(true, "")`. All four `nesting.go` "permissive" branches are reached.
- 1.10 **Coverage ≥ 90% on `nesting.go`.** `mage ci` reports 96.9% statement coverage on the entire `internal/templates` package. `nesting.go` has 6 distinct flow branches (parent-zero shortcut, parent-allow-contains, parent-allow-rejects, child-zero fallback, child-allow-contains, child-allow-rejects, final universal-allow); every branch is reached by at least one test (per checks 1.3-1.5 + 1.9). With only 73 source lines and exhaustive branch coverage, `nesting.go`'s file-level coverage is at or near 100% — cleanly above the 90% acceptance bar.
- 1.11 **No `tc := tc` / loop-variable shadow.** Searched both test functions; the only `:=` patterns inside loops are `r := row{...}` at `:132` (struct construction, not shadow) and the loop variable in `for _, r := range rows` at `:145` (Go 1.22+ per-iteration scope makes shadow unnecessary). Subtests close over the per-iteration loop variable directly — correct idiom.
- 1.12 **Reverse-hierarchy prohibitions encoded per PLAN.md § 19.3.** `fixtureTemplate()` and `expectedReject()` together encode the four canonical prohibitions:
  - closeout-no-closeout-parent: `KindCloseout` parent excludes `KindCloseout` child (`:62-65`, `:101`).
  - commit-no-plan-child: `KindCommit` parent excludes `KindPlan` child (`:68-71`, `:103`).
  - human-verify-no-build-child: `KindHumanVerify` parent excludes `KindBuild` child (`:74-77`, `:105`).
  - build-qa-proof-no-plan-child: `KindBuildQAProof` parent excludes `KindPlan` child (`:80-83`, `:107`).
  - build-qa-falsification-no-plan-child: `KindBuildQAFalsification` parent excludes `KindPlan` child (`:86-89`, `:107`).
  Net cartesian rejection count: 5 cells (closeout/closeout, commit/plan, human-verify/build, build-qa-proof/plan, build-qa-falsification/plan). Five rejections + 139 universal-allow rows = 144 total. Encoding strategy via `allKindsExcept` correctly inverts "prohibit X" into "allow every kind except X" so the ChildKinds list is non-empty (forcing the parent-side rule branch) while still allowing all other pairings.

## 2. Verification Gates

- 2.1 **`mage ci` green.** Full ci run from `/Users/evanschultz/Documents/Code/hylla/tillsyn/main/`: 1743 tests passed, 0 failed across 20 packages. `internal/templates` reports 96.9% coverage (above 70% gate, above 90% droplet AC). Build step succeeds. No format drift on tracked files.
- 2.2 **`mage test-pkg ./internal/templates` green.** 189 tests pass, 0 failures. Matches builder report.
- 2.3 **`mage testFunc ./internal/templates TestAllowsNestingCartesianProduct` green.** 145 tests pass (1 parent + 144 subtests). Matches the 144-row acceptance criterion.
- 2.4 **`mage format` clean for the new files.** Running `mage format ./internal/templates` reports `[SUCCESS] Formatted Go sources` and produces no diff on `nesting.go` / `nesting_test.go`. (Working tree shows unstaged format/code edits to `load.go` + `schema.go` + a new `agent_binding_test.go` — those are droplet 3.13's work, co-resident but disjoint per the spawn-prompt note.)
- 2.5 **No raw `go` toolchain invocations.** All verification ran through mage targets per CLAUDE.md.
- 2.6 **No `mage install`.** Confirmed not invoked.

## 3. Findings

- 3.1 **None.** Every required check passes with exact-match evidence. Two minor nits worth recording but neither rises to a finding:
  - 3.1.a The cartesian-product test name format `"%s_under_%s"` with hyphenated kind values produces subtest names like `build-qa-proof_under_human-verify` — Go's `testing` package replaces `/` and quoting in subtest names but accepts hyphens fine. Output is readable.
  - 3.1.b `isZeroKindRule` (lines 67-72) is exported-style documented but lowercase-named — correct for a package-private helper, doc comment matches Go convention. No issue; called only from `AllowsNesting` and not directly tested, but reachable through every test case that hits the empty-template / absent-from-map / explicit-zero paths, so it's covered transitively.

## 4. Verdict Summary

**PASS.** All 12 required proof checks satisfied with explicit code citations. All verification gates green. Coverage on the package is 96.9%, with `nesting.go` exercising every branch via three complementary test functions: the 144-row cartesian product covers parent-side rules + universal-allow defaults; the empty-template test covers the cold-start zero-value short-circuit; the child-side test covers the branches the cartesian product cannot reach (because `fixtureTemplate` only encodes parent-side prohibitions). Reverse-hierarchy prohibitions match PLAN.md § 19.3 encoding strategy via the `allKindsExcept` inversion. Reason-string format and rule literals are byte-exact to acceptance criteria. Hand-coded fixture confirmed (no `default.toml` load), satisfying finding 5.B.12's two-distinct-paths-one-source-of-truth invariant.

The implementation is short (73 lines), idiomatic, and the resolution-order doc comment (lines 16-34) maps 1:1 to the test structure — auditing future maintenance changes against the spec will be straightforward.

## 5. Hylla Feedback

Not exercised this round — the four files I needed to confirm (`nesting.go`, `nesting_test.go`, `schema.go`, `kind.go`) are at known paths and were read directly via `Read`. PLAN.md is non-Go (markdown) so falls outside Hylla's index per CLAUDE.md §"Code Understanding Rules" rule 3. No Hylla queries were necessary, so no fallback misses to record.
