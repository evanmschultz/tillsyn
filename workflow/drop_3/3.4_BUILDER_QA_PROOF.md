# Droplet 3.4 — BUILDER QA PROOF

**Reviewer:** go-qa-proof-agent (subagent)
**Droplet:** 3.4 — App-service + MCP plumbing for `structural_type` + cross-package fixture migration
**Date:** 2026-05-02
**Verdict:** PASS

## 1. Required Proof Checks

### 1.1 `app.CreateActionItemInput` gains `StructuralType` (rejected-empty doc)

PASS. `internal/app/service.go:404` — `CreateActionItemInput` struct at `:404` adds `StructuralType domain.StructuralType` directly after `Role` (`:413`). The doc comment at `:414-419` reads:

> "StructuralType places the action item on the cascade tree's shape axis (drop|segment|confluence|droplet). Empty is REJECTED on create — domain.NewActionItem returns ErrInvalidStructuralType. This diverges from Role's permissive empty: the cascade-methodology shape axis is mandatory at creation time."

Doc-comment matches contract verbatim ("Empty is REJECTED on create"); divergence from Role's permissive-empty cited.

### 1.2 `app.UpdateActionItemInput` gains `StructuralType` (preserves-empty doc)

PASS. `internal/app/service.go:435` — `UpdateActionItemInput` struct adds `StructuralType domain.StructuralType` at `:451` directly after `Role` (`:445`). Doc at `:446-450`:

> "StructuralType optionally updates the action item's closed-enum structural type. Empty preserves the existing value (no-op) — mirrors Role's required-on-create / optional-on-update split. A non-empty value must match the closed StructuralType enum or the service returns ErrInvalidStructuralType."

Preserves-empty semantics + Role-mirroring split documented; ErrInvalidStructuralType mismatch contract documented.

### 1.3 `service.go:CreateActionItem` threads field; placeholder fallback removed

PASS. `internal/app/service.go:586-608` — the `domain.NewActionItem` literal now passes `StructuralType: in.StructuralType` (`:593`) instead of the prior placeholder. The `git diff` for service.go explicitly shows the deleted block:

```go
// REMOVED:
//   // StructuralType defaults to droplet — the dominant cascade-leaf
//   // shape — until CreateActionItemInput grows a StructuralType field
//   // ...
//   StructuralType: domain.StructuralTypeDroplet,
```

replaced with `StructuralType: in.StructuralType,`. The empty-rejection enforcement in `internal/domain/action_item.go:188-193` (`if in.StructuralType == "" { return ActionItem{}, ErrInvalidStructuralType }`) is now reachable from the production path — proven by the new real-adapter-chain test in §1.8.

### 1.4 `service.go:UpdateActionItem` adds Role-mirroring sibling block validating non-empty

PASS. `internal/app/service.go:808-819` — sibling block lands directly after the existing Role update block at `:797-807`:

```go
if normalized := domain.NormalizeStructuralType(in.StructuralType); normalized != "" {
    if !domain.IsValidStructuralType(normalized) {
        return domain.ActionItem{}, domain.ErrInvalidStructuralType
    }
    actionItem.StructuralType = normalized
    actionItem.UpdatedAt = s.clock().UTC()
}
```

Empty short-circuits (no-op preserves prior); non-empty normalizes + validates via `IsValidStructuralType`; mismatch returns `ErrInvalidStructuralType`. Mirrors `Role` precedent line-for-line.

### 1.5 `mcp_surface.go` — both request structs gain `StructuralType string`

PASS.

- `CreateActionItemRequest` at `internal/adapters/server/common/mcp_surface.go:57-81` — `StructuralType string` lands at `:72` after `Role` (`:66`). Doc at `:67-71` carries the empty-rejected contract verbatim plus the cross-reference to `domain.IsValidStructuralType`.
- `UpdateActionItemRequest` at `:84-103` — `StructuralType string` lands at `:100` after `Role` (`:94`). Doc at `:95-99` carries the preserves-empty contract.

Both transport DTOs use plain `string` (consumed by the adapter's `domain.StructuralType(strings.TrimSpace(...))` cast); type discipline is correct for an MCP boundary.

### 1.6 `app_service_adapter_mcp.go` — both adapter literals thread the field

PASS.

- `CreateActionItem` adapter at `:666-684` adds `StructuralType: domain.StructuralType(strings.TrimSpace(in.StructuralType))` at `:672` directly after `Role` (`:671`).
- `UpdateActionItem` adapter at `:709-722` adds the same line at `:717` directly after `Role` (`:716`); existing field-tag alignment was reformatted (the `git diff` shows whitespace realignment from `:` to `:           ` on every field — a benign gofumpt artifact, no semantic change).

Both adapters apply `strings.TrimSpace` before the cast — the trim is idempotent with the service-layer `NormalizeStructuralType` call but gives the boundary defense-in-depth against whitespace-only inputs.

### 1.7 `extended_tools.go` — args struct + both case bodies + 3 schema decls

PASS.

- Anonymous-struct `args` at `:865` adds `StructuralType string \`json:"structural_type"\`` after `Role` (`:864`) — JSON tag is the canonical snake_case form per contract.
- `case "create"` block at `:1034-1051` threads `StructuralType: args.StructuralType` (`:1038`) into `common.CreateActionItemRequest{}`.
- `case "update"` block at `:1094-1105` threads `StructuralType: args.StructuralType` (`:1101`) into `common.UpdateActionItemRequest{}`.
- Three tool-schema declarations gain `mcp.WithString("structural_type", ..., mcp.Enum("drop", "segment", "confluence", "droplet"))`:
  - `till.action_item` (`:1374`) — required-on-create / preserves-on-update.
  - Legacy `till.create_task` (`:1429`) — required-on-create.
  - Legacy `till.update_task` (`:1457`) — preserves-on-update.

All three schema declarations enforce the closed enum at the MCP boundary via `mcp.Enum(...)`. Each description carries the cross-reference to `WIKI.md §Cascade Vocabulary` for adopter clarity.

### 1.8 `extended_tools_test.go` — stub semantics + new boundary-test

PASS — and stronger than the contract requires.

**Stub `CreateActionItem` at `:441-467`:**
- Empty `StructuralType` defaults to `domain.StructuralTypeDroplet` (per finding 5.A.9 mitigation — keeps legacy fixtures round-tripping).
- Non-empty non-member rejects with `errors.Join(common.ErrInvalidCaptureStateRequest, domain.ErrInvalidStructuralType)`.
- Doc comment at `:429-440` explicitly notes the divergence from production: "production rejects empty StructuralType with ErrInvalidStructuralType, but the stub defaults empty input to StructuralTypeDroplet so existing fixture rows ... keep round-tripping."

**Stub `UpdateActionItem` at `:481-505`:**
- Empty `StructuralType` preserves prior (matches production no-op semantics; the stub simply emits empty since there is no persisted prior to echo).
- Non-empty non-member rejects with the same wrapped error shape.

**`TestActionItemMCPRejectsEmptyOrInvalidStructuralType` at `:3560-3691`** — three sub-tests, all `t.Parallel()`:

1. **`empty structural_type rejected on create through real adapter chain`** — wires up real `sqlite.Open` + real `app.NewService` + real `common.NewAppServiceAdapter` (no stub), creates a real project + columns, then issues `adapter.CreateActionItem(...)` with `StructuralType` deliberately omitted. Asserts `errors.Is(err, domain.ErrInvalidStructuralType)`. This proves empty-rejection through the FULL production stack — exactly the strongest possible boundary check.
2. **`unknown structural_type rejected on create at MCP boundary`** — full HTTP roundtrip (`postJSONRPC` against an `httptest.NewServer`) with `structural_type: "not-a-structural-type"`; asserts `isError == true` AND error text starts with `invalid_request:`. Stub-driven (no real DB needed for unknown-value mapping).
3. **`unknown structural_type rejected on update at MCP boundary`** — same shape as (2) but on the `update` operation. Asserts the same error mapping.

Test exercises BOTH "empty-via-real-adapter-chain on create" AND "unknown-via-stub on create + update" — full coverage of the contract.

### 1.9 Cross-package fixture migration complete

PASS. Verified via `git grep -n "app.CreateActionItemInput{" -- "*.go"` enumerating 18 callsites:

| File                                                                                       | Callsites | StructuralType supplied |
| ------------------------------------------------------------------------------------------ | --------- | ----------------------- |
| `cmd/till/action_item_cli_test.go`                                                         | 2         | both explicit `:Droplet` |
| `cmd/till/embeddings_cli_test.go`                                                          | 1         | explicit `:Droplet` |
| `internal/adapters/server/common/app_service_adapter_auth_context_test.go`                 | 4         | helper `mustCreateActionItemForTest` defaults empty → Droplet |
| `internal/adapters/server/common/app_service_adapter_mcp.go` (production adapter)          | 1         | threaded from request DTO |
| `internal/adapters/server/common/app_service_adapter_mcp_actor_attribution_test.go`        | 1         | explicit `:Droplet` |
| `internal/adapters/server/httpapi/handler_integration_test.go`                             | 3         | all 3 explicit `:Droplet` |
| `internal/adapters/server/mcpapi/handler_integration_test.go`                              | 3         | all 3 explicit `:Droplet` |
| `internal/adapters/storage/sqlite/repo_test.go`                                            | 2         | both explicit `:Droplet` |
| `internal/tui/model.go` (production TUI form-submit)                                       | 1         | explicit `:Droplet` + Drop 4.5 placeholder comment |

Every callsite either supplies `StructuralType` explicitly OR routes through a helper that defaults it. No bare `app.CreateActionItemInput{}` literal remains that omits the field. The `mustCreateActionItemForTest` helper diff at `internal/adapters/server/common/app_service_adapter_auth_context_test.go:230-243` defaults empty input to `domain.StructuralTypeDroplet` with a doc comment naming droplet 3.4 + the legacy-fixture rationale.

Tests in `internal/app/service_test.go` (21), `internal/app/kind_capability_test.go` (6), `internal/app/embedding_runtime_test.go` (1), `internal/app/attention_capture_test.go` (3), `internal/adapters/server/common/app_service_adapter_lifecycle_test.go` (5), `internal/adapters/server/common/app_service_adapter_outcome_test.go` (4) — all migrated as evidenced by green `mage ci` (any missed callsite would have failed `domain.NewActionItem`'s empty-rejection at runtime).

### 1.10 TUI form-submit production callsite has Drop 4.5 placeholder comment

PASS. `internal/tui/model.go:11587` — comment reads:

> `// Drop 3 placeholder default — TUI picker is Drop 4.5.`

Followed by `m.createActionItem(app.CreateActionItemInput{... StructuralType: domain.StructuralTypeDroplet, ...})`. Comment names the deferral target (Drop 4.5) so future readers don't mistake the hardcoded default for an oversight.

### 1.11 `mage ci` green at 1533/1533

PASS. Verified locally with two consecutive runs:

```
Test summary
  tests: 1533
  passed: 1533
  failed: 0
  skipped: 0
  packages: 20
  pkg passed: 20
  pkg failed: 0
  pkg skipped: 0

Minimum package coverage: 70.0%.
[SUCCESS] Coverage threshold met
```

All 20 packages ≥ 70% coverage. Build step also succeeds (`Built till from ./cmd/till`).

## 2. Verification Gates

- 2.1 **`mage ci` GREEN** — confirmed twice. 1533/1533. All packages ≥70%.
- 2.2 **Format clean** — `mage ci` runs `[RUNNING] Checking Go formatting` → `[SUCCESS] Checked Go formatting` as part of its gate sequence.
- 2.3 **`git grep "app.CreateActionItemInput{"`** — every callsite supplies `StructuralType` directly OR routes through `mustCreateActionItemForTest` which defaults empty → Droplet. See § 1.9 table.
- 2.4 **No `tc := tc`** introduced — `git diff | fgrep "tc := tc"` returns empty (Go 1.22+ loop-scoping makes this anti-pattern unnecessary; absent in the diff).
- 2.5 **Placeholder fallback REMOVED** — `internal/app/service.go` diff at `:572-595` shows the prior `StructuralType: domain.StructuralTypeDroplet` block + its comment lines deleted, replaced with `StructuralType: in.StructuralType`. The empty-rejection at `domain/action_item.go:188-193` is now the canonical gate, exercised by the new real-adapter-chain test.

## 3. Findings

None. Every acceptance criterion in droplet 3.4's PLAN.md spec is supported by evidence in the diff and verified at runtime via `mage ci`.

**Minor observations (non-blocking):**

- The TUI hardcodes `domain.StructuralTypeDroplet` for every form-submit until Drop 4.5. This is the agreed Drop 3 deferral; the placeholder comment makes the intent explicit. No action required.
- The MCP stub's permissive-empty behavior (defaults empty → Droplet on create) diverges from production's strict empty-rejection. Documented in stub doc comment at `extended_tools_test.go:429-440` AND tested through the real adapter chain. Acceptable per finding 5.A.9 mitigation cited in PLAN.md.
- Test field re-alignment in `app_service_adapter_mcp.go:709-722` is a gofumpt-driven whitespace artifact; no semantic change.

## 4. Verdict Summary

**PASS.** All 11 required proof checks land with first-line evidence in the diff or in the existing tree (Hylla-resolvable). All verification gates pass. `mage ci` green at 1533/1533 across 20 packages with every package ≥ 70% coverage. The placeholder fallback is genuinely removed (the production path now reaches `domain.NewActionItem`'s empty-rejection check), and the new test exercises that exact path through the full real-adapter chain. Cross-package fixture migration is complete; the test-helper at `mustCreateActionItemForTest` correctly defaults empty for legacy fixtures while keeping production strict.

Pass-1 (cmd/till + TUI) and Pass-2 (sqlite + httpapi) are fully integrated into the 3.4 work; no orphan callsites remain.
