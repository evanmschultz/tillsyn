# Drop 4c · F.7.17.3 — QA Proof Review (Round 1)

**Reviewer:** go-qa-proof-agent
**Droplet:** F.7.17.3 — `claudeAdapter` struct implementing `dispatcher.CLIAdapter`
**Spawn anchor:** `workflow/drop_4c/F7_17_CLI_ADAPTER_PLAN.md` §F.7.17.3 + REVISIONS POST-AUTHORING (REV-1, REV-2, REV-5, REV-7)
**Verdict:** **PROOF GREEN**

---

## 1. Findings

- **1.1 Interface compliance (compile-time + runtime)** — `claudeAdapter` declared at `internal/app/dispatcher/cli_claude/adapter.go:50` as `type claudeAdapter struct{}`. Compile-time assertion `var _ dispatcher.CLIAdapter = (*claudeAdapter)(nil)` at `adapter.go:55` pins the three-method contract. `New() dispatcher.CLIAdapter` constructor at `adapter.go:60` returns the interface, not the concrete type — adapter-private fields cannot accidentally leak. `TestNewReturnsCLIAdapter` (`adapter_test.go:22`) verifies non-nil construction. The interface contract at `internal/app/dispatcher/cli_adapter.go:61-83` declares exactly three methods (`BuildCommand`, `ParseStreamEvent`, `ExtractTerminalReport`); each is implemented at `adapter.go:74`, `adapter.go:93`, `adapter.go:101` respectively.

- **1.2 REV-1 hardcoded `claude` binary, no Command/ArgsPrefix surface** — Constant `claudeBinaryName = "claude"` at `adapter.go:40`. Direct `exec.CommandContext(ctx, claudeBinaryName, argv[1:]...)` invocation at `adapter.go:85`. Ripgrep over `internal/app/dispatcher/cli_claude/` for `ArgsPrefix|\.Command\b` returned only doc-comment references describing what is absent (`adapter.go:6`, `adapter.go:36`, `argv.go:26`, `argv.go:60`) — zero code references. `BindingResolved` (`cli_adapter.go:102-179`) does not carry `Command` or `ArgsPrefix` fields, confirmed at `cli_adapter.go:90-95` REV-1 supersession comment. `TestBuildCommandHardcodedBinary` (`adapter_test.go:38`) asserts `filepath.Base(cmd.Path) == "claude"` and `cmd.Args[0] == "claude"`.

- **1.3 REV-2 closed POSIX baseline of 18 names** — `closedBaselineEnvNames` at `env.go:37-58` enumerates exactly 18 string literals. Process-basics (9): `PATH`, `HOME`, `USER`, `LANG`, `LC_ALL`, `TZ`, `TMPDIR`, `XDG_CONFIG_HOME`, `XDG_CACHE_HOME` (`env.go:39-47`). Network/TLS conventions (9): `HTTP_PROXY`, `HTTPS_PROXY`, `NO_PROXY`, `http_proxy`, `https_proxy`, `no_proxy`, `SSL_CERT_FILE`, `SSL_CERT_DIR`, `CURL_CA_BUNDLE` (`env.go:49-57`). Both case variants of proxy names ship per REV-2 rationale (`env.go:30-32`). `TestEnvBaselineNamesAllInherited` (`adapter_test.go:169`) iterates the slice and verifies each propagates when set; `TestEnvBaselineUnsetNamesOmitted` (`adapter_test.go:203`) verifies absent baseline names silently omit (no `NAME=` emission).

- **1.4 REV-2 `os.Environ()` never called in package** — Ripgrep for `os\.Environ` over `internal/app/dispatcher/cli_claude/` returned only doc-comment / test-comment references (`adapter.go:11`, `adapter_test.go:344`, `env.go:62`) describing the L8 isolation guarantee. Zero code-level invocations. `TestEnvOSEnvironNotInherited` (`adapter_test.go:312`) sets a sentinel `TILLSYN_TEST_LEAK_SENTINEL_xyzzy=must-not-leak` and asserts neither name nor value appears in `cmd.Env`. `TestEnvNotInheritedFromOSEnviron` (`adapter_test.go:345`) sets every baseline + a binding-only var + an outsider, then asserts strict `len(cmd.Env) == len(closedBaselineEnvNames) + 1` — bounded size catches the `cmd.Env = append(os.Environ(), ...)` regression vector.

- **1.5 REV-5 method named `ExtractTerminalReport`** — Method signature at `adapter.go:101`: `func (a *claudeAdapter) ExtractTerminalReport(ev dispatcher.StreamEvent) (dispatcher.TerminalReport, bool)`. Ripgrep for `ExtractTerminalCost` over the package returned `NO_MATCHES`. Interface definition at `cli_adapter.go:82` carries the same name with the rename anchor comment at `cli_adapter.go:81`. Implementation delegates to `extractTerminalReport` at `stream.go:191`.

- **1.6 REV-7 stream parsing inside `claudeAdapter.ParseStreamEvent`** — `ParseStreamEvent` method at `adapter.go:93` delegates to `parseStreamEvent` (package-internal) at `stream.go:90`. Per-line stream parsing logic — JSON discriminator decode, type-switch over four event families, content-block extraction for assistant events — lives entirely in `stream.go`. No external dependency on `internal/app/dispatcher/spawn.go` or any monitor package; the function takes raw bytes and returns the canonical `StreamEvent`.

- **1.7 `BuildCommand` returns `*exec.Cmd` with correct shape** — `BuildCommand` at `adapter.go:74-88` builds `env` via `assembleEnv(binding)` then `argv` via `assembleArgv(binding, paths)`, then constructs `exec.CommandContext(ctx, claudeBinaryName, argv[1:]...)`. `cmd.Path` resolves to `"claude"` (or absolute path via `exec.LookPath` fallback inside `exec.CommandContext`). `cmd.Args` includes argv[0]="claude" prepended by `exec.Command`. `cmd.Env` set explicitly via `cmd.Env = env` at `adapter.go:86`. Argv recipe per memory §3 verified by `TestBuildCommandArgvShapeMinimal` (`adapter_test.go:58`) — checks every always-on flag and pinned values: `--bare`, `--plugin-dir <root>/plugin`, `--agent <name>`, `--system-prompt-file <path>`, `--settings <root>/plugin/settings.json`, `--setting-sources ""`, `--strict-mcp-config`, `--permission-mode acceptEdits`, `--output-format stream-json`, `--verbose`, `--no-session-persistence`, `--exclude-dynamic-system-prompt-sections`, `--mcp-config <root>/plugin/.mcp.json`, `-p ""`. Conditional flags absent on minimal binding (`adapter_test.go:95-99`). `TestBuildCommandArgvShapeFullyPopulated` (`adapter_test.go:106`) asserts every conditional flag emits when its pointer is non-nil (`--max-budget-usd`, `--max-turns`, `--effort`, `--model`, `--tools`, `--append-system-prompt-file`).

- **1.8 `assembleEnv` fails loud on missing required env** — `ErrMissingRequiredEnv` declared at `env.go:17` as a sentinel error. `assembleEnv` at `env.go:95-101` resolves binding.Env names FIRST via `os.LookupEnv`; on `!ok` returns `fmt.Errorf("%w: name=%q", ErrMissingRequiredEnv, name)` — name appears in the wrapped error. `BuildCommand` at `adapter.go:80-82` propagates the error wrapped as `"cli_claude: build command: %w"`. `TestEnvMissingBindingNameFailsLoud` (`adapter_test.go:277`) verifies (a) `errors.Is(err, ErrMissingRequiredEnv)` (b) error message contains the missing name (c) `cmd == nil` on the error path. Eager-fail-before-baseline-resolve at `env.go:91-94` ensures no partial state leaks into the returned slice.

- **1.9 `ParseStreamEvent` maps the 4 event families to canonical shapes** — Type-switch at `stream.go:110-148` handles all four:
  - `system` + subtype `init` → `Type="system_init"` (`stream.go:116-117`); verified by `TestParseStreamEventSystemInit` (`adapter_test.go:386`).
  - `assistant` → `Type="assistant"` + `populateAssistantBlocks` extracts first text + first tool_use block (`stream.go:123-134`, helper at `stream.go:158-179`); verified by `TestParseStreamEventAssistantWithTextAndToolUse` (`adapter_test.go:411`) which asserts ordered preference: thinking-block ignored, first text → `ev.Text`, first tool_use → `ev.ToolName` + `ev.ToolInput`.
  - `user` → `Type="user"`, `IsTerminal=false` (`stream.go:135-141`); verified by `TestParseStreamEventUserToolResult` (`adapter_test.go:448`).
  - `result` → `Type="result"`, `IsTerminal=true` (`stream.go:142-144`); verified by `TestParseStreamEventResultTerminal` (`adapter_test.go:466`) including `Subtype="success"` propagation.
  Forward-compat unknown-type passthrough at `stream.go:145-147` verified by `TestParseStreamEventUnknownType` (`adapter_test.go:522`). Malformed JSON returns wrapped `ErrMalformedStreamLine` (`stream.go:99`, `stream.go:102`); verified by `TestParseStreamEventMalformedJSON` (`adapter_test.go:488`) and `TestParseStreamEventMissingType` (`adapter_test.go:506`). `Raw` retention via `append(json.RawMessage(nil), line...)` at `stream.go:95` detaches the buffer — important for scanner reuse.

- **1.10 `ExtractTerminalReport` honors `(populated, true)` / `(zero, false)` contract** — `extractTerminalReport` at `stream.go:191` short-circuits on `!ev.IsTerminal` returning `(TerminalReport{}, false)` (`stream.go:192-194`). Best-effort decode of `ev.Raw` into `claudeResultEvent` (`stream.go:201-203`) — even malformed terminal payload returns `(_, true)` so the dispatcher knows the spawn ended. Populated branch builds report with `Cost` (`*float64`), `Reason`, `Errors`, `Denials` via `convertDenials` (`stream.go:217-229`). `TestExtractTerminalReportPopulated` (`adapter_test.go:541`) asserts cost=0.0123, reason="completed", denial of `Bash` with `curl evil.com` input, errors=`["something happened"]`. `TestExtractTerminalReportNoCost` (`adapter_test.go:585`) verifies `Cost == nil` distinct from zero on absent `total_cost_usd` (the F.7.17 L11 *float64 contract). `TestExtractTerminalReportNonTerminalReturnsZeroFalse` (`adapter_test.go:606`) verifies an assistant event returns zero-value report with `ok=false`.

- **1.11 Recorded fixture exists with all four event types** — `internal/app/dispatcher/cli_claude/testdata/claude_stream_minimal.jsonl` is 4 lines covering: line 1 `system/init`, line 2 `assistant` with thinking + text + tool_use blocks, line 3 `user` with `tool_result`, line 4 `result` with `total_cost_usd=0.0123`, `terminal_reason="completed"`, one `Bash` permission denial. `TestRecordedFixtureRoundTrip` (`adapter_test.go:628`) opens the file, decodes every line, asserts assistant text="hello world", terminal cost=0.0123, reason="completed", denial[0].ToolName="Bash".

- **1.12 Scope clean** — `git status --porcelain` confined to `internal/app/dispatcher/cli_claude/` (4 source files + 1 testdata + 1 test file) plus `workflow/drop_4c/4c_F7_17_3_BUILDER_WORKLOG.md`. Zero edits to `internal/app/dispatcher/cli_adapter.go`, `internal/app/dispatcher/spawn.go`, or any other tracked file. The orphan files in `git status` (`mock_adapter_test.go`, `testdata/mock_stream_minimal.jsonl`, other drop_4c MDs) are out of this droplet's scope and are NOT touched here — they belong to other droplets in flight.

- **1.13 `mage ci` green** — Worklog (`4c_F7_17_3_BUILDER_WORKLOG.md:127-137`) cites `tests: 2351, passed: 2350, failed: 0, skipped: 1, packages: 22`. Package coverage for `internal/app/dispatcher/cli_claude` reported at 95.6% — well over the 70% gate. Skipped test (`TestStewardIntegrationDropOrchSupersedeRejected` in mcpapi) is pre-existing per worklog §6 and orthogonal to this droplet.

## 2. Missing Evidence

- **2.1 None.** Every spec checklist item (1–13) has direct file:line evidence. No premise is unsubstantiated.

## 3. Summary

**PROOF GREEN.** All 13 spec items verified with file:line evidence. The implementation honors REV-1 (hardcoded `claude` binary, no Command/ArgsPrefix surface), REV-2 (closed 18-name baseline, `os.Environ()` never invoked), REV-5 (`ExtractTerminalReport` rename), and REV-7 (stream parsing inside the adapter). Interface compliance is pinned at compile time. Tests cover argv shape (minimal + fully-populated), env baseline (set + unset + binding-append + missing-fail-loud + size-bounded leak guard), stream parsing (all four families + unknown + malformed + missing-type), terminal extraction (populated + no-cost + non-terminal-zero-false), and a recorded fixture round-trip. `mage ci` green at 2350/2351 with package coverage 95.6%.

## TL;DR

- T1: PROOF GREEN. All 13 spec items verified with file:line citations against `internal/app/dispatcher/cli_claude/`. Compile-time interface assertion, hardcoded `claude` binary, 18-name closed POSIX env baseline, `os.Environ()` absent from package, `ExtractTerminalReport` (not Cost), in-adapter stream parsing for all four claude event families, fail-loud on missing required env, recorded fixture round-trip, scope clean, `mage ci` 2350/2351 at 95.6% package coverage.
- T2: No missing evidence.
- T3: Verdict — PROOF GREEN. Ready for QA Falsification convergence.

## Hylla Feedback

N/A — review used Read/ripgrep only; no Hylla queries issued. Per project CLAUDE.md the active checkout has uncommitted-since-last-ingest files (the entire `cli_claude/` package is `??`), so committed-Hylla queries would be stale by definition. The spawn-prompt-mandated "no Hylla calls" constraint matched that reality.
