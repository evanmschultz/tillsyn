# Plan-QA-Falsification Round 2 — DROP_4c.6.W0.5

**Reviewer:** L2 plan-QA-falsification (Round 2)
**Target:** `workflow/drop_4c_6/DROP_4c.6.W0.5_TEMPLATE_VALIDATORS/PLAN.md` (Round 2 revised, 6 droplets D1–D6)
**Round-1 verdict:** PASS-with-medium-drift — 3 medium findings (FF1 TOML-line disclosure gap, FF2 D2/W1.D1 misattribution, FF3 sorted-key DFS determinism omission) + 5 low/PASS context findings.
**Round-2 scope:** verify 3 medium findings fixed (Half A) + fresh 7-family attack pass on revised plan (Half B).
**Inputs cross-checked:**
- L1 contract: `workflow/drop_4c_6/PLAN.md:71-85` + `:95-101` (W1.D1 acceptance for FF2 verification).
- Round-1 verdict: `workflow/drop_4c_6/DROP_4c.6.W0.5_TEMPLATE_VALIDATORS/PLAN_QA_FALSIFICATION.md`.
- Sibling ledger: `workflow/drop_4c_6/DROP_4c.6.W0.5_TEMPLATE_VALIDATORS/_BLOCKERS.toml`.
- FF1 anchor (post-decode validator no line numbers): `internal/templates/load.go:462-494` (canonicalizeMapKeys body — error wrap `fmt.Errorf("%w: %s map key %q", ...)` at line 472, no `line=N` field).
- FF3 anchor (current non-deterministic DFS at load.go:553): see `_test` evidence in round-1 finding 1.5.

## 1. Round-1 Findings Verification

- 1.1 [FF1 — TOML-line-pointer disclosure gap] **FIXED** → repro: D1 ContextBlock list (`PLAN.md:80`) carries `warning (high): TOML-line pointers in error envelopes — pelletier/go-toml/v2 post-decode validators do NOT receive original-source line numbers (verified at internal/templates/load.go:472); the closed-sentinel error message names the offending field/value but cannot include line=N. Mitigation: error messages include the field-path... explicit acceptance that L1 W0.5 acceptance bullet 1's "TOML-line pointer" wording is aspirational pending a pelletier/go-toml/v2 API extension.` Identical (modulo field-path examples) `warning (high)` ContextBlock present at D2:114, D3:147, D4:181, D5:214, D6:249. Six droplets × six warning ContextBlocks confirmed. **Anchor verified**: `internal/templates/load.go:472` shows `fmt.Errorf("%w: %s map key %q", ErrUnknownKindReference, fieldName, k)` — no line number; the citation is correct. **Disclosure is at-the-droplet level (ContextBlocks), not just Scope-paragraph level — round-1 ask satisfied.** L1↔L2 inconsistency assessment under fresh attack 2.1 below.

- 1.2 [FF2 — D2 W1.D1 misattribution] **FIXED** → repro: D2 acceptance bullet 3 (`PLAN.md:91`) now reads `Embedded FS query goes through the existing embed.FS exposed at internal/templates/embed.go. D2's default lookupFn func(name string) bool walks the embedded internal/templates/builtin/agents/{till-gen,till-go,till-gdd}/<name>.md FS unconditionally — it does NOT depend on W1.D1 rewiring anything.` D2 RiskNote (`PLAN.md:105`) carries the same decoupling: `Default lookupFn walks embed.FS unconditionally; no rewire from W1.D1 needed.` D2 ContextBlock `warning (high)` at line 112: `pre-W1.D1 the default lookupFn walks the embed.FS path that contains no agent *.md files yet, so the default returns false for every name. D2's unit tests MUST inject a synthetic lookupFn returning true for known synthetic names; the default walker is exercised end-to-end only after W1.D1 lands placeholder files into the same FS path. The validator code itself does not change between pre-W1.D1 and post-W1.D1 — only the FS contents change.` The round-1 false claim ("W1.D1 explicitly carries the validator-rewire") is gone; replaced by the cleaner option (a) from round-1 fix-hint — the unconditional embed.FS walk. L1 W1.D1 acceptance (`workflow/drop_4c_6/PLAN.md:95-100`) verified to NOT carry any "rewire D2's default lookupFn" bullet, which is now correct: no rewire is needed. **Decoupling is genuine.**

- 1.3 [FF3 — sorted-key DFS determinism omission] **FIXED** → repro: D3 acceptance bullet 6 (`PLAN.md:128`) now reads `Cycle-DFS shared helper iterates root-set in sorted-key order. D3 lands a private generic helper dfsDetectCycle[K comparable](graph map[K][]K) (cyclePath []K, found bool) that sorts the input graph's root keys (sort.Strings over []string projection of domain.Kind keys) before iterating. Reproducible error messages — the same cyclic graph produces the same cyclePath rendering across runs / OSes / Go map-iteration orderings. The current validateChildRuleCycles at internal/templates/load.go:553 uses for node := range graph (non-deterministic Go map iteration); D3 fixes that as part of the helper extraction. D4 and D5 reuse the same helper via the blocked_by chain (D4 blocked_by D3, D5 blocked_by D4) and inherit the sorted-key determinism for free.` Companion D3 ContextBlock `constraint (high)` at line 146 reinforces: `cycle-DFS shared helper iterates root-set in sorted-key order... Same helper used by D3/D4/D5 — D3 lands the helper; D4/D5 call into it via the blocked_by chain.` Transitive coverage to D4 + D5 is correct: D4's KindPayload (`PLAN.md:182`) says "reuses graph helper D3 produces"; D5's RiskNote (`PLAN.md:207`) says "extracts the colored-DFS into a private generic helper `dfsDetectCycle[K comparable]`... reuses it across D3 + D5." Same helper, same sort. **SKETCH §26.W0.5 line 788 mandate ("Cycle-detection determinism: sorted-key traversal") now propagated into L2 plan.**

## 2. Fresh Counterexamples

Round-2 attack pass per directive: A1-A6 standard families + Phase-2-step-1 mandatory checks + targeted attacks on the round-2 edits themselves (A2 contract-mismatch on FF1 disclosure, A3 hidden-coupling on FF2 redesign, A6 atomicity on FF3 helper extraction).

- 2.1 [Family: A2 contract-mismatch / L1↔L2 inconsistency from FF1 disclosure] [severity: low] FF1 fix surfaces a latent L1↔L2 wording contradiction. L1 W0.5 acceptance bullet 1 (`workflow/drop_4c_6/PLAN.md:78`) still reads "All six validators land in `internal/templates/load.go` with closed sentinel errors **and TOML-line pointers**." L2 PLAN.md now explicitly disclaims TOML-line pointers as `aspirational pending a pelletier/go-toml/v2 API extension` in 6 separate ContextBlocks. The disclaimer is technically correct but creates a documentation gap: an L1 reader (e.g., a future drop-orch reading only L1 acceptance) sees "TOML-line pointers" mandated; an L2 reader sees them disclaimed. → repro: read `workflow/drop_4c_6/PLAN.md:78` (still mandates TOML-line pointers) vs `workflow/drop_4c_6/DROP_4c.6.W0.5_TEMPLATE_VALIDATORS/PLAN.md:80` (disclaims). The round-1 finding 1.1 fix-hint named TWO options: "(a) add an explicit `wrapped error UX matches existing chain — no TOML-line pointer for post-decode validators (pelletier/go-toml/v2 limitation)` bullet to each of D1/D2/D3/D4/D5/D6's RiskNotes / AcceptanceCriteria, OR (b) escalate to L1 to revise W0.5 acceptance bullet 1 wording so L2's hedge is L1-anchored." The planner picked (a). Option (a) closes the disclosure gap (round-1 ask satisfied) but does NOT close the L1↔L2 inconsistency the disclosure surfaces. → fix hint: REFUTED as a hard counterexample because the planner picked one of the two valid round-1 fix options and that option is fully implemented; logging as low-severity context for build-QA awareness. The cleaner final state is to ALSO escalate option (b) to L1 — revise L1 W0.5 acceptance bullet 1 to "closed sentinel errors and TOML-line pointers where `pelletier/go-toml/v2` returns one (post-decode validators do not)". L2-author cannot edit L1 in scope of this drop; flagging for orchestrator-level routing if desired. **Severity low because round-1 finding 1.1 is the round-1-asked fix and that fix is in.**

- 2.2 [Family: A3 hidden-coupling / FF2 unconditional walk closure] [severity: PASS] Verify the FF2 redesign genuinely closes the W1.D1 coupling. → repro: walked the proposed default walker shape: D2's default `lookupFn` queries `embed.FS` for `internal/templates/builtin/agents/{till-gen,till-go,till-gdd}/<name>.md` — that FS path is invariant across W1.D1 ship state. Pre-W1.D1: the FS resolves the path to "no entries"; default returns false for every name. Post-W1.D1: the same FS path resolves to W1.D1's 21 placeholder files; default returns true for the 7 standard names. **The validator code path does not change; only the FS contents change.** No W1.D1 rewire is needed because W1.D1's deliverables (per L1 acceptance bullets at `workflow/drop_4c_6/PLAN.md:95-100`) include "`//go:embed` directive in `internal/templates/embed.go` extends to include `builtin/agents/till-gen/*.md`, `builtin/agents/till-go/*.md`, `builtin/agents/till-gdd/*.md`" — exactly the FS paths D2's default walks. **The decoupling is real.** No fresh hidden-coupling counterexample. → fix hint: nothing to fix. PASS.

- 2.3 [Family: A6 atomicity / FF3 helper extraction added scope to D3] [severity: low] FF3 fix added a new acceptance bullet (D3 bullet 6, `PLAN.md:128`) requiring D3 to land the `dfsDetectCycle[K comparable]` generic helper + sort-keys logic + rewrite of the existing non-deterministic `for node := range graph` at load.go:553. This is net-new scope that did not exist in round 1's D3. → repro: round-1 D3 acceptance had 7 bullets (graph-extension + edge-type label + 2 fixtures + table test + mage targets); round-2 D3 acceptance has 8 bullets — the new bullet 6 adds (a) helper extraction, (b) generic-typing of helper over `K comparable`, (c) sort-keys at outer-loop entry, (d) consumer rewire from existing in-place DFS to helper call, (e) preservation of colored-DFS pattern through the extraction. PLAN.md "Atomic sizing check" (line 258) estimates "~30-80 LOC of validator code + ~30-50 LOC of test code" per droplet, well within 80-120 LOC + tests budget. The helper extraction adds ~20-30 LOC for the helper itself + ~10-15 LOC of consumer-site rewire + ~5 LOC of sort-keys boilerplate = ~35-50 LOC additional. D3's new total: ~50-80 LOC validator + ~30-50 LOC test. Still within budget but at the upper end. → fix hint: REFUTED on borderline grounds. The helper extraction is genuinely small (`sort.Strings` over a `[]string` projection of map keys is ~5 LOC; the generic helper is ~20 LOC of DFS body that's mostly moved-not-added; the rewire is one call-site change). The cycle-DFS body already exists at load.go:517-561 — D3 is REFACTORING it into a helper, not authoring fresh DFS code. Net-new LOC is closer to ~30-40 than ~50. D3 stays atomic. Logging as low-severity context for build-QA: confirm D3's actual diff respects the budget; the upper-end estimate is borderline.

- 2.4 [Family: A1 / Phase-2-step-1 — sibling-overlap-without-blockers, drift, cycles] [severity: PASS] Required Phase-2-step-1 attacks all pass cleanly post-revision → repro: (a) cycles — chain D1→D2→D3→D4→D5→D6 unchanged from round 1, walked `_BLOCKERS.toml:23-46` and confirmed acyclic; (b) drift — every L2 PLAN.md inline `Blocked by:` bullet (lines 66, 99, 131, 166, 199, 233) matches `_BLOCKERS.toml` rows exactly; (c) sibling-overlap-without-blockers — all 6 droplets share `internal/templates/load.go` + `internal/templates/load_test.go` + `internal/templates` package; serial chain covers every overlap. Round-2 revisions did not alter the chain shape — FF1 was per-droplet ContextBlock additions (no `paths` change), FF2 was D2 acceptance/RiskNote text rewrites (no `paths` change), FF3 was D3 acceptance bullet 6 + ContextBlock additions (no `paths` change). → fix hint: nothing to fix. Phase-2-step-1 PASS.

- 2.5 [Family: A1 / cross-wave wiring] [severity: PASS] L1 cross-wave `_BLOCKERS.toml` (`workflow/drop_4c_6/_BLOCKERS.toml`) unchanged across rounds — W0.5 → W1.D1 → W5.D1 → W5.D2 → W5.D3 chain still correct. FF2's decoupling claim (D2's default walker does not depend on W1.D1 rewiring) is consistent with W1.D1 ship state per L1 acceptance bullet 3 (extends `//go:embed` to the `builtin/agents/till-{gen,go,gdd}/` paths). → fix hint: nothing to fix. Cross-wave PASS.

- 2.6 [Family: A4 YAGNI / round-2 surface unchanged] [severity: PASS] Round-2 revisions added (a) 6 disclosure ContextBlocks (round 1 finding 1.1), (b) D2 acceptance/RiskNote rewrites (round 1 finding 1.2), (c) D3 acceptance bullet 6 + ContextBlock for sorted-key DFS helper (round 1 finding 1.5). All three additions are textual-clarity / determinism-mandate fixes responding to round-1 findings; none introduces fresh shipped-but-not-wired surface. The YAGNI surface from round 1 (D5 forward-looking + D6 empty-set) is unchanged and remains REFUTED on L1-mandate grounds. → fix hint: nothing to fix. YAGNI PASS.

- 2.7 [Family: A5 shipped-but-not-wired / D6 + D5 round-2 unchanged] [severity: PASS] D6 (empty `knownWiredConsumers` set + `LoadOptions.ClaimedConsumersFn` test-only injection) and D5 (`validateBlockedByAcyclicity` + `LoadOptions.BlockedByGraphFn` test-only injection) round-2 surface unchanged from round 1. Round 1 finding 1.12 (D6 deferral L1-grounded) and round 1 finding 1.4 (D5 forward-looking L1-mandated) both remain PASS post-revision. → fix hint: nothing to fix. PASS.

- 2.8 [Family: A7 prompt-injection] [severity: EXHAUSTED] DORMANT pre-team-feature per `~/.claude/agents/go-qa-falsification-agent.md` § attack-family A7 + memory `feedback_prompt_injection_team.md`. No team functionality lands in 4c.6.W0.5. Scanned the 6 disclosure ContextBlocks added in FF1 + the FF2 rewrites + FF3 helper extraction — no attacker-injectable strings, no Section-0 patterns, no role-confusion phrases. → fix hint: marked EXHAUSTED.

## 3. Summary

**Verdict: PASS**

All three round-1 medium findings (FF1 / FF2 / FF3) are FIXED with the round-1-asked surface present and grounded in concrete file:line evidence. Fresh round-2 attack pass produced ZERO hard CONFIRMED counterexamples. Two low-severity context findings logged for build-QA awareness:
- 2.1 latent L1↔L2 wording inconsistency surfaced by FF1 disclosure (low; round-1 fix-option-(a) was correctly chosen; option-(b) L1 escalation is optional cleanup).
- 2.3 D3 atomic-sizing borderline post FF3 helper extraction (low; estimate stays within 80-120 LOC budget; build-QA confirms diff at build time).

Falsification-attack family results (round-2):

| Family                                | Result    | Counterexample? | Notes                                                    |
| ------------------------------------- | --------- | --------------- | -------------------------------------------------------- |
| A1 concurrency / blocked_by drift     | PASS      | None            | Chain D1→D2→D3→D4→D5→D6 strictly linear, no drift, no cycles, sibling-overlap covered. Round-2 revisions did not alter chain shape. |
| A2 contract-mismatch                  | PASS      | None hard       | FF1 fix in (6 disclosure ContextBlocks). 2.1 logs latent L1↔L2 wording inconsistency as low-severity; round-1 fix option-(a) was implemented correctly. |
| A3 hidden-coupling                    | PASS      | None            | FF2 fix in (default walker decoupled from W1.D1 via unconditional embed.FS walk). 2.2 confirms decoupling is genuine. |
| A4 YAGNI                              | PASS      | None            | Round-2 surface unchanged; round-1 D5 + D6 forward-looking PASS persists. |
| A5 shipped-but-not-wired              | PASS      | None            | D6 empty-set deferral + D5 forward-looking PASS persists; FF3 helper extraction is real-use scaffolding (D3+D4+D5 consumers), not shipped-but-not-wired. |
| A6 atomicity                          | PASS      | None            | All 6 droplets within atomic budget. 2.3 logs D3 borderline post-FF3 helper extraction as low-severity. |
| A7 prompt-injection                   | EXHAUSTED | None            | Dormant pre-team-feature; no attacker-injectable strings in round-2 additions. |
| Phase-2-step-1 (cycles / drift / overlap) | PASS  | None            | Required checks all clean; chain shape unchanged from round 1. |
| Cross-wave wiring                     | PASS      | None            | W0.5 → W1.D1 → W5.D1 → W5.D2 → W5.D3 chain still correct; FF2 decoupling consistent with W1.D1 acceptance bullet 3. |

**Counterexample count: 0 hard CONFIRMED**, 0 medium drift findings (all 3 round-1 mediums fixed), 2 low-severity context findings (2.1 + 2.3) logged for build-QA awareness.

The round-2 revised L2 plan ships a sound 6-droplet decomposition with all round-1 mediums resolved. No re-decomposition needed; build dispatch is unblocked from the plan-QA-falsification axis.

## 4. Hylla Feedback

N/A — review touched non-Go files only (the round-2 PLAN.md being attacked, the round-1 verdict, L1 PLAN.md cross-references, _BLOCKERS.toml). The single Go-source touch (`internal/templates/load.go:462-494` for FF1 anchor verification) was a narrow line-range Read pull rather than symbol-graph navigation — Hylla's `hylla_node_full` / `hylla_refs_find` would not have surfaced anything beyond what `Read` produced for a 30-line range. Hylla was not exercised because the attack surface was MD/TOML drift on a revised plan, and the one Go-source citation was anchor verification on text the round-1 verdict had already cited.
