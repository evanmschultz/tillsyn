# Plan-QA-Falsification Round 1 — DROP_4c.6.W0.5

**Reviewer:** L2 plan-QA-falsification (Round 1)
**Target:** `workflow/drop_4c_6/DROP_4c.6.W0.5_TEMPLATE_VALIDATORS/PLAN.md` (6 droplets D1–D6)
**Inputs cross-checked:**
- L1 contract: `workflow/drop_4c_6/PLAN.md:71-85`
- Sketch source: `workflow/drop_4c_6/SKETCH.md:775-796` (§26.W0.5) + lines 121-167 (§3.x)
- Sibling ledger: `workflow/drop_4c_6/DROP_4c.6.W0.5_TEMPLATE_VALIDATORS/_BLOCKERS.toml`
- Cross-wave ledger: `workflow/drop_4c_6/_BLOCKERS.toml`
- Existing chain reality: `internal/templates/load.go:144-227` (validator chain), 232-395 (sentinels), 429-580 (validateMapKeys + validateChildRuleCycles)

## 1. Findings

- 1.1 [Family: A2 contract-mismatch] [severity: medium] L1 W0.5 acceptance bullet 1 (`workflow/drop_4c_6/PLAN.md:78`) requires "All six validators land in `internal/templates/load.go` with closed sentinel errors **and TOML-line pointers**." SKETCH §26.W0.5 ContextBlocks (`workflow/drop_4c_6/SKETCH.md:793`) escalates this to "every malformed-template error MUST include TOML-line pointer." L2 PLAN.md narrows the requirement in its Scope paragraph (line 15: "TOML-line pointer where `pelletier/go-toml/v2` returns one"), but NONE of the 6 droplet acceptance bullets propagate the hedge or disclose the gap → repro: `internal/templates/load.go:432` shows the existing post-decode validator pattern (`fmt.Errorf("%w: %s map key %q", ...)`) returns no line number because `pelletier/go-toml/v2` only emits position-aware `DecodeError` during the decode pass, not during post-decode validator walks; D1–D6 inherit the same shape but their acceptance bullets remain silent on the gap → fix hint: either add an explicit "wrapped error UX matches existing chain — no TOML-line pointer for post-decode validators (pelletier/go-toml/v2 limitation)" bullet to each of D1/D2/D3/D4/D5/D6's RiskNotes / AcceptanceCriteria, OR escalate to L1 to revise W0.5 acceptance bullet 1 wording so L2's hedge is L1-anchored. Drift severity is medium, not high, because L2's Scope paragraph already acknowledges the limit — the gap is droplet-acceptance-level disclosure, not behavior.

- 1.2 [Family: A3 hidden-coupling] [severity: medium] L2 D2 RiskNote (`workflow/drop_4c_6/DROP_4c.6.W0.5_TEMPLATE_VALIDATORS/PLAN.md:104`) asserts: "(W1.D1 explicitly carries the validator-rewire in its own droplet acceptance)." This claim is FALSE → repro: L1 W1.D1 acceptance bullets (`workflow/drop_4c_6/PLAN.md:95-100`) enumerate exactly 5 deliverables (21 placeholder agent .md files, agents.example.toml, embed.go directive extension, embed_test.go FS-introspection test, mage test-pkg pass) — none of them mention "rewire D2's default `lookupFn` to walk the new embed list" or any analogous validator-rewire scope. D2 ships a default `lookupFn` that "returns `false` for every name" pre-W1.D1 (line 90); when W1.D1 ships, no W1.D1-owned droplet rewires that default → fix hint: pick one — (a) redesign D2's default `lookupFn` to walk `embed.FS` unconditionally so it picks up W1.D1's directories automatically without any rewire (and update D2's RiskNote prose to reflect this), OR (b) add an explicit acceptance bullet to L1 W1.D1 declaring "rewire `internal/templates/load.go` `LoadOptions.AgentLookupFn` default to walk `embed.FS` for `internal/templates/builtin/agents/till-{gen,go,gdd}/`." Today's plan has neither — the validator works in tests via injection but the production default is silently broken until someone rewires it.

- 1.3 [Family: A4 YAGNI / atomic-sizing] [severity: low] L2 D6 introduces THREE new mechanisms simultaneously (`workflow/drop_4c_6/DROP_4c.6.W0.5_TEMPLATE_VALIDATORS/PLAN.md:215-242`): a new sentinel (`ErrClaimVsImplUnknownConsumer`), a Go-internal closed set (`knownWiredConsumers`) that is documented to be EMPTY for this drop, AND a test-only `LoadOptions.ClaimedConsumersFn` injection field whose production default returns an empty slice → repro: D6 acceptance bullet 4 (line 219): "Drop 4c.6 has zero claimed-feature markers" + bullet 5: "Default `ClaimedConsumersFn = nil` resolves to a production walker that returns an empty slice for every template (no schema field claims a consumer today)." The validator passes vacuously on every production template; the only RED→GREEN test path goes through the injected `ClaimedConsumersFn`. This is shipping a closed-set type + a sentinel + a `LoadOptions` field that ALL exist solely to satisfy a synthetic test → fix hint: this is REFUTED on YAGNI grounds because L1 acceptance bullet 4 (`workflow/drop_4c_6/PLAN.md:81`) explicitly mandates "The set's existence + the test that exercises an 'unknown-consumer claim' sentinel ARE in scope" — the L2 plan implements exactly the L1 mandate. Logging as low-severity context for the build-QA reviewer to confirm `ClaimedConsumersFn` does not survive past Drop 4c.7 W7 / W8 wiring (i.e., that the injection field is removed once real producers exist), to keep the test-only escape hatch from becoming a permanent surface.

- 1.4 [Family: A4 YAGNI / D5 distinction] [severity: low] L2 D5 ships `validateBlockedByAcyclicity` + `ErrTemplateBlockedByCycle` + `LoadOptions.BlockedByGraphFn` injection field, with the explicit caveat (D5 RiskNote, line 200): "Today's `BlockedByParent`-induced graph is degenerate — only child→parent edges, trivially acyclic." The validator therefore never fires in production for Drop 4c.6 → repro: `internal/templates/schema.go` (per L2 plan line 139 reference) declares `ChildRule.BlockedByParent bool`; the induced kind-graph for `BlockedByParent: true` is purely child→parent, trivially acyclic. D5's RED→GREEN cycle relies entirely on the injected `BlockedByGraphFn` synthetic graph → fix hint: REFUTED — L1 W0.5 acceptance bullet 1 (`workflow/drop_4c_6/PLAN.md:78`) names "blocked_by acyclicity" as one of the 6 mandated validators (matched by SKETCH §26.W0.5 line 780), so D5 is L1-mandated. The forward-looking framing is also defended in D5's RiskNote (line 199-200). Logging as low-severity context for build-QA: the validator's production code path must be exercisable without `BlockedByGraphFn` injection once a future schema landing introduces a non-degenerate kind-level blocked_by edge type — `BlockedByGraphFn`'s removal trigger should track that schema landing.

- 1.5 [Family: A2 contract-mismatch / determinism] [severity: medium] SKETCH §26.W0.5 RiskNotes (`workflow/drop_4c_6/SKETCH.md:788`) explicitly mandates: "Cycle-detection determinism: sorted-key traversal for reproducible error messages." L2 D3 EXTENDS the existing `validateChildRuleCycles` (`internal/templates/load.go:517-561`) which today iterates the graph map non-deterministically (`for node := range graph` at line 553). L2 D3 acceptance bullets (lines 121-127) and RiskNotes (line 134: "preserve the colored-DFS pattern") preserve the existing pattern → repro: read load.go:553 — `for node := range graph` is Go-map non-deterministic; SKETCH RiskNote requires sorted-key traversal; L2 D3 plan does NOT fix the existing nondeterminism nor mention sorted-key traversal in any acceptance bullet, RiskNote, or ContextBlock. Same omission applies to D4's depth-DFS (line 156: "walks the same parent→child kind graph D3 / `validateChildRuleCycles` already builds" — inherits the nondeterminism) and D5's blocked-by DFS (line 201: extracts shared private helper `dfsDetectCycle`, but that helper inherits the same root-iteration shape unless explicitly mandated otherwise). Failure mode: identical malformed templates produce different cycle-path error messages across Load invocations because `for node := range graph` selects a different starting root → fix hint: add a "sorted-key root iteration" acceptance bullet to D3 (and a transitively-applies-to-D4-D5 ContextBlock) so the SKETCH-mandated determinism propagates to the shared cycle-DFS helper. The fix is small (sort the keys via `slices.Sorted` before the outer for-loop in load.go:553).

- 1.6 [Family: A4 YAGNI / D3 collapse hedge] [severity: low] L2 PLAN.md "Cross-Cutting Decisions" (line 29) carries a hedge: "If the builder finds the existing DFS already covers the new edge type via accidental closure, D3 collapses to 'audit + fixture-and-test only, no production code change' — but the L1 directive lists it as a distinct droplet, so the slot stays." The hedge concedes that the existing `validateChildRuleCycles` MAY already cover D3's new edge type because today's `BlockedByParent: true` edges are structurally child→parent (same vocabulary `domain.Kind`-typed nodes that the existing DFS already walks if the graph build added those edges) → repro: D3's claim is to extend the graph build to also include `BlockedByParent: true` induced edges. The existing graph build at load.go:522-525 only includes parent→child edges from `WhenParentKind` → `CreateChildKind`; it does NOT include `BlockedByParent`-induced edges. So D3 is genuinely additive — the hedge is over-cautious → fix hint: REFUTED. D3 is real net-new graph-build scope (the existing build at load.go:522-525 is `WhenParentKind → CreateChildKind` only); D3 adds the `BlockedByParent: true` edge synthesis. The collapse-to-audit-only hedge is theoretically safe but practically does not fire. Low-severity log so build-QA confirms D3 ships actual code changes, not just a fixture-and-test addition that re-asserts the existing validator's behavior unchanged.

- 1.7 [Family: A6 atomicity] [severity: low] L2 D3's `KindPayload` (line 143) declares 4 file changes: load.go (modify), load_test.go (add), 2 NEW fixtures. PLAN.md Notes (line 250) estimates "~30-80 LOC of validator code + ~30-50 LOC of test code + ~10-20 LOC of TOML fixture" per droplet, totaling 70-150 LOC. Project CLAUDE.md atomic budget: "1-4 code blocks, 80-120 LOC + tests" → repro: D3 specifically extends an existing function AND adds 2 fixtures (parent→child cycle + blocked_by cycle) AND adds a 4-row table-driven test (parent→child cycle, blocked_by cycle, mixed cycle, acyclic happy path), pushing the upper end of the LOC budget → fix hint: REFUTED on borderline grounds — the extension itself is small (~20-30 LOC per the plan), the formatCyclePath edge-type label is small (~5-10 LOC), and the test adds ~40-60 LOC. Total for D3 lands within the 80-120 LOC + tests budget. Logging as low-severity context for build-QA: confirm D3's actual diff respects the budget; if it spills over, the build-QA flag is already wired for atomicity.

- 1.8 [Family: A2 contract-mismatch / "structured envelope"] [severity: low] L1 W0.5 acceptance bullet 3 (`workflow/drop_4c_6/PLAN.md:80`): "Each validator emits the structured 'template is broken because X; cannot ingest' envelope." None of the 6 L2 droplets' wrapped-error message specs include the literal "template is broken because" / "cannot ingest" phrasing → repro: D1 bullet 3 (line 61): "wrapping a message naming `agents` field + `totally-bogus` key"; D2 bullet 4 (line 91): "wrapping a message naming the binding's kind, the offending agent_name, and a hint"; D3 bullet 4 (line 122): "extended to name the edge type"; D4 bullet 4 (line 154): "names the offending kind, the depth observed, the bound, and the path"; D5 bullet 4 (line 187): "names the participating kinds in path order"; D6 bullet 6 (line 221): "wrapping a message naming `unknown_consumer`." None match the L1-mandated envelope shape → fix hint: SOFT contract mismatch. L1's "template is broken because X; cannot ingest" reads as a STYLE guide, not a literal-string contract — the existing chain's wrapped messages (`internal/templates/load.go:472`: `"%w: %s map key %q"`) also do not use the literal phrase. Low severity because the existing chain has already established the precedent that the L1 envelope phrasing is illustrative, not literal. Logging as low-severity for build-QA awareness; do not require code change.

- 1.9 [Family: A1 / Phase-2-step-1 — sibling-overlap-without-blockers, drift, cycles] [severity: PASS] All required Phase-2-step-1 attacks pass cleanly → repro: (a) cycles — chain D1→D2→D3→D4→D5→D6 is strictly linear, walked the `_BLOCKERS.toml` rows (`workflow/drop_4c_6/DROP_4c.6.W0.5_TEMPLATE_VALIDATORS/_BLOCKERS.toml:23-46`) and confirmed acyclic; (b) drift — every L2 PLAN.md inline `Blocked by:` bullet (lines 66, 98, 128, 161, 193, 226) matches the corresponding `_BLOCKERS.toml` row exactly; (c) sibling-overlap-without-blockers — all 6 droplets share `internal/templates/load.go` + `internal/templates/load_test.go` + `internal/templates` package and the strict serial chain covers every overlap → fix hint: nothing to fix. Required Phase-2-step-1 checks PASS.

- 1.10 [Family: A3 cross-wave wiring] [severity: PASS] L1 cross-wave `_BLOCKERS.toml` (`workflow/drop_4c_6/_BLOCKERS.toml:27-30`) declares `4c.6.W1.D1 blocked_by = ["4c.6.W0.5"]`, satisfying the L2 plan's expectation that W0.5 → W1.D1 → W5.D1 → W5.D2 → W5.D3 (line 17 of L2 PLAN.md, line 21-25 of L1 _BLOCKERS.toml) → repro: walked the L1 ledger; W1.D1's blocker on W0.5 is correct, W5.D1 chains via W1.D1, W5.D2 chains via W5.D1, W5.D3 chains via W5.D1 + W5.D2 + W1.D1. No cross-wave wiring drift → fix hint: nothing to fix. Cross-wave PASS.

- 1.11 [Family: A1 / chain ordering rationale] [severity: PASS] L2 chain order D1→D2→D3→D4→D5→D6 matches L1 spawn directive verbatim (`workflow/drop_4c_6/PLAN.md:85`: "kind-enum + agent_name first (load-time atomic), then cycles, then recursion-depth, then `blocked_by` acyclicity (load-time mirror of Drop 4a Wave 1.7), then claim-vs-impl last") → repro: D1 = kind-enum (matches "kind-enum first"); D2 = agent_name (matches "+ agent_name first"); D3 = cycles (matches "then cycles"); D4 = recursion-depth (matches "then recursion-depth"); D5 = blocked_by acyclicity (matches "then `blocked_by` acyclicity"); D6 = claim-vs-impl (matches "then claim-vs-impl last") → fix hint: nothing to fix. Chain order PASS.

- 1.12 [Family: A5 shipped-but-not-wired (D6 deferral)] [severity: PASS] L2 D6 ships an empty `knownWiredConsumers` set explicitly per L1 W0.5 acceptance bullet 4 (`workflow/drop_4c_6/PLAN.md:81`); the deferral to Drop 4c.7 W7 + W8 is L1-grounded; the LOUD WARNING doc-comment is in scope (D6 RiskNote line 235); the SKETCH F1 (line 7) ensures W7 + W8 carry the "update known-wired set" acceptance bullet → repro: walked L1 W0.5 acceptance bullet 4 + SKETCH F1 (`workflow/drop_4c_6/SKETCH.md:7`) + D6 RiskNote (`workflow/drop_4c_6/DROP_4c.6.W0.5_TEMPLATE_VALIDATORS/PLAN.md:235`); the deferral, the LOUD WARNING, and the future-drop hook are all in place → fix hint: nothing to fix. D6 deferral PASS.

- 1.13 [Family: A7 prompt-injection] [severity: EXHAUSTED] DORMANT pre-team-feature per `~/.claude/agents/go-qa-falsification-agent.md` § attack-family A7 + memory `feedback_prompt_injection_team.md`. No team functionality lands in 4c.6.W0.5 → fix hint: marked EXHAUSTED.

## 2. Counterexamples

No CONFIRMED hard counterexamples that block PASS. Two MEDIUM-severity drift findings (1.1 + 1.5) and one MEDIUM hidden-coupling finding (1.2) raise legitimate plan-level disclosure / wire-up gaps, but each has a small, contained fix that does NOT change the overall droplet decomposition shape and does NOT trigger plan re-decomposition. Logging in §1 above; itemizing here for orchestrator clarity:

- 2.1 [from 1.1 — TOML-line-pointer disclosure gap] L1 W0.5 acceptance bullet 1 mandates TOML-line pointers; L2 droplet acceptance bullets are silent; the gap should be either escalated to L1 (revise wording) OR disclosed at every droplet's RiskNote level. Recommended fix: add a one-line ContextBlock `warning` (high) to each of D1–D6 stating "TOML-line pointer included only when `pelletier/go-toml/v2` returns one; post-decode validator failures use kind-name-keyed UX, mirroring the existing chain at load.go:472."

- 2.2 [from 1.2 — D2 W1.D1 misattribution] L2 D2 RiskNote falsely claims W1.D1 carries the validator-rewire. Recommended fix: replace the misattribution with one of (a) "default `lookupFn` walks `embed.FS` unconditionally; W1.D1's directory addition is picked up automatically via the embed extension" OR (b) add an explicit acceptance bullet to L1 W1.D1 declaring the rewire scope.

- 2.3 [from 1.5 — sorted-key DFS determinism omission] SKETCH §26.W0.5 RiskNote line 788 mandates sorted-key traversal for cycle-detection determinism; L2 D3 / D4 / D5 do not propagate this. Recommended fix: add a "sorted-key root iteration in shared cycle-DFS helper" acceptance bullet to D3 (since D3 is the chain step that introduces / first reuses the DFS pattern); D4 + D5 inherit transitively.

## 3. Summary

**Verdict: PASS**

Falsification-attack family results:

| Family                                | Result    | Counterexample? | Notes                                                    |
| ------------------------------------- | --------- | --------------- | -------------------------------------------------------- |
| A1 concurrency / blocked_by drift     | PASS      | None            | Chain D1→D2→D3→D4→D5→D6 strictly linear, no drift, no cycles, sibling-overlap covered by serial chain. |
| A2 contract-mismatch                  | PASS-with-medium-drift | None hard | 3 medium drift findings (1.1 TOML-line, 1.5 sorted-key, 1.8 envelope phrasing) — disclosure-level, not behavior-level. |
| A3 hidden-coupling                    | PASS-with-medium-drift | None hard | 1 medium misattribution finding (1.2 D2/W1.D1) — small fix, no re-decomposition. |
| A4 YAGNI                              | PASS      | None            | D5 + D6 forward-looking scaffolding is L1-mandated; D3 hedge is over-cautious but does not affect ship behavior. |
| A5 shipped-but-not-wired              | PASS      | None            | D6 empty-set deferral L1-grounded; LOUD WARNING + SKETCH F1 + W7/W8 hook all in place. |
| A6 atomicity                          | PASS      | None            | All 6 droplets within atomic budget per project CLAUDE.md / `feedback_plan_down_build_up.md`. |
| A7 prompt-injection                   | EXHAUSTED | None            | Dormant pre-team-feature.                                |
| Phase-2-step-1 (cycles / drift / overlap) | PASS  | None            | Required checks all clean.                                |

**Counterexample count: 0 hard CONFIRMED**, 3 medium drift findings (1.1 + 1.2 + 1.5) routed for L2-author cleanup before build dispatch, 5 low-severity context findings logged for build-QA awareness.

The L2 plan ships a sound 6-droplet decomposition matching the L1 W0.5 contract verbatim. The serial chain is correctly enforced at file-lock + package-lock granularity. The medium drift findings are disclosure-level — they should be patched into D1–D6 RiskNotes before build dispatch but do NOT trigger plan re-decomposition.

## 4. Hylla Feedback

N/A — review touched non-Go files only (PLAN.md, _BLOCKERS.toml, SKETCH.md, the L2 PLAN.md being attacked). Source-of-truth grounding for the existing chain (`internal/templates/load.go:144-580`) was via Read directly; Hylla was not exercised because the goal was attack-surface enumeration on Markdown + TOML drift and the Go-source reads were narrow line-range pulls rather than symbol-graph navigation.
