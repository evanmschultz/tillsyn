# DROP_4c.6.W0_AGENTS_TOML_SCHEMA — Builder QA Falsification

Append `## Droplet N.M — Round K` per falsification round. Per `workflow/example/drops/WORKFLOW.md` § "File Lifecycle", durable; never `git rm`d.

## Droplet 4c.6.W0.D1 — Round 1

### Counterexamples

No CONFIRMED counterexamples. Every attack family was exercised against the shipped surface; each landed REFUTED with evidence, EXHAUSTED (dormant family), or as a low-severity test-coverage ergonomic note that does not break the L2 acceptance bar.

The findings below are ergonomic / coverage-completeness observations recorded for the build round's awareness — none rise to a fail-the-droplet counterexample. Builder may choose to address inline; orchestrator may choose to defer to a later W0 droplet (D2 / D3 / D5 each touch `agents_test.go` and can absorb a delta).

- 1.1 [Family: B1-test-coverage] [severity: low] `agentsTOMLBlock` embeds `Preset`; behavior when a fixture has ONLY `[agents.<kind>]` blocks (no `[agents]` defaults at all) is UNTESTED. → repro: no fixture in `internal/config/testdata/agents/` exercises an "overrides-only-no-preset-fields" file (the only nearby fixture, `preset_only.toml`, exercises the converse). → fix_hint: optional follow-up adds a fixture e.g. `overrides_only.toml` with just `[agents.build] tools_allow = ["Read"]` and asserts `Preset` is the zero `Preset{}` while `Overrides[KindBuild].ToolsAllow` round-trips. Does NOT block D1 — TOML semantics make `[agents.build]` imply the `agents` table, so pelletier's decode of the embedded `Preset` simply leaves zero values; D2's resolver is the layer that exposes any latent issue. PLAN.md acceptance bullet "the [agents.build] override surfaces with a non-nil tools_allow pointer" is satisfied by the existing `TestLoadRegistry_Baseline` assertion at `agents_test.go:90`.
- 1.2 [Family: B5-spec-compliance] [severity: low] `TestLoadRegistry_MalformedTOML` (`agents_test.go:106-122`) asserts `row > 0` from `decodeErr.Position()` — does NOT pin `row == 6` (the actual unterminated-string line in `malformed.toml`) and does NOT string-match the rendered `err.Error()` for digit content. → repro: `agents_test.go:118-121` extracts row via the typed accessor and only checks positivity. → fix_hint: pinning to `row == 6` would be brittle if pelletier shifts which token-position it reports for unterminated strings (lib-defined; varies across versions). The current shape PROVES line-tracking is active (the load-bearing semantic) without coupling to library version. NOT a counterexample — defensive testing is appropriate per `RiskNote` "the lib has minor API churn across versions." PLAN.md acceptance bullet "error message includes the TOML line number" is met because `*toml.DecodeError.Error()` (pelletier convention) renders the row in its message — the test verifies the structural source (`Position()` returns positive row), which is what wraps into the message. REFUTED.

### Summary

**Verdict: pass**

| Family | Attack target | Result |
| ------ | ------------- | ------ |
| B1 (test-coverage) | empty TOML / preset-only / overrides-only / `[agents]`-only / tools_allow ∩ tools_deny / env_set collisions / multiple kinds with conflicting overrides | REFUTED — `preset_only.toml` covers absent-blocks-nil-safe; `unknown_field.toml` covers strict-decode rejection; `baseline.toml` covers `[agents]` + one per-kind block; `malformed.toml` covers position-aware decode error; tools_allow ∩ tools_deny / env_set collisions / multi-kind override-conflict are out of D1's scope (D2's `Resolve` is the layer that surfaces conflicting overrides; D1 is decode-only). One ergonomic gap (1.1) noted as low-severity coverage. |
| B2 (contract-preservation) | `Preset` field set vs SKETCH §4.1; field types; `Override` `*T` pointer pattern | REFUTED — 15-field 1-1 correspondence verified across `Preset` (`agents.go:35-51`), `Override` (`agents.go:62-78`), `AgentRuntime` (`agents.go:84-100`). Field types match SKETCH §4.1 (`max_tries int`, `max_budget_usd float64`, `auto_push bool`, `env_set map[string]string`). TOML key naming `snake_case` via struct tags. Pointer pattern correctly distinguishes nil (inherit) from non-nil-zero (explicit override). |
| B3 (hidden-coupling) | `internal/domain.Kind` reverse-import; `BindingOverrides` shape consistency | REFUTED — `git grep "tillsyn/internal/config"` under `internal/domain/` returns empty (no reverse-import). `BindingOverrides` at `internal/app/dispatcher/binding_resolved.go:44` uses `*string` / `*int` / `*float64` / `*bool` / `*time.Duration` for scalars; new `Override` mirrors the same pointer-scalar idiom and extends to `*map[string]string` / `*[]string` for collections. Pattern is consistent across the codebase. |
| B4 (YAGNI) | `AgentRuntime` shipped today as type-only, no producer until D2 | REFUTED — PLAN.md L2 acceptance bullet explicitly requires `AgentRuntime` defined at D1 ("AgentRuntime (effective per-kind merged result, same fields as Preset but resolved)"). BUILDER_WORKLOG §"Design notes" #6 documents the choice: type lands in D1 so D2 is a pure-function add. Appropriate scoping per L1 W0 directive — not premature. |
| B5 (spec-compliance) | each L2 acceptance bullet has a verifying test that ACTUALLY verifies the bullet; `*toml.DecodeError` line-number assertion specificity | REFUTED — every L2 bullet maps to a specific assertion: 4 exported types defined → presence at `agents.go:35,62,84,106`; `LoadRegistry` strict decode → `TestLoadRegistry_UnknownTopLevelField` at `agents_test.go:127`; line-number preservation → `TestLoadRegistry_MalformedTOML` at `:106` (specificity discussion in 1.2 above); `Kind` reuse from `internal/domain` → `agents.go:24` import + `agents.go:108,167-178` use; `TestLoadRegistry_Baseline` asserts every Preset field + `Override.ToolsAllow` non-nil pointer at `:33-99`; nil-safe absent-blocks → `TestLoadRegistry_AbsentBlocksNilSafe`. |
| B6 (shipped-but-not-wired) | `LoadRegistry` exported but no caller exists today | REFUTED — `LoadRegistry` is the W0 schema layer; W2's `till init` and W3's render-layer consumer wire it per SKETCH §26.W0 Objective ("Foundation for everything else"). BUILDER_WORKLOG §"Decisions deferred" documents D2/D3/D5 as same-package follow-ups. PLAN.md "Out of scope for W0" §1 explicitly carves out the wiring as W2/W3's responsibility. Documented deferral, not orphan. |
| B7 (prompt-injection) | DORMANT pre-team-feature | EXHAUSTED — per appendix directive. Re-engages once team feature lands per `feedback_prompt_injection_team.md`. |

**File and package gating attacks (post-Drop-1 surface):**

- Edits stay within declared `paths`: `internal/config/agents.go` (NEW), `internal/config/agents_test.go` (NEW), `internal/config/testdata/agents/{baseline,malformed,unknown_field,preset_only}.toml` (NEW). Builder ALSO added 2 fixture files (`malformed.toml`, `unknown_field.toml`, `preset_only.toml`) beyond the single `baseline.toml` named in the PLAN.md `Paths` line, BUT the PLAN.md `Paths` line wildcards `internal/config/testdata/agents/*.toml` (the wave plan: "internal/config/testdata/agents/*.toml (NEW golden fixtures, one per merge edge case)"). Additional fixtures fit within the declared glob. REFUTED.
- Package gate: `internal/config` package; no edits leak to `internal/domain`, `internal/app/dispatcher`, etc. `git status` shows no extraneous Go-source changes outside `internal/config/*` for D1's commits. REFUTED.

**Concurrency / interface-misuse / error-swallowing / goroutine-leak / raw-go-command / `mage install` attack surfaces:** D1's surface is pure schema decode — no goroutines, no mutexes, no channels, no `init()` side effects, no panic-able assertions. `LoadRegistry` returns wrapped errors via `fmt.Errorf %w` for both `os.ReadFile` failure (`agents.go:156`) and decode failure (`:163`); no error swallowing. `mage test-pkg` is the gate (no raw `go test` in worklog). `mage install` not invoked. ALL EXHAUSTED.

**Gate run record:**

- `mage test-pkg ./internal/config` — 37/37 GREEN, 32 pre-existing + 5 new (`TestLoadRegistry_Baseline`, `TestLoadRegistry_MalformedTOML`, `TestLoadRegistry_UnknownTopLevelField`, `TestLoadRegistry_FileNotFound`, `TestLoadRegistry_AbsentBlocksNilSafe`). Wall-clock 0.00s.

### Hylla Feedback

None — Hylla not queried in this round. Round 1 falsification needed (a) re-reading the L2 acceptance bullets and the `agents.go` body for 1-1 field correspondence, (b) `git grep` for reverse-imports, (c) `Read` of an existing dispatcher source for `BindingOverrides` shape, (d) `mage test-pkg` gate verification. None of those are Hylla-shaped queries — (a) is direct file reading, (b) is `git grep`, (c) is direct file reading of a known-path file, (d) is build-tool. Worklog already records the one Hylla miss from D1's build round (enrichment-still-running on the post-recent-commits state); this round adds nothing.

## Droplet 4c.6.W0.D2 — Round 1

### Counterexamples

No CONFIRMED counterexamples. Every attack family was exercised against the shipped surface; each landed REFUTED with evidence, EXHAUSTED (dormant family), or as low-severity ergonomic / doc-drift observations that do not break the L2 acceptance bar.

The findings below are recorded for the build round's awareness — none rise to a fail-the-droplet counterexample. Builder may absorb inline at D3 / D5 (each touches `agents.go` and can refresh adjacent prose); orchestrator may defer.

- 1.1 [Family: B1-test-coverage] [severity: low] **Empty-map override semantic ambiguity vs D1 design note.** D1 BUILDER_WORKLOG §"Design notes" #4 stated `*map[string]string` discriminates `non-nil empty = "explicitly drop all defaults"`. D2's `Resolve` at `agents.go:287-302` does NOT drop preset keys on a non-nil empty override map — the predicate `if ov.EnvSet != nil` enters the branch but the inner `for k, v := range *ov.EnvSet` iterates zero times, leaving the copied Preset keys intact. → repro: an in-code `Override{EnvSet: ptrMap(map[string]string{})}` over a Preset with `EnvSet={A:"1"}` yields resolved `EnvSet={A:"1"}` — preset keys preserved, NOT dropped. → fix_hint: NOT a counterexample to D2's L2 acceptance bullet, which reads "merge with override keys winning, default keys absent in override surviving (NOT full-replace)." D2's actual behavior matches SKETCH §4.2.2 + the L2 bullet exactly. The drift lives in D1's design-note prose (`non-nil empty = drop all defaults`), which conflicts with the spec D2 implements. D5 (or a later refinement) can either (a) trim D1's design-note hedge to match D2, or (b) add a sentinel-style "explicit drop" mechanism if downstream genuinely needs the drop-all-defaults case. Today no consumer exists.

- 1.2 [Family: B1-test-coverage] [severity: low] `TestResolve_MapMergeOverrideWins` and `TestResolve_AbsentKindReturnsPreset` are extra tests beyond PLAN.md's `ValidationPlan` list (which names `TestResolve_FullInherit`, `TestResolve_PartialOverride`, `TestResolve_MapMerge`, `TestResolve_ListReplace`, `TestResolve_ExplicitEmptyList`). → repro: `agents_test.go:316-338` (`TestResolve_MapMergeOverrideWins`) + `:400-431` (`TestResolve_AbsentKindReturnsPreset`) — both pass; both exceed scope. → fix_hint: extra coverage is strictly additive (more semantics proved) — not a counterexample. Recorded as a positive note: collision-precedence + per-kind absent-key paths get explicit verification in addition to the named ValidationPlan tests. REFUTED.

- 1.3 [Family: B3-hidden-coupling] [severity: low] **`copyMap` near-duplicates exist elsewhere in the tree.** `git grep` reveals three structurally related helpers: `cloneStringMap` (`internal/adapters/server/common/app_service_adapter.go:327`, exact same `map[string]string → map[string]string` signature, BUT returns `nil` for `len(in) == 0` — different empty-vs-nil semantics), `cloneJSONMap` (`internal/adapters/auth/autentauth/service.go:1391`, `map[string]any`), `cloneCLIObjectMap` (`cmd/till/main.go:3774`, `map[string]any`). → repro: 4 helpers across 4 packages, 3 of them in production. → fix_hint: NOT a D2 counterexample — `cloneStringMap` lives in a different package + has different empty-map semantics (returns nil for empty), so `copyMap` cannot reuse it without changing behavior (D2's Resolve depends on `copyMap` returning a non-nil empty map for the in-out chain to layer override keys onto). Worth a future refinement entry: consolidate into an `internal/util` `CopyStringMap` with an explicit `EmptyAsNil` bool param, then call-sites pick. Out of D2's scope. REFUTED.

- 1.4 [Family: B2-contract-preservation] [severity: low] **`Resolve` defensive nil-check returns a non-typed error.** `agents.go:228-230` returns `fmt.Errorf("Resolve: registry is nil")` — bare formatted error, no sentinel. → repro: `Resolve(nil, KindBuild)` returns `(AgentRuntime{}, errors.New-equivalent)`. Untestable for `errors.Is(err, ErrXxx)`. → fix_hint: NOT a D2 counterexample — PLAN.md L2 acceptance for D2 doesn't specify a sentinel for nil-registry; D5 ships `*ConfigError` envelope and could elevate this defensive case if needed. The doc-comment at `agents.go:228` (the `if registry == nil` guard) is currently silent on what error the caller sees — a one-line refresh in D5's pass would help. REFUTED.

### Summary

**Verdict: pass**

| Family | Attack target | Result |
| ------ | ------------- | ------ |
| B1 (test-coverage) | nil registry / nil per-kind block in Overrides map / empty-map override / nil-Overrides-map field / map collision precedence / list nil-vs-empty distinction / multiple overrides per registry round-trip | REFUTED — `TestResolve_FullInherit` / `_PartialOverride` / `_MapMerge` / `_MapMergeOverrideWins` / `_ListReplace` / `_ExplicitEmptyList` / `_AbsentKindReturnsPreset` cover 7 named cases. `Resolve(nil, ...)` defensive guard at `agents.go:228-230` (1.4 note above). nil-Overrides-map case via `_AbsentKindReturnsPreset` exercises the `, ok` lookup that handles both nil-map AND missing-key paths identically (Go map zero-value lookup semantics — both return zero Override with `ok=false`). Empty-map override semantic note in 1.1 is doc-drift, not behavior bug. |
| B2 (contract-preservation) | `(AgentRuntime, error)` signature; nil-registry behavior; `errors.Unwrap` chain across `*toml.DecodeError` (D1 chain still intact post-D2 edits) | REFUTED — signature matches PLAN.md L2 acceptance verbatim (`agents.go:227`). nil-registry returns `(AgentRuntime{}, error)` with non-nil error (defensive guard). D1's `errors.As(err, &decodeErr)` regression-checks at `agents_test.go:114-117` still GREEN per round 1 gate run — D2 added no new error-wrapping logic that could perturb the chain. 1.4 note: defensive-error is `fmt.Errorf` not sentinel — appropriate for D2 (D5 unifies). |
| B3 (hidden-coupling) | `copyMap` shared semantics with codebase clone-map helpers; `Resolve` symbol collision with other packages | REFUTED — `copyMap` is package-private to `internal/config`, structurally similar to but semantically distinct from `cloneStringMap` (1.3 above). `Resolve` symbol in `internal/config` does not collide with `app/dispatcher/context/aggregator.go:243 Resolve` or `domain/attention.go:291 (item).Resolve` because Go disambiguates by package qualifier. No coupling that would break tomorrow if either helper is touched. |
| B4 (YAGNI) | `(AgentRuntime, error)` signature reserves error for future use — premature? `copyMap` private helper that only the merge engine calls | REFUTED — `(AgentRuntime, error)` is mandated by PLAN.md L2 acceptance + reserved for D5's `*ConfigError` envelope. `copyMap` has 2 call sites today (lines 243-244) and is the smallest concrete helper for the per-kind merge step — replacing with inline `for-range` would duplicate 5 lines × 2 maps. Both choices are smallest-concrete-design appropriate. |
| B5 (spec-compliance) | each L2 acceptance bullet has a verifying test that ACTUALLY verifies; empty-list-vs-nil discrimination test is in-code (TOML can't express); map-merge override-wins precedence test exists | REFUTED — every L2 bullet maps to a specific assertion: scalar nil-pointer-inherit-else-override → `TestResolve_PartialOverride` at `:239`; per-key map merge → `TestResolve_MapMerge` at `:279` (disjoint) + `_MapMergeOverrideWins` at `:316` (collision); list full-replace → `TestResolve_ListReplace` at `:343`; explicit-empty-list replaces non-empty → `TestResolve_ExplicitEmptyList` at `:368` (in-code construction via `ptrSlice([]string{})` per the doc-comment justification at `:451-454`); absent-kind = pure inherit → `TestResolve_AbsentKindReturnsPreset` at `:400`; full inherit → `TestResolve_FullInherit` at `:176`. The 1.1 doc-drift note is a doc concern, not an L2-bullet-violation. |
| B6 (shipped-but-not-wired) | `Resolve` exported but no caller exists today | REFUTED — `Resolve` is the W0 schema-merge layer; W3's frontmatter strip wiring + W2's `till init` consumer wire it per SKETCH §26.W0 + appendix directive ("`Resolve` will be consumed by W3 + W2. Acceptable cross-wave deferral per L1 W0 directive."). PLAN.md "Out of scope for W0" §1 explicitly carves out the wiring as W2/W3 territory. Documented deferral, not orphan. |
| B7 (prompt-injection) | DORMANT pre-team-feature | EXHAUSTED — per appendix directive. Re-engages once team feature lands. |

**File and package gating attacks (post-Drop-1 surface):**

- Edits stay within declared `paths`: `internal/config/agents.go` (MODIFY), `internal/config/agents_test.go` (MODIFY), `internal/config/testdata/agents/inheritance_*.toml` (NEW, four fixtures matching the PLAN.md `Paths` glob `internal/config/testdata/agents/inheritance_*.toml`). `git status --porcelain internal/config/` returns clean (D2 already committed at `b56df9c feat(config): w0.d2 resolve inheritance merge engine`); no uncommitted leakage. REFUTED.
- Package gate: `internal/config` package; no edits leak to `internal/domain`, `internal/app/dispatcher`, etc. `git log --oneline -5 -- internal/config/` shows D2's commit `b56df9c` immediately follows D1's `4f14547` — clean per-droplet boundary. REFUTED.

**Concurrency / interface-misuse / error-swallowing / goroutine-leak / raw-go-command / `mage install` attack surfaces:** D2's surface is pure synchronous merge — no goroutines, no mutexes, no channels, no `init()` side effects. `Resolve`'s only non-nil error path is the defensive `registry == nil` guard at `agents.go:228-230` (returns wrapped `fmt.Errorf` — no swallowing). `mage test-pkg` is the gate (no raw `go test` invoked in worklog or this round's verification). `mage install` not invoked. ALL EXHAUSTED.

**Gate run record:**

- `mage test-pkg ./internal/config` — 44/44 GREEN (37 from D1 baseline + 7 new from D2: `TestResolve_FullInherit`, `_PartialOverride`, `_MapMerge`, `_MapMergeOverrideWins`, `_ListReplace`, `_ExplicitEmptyList`, `_AbsentKindReturnsPreset`). Wall-clock 0.00s.
- `git grep "copyMap"` — 4 references (1 def + 2 call sites in `agents.go`, 1 doc-comment); no consumer outside `internal/config`. `git grep` for sibling clone-map helpers (1.3 above) located 3 same-shape but different-package helpers — recorded as future-refinement candidate.
- `git status --porcelain internal/config/` — clean (D2 committed at `b56df9c`).

### Hylla Feedback

None — Hylla not queried in this round. Round 1 falsification needed (a) re-reading PLAN.md D2 L2 acceptance vs `agents.go:227-319` Resolve body, (b) `git grep` for reverse-imports + `copyMap` reuse + clone-map helper neighbors, (c) `go doc github.com/pelletier/go-toml/v2.DecodeError` to confirm the D1 chain survives (no API changes consumed by D2), (d) `mage test-pkg ./internal/config` gate verification. None are Hylla-shaped queries — (a) is direct file reading, (b) is `git grep`, (c) is `go doc`, (d) is build-tool. The worklog round-2 `## Hylla Feedback` for D2 already records "N/A — task touched only `internal/config` Go code already in scope from W0.D1"; this round adds nothing.

## Droplet 4c.6.W0.D3 — Round 1

### Counterexamples

**One CONFIRMED counterexample (W0-D3-CX1) — Family B5 spec-compliance, severity high.** The wrap text the builder added at `agents.go:394` and `:398` violates the round-3 finalized D3↔D5 separation contract. Builder honestly flagged the deviation as Unknown in BUILDER_WORKLOG line 128; this falsification confirms it as a counterexample, not a justified deviation. The W0 plan-QA loop ran 3 rounds explicitly to lock the strict bare-sentinel contract; the wrap re-introduces exactly the prefix language those 3 rounds eliminated.

- 1.1 [Family: B5-spec-compliance] [severity: high] **D3's `tools_deny` rejection wraps the bare sentinel with a file + block prefix, violating PLAN.md:112's "no file/line/block prefix at the D3 boundary" requirement.** → repro: `agents.go:393-400` returns `fmt.Errorf("agents.local.toml [agents]: %w", ErrToolsDenyNotOverridable)` for the defaults-block path and `fmt.Errorf("agents.local.toml [agents.%s]: %w", kind, ErrToolsDenyNotOverridable)` for the per-kind path. Calling `err.Error()` yields e.g. `"agents.local.toml [agents.build]: tools_deny is not user-overridable; remove the field"`. The literal string `"agents.local.toml"` is a **file** prefix; `"[agents]"` / `"[agents.build]"` is a **block** prefix. PLAN.md:112 (the D3 acceptance bullet finalized in round 3) reads verbatim: `"The bare sentinel's message reads \"tools_deny is not user-overridable; remove the field\" (no file/line/block prefix at the D3 boundary). D5 wraps the sentinel into *ConfigError so the user-facing message renders as \"agents.local.toml [agents.<kind>]:<line>: tools_deny is not user-overridable; remove the field\" (or [agents]:<line> for the defaults block)."` The spec assigns that exact prefix shape to D5's envelope — `agents.go:394`'s `fmt.Errorf` literally pre-builds the D5-prefix-shape minus the `<line>` field, leaving D5 the choice of either (a) emitting a duplicated prefix (`"agents.local.toml [agents.build]:42: agents.local.toml [agents.build]: tools_deny ..."`) or (b) stripping D3's prefix and re-wrapping. Either path is wasted work or contradiction; the bare sentinel was supposed to ship without any prefix so D5 owns the envelope unambiguously. → fix_hint: revert the two `fmt.Errorf` calls at `agents.go:394` and `:398` to bare sentinel returns: `return nil, ErrToolsDenyNotOverridable` (preserves `errors.Is` chain, removes the prefix). If the builder needs the kind context for D5's eventual envelope, thread the offending kind via a typed wrapper that does NOT format into the message (e.g., a small `*toolsDenyRejection{Kind domain.Kind}` struct whose `Error()` returns the bare sentinel's message and whose `Unwrap()` returns `ErrToolsDenyNotOverridable`; D5 then uses `errors.As` to extract the kind for its envelope). The simpler revert (bare sentinel) is acceptable today since D5 will re-decode the local TOML to recover the offending kind from line position anyway per PLAN.md:202-203 RiskNote. Builder's BUILDER_WORKLOG line 128 already acknowledges "the `fmt.Errorf` call is one line to revert."

### Counterexamples — REFUTED attacks (recorded for completeness)

The following families were attacked and landed REFUTED (no counterexample); the round-3 spec violation above stands as the sole CONFIRMED finding.

- 1.2 [Family: B1-test-coverage] [severity: low] **`tools_deny` simultaneous on per-kind block AND defaults block.** → repro: hypothetical fixture sets BOTH `[agents].tools_deny` AND `[agents.build].tools_deny`. → behavior: `MergeLocal` at `agents.go:392-400` first checks `local.Preset.ToolsDeny` (the defaults block), so the defaults-block error fires first; the per-kind path is unreachable in this case. The error message identifies `[agents]:` (defaults block) — the user fixes that, re-runs, then hits the per-kind error on the next attempt. Acceptable progressive surface; not a counterexample. REFUTED.
- 1.3 [Family: B1-test-coverage] [severity: low] **Empty `tools_deny` slice in local.** → repro: `local.Preset.ToolsDeny = []string{}` (explicit empty list). → behavior: `agents.go:393` predicate `len(local.Preset.ToolsDeny) > 0` returns false; the empty list is treated as "absent" and merge proceeds. Mirrored at `:397` for per-kind via `ov.ToolsDeny != nil && len(*ov.ToolsDeny) > 0`. Aligns with the PLAN.md L2 acceptance spirit — the safety floor only kicks in for non-empty user-supplied denials. NOT explicitly tested today, but no observable defect. Optional follow-up: add `TestMergeLocal_ToolsDenyEmptyAcceptedAsAbsent` to lock the contract. NOT a counterexample. REFUTED.
- 1.4 [Family: B2-contract-preservation] [severity: low] **`MergeLocal` deep-clone correctness — does the output ever alias into project / local inputs?** → repro: `agents.go:402-433` walks every Preset map/list field through `copyMap` / `copySlice` and every Override through `cloneOverride`; `cloneOverride` (`agents.go:592-655`) duplicates every pointer field with a fresh `v := *ov.X; out.X = &v` pattern. Maps + slices threaded through `copyMap` / `copySlice`. → check: I read every code path at `:402-435` and confirmed no input pointer survives unmodified into the output. Mutating the merged registry's `Preset.EnvSet` does not perturb `project.Preset.EnvSet`; same for every Override. NOT a counterexample. REFUTED.
- 1.5 [Family: B2-contract-preservation] [severity: low] **`MergeLocal(project, nil)` returns deep-clone, not the project pointer.** → repro: `agents.go:418-420` short-circuits AFTER the deep-clone block at `:402-417`, returning the freshly-cloned `out` rather than `project`. `TestMergeLocal_NilLocal` at `agents_test.go:537` exercises the contract. → check: caller mutating the returned registry never corrupts `project`. PLAN.md:113 says "MergeLocal(project, nil) returns project unchanged" — the worklog note at line 136 reads "Returning a clone (rather than the project pointer itself) keeps the contract symmetric." Builder's interpretation is "unchanged" = "deep-equal to project," not "is project." Defensible interpretation; the visible behavior (caller cannot tell the difference) matches the spec language. NOT a counterexample. REFUTED.
- 1.6 [Family: B3-hidden-coupling] [severity: low] **Map collision in per-kind `mergeOverride.EnvSet` — local-wins or existing-wins?** → repro: `agents.go:547-559` allocates a fresh `merged` map, copies `out.EnvSet` first, then writes `local.EnvSet` keys on top. Last-write-wins semantics: local wins on collision. Mirrors D2's Resolve at `agents.go:298-305` (per-kind override key wins over Preset key). Consistent with SKETCH.md §4.2.2 + the worklog note at line 132 ("local wins on collision"). NOT a counterexample. REFUTED.
- 1.7 [Family: B3-hidden-coupling] [severity: low] **Concurrency — is `MergeLocal` safe to call from goroutines simultaneously?** → repro: function is pure (no globals, no mutexes), reads from input pointers, allocates fresh output. Multiple concurrent calls with disjoint inputs are safe by construction. Multiple concurrent calls SHARING input pointers are read-only (`MergeLocal` never mutates inputs), so still safe — `copyMap` / `copySlice` / `cloneOverride` only read. NOT a counterexample. REFUTED.
- 1.8 [Family: B4-YAGNI] [severity: low] **Was the wrap text necessary?** Builder rationale (BUILDER_WORKLOG line 128): "useful when MergeLocal runs standalone." But MergeLocal has no standalone consumer today (per BUILDER_WORKLOG "Decisions deferred" — D5 supersedes; W2/W3 wire later). The wrap creates a transient-only "useful" message that D5 supersedes. Strictly YAGNI: the wrap solves a no-existing-consumer problem at the cost of a load-bearing spec deviation (1.1 above). The B4 attack lands as a contributing factor to 1.1's severity-high verdict; calling it out separately is duplicative. Folded into 1.1.
- 1.9 [Family: B5-spec-compliance] [severity: low] **Test assertions for `tools_deny` rejection use `errors.Is`, which works through wraps.** → repro: `TestMergeLocal_ToolsDenyRejected` at `agents_test.go:484` and `TestMergeLocal_ToolsDenyDefaultsBlockRejected` at `:511` both assert via `errors.Is(err, ErrToolsDenyNotOverridable)`. `errors.Is` traverses the `%w` chain so the assertion succeeds whether D3 wraps or not. → implication: the tests do NOT catch the round-3 spec violation in 1.1 because they only verify sentinel reachability, not message-shape. PLAN.md:117 explicitly carves "Position-wrapping at the envelope layer is asserted separately by D5's `TestMergeLocal_ToolsDenyPositionWrapped`; this bullet covers only the sentinel-rejection contract D3 owns." So D3's tests are correctly scoped per the plan's assertion boundary — but the WRAP-TEXT spec violation lives outside what D3's tests assert. Test coverage per spec is correct; the production code drifts from spec. NOT itself a counterexample (tests match spec). The drift is in production code, captured by 1.1.
- 1.10 [Family: B6-shipped-but-not-wired] **`MergeLocal` exported but no caller exists today.** → REFUTED — appendix directive: "B6 ... `MergeLocal` consumer is W2/W3 (deferred per L1). Acceptable." PLAN.md "Out of scope for W0" §1 explicitly defers the wiring to W2 (`till init`) + W3 (render-layer consumer). Documented deferral, not orphan. REFUTED.
- 1.11 [Family: B7-prompt-injection] DORMANT pre-team-feature — EXHAUSTED per appendix directive.

**File and package gating attacks (post-Drop-1 surface):**

- Edits stay within declared `paths`: `internal/config/agents.go` (MODIFY), `internal/config/agents_test.go` (MODIFY), `internal/config/testdata/agents/local_*.toml` (NEW — 3 fixtures matching the PLAN.md `Paths` glob `internal/config/testdata/agents/local_*.toml`). `git status --porcelain internal/config/` clean for D3's commits. REFUTED.
- Package gate: `internal/config` package; no edits leak to `internal/domain`, `internal/app/dispatcher`, etc. `git log --oneline -5 -- internal/config/` shows clean per-droplet boundary. REFUTED.

**Concurrency / interface-misuse / error-swallowing / goroutine-leak / raw-go-command / `mage install` attack surfaces:** D3's surface is pure synchronous deep-merge — no goroutines, no mutexes, no channels, no `init()` side effects. Errors all wrapped via `fmt.Errorf %w` (the wrap-text issue is shape-spec violation, NOT swallowing). `mage test-pkg` is the gate (worklog confirms). `mage install` not invoked. ALL EXHAUSTED for non-spec attacks; B5 spec-compliance lands the CONFIRMED counterexample.

**Gate run record:**

- `mage test-pkg ./internal/config` — 52/52 GREEN, wall-clock 0.00s.
- `mage test-func ./internal/config "TestMergeLocal_.*"` — 8/8 GREEN, wall-clock 1.21s (includes `-race`).

### Summary

**Verdict: fail** — 1 CONFIRMED counterexample (W0-D3-CX1, Family B5 severity high). All other families exhausted with REFUTED outcomes (1.2-1.11).

**Explicit verdict on the wrap-text deviation: round-3-spec-violation, NOT a justified deviation.** The W0 plan-QA loop ran 3 rounds specifically to lock strict D3↔D5 separation (R1 caught W0-FF1, R2 caught W0-FF2-R2, R3 verified the spec converged). PLAN.md:112 (round-3 finalized) reads verbatim: `"(no file/line/block prefix at the D3 boundary)"`. The wrap text `"agents.local.toml [agents.<kind>]:"` IS a file + block prefix — 2 of the 3 prefix axes the spec explicitly forbids. Builder's "useful when MergeLocal runs standalone" rationale (BUILDER_WORKLOG line 128) does not align with the round-3 spec, which had no standalone-use carve-out and explicitly assigned the prefix-shape to D5's envelope (PLAN.md:112 second sentence: `"D5 wraps the sentinel into *ConfigError so the user-facing message renders as \"agents.local.toml [agents.<kind>]:<line>: ...\""`). The wrap creates exactly the D5-shaped prefix minus the line number, leaving D5 with a forced choice: emit a duplicated prefix (visibly broken) or strip-and-re-wrap (wasted code). Either path is the wrong outcome — the spec wanted D3 to ship a bare sentinel so D5 owns the envelope unambiguously.

| Family | Attack target | Result |
| ------ | ------------- | ------ |
| B1 (test-coverage) | empty `tools_deny` slice / per-kind + defaults simultaneous / nil local / nil project / rapid-merge race | REFUTED — defaults-block-first ordering acceptable; empty-slice predicate gates correctly; nil-handling tested at `_NilLocal` + `_NilProject`; race-safe by construction. |
| B2 (contract-preservation) | deep-clone correctness; nil-local symmetric-clone vs alias | REFUTED — `cloneOverride` + `copyMap` + `copySlice` thread every input pointer; output never aliases input. Spec says "unchanged" — clone interpretation defensible. |
| B3 (hidden-coupling) | map-collision precedence; concurrency | REFUTED — local-wins on collision (mirrors D2); pure function safe across goroutines. |
| B4 (YAGNI) | wrap-text necessity | CONFIRMED contributor to 1.1 — solves no-consumer problem at cost of spec deviation. Folded into B5 finding. |
| **B5 (spec-compliance)** | **bare-sentinel contract per PLAN.md:112 round-3 final** | **CONFIRMED — 1.1. Wrap text contains file + block prefix; spec mandates none.** |
| B6 (shipped-but-not-wired) | MergeLocal consumer | REFUTED — appendix-acceptable deferral to W2/W3. |
| B7 (prompt-injection) | DORMANT pre-team-feature | EXHAUSTED. |

**Required orchestrator action:** route W0-D3-CX1 to a fresh builder spawn for round 2. Fix is a one-line revert per builder's own escape hatch: replace `fmt.Errorf("agents.local.toml [agents]: %w", ErrToolsDenyNotOverridable)` and `fmt.Errorf("agents.local.toml [agents.%s]: %w", kind, ErrToolsDenyNotOverridable)` at `agents.go:394` + `:398` with bare `ErrToolsDenyNotOverridable` returns. `errors.Is` chain preserved (sentinel is the chain root). Existing tests at `agents_test.go:484` + `:511` continue to pass via `errors.Is`. D5 will own the entire envelope per PLAN.md:188-194.

### Hylla Feedback

None — Hylla not queried in this round. Round 1 falsification needed (a) re-reading PLAN.md D3 L2 acceptance bullets at lines 109-120 + RiskNote at 128 + ContextBlock at 132 + KindPayload at 137, (b) re-reading PLAN_QA_FALSIFICATION_R3.md to confirm the round-3 spec lock, (c) re-reading BUILDER_WORKLOG line 128's honest deviation flag, (d) reading `agents.go:392-400` wrap text verbatim, (e) reading `agents_test.go:484-532` for assertion-shape verification, (f) running `mage test-pkg ./internal/config` + `mage test-func ./internal/config "TestMergeLocal_.*"` gates. None are Hylla-shaped queries — (a)-(e) are direct file reading of post-D2-uncommitted in-flight working tree (Hylla would be stale by definition for D3's just-edited code), (f) is build-tool. Confirms the project rule "use `git diff` / `Read` for files touched after the last Hylla ingest." No fallback miss to log; Hylla ergonomic-gripe-free for this round.

## Droplet 4c.6.W0.D3 — Round 2

### Round-1 Counterexample Verification

**W0-D3-CX1: FIXED.**

Round-1 finding (recap): the wrap text at `agents.go:393-400` returned `fmt.Errorf("agents.local.toml [agents]: %w", ...)` and `fmt.Errorf("agents.local.toml [agents.%s]: %w", kind, ...)` — file + block prefix violating PLAN.md:112 verbatim ("no file/line/block prefix at the D3 boundary").

Round-2 production code at `internal/config/agents.go:391-401` verbatim:

```go
if local != nil {
    if len(local.Preset.ToolsDeny) > 0 {
        return nil, ErrToolsDenyNotOverridable
    }
    for _, ov := range local.Overrides {
        if ov.ToolsDeny != nil && len(*ov.ToolsDeny) > 0 {
            return nil, ErrToolsDenyNotOverridable
        }
    }
}
```

- **Defaults-block path (`agents.go:394`)**: bare `return nil, ErrToolsDenyNotOverridable`. CONFIRMED-bare.
- **Per-kind path (`agents.go:398`)**: bare `return nil, ErrToolsDenyNotOverridable`. CONFIRMED-bare.
- **Sentinel-chain preserved**: both returns ARE the sentinel itself; `errors.Is(ErrToolsDenyNotOverridable, ErrToolsDenyNotOverridable) == true` trivially. `TestMergeLocal_ToolsDenyRejected` and `TestMergeLocal_ToolsDenyDefaultsBlockRejected` both GREEN per gate-run record below.
- **Residual prefix language scan**: `rg -n "agents\.local\.toml" internal/config/agents.go` returns 1 hit at `:29` (the `ErrToolsDenyNotOverridable` doc-comment, prose describing the contract — NOT a runtime error string). `rg -n "\[agents" internal/config/agents.go` returns 7 hits, all in `//`-style doc-comments (lines 29, 38, 113, 123, 129-130, 198, 372) — descriptive prose explaining schema layout, never formatted into a runtime error message. ZERO file/block prefix language survives in any runtime error path post-revert.

CX1 cleanly fixed by the two-line revert. D3 surface now matches PLAN.md:112 strict bare-sentinel contract; D5's envelope owns the entire prefix shape.

### Fresh Counterexamples

No CONFIRMED counterexamples in round 2. Every fresh attack family lands REFUTED. The loop-variable-drop attack is REFUTED with positive evidence (loop body verified `kind`-free post-revert).

- 2.1 [Family: B2-contract-preservation] [severity: low] **Loop-variable drop semantic check — `for _, ov := range local.Overrides` vs round-1's `for kind, ov := range local.Overrides`.** → repro: `agents.go:396-400` post-revert reads `for _, ov := range local.Overrides { if ov.ToolsDeny != nil && len(*ov.ToolsDeny) > 0 { return nil, ErrToolsDenyNotOverridable } }`. → check: I read every line of the loop body (lines 397-400). The body references ONLY `ov.ToolsDeny` and the literal `ErrToolsDenyNotOverridable` sentinel — `kind` does not appear. The round-1 loop body referenced `kind` ONLY in the now-deleted `fmt.Errorf("...%s...", kind, ...)` formatting; with that formatting gone, `kind` was dead. Dropping the unused loop-variable is a Go compile-required cleanup (`declared and not used`). Iteration semantics over a `map[domain.Kind]Override` are unchanged: `for k, v := range m` and `for _, v := range m` both visit the same set of `(k, v)` pairs in the same (undefined) order; only the binding of the key variable differs. No semantic change. REFUTED.
- 2.2 [Family: B2-contract-preservation] [severity: low] **Could the kind-drop have orphaned a different reference to `kind` further down the function body?** → repro: `rg -n "\bkind\b" internal/config/agents.go` → 36 hits. Two production-code loops outside the rejection block both LEGITIMATELY use `kind` as a map key: `:414 for kind, ov := range project.Overrides { out.Overrides[kind] = cloneOverride(ov) }` and `:426 for kind, lov := range local.Overrides { ... out.Overrides[kind] = ... }`. These are distinct `for-range` loops with their own scope; the round-1 `kind` deletion is local to the rejection block alone. The other 34 `kind` references are doc-comment prose (`per-kind`, `[agents.<kind>]`, `domain.Kind`, etc.) or the `Resolve(registry, kind)` parameter — none affected by the rejection-block edit. NOT a counterexample. REFUTED.
- 2.3 [Family: B1-test-coverage] [severity: low] **Test contract preserved through the revert.** → repro: `TestMergeLocal_ToolsDenyRejected` (`agents_test.go:484`) and `TestMergeLocal_ToolsDenyDefaultsBlockRejected` (`:511`) both assert via `errors.Is(err, ErrToolsDenyNotOverridable)`. Pre-revert: `errors.Is(fmt.Errorf("...: %w", sentinel), sentinel) == true` via `%w` chain. Post-revert: `errors.Is(sentinel, sentinel) == true` via root identity. Identical verdict; both tests GREEN per gate-run record. NOT a counterexample. REFUTED.
- 2.4 [Family: B5-spec-compliance] [severity: low] **PLAN.md:112 verbatim spec satisfied?** → repro: spec reads "The bare sentinel's message reads `\"tools_deny is not user-overridable; remove the field\"` (no file/line/block prefix at the D3 boundary)." Production code post-revert returns `ErrToolsDenyNotOverridable` directly; `err.Error()` returns `"tools_deny is not user-overridable; remove the field"` exactly. Zero file prefix, zero line prefix, zero block prefix at the D3 boundary. NOT a counterexample. REFUTED.
- 2.5 [Family: B3-hidden-coupling] [severity: low] **Did the revert change `MergeLocal`'s exported behavior?** → repro: function signature unchanged (`MergeLocal(project, local *AgentsRegistry) (*AgentsRegistry, error)`); error type changed from "wrapped sentinel" to "bare sentinel"; `errors.Is(err, ErrToolsDenyNotOverridable)` succeeds either way. No documented external consumer depends on the wrap-text shape (D5 supersedes per PLAN.md:188-194; W2/W3 consumer wiring is downstream). Doc-comment at lines 371-375 already documented "D3 surfaces only the sentinel" as the contract — round 2 brings code into alignment with prose. NOT a counterexample. REFUTED.
- 2.6 [Family: B4-YAGNI] [severity: low] **Round-2 minimality.** → repro: round-2 diff per BUILDER_WORKLOG round-2 entry is exactly -3 LOC / +3 LOC: revert two `fmt.Errorf` lines + replace `for kind, ov` with `for _, ov`. No additional refactor, no scope creep. The revert removed runtime work (no more `fmt.Errorf` allocation on the hot path) without changing the error-recognition contract. Smallest-concrete-design respected. REFUTED.
- 2.7 [Family: B6-shipped-but-not-wired] DEFERRED — same as round 1; W2/W3 wiring acceptable per L1 directive.
- 2.8 [Family: B7-prompt-injection] EXHAUSTED — DORMANT pre-team-feature.

**File and package gating attacks (post-Drop-1 surface):**

- Edits stay within declared `paths`: `internal/config/agents.go` (MODIFY) only — no test files, no fixtures, no MD files outside the workflow worklog. `git status --porcelain internal/config/agents.go` shows the round-2 modification cleanly. REFUTED.
- Package gate: `internal/config` only. REFUTED.

**Concurrency / interface-misuse / error-swallowing / goroutine-leak / raw-go-command / `mage install` attack surfaces:** Round-2 surface is two reverted return statements + one loop-variable name change. No goroutines, no mutexes, no channels, no `init()` side effects, no panic-able assertions, no error swallowing (the bare sentinel IS the error — properly returned). `mage test-pkg` + `mage test-func` are the gates (no raw `go test`). `mage install` not invoked. ALL EXHAUSTED.

**Gate run record:**

- `mage test-pkg ./internal/config` — 52/52 GREEN, wall-clock 0.28s.
- `mage test-func ./internal/config "TestMergeLocal_.*"` — 8/8 GREEN, wall-clock 1.25s (includes `-race`).
- `rg -n "\bkind\b" internal/config/agents.go` — 36 hits; all confirmed legitimate (loop-variables in different loops at `:414` + `:426`, `Resolve` parameter at `:238`, `addOverride` parameter at `:201`, doc-comment prose).
- `rg -n "agents\.local\.toml" internal/config/agents.go` — 1 hit at `:29` (doc-comment).
- `rg -n "\[agents" internal/config/agents.go` — 7 hits, all in `//`-style doc-comments (lines 29, 38, 113, 123, 129-130, 198, 372). ZERO runtime-error-string hits.

### Summary

**Verdict: pass** — W0-D3-CX1 cleanly fixed via two-line revert; loop-variable drop semantically equivalent (loop body verified `kind`-free post-revert); all fresh attack families landed REFUTED or EXHAUSTED.

| Family | Attack target | Result |
| ------ | ------------- | ------ |
| B1 (test-coverage) | sentinel-chain test contract preserved through revert; existing tests still cover both rejection paths | REFUTED — `TestMergeLocal_ToolsDenyRejected` + `TestMergeLocal_ToolsDenyDefaultsBlockRejected` both GREEN; `errors.Is` chain works identically against bare sentinel and wrapped sentinel. |
| B2 (contract-preservation) | loop-variable drop semantic equivalence; orphaned `kind` references; exported behavior change | REFUTED — loop body `kind`-free post-revert; other `kind` references in distinct scopes; signature unchanged; `errors.Is` chain preserved. |
| B3 (hidden-coupling) | external consumer dependency on wrap-text shape | REFUTED — no consumer exists today (W2/W3 deferred); doc-comment prose already documented bare-sentinel contract. |
| B4 (YAGNI) | round-2 scope minimality | REFUTED — exactly -3/+3 LOC; no scope creep; runtime allocation removed from hot path. |
| **B5 (spec-compliance)** | **PLAN.md:112 verbatim "no file/line/block prefix at the D3 boundary"** | **REFUTED — production code returns bare sentinel; `err.Error()` is the exact spec-mandated string; zero forbidden prefix axes survive.** |
| B6 (shipped-but-not-wired) | MergeLocal consumer | REFUTED — appendix-acceptable deferral to W2/W3. |
| B7 (prompt-injection) | DORMANT pre-team-feature | EXHAUSTED. |

**W0-D3-CX1 verdict: FIXED.** D3 surface now strictly matches PLAN.md:112 round-3-finalized contract. D5 owns the entire envelope. Round 2 PASSES.

### Hylla Feedback

None — Hylla not queried in round 2. Verification needed (a) `Read` of `internal/config/agents.go` post-revert verbatim, (b) `Read` of round-1 BUILDER_QA_FALSIFICATION.md to recall CX1, (c) `Read` of BUILDER_WORKLOG round-2 entry, (d) `Read` of PLAN.md line 112 verbatim spec, (e) `mage test-pkg` + `mage test-func` gates, (f) `rg -n` scans for residual prefix language and `kind` references. None are Hylla-shaped — (a)-(d) are direct file reading of post-D3-uncommitted working tree (Hylla stale by definition); (e) is build-tool; (f) is `rg` text search appropriate for residual-string verification (not symbol-discovery, which would have used Hylla). No fallback miss to log; ergonomic-gripe-free this round.
