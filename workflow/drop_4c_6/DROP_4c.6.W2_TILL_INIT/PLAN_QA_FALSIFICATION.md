# Plan-QA-Falsification Round 1 — DROP_4c.6.W2

**Sub-drop:** `4c.6.W2` — `till init` (TUI walk + JSON mode + vendor `fsatomic`/`configmerge` + remove `init-dev-config`).
**Round:** 1 (initial L2 plan).
**Reviewer:** L2 plan-QA-falsification (per-role, post-spawn).
**Inputs:**

- `workflow/drop_4c_6/DROP_4c.6.W2_TILL_INIT/PLAN.md` (9 droplets: D1, D2, D3, D4, D5, D6, D7, D7.5, D8).
- `workflow/drop_4c_6/DROP_4c.6.W2_TILL_INIT/_BLOCKERS.toml` (sibling blocker ledger).
- `workflow/drop_4c_6/PLAN.md` lines 117-133 (L1 directive for W2).
- `workflow/drop_4c_6/SKETCH.md` § 9 (`till init` contract) + § 26.W2 (per-wave Specify).
- `cmd/till/main.go:1884-2094` (current `init-dev-config` Cobra cmd + `runInitDevConfig` body).
- `cmd/till/help.go:377-390` (rich-help table entry to remove).
- `cmd/till/main_test.go:476, 732-734, 2906-3011, 3105` (tests to port / remove / update).

**Verdict:** **PASS-with-one-low-severity-doc-tightening** (8 attack families exhausted; D7.5 expansion verified justified; one verbatim-port nuance flagged for D7.5 builder spawn-prompt clarity).

---

## 1. Findings

- **1.1 [Family: A1 — Concurrency / blocked_by] [severity: low] No cycles in 9-node graph; multi-edge parallelism verified.** Walked every `blocked_by` edge in `_BLOCKERS.toml`: D1→[], D2→[D1], D3→[], D4→[D3], D5→[D1, D2, D4], D6→[D5], D7→[D6], D7.5→[D3], D8→[D3, D7.5]. Reachability matrix confirms acyclic. Parallel-able pairs: (D1, D3) at start; D2 unblocks after D1 (parallel to D3, D4 if D3 done); D7.5 unblocks after D3 (parallel to D4, D5, D6, D7 chain). REFUTED — no cycle counterexample.
- **1.2 [Family: A1 — sibling overlap without `blocked_by`] [severity: low] Same-package + same-file locks all wired correctly.** All cmd/till droplets serialize on `cmd/till/init_cmd.go` (D3→D4→D5→D6→D7) and on `cmd/till/main.go` (D3→D7.5; D3+D7.5→D8). D5's cross-package blocker on D1+D2 (imports `internal/vendor/fsatomic` + `internal/vendor/configmerge`) is correctly declared. D7.5 introduces NEW file `cmd/till/install_cmd.go` (no overlap with D3-D7's `init_cmd.go`) but correctly blocks on D3 for the shared `main.go` rootCmd.AddCommand line. **NIT (low):** `cmd/till/help.go` is touched by D3, D7.5, AND D8 — all three add or remove entries to the `commandHelpSpecs` map. The `_BLOCKERS.toml` `reason` field for D7.5 (line 52) cites only `main.go`, not `help.go`. The D7.5→D3 + D8→D3 + D8→D7.5 chain is sufficient to serialize `help.go` (transitively D8 waits for both D3 and D7.5), but the `reason` text should mention `help.go` too for builder clarity. REFUTED as a hard counterexample (transitive chain holds); flagged as docstring-precision NIT.
- **1.3 [Family: A2 — Contract mismatch] [severity: low] L2 acceptance bullets cover every L1 contract criterion in PLAN.md:122-129.** Mapping check:
  - L1 "till init registered in main.go with help text + JSON flag" → D3 acceptance covers (lines 95-104).
  - L1 "internal/vendor/fsatomic + configmerge with VENDOR_SOURCE.md provenance" → D1+D2 acceptance covers (lines 47-58, 73-83).
  - L1 "TUI mode copies 7 .md files FLAT + agents.toml + .gitignore + optional .mcp.json + project record + Laslig" → D4 (TUI walk) + D5 (file copy + .gitignore) + D6 (.mcp.json) + D7 (project record + Laslig) covers (lines 117-187).
  - L1 "JSON mode equivalent" → D3 (parser) + D5/D6/D7 (shared pipeline) covers (lines 98, 138, 161).
  - L1 "re-run safety: invocation #2 added=0, skipped=N" → D5 acceptance line 137 + tests `TestInit_RerunSafety_NoOverwrite` line 141 covers.
  - L1 "till init-dev-config removed; help.go updated; main_test.go references updated to till install" → D8 acceptance covers (lines 224-231).
  - L1 "mage test-pkg ./cmd/till/... + integration + re-run + JSON-equiv" → every droplet's `mage test-pkg ./cmd/till` bullet, plus D5's 4 dedicated tests, plus D7.5's 2 ported tests. REFUTED — no L1 bullet uncovered.
- **1.4 [Family: A2 — SKETCH § 9.1 contract] [severity: low] L2 PLAN treats `till install` as the destination, matching SKETCH §9.1 verbatim.** SKETCH §9.1 reads: "`till init-dev-config` is removed; install-time setup (DB creation, default config) folds into `till install`." Note SKETCH §9.1 mentions BOTH "DB creation" AND "default config" folding into `till install`. Current `runInitDevConfig` body (lines 2039-2094) does ONLY default-config-creation + debug-logging-rewrite — there is NO DB-creation behavior in `runInitDevConfig`. **D7.5 acceptance bullet (lines 202-203) ports ONLY the dev-config-creation behavior, NOT a DB-creation step that doesn't exist in source today.** This is correct — the SKETCH §9.1 phrase "DB creation" referred to a future-direction descriptor, not present-source behavior. The OQ#3 verification block at PLAN.md:21-22 confirms: "`runInitDevConfig` semantically does TWO things: (a) creates `<dev-paths>/till.toml`... (b) rewrites the `[logging]` section to `level = "debug"`." Both folded. REFUTED — no contract gap.
- **1.5 [Family: A3 — Hidden coupling on D7.5 verbatim port] [severity: low — DOC TIGHTENING NEEDED] `runInstall`'s Laslig title is implicitly required to remain `"Dev Config"`, but D7.5 spec doesn't make this explicit.** D7.5 acceptance bullet (line 203) calls out: `(a) platform.DefaultPathsWithOptions`, `(b) os.MkdirAll + create-if-missing`, `(c) ensureLoggingSectionDebug rewrite`, `(d) writeCLIKV Laslig success message with status / config path / logging level keys`. The current title at `main.go:2089` is `"Dev Config"`. The D7.5 test-port bullet at PLAN.md:207 says "Same body, just `[]string{"install"}` instead of `[]string{"init-dev-config"}`." But the existing tests (`main_test.go:2936, 2991`) assert `"Dev Config"` as a string in the output. If D7.5 builder reads "Laslig success message" loosely and renames the title to `"Install"` or `"Tillsyn Install"` in `runInstall`, the ported test bodies (which assert `"Dev Config"` substring) will FAIL. **Repro:** D7.5 builder writes `runInstall` with `writeCLIKV(stdout, "Install", ...)` instead of `writeCLIKV(stdout, "Dev Config", ...)`; ports test verbatim; `mage test-pkg ./cmd/till` fails because `TestRunInstall_CreatesDebugConfig` asserts `"Dev Config"` substring in output. **Fix hint:** spawn-prompt for D7.5 builder MUST explicitly say "preserve the laslig title `"Dev Config"` byte-for-byte" — OR (cleaner) "rename the title to `"Install"` AND update the ported tests' expected substring from `"Dev Config"` to `"Install"` consistently." Either is fine; ambiguity in the L2 plan is the actual finding. CONFIRMED as a low-severity ambiguity, fixable with one acceptance-bullet edit on D7.5 (e.g. add: "Title string in `writeCLIKV` is `"Install"` (not `"Dev Config"`); D7.5's ported test assertions match the new title").
- **1.6 [Family: A3 — Hidden coupling on D8 doc-comment update] [severity: low] D8 acceptance line 222 says "update the `TestShellEscapePath` doc-comment at line 3105 to drop the 'init-dev-config' mention," but the current doc-comment at 3105 reads "TestShellEscapePath verifies init-dev-config path output is shell-token safe." This phrasing-rewrite has TWO acceptable shapes — replace with `"till install"` (since `runInstall` now uses `shellEscapePath`) OR drop the command reference entirely. D8's acceptance is satisfied by either, but builder ambiguity exists. REFUTED as counterexample (D8's `git grep -n init-dev-config cmd/till/` zero-matches assertion at line 225 forces this); flagged as low-severity NIT for builder spawn-prompt — explicitly state "rewrite the doc-comment to: `// TestShellEscapePath verifies till install path output is shell-token safe.`" or equivalent.
- **1.7 [Family: A4 — YAGNI / scope-creep on D7.5] [severity: low] D7.5 expansion is justified, NOT scope-creep.** L1 directive (PLAN.md:117-132) explicitly conditions D8 on D7.5's existence: "**PRECONDITION (ROUND-2 OQ#3 disposition): the L2 W2 sub-planner MUST verify `till install` already covers (or is extended in this sub-plan to cover) the dev-config-creation behavior currently in `cmd/till/main.go:2039 runInitDevConfig` ... BEFORE D8 finalizes the removal. If `till install` does NOT cover this behavior, the L2 sub-plan MUST add a new droplet that extends `till install` first.**" The OQ#3 verification at L2 PLAN.md:17-29 documents `git grep -n "runInstall\|installCmd" cmd/till/ internal/` returns NO matches → `till install` does NOT exist → expansion is mandatory per L1. Could D8 instead defer the removal to a future drop and leave `init-dev-config` alive? Theoretically yes, but L1 lists D8 in scope (PLAN.md:128: "till init-dev-config removed from CLI surface; cmd/till/help.go updated accordingly"). Deferring D8 would violate L1's W2 contract. **Counter-argument considered:** could the expansion have been smaller (e.g. fold the body inline into D8 rather than create a separate D7.5 droplet)? D7.5's NOTES bullet at PLAN.md:213-214 explicitly addresses this: "Why not a same-droplet add+remove? Because D7.5 + D8 have different scopes: D7.5 ADDS a CLI command (low risk, additive); D8 REMOVES a CLI command (higher risk — touches help.go, main_test.go with multiple test functions, the rootCmd.AddCommand line, the commandHelpSpecs map). Splitting lets D7.5 ship + verify-green BEFORE D8's surgery starts." This justification is sound: split lets D7.5 land a clean additive commit with green CI, then D8 cleans up with reduced surface. REFUTED — D7.5 expansion is L1-mandated AND D7.5/D8 split is well-justified.
- **1.8 [Family: A5 — Shipped-but-not-wired] [severity: low] Every new symbol consumed in this wave.** Per `feedback_tillsyn_enforces_templates.md`, shipped-but-not-wired is a Drop 3 droplet 3.20 anti-pattern. Survey:
  - D1 ships `internal/vendor/fsatomic` package → consumed by D5 (file-copy uses atomic writes per line 134). WIRED.
  - D2 ships `internal/vendor/configmerge` package → consumed by D5 for `.gitignore` merge (line 136 — falls back to hand-written if doesn't fit). WIRED.
  - D3 ships `newInitCommand` → consumed by `main.go`'s `rootCmd.AddCommand` at line 99. WIRED.
  - D3 ships `runInitTUI` + `runInitJSON` stubs → D4 fills `runInitTUI`; D5 fills the file-copy pipeline both call. WIRED in subsequent droplets.
  - D5 ships `copyAgentFiles` / `copyAgentsTOML` / `ensureGitignore` → called by `runInitTUI` + `runInitJSON` in D5. WIRED.
  - D6 ships `registerMCPJSON` → called by `runInitJSON` + `runInitTUI` in D6. WIRED.
  - D7 ships `createProjectDBRecord` (or analogous) → called by both run paths in D7. WIRED.
  - D7.5 ships `newInstallCommand` + `runInstall` → consumed by `main.go`'s `rootCmd.AddCommand` at line 205. WIRED.
  - D7.5 ships `TestRunInstall_*` tests → cover the equivalent behavior that D8 then removes — WIRED to D8's pre-flight check (line 234). REFUTED — no orphan symbol.
- **1.9 [Family: A5 — VENDOR_SOURCE.md] [severity: low] D1 creates VENDOR_SOURCE.md; D2 appends. The `_BLOCKERS.toml` D2→D1 wiring (line 15) explicitly cites this shared-file lock. PLAN.md:82-84 documents the correction. REFUTED — no shipped-but-not-wired counterexample.
- **1.10 [Family: A6 — Atomicity on D3] [severity: low — flagged for builder] D3 is borderline-large; planner already flagged it.** D3's NOTE at PLAN.md:106 explicitly says: "if D3's combined LOC (skeleton + JSON-payload struct + dispatch + tests + main.go register + help.go entry) exceeds ~120 LOC across production files, escalate to orchestrator. D3 may need to split into D3a (skeleton + register, no JSON) and D3b (JSON parser); for now keep one droplet." This is the planning-agent's "atomic-droplet-borderline" flag; the orchestrator-on-spawn will catch via builder escalation if needed. REFUTED — escalation path is wired.
- **1.11 [Family: A6 — Atomicity on other droplets] [severity: low] D1 (vendor 52 LOC + 1 test file), D2 (vendor ~12kB + tests — large but vendoring is a single byte-for-byte copy operation, not authored LOC), D4 (TUI walk single-purpose), D5 (file-copy pipeline + 4 tests — borderline-large but tightly cohesive), D6 (.mcp.json + 4 tests — cohesive), D7 (DB record + Laslig + 2 tests — cohesive), D7.5 (install command port + 2 tests — small additive surface), D8 (deletion across 3 files — surgical but mechanical). D5 is the largest authored droplet (4 tests + the production pipeline); could split into D5a (copyAgentFiles + copyAgentsTOML) and D5b (ensureGitignore), but the four functions share a single re-run-safety contract per L1 line 127 ("invocation #2 reports added=0, skipped=N already-present, no overwrites") — splitting would force the second half of the contract to rebuild the same idempotency machinery. Cohesive grouping is justified. REFUTED — atomicity bounds OK across all 9 droplets.
- **1.12 [Family: A7 — Prompt injection] [severity: dormant] Pre-team-feature; mark EXHAUSTED per `feedback_prompt_injection_team.md`.** No team-aware action-item content surfaces in this wave. EXHAUSTED.
- **1.13 [Family: A1 — Required Phase-2-step-1 attacks: missing blocked_by, cycles, drift] [severity: low] Sweep complete:**
  - **Cycles:** none (see 1.1).
  - **Missing blocked_by (sibling file-overlap):** D3, D4, D5, D6, D7 share `cmd/till/init_cmd.go`. D3 → D4 → D5 → D6 → D7 chain established. D7.5 + D8 both touch `cmd/till/main.go` shared with D3. D7.5 → D3, D8 → D3, D8 → D7.5 all declared. D8 + D3 also share `cmd/till/help.go` (D7.5 amends help.go too — see 1.2 NIT). The transitive chain D8 → D3 + D8 → D7.5 → D3 covers help.go as well. No missing blocker.
  - **Missing blocked_by (cross-drop):** PLAN.md container line 4 declares `Blocked by: 4c.6.W1.D1` (W2 needs W1's embedded agent files). The dependency on W0 (`agents.example.toml`) is implicit via "W1.D1 ships the embed list" — verify W1.D1 acceptance includes `agents.example.toml`. PLAN.md:112 (Wave W1 KindPayload at L1 PLAN.md) shows W1.D1 explicitly ships `agents.example.toml`. WIRED.
  - **Drift:** L2 acceptance bullets do not contradict L1 directive or SKETCH §9/§26.W2 — see 1.3 + 1.4. No drift.
  - REFUTED.

## 2. Counterexamples

No CONFIRMED counterexamples that block merge. One CONFIRMED low-severity doc-precision finding:

- **2.1 [from 1.5; severity: low; CONFIRMED]** D7.5 acceptance bullet on Laslig title is ambiguous → builder may rename title and break the verbatim-test-port assumption. **Repro:** builder spawn-prompt for D7.5 reads "writeCLIKV Laslig success message with status / config path / logging level keys" without title-string pinning; builder picks `"Install"` as the natural new title; ports test from `main_test.go:2936` adding `"Install"` substring assertion; meanwhile `runInstall`'s actual `writeCLIKV` first arg gets `"Install"`; tests pass; D8 removes the original `TestRunInitDevConfigCreatesDebugConfig` (which asserts `"Dev Config"`); coverage shifts; behavior is technically correct but the planner-spec ambiguity creates a silent renaming. Not a bug per se but a planner-discipline gap. **Fix hint:** add a single acceptance bullet to D7.5 (PLAN.md:200-211): "Title string passed to `writeCLIKV` is `"Install"` (lowercase if convention dictates, builder picks per existing CLI surface — but DECIDE EXPLICITLY in `BUILDER_WORKLOG.md`); the ported `TestRunInstall_*` tests assert the chosen title substring consistently." Alternatively: "preserve title `"Dev Config"` byte-for-byte; rename the function but not the user-visible label."

## 3. Summary

**Verdict: PASS-with-one-low-severity-doc-tightening.**

| Family | Status | Findings |
| --- | --- | --- |
| A1 — Concurrency / blocked_by | EXHAUSTED | 1.1, 1.2 (NIT), 1.13 — no cycles, no missing blockers (transitive chains valid for help.go) |
| A2 — Contract mismatch | EXHAUSTED | 1.3, 1.4 — every L1 + SKETCH §9/§26.W2 bullet covered |
| A3 — Hidden coupling | EXHAUSTED | 1.5 (CONFIRMED low NIT on Laslig title), 1.6 (NIT on D8 doc-comment phrasing) |
| A4 — YAGNI / scope-creep | EXHAUSTED | 1.7 — D7.5 L1-mandated; split well-justified |
| A5 — Shipped-but-not-wired | EXHAUSTED | 1.8, 1.9 — every new symbol consumed; VENDOR_SOURCE.md wired D1→D2 |
| A6 — Atomicity | EXHAUSTED | 1.10 (D3 escalation path wired), 1.11 (other droplets within budget) |
| A7 — Prompt injection | DORMANT | 1.12 — pre-team-feature |
| Required Phase-2-step-1 sweep | EXHAUSTED | 1.13 — cycles / missing blockers / drift all clean |

**Verdict on D7.5 expansion:** **JUSTIFIED.** L1 directive at PLAN.md:117-132 explicitly conditioned D8 on `till install` covering the dev-config-creation behavior; OQ#3 verification confirmed `till install` does NOT exist today; therefore L2 expansion is mandatory, not scope-creep. D7.5/D8 split (additive-then-subtractive) is well-justified per PLAN.md:213-214's NOTE block — splits low-risk-add from higher-risk-remove for clean CI verification between the two.

**Verdict on D7.5 verbatim-port soundness:** **SOUND with one ambiguity.** All five behavior elements (`platform.DefaultPathsWithOptions`, `os.MkdirAll`, create-if-missing, `ensureLoggingSectionDebug`, `writeCLIKV` row keys) are captured. `shellEscapePath` and `ensureLoggingSectionDebug` remain in `main.go` as shared helpers (verified line 2096-2167) — `runInstall` in `install_cmd.go` imports them from the same package, no cross-file move needed. The `created bool` flag's status-message delta (`"created dev config"` vs `"dev config already exists"`) is preserved by verbatim port. The ONLY behavioral ambiguity is the Laslig table title string `"Dev Config"` — see finding 1.5 + counterexample 2.1.

**Recommended action before D7.5 builder spawn:** orchestrator adds one bullet to D7.5 acceptance pinning the laslig title decision (preserve `"Dev Config"` OR explicitly rename to `"Install"` AND update test substrings). Either resolution is fine; the choice should be made in the planner artifact, not left for the builder to guess. Round 2 of plan-QA could verify that single edit lands; alternatively the orchestrator can pin it in the spawn prompt directly if Round 2 is skipped.

**Routing:** finding 1.5 / counterexample 2.1 routes back to orchestrator as a single-bullet edit to PLAN.md D7.5 acceptance. NIT 1.2 (help.go citation in D7.5 `_BLOCKERS.toml` reason) and NIT 1.6 (D8 doc-comment exact rewrite) are spawn-prompt clarifications, NOT plan amendments.

## 4. Hylla Feedback

N/A — this falsification reviewed plan-level MD artifacts (PLAN.md, _BLOCKERS.toml, SKETCH.md, L1 PLAN.md) and Go source via Read/Grep. No Hylla queries were issued because (a) plan-QA-falsification is a contract-level review against MD specs, not a code-symbol review, and (b) the Go source touched (`cmd/till/main.go:1884-2094`, `cmd/till/help.go:377-390`, `cmd/till/main_test.go:2906-3105`) was read directly to verify exact line spans cited in the L2 plan. Per `feedback_hylla_go_only_today.md`, Hylla covers committed Go but not MD/TOML — the inputs here are predominantly MD/TOML, and the Go cross-checks were exact-line-span verifications where Read is the authoritative tool.

No Hylla miss to report.

---

## Round 2 Verdict

**Reviewer:** L2 plan-QA-falsification (Round 2, 2026-05-09).
**Inputs:**

- `workflow/drop_4c_6/DROP_4c.6.W2_TILL_INIT/PLAN.md` (round-2 plan: D2 removed; D3 split into D3a + D3b; D7.5 acceptance pinned with three contracts).
- `workflow/drop_4c_6/DROP_4c.6.W2_TILL_INIT/_BLOCKERS.toml` (round-2 ledger).
- `workflow/drop_4c_6/DROP_4c.6.W2_TILL_INIT/PLAN_QA_PROOF.md` (round-1 proof, 8 findings + 3 missing-evidence).
- `cmd/till/main.go:1880-2094` (current `init-dev-config` Cobra cmd + `runInitDevConfig` body).
- `cmd/till/main.go:104` (`run` signature: `run(ctx context.Context, args []string, stdout, stderr io.Writer) error`).
- `cmd/till/main_test.go:476, 732-734, 2906-3011, 3105` (existing assertions).
- `cmd/till/help.go:377-391` (rich-help table; `commandHelpSpecs` is a `map[string]commandHelpSpec` keyed by `cmd.CommandPath()`).
- `internal/config/` directory listing (`agents.go`, `agents_test.go`, `config.go`, `config_test.go`, `frontmatter.go`, `frontmatter_test.go`) — no `configmerge` subpackage exists.

**Verdict (single-line preview):** **PASS-with-three-low-severity findings (W2-FF6/7/8) + one MEDIUM doc-precision finding (W2-FF9) + one EXHAUSTED-but-noted observation (W2-FF10).** The round-2 patch (D3 split + D2 removal + D7.5 acceptance pins) holds up under attack: file-locks correctly serialize, the Laslig title pin is byte-aligned with the existing source, the test-name contract is internally consistent, and `bytes.Contains` substring corner-cases are bounded. Round 2 advances to W2 builders; one orchestrator-level decision needed on W2-FF9 (D5 `.gitignore` substring contract).

---

### 1. Findings

#### Attack the W2-FF1 fix (D3 split into D3a + D3b)

- **W2-FF6 [Family: A6 — atomicity / D3a skeleton scope ambiguity] [severity: low — DOC PRECISION]** D3a's "skeleton" body straddles the boundary between "fully-defined stub" and "empty placeholder." PLAN.md:90 says: "the parser body is a STUB in D3a; the flag is registered + readable but the value is not parsed. D3a's stub `RunE` dispatches: if `--json <payload>` is set (non-empty), it returns `errors.New("till init: JSON parse not yet wired (W2.D3b)")`; otherwise calls `runInitTUI(stdout, opts)` which itself returns `errors.New("till init: TUI walk not yet wired (W2.D4)")`." This is sufficient — the stub returns a meaningful sentinel error. PLAN.md:92 then specifies the smoke test: "`till init` (bare) returns the TUI-stub error AND `till init --json '{...}'` returns the JSON-stub error. No JSON-payload parsing assertions in D3a — those move to D3b." The contract is internally consistent. **Counterexample probe:** does D3a's smoke test actually exercise the registration via `run(...)` end-to-end, or does it call `newInitCommand(...)` directly? PLAN.md:84 says "minimal smoke test that confirms `--json ""` (empty) and bare invocation both route through `RunE`" — "route through `RunE`" is ambiguous. If the builder reads it as "call `cmd.RunE(...)` directly," the registration in `main.go` is NOT exercised, mirroring the W2-FF3 (round-1) shipped-but-not-wired hazard. If the builder reads it as "invoke via `run(ctx, []string{"init"}, ...)`," it IS exercised. **Fix hint:** PLAN.md D3a acceptance line 92 should explicitly mirror W2-FF3's wording: "Each smoke test MUST invoke `run(context.Background(), []string{"init"}, &out, io.Discard)` (NOT call `cmd.RunE` directly) so the route → cobra-command → RunE chain is exercised. Verify wiring via `mage test-func ./cmd/till TestInit_BareInvocation_TUIStubError` (or whatever the chosen test name is)." The contract should be symmetric across D3a smoke + D7.5 ported tests. Severity: low because the round-1 W2-FF3 fix (PLAN.md:213-215 D7.5 contract) shows the orchestrator already understands the route-not-direct-call discipline; D3a just needs the same wording. CONFIRMED ambiguity, not a structural defect. → routes back to orchestrator as a one-line acceptance edit on D3a.
- **W2-FF7 [Family: A1 — file-lock / D3a + D3b sequential semantic] [severity: low — REFUTED as defect]** D3a writes `cmd/till/init_cmd.go`, `cmd/till/main.go`, `cmd/till/help.go`. D3b writes `cmd/till/init_cmd.go`. The dispatcher's lock manager (Drop 4a Wave 2) keys on `paths` strings; D3a + D3b SHARE `cmd/till/init_cmd.go` and serialize via `D3b.blocked_by = ["D3a"]` (verified `_BLOCKERS.toml:32-34`). Could a future scheduler dispatch them in parallel? No — `blocked_by` is the authoritative ordering primitive (CLAUDE.md "Blocker Semantics" lines 178-184); the lock manager INSERTS runtime `blocked_by` on conflict, but a planner-declared `blocked_by` is honored unconditionally. The split into D3a + D3b is a **logical** decomposition (skeleton-vs-body) layered on a **physical** sequential lock — the latter remains binding regardless of the former. The plan's framing is correct: "D3b is `Blocked by: D3a` (same-file lock on `cmd/till/init_cmd.go`)" (PLAN.md:115). REFUTED — no concurrency hazard. NIT consideration: PLAN.md:99 says "D3a is the FIRST `cmd/till/init_cmd.go` write — D3b, D4, D5, D6, D7 all serialize behind it on the same file lock." Accurate. No defect.
- **W2-FF8 [Family: A1 — help.go entry positioning] [severity: low — REFUTED]** PLAN.md:90-92 places D3a's help-entry "to the rich-help table at the analogous position to the existing `"till init-dev-config"` block." `commandHelpSpecs` is a `map[string]commandHelpSpec` keyed by `cmd.CommandPath()` (verified `cmd/till/help.go:16-17`); Go map iteration order is randomized, so source-position has NO behavioral effect — the rich-help table is addressed by key lookup at `applyCommandHelpSpecs` (`cmd/till/help.go:403-416`). Position is purely cosmetic / source-readability. The plan does NOT need to specify alphabetical placement. REFUTED — the round-1 plan's framing was sufficient.

#### Attack the W2-FF2 fix (D7.5 test-name contract)

- **W2-FF9 [Family: A2 — test-name contract drift between D7.5 and D8] [severity: MEDIUM — CONFIRMED]** The pre-existing tests at `cmd/till/main_test.go:2907` and `cmd/till/main_test.go:2956` are named **`TestRunInitDevConfigCreatesDebugConfig`** and **`TestRunInitDevConfigUpdatesExistingConfig`** (verified by Read at lines 2906 and 2955). The round-2 W2-FF2 contract (PLAN.md:208 + 235) pins **`TestRunInstall_CreatesDebugConfig`** and **`TestRunInstall_UpdatesExistingConfig`** as the new D7.5-shipped test names. Note the **subtle name shape difference:** original uses `TestRunInitDevConfigCreatesDebugConfig` (no underscore between "InitDevConfig" and "CreatesDebugConfig"); the new contract uses `TestRunInstall_CreatesDebugConfig` (with underscore). **Counterexample probe:** could a builder reading "Same body, just `[]string{"install"}` instead of `[]string{"init-dev-config"}`" (PLAN.md:208) preserve the original test-name shape and rename to `TestRunInstallCreatesDebugConfig` (no underscore, mirroring `TestRunInitDevConfigCreatesDebugConfig`)? **YES, very likely.** The "verbatim port" framing at PLAN.md:208 ("Same body, just `[]string{"install"}` instead of `[]string{"init-dev-config"}`") implies test-name shape is preserved by the port — but the W2-FF2 contract pin REQUIRES an underscore-shape change. The shape contradiction between "verbatim port" and "renamed-with-underscore-style" is exactly the ambiguity the W2-FF2 fix was supposed to close. **Reproduction:** D7.5 builder reads "verbatim port"; preserves test-name shape; ships `TestRunInstallCreatesDebugConfig` + `TestRunInstallUpdatesExistingConfig` (no underscores). D8 pre-flight check (PLAN.md:235) hard-codes `TestRunInstall_CreatesDebugConfig` (with underscore); pre-flight FAILS; D8 escalates per the contract. The escalation is the right safety valve — but the silently-broken contract is a planner-discipline gap. **Severity:** medium because it WILL trigger an escalation in production (low cost, but unnecessary churn) AND because the underscore-style mismatch is asymmetric vs the original codebase (which uses no-underscore for `TestRunInitDevConfigCreatesDebugConfig`); the new style choice should be deliberate, not accidental. **Fix hint:** add to D7.5 acceptance (PLAN.md:208, after the test-name contract bullet): "**Test-name shape note:** the new test names use UNDERSCORE between `Install` and the rest (`TestRunInstall_CreatesDebugConfig`), NOT camelCase like the originals. This is a deliberate style change for the new install-cmd test suite — the originals used no-underscore camelCase (`TestRunInitDevConfigCreatesDebugConfig`)." OR (cleaner) — accept the no-underscore style: change the W2-FF2 contract pin to `TestRunInstallCreatesDebugConfig` + `TestRunInstallUpdatesExistingConfig` and update D8's pre-flight to match. Either resolution is fine; the choice should be made in the plan, not left for the builder to guess. CONFIRMED — routes back to orchestrator.
- D8's pre-flight bullet (PLAN.md:235) reads as a documentation pointer with an explicit STOP-and-escalate clause — it does gate execution. Verified.

#### Attack the W2-FF3 fix (D7.5 consumer-tie test)

- The pinned invocation `run(context.Background(), []string{"--app", "tillsyn-init", "install"}, &out, io.Discard)` matches the existing test pattern verbatim (`cmd/till/main_test.go:2928, 2988` use `run(context.Background(), []string{"--app", "tillsyn-init", "init-dev-config"}, &out, io.Discard)` — identical shape). The `run` function is at `cmd/till/main.go:104` with signature `func run(ctx context.Context, args []string, stdout, stderr io.Writer) error` — matches the plan's pinned invocation signature exactly. The `--app tillsyn-init` flag is a valid `--app` value (it's just a runtime app-name override that flows into `rootCommandOptions.appName`; existing tests already use it as a sandboxing trick to isolate XDG paths). REFUTED — no signature mismatch.

#### Attack the W2-FF4 fix (D2 REMOVED, D5 hand-rolls)

- **`internal/config/configmerge` does NOT exist in the repo today.** Verified by directory listing of `internal/config/`: only `agents.go`, `agents_test.go`, `config.go`, `config_test.go`, `frontmatter.go`, `frontmatter_test.go` are present. So the round-1 D2 droplet was proposing to **CREATE** a vendored `configmerge` package — both rounds correctly avoided it. PLAN.md:17 framing ("If a future drop needs section-merging TOML behavior, vendor it then") is consistent with this ground truth. NIT: round-1 PLAN_QA_PROOF.md finding 1.5 read as if `configmerge` already existed (line 30: "`configmerge` is ~12kB of dep that vendors entire-file/section TOML merging per SKETCH §9.6 — it does NOT obviously fit a single-line `.gitignore` append"). Wording-only artifact; current PLAN.md is correct. No defect.
- **W2-FF10 [Family: A3 — D5 `bytes.Contains` substring corner-case] [severity: low — REFUTED with one corner-case noted]** PLAN.md:151 specifies: "hand-written `os.ReadFile` + `bytes.Contains([]byte("\nagents.local.toml\n"))` + `os.WriteFile` atomically via `fsatomic`. Handle the trailing-newline corner case (file ends with no `\n` → append `\n` + `agents.local.toml\n`; file ends with `\n` → append `agents.local.toml\n`)." **Counterexample probe:** can `bytes.Contains` match a substring incorrectly? Three corner cases:
  - **(a)** `.gitignore` contains `agents.local.toml.example` (longer line). Does `bytes.Contains([]byte("\nagents.local.toml\n"))` match? NO — the trailing `\n` in the search pattern requires a literal newline immediately after `.toml`; `.toml.example\n` would NOT match `\nagents.local.toml\n` because the byte after `.toml` is `.`, not `\n`. REFUTED.
  - **(b)** `.gitignore` contains `not-agents.local.toml` (longer prefix). Search pattern `\nagents.local.toml\n` requires a leading `\n` immediately before `agents`; `t-agents` would NOT match because the byte before `agents` is `t`, not `\n`. REFUTED.
  - **(c)** `.gitignore` first line is exactly `agents.local.toml\n` (no leading newline because it's the first line). Search pattern `\nagents.local.toml\n` REQUIRES a leading `\n` — so the first-line case would NOT match, and the function would APPEND a duplicate. **CONFIRMED corner case.** Mitigation: prefix the file with a leading `\n` before searching, OR use `strings.Split` + slice-equality OR `bufio.Scanner` line-iteration. The plan's spec is silent on this. Severity: low because (1) `.gitignore` files virtually always have a header comment or other entries before user-added rules, so first-line-only is a degenerate case, AND (2) the duplicate-append is cosmetically ugly but not behaviorally broken (`git` still respects the rule once). But (3) the round-2 plan EXPLICITLY pins `bytes.Contains` as the contract — so a builder following the plan literally WILL ship the bug. **Fix hint:** strengthen D5 acceptance (PLAN.md:151) to say: "Idempotency check: split content on `\n`, trim each line, compare against `agents.local.toml` (exact equality OR `strings.HasPrefix` modulo trailing whitespace). DO NOT use raw `bytes.Contains([]byte("\nagents.local.toml\n"))` — it misses the first-line case. Or: prepend `\n` to the file content in-memory before the substring search, then strip the synthetic prefix when writing back." The line-iteration approach is the cleanest fix. CONFIRMED corner-case; routes back to orchestrator as a one-line D5 acceptance strengthening.
- **D5 atomic-droplet sizing post-D2-removal:** D5's spec at PLAN.md:138-153 covers `copyAgentFiles` + `copyAgentsTOML` + `ensureGitignore` + 4 tests + the embedded-FS access. With D2 removed, D5's `ensureGitignore` is now hand-rolled (small — ~10 LOC) instead of importing `configmerge`. Net LOC delta: D5 GAINED ~10 LOC of hand-rolled idempotency logic. Estimated total for D5: production ~80-100 LOC + tests ~80-100 LOC. Sits within the till-go atomic-droplet bound (1-4 code blocks, 80-120 LOC + tests, ideally one production file — D5 touches one production file `cmd/till/init_cmd.go`). REFUTED — D5 atomicity envelope holds.
- **`_BLOCKERS.toml` D2 commented-out entry handling:** the lock manager (Drop 4a Wave 2) reads valid TOML; commented-out `[[blockers]]` rows (lines 18-21) are NOT TOML-valid blocker entries (commented at the `#` level, not parsed). REFUTED — D2 does not exist virtually.

#### Attack the W2-FF5 fix (Laslig title `"Dev Config"` byte-for-byte)

- The pinned title is from `cmd/till/main.go:2089`. Verified by Read: line 2089 reads `return writeCLIKV(stdout, "Dev Config", [][2]string{`. The plan's W2-FF5 contract (PLAN.md:217 + the test-assertion preservation logic) is byte-aligned with source. REFUTED — no title drift.
- **NIT (raised in round-1, still applicable):** the test names will be `TestRunInstall_*` (per W2-FF2) but assert `"Dev Config"` substring (per W2-FF5). Functional? Yes — the title remains `"Dev Config"`, substring assertions pass. Cosmetic-confusion only: a future reader sees `TestRunInstall_CreatesDebugConfig` and expects `"Install"` substring in the assertion. Note this in the round-1 PLAN_QA_PROOF.md line 38 already; round-2 fix is sound. No new finding.

#### Attack the round-2 patch as a whole

- **`_BLOCKERS.toml` reason-field preservation:** verified. Each round-2 entry includes a substantive `reason =` field; the D2-removed comment block (lines 13-21) preserves audit-trail context. The round-2 sub-planner did NOT silently drop reasons.
- **Cross-droplet stale-prose check:** searched PLAN.md for orphan `D2`, `D3` (without `a`/`b` suffix) references. PLAN.md:230 references `D3` in D7.5's Notes — checked: line 232's "round-2 W2-FF1 update: was `D3` round-1; D3a is the part of the round-1 D3 split..." is a deliberate audit-trail comment, not a stale ref. D8 acceptance (PLAN.md:251 — "blocked_by D3a") is current. PLAN.md:267-269 contains `cmd/till` serialization narrative referencing `D3a, D3b, D4, D5, D6, D7, D7.5, D8` — all current. REFUTED — no stale prose.
- **YAGNI on TEST-NAME CONTRACT formalization:** the round-2 W2-FF2 fix introduces a `**TEST-NAME CONTRACT (W2-FF2 ROUND-2):**` block (PLAN.md:208) and a corresponding D8 pre-flight bullet (PLAN.md:235). Is this over-formalization? Counter-attack: without the contract, D7.5 ships `TestRunInstallEnsuringFreshDebugConfig` (or any other readable name) and D8's pre-flight check fails. The escalation is the right safety valve, but it forces re-planning under failure pressure — exactly what the planner-agent atomic-decomposition rule says to avoid (`go-planning-agent.md:151` — "atomic-droplet-borderline" flag). The TEST-NAME CONTRACT is load-bearing — NOT YAGNI. REFUTED.
- **D7.5 atomicity post-W2-FF2/3/5 pins:** D7.5 acceptance now contains FOUR pinned contracts (test names W2-FF2, consumer-tie W2-FF3, Laslig title W2-FF5, plus the verbatim port itself) plus 5 acceptance bullets plus 2 ported tests plus a Notes-for-builder block. **LOC re-estimate:** verbatim port ~55 LOC + 2 ported tests ~60 LOC each = ~120 LOC + main.go register edit (~3 LOC) + help.go entry (~14 LOC) = production ~75 LOC + tests ~120 LOC. Sits within till-go atom (80-120 LOC + tests, one production file — D7.5 touches `install_cmd.go` PLUS `main.go` PLUS `help.go`). The "ideally one production file" smell at 3 production files is the same as round-1's D3 — but D7.5's mechanical-port nature mitigates per round-1 PLAN_QA_PROOF.md finding 1.2. REFUTED — within budget.
- **Re-graph chain integrity:** walked round-2 _BLOCKERS.toml: D1→[], D3a→[], D3b→[D3a], D4→[D3b], D5→[D1, D4], D6→[D5], D7→[D6], D7.5→[D3a], D8→[D3a, D7.5]. Acyclic; reachability from D8: {D3a, D7.5, D3a (transitive via D7.5)}. Reachability from D5 (cross-package + same-file): {D1, D4, D3b, D3a}. No missing transitivity. REFUTED — graph is well-formed.

---

### 2. Counterexamples Summary

| ID | Severity | Family | Status |
| --- | --- | --- | --- |
| W2-FF6 | low | A2 / consumer-tie symmetry | CONFIRMED — D3a smoke test should explicit-pin `run(...)` invocation |
| W2-FF7 | low | A1 / file-lock semantic | REFUTED — `blocked_by` is the authoritative ordering primitive |
| W2-FF8 | low | A2 / help.go positioning | REFUTED — map keyed by `CommandPath()`, position cosmetic |
| W2-FF9 | **MEDIUM** | A2 / test-name shape contradiction | **CONFIRMED — verbatim-port vs underscore-style mismatch needs orchestrator decision** |
| W2-FF10 | low | A3 / D5 substring first-line corner | CONFIRMED — `bytes.Contains` misses first-line-only `.gitignore` |

**Two CONFIRMED findings need plan amendments before W2 builders fire:**

- **W2-FF6** — D3a smoke test acceptance (PLAN.md:92) should explicitly pin `run(ctx, []string{"init"}, &out, io.Discard)` invocation, mirroring W2-FF3's discipline.
- **W2-FF9** — D7.5 acceptance (PLAN.md:208) needs to clarify whether the new test-name shape uses an underscore (`TestRunInstall_CreatesDebugConfig`) or matches the existing no-underscore camelCase convention (`TestRunInstallCreatesDebugConfig`). Currently the W2-FF2 contract pin uses the underscore form while the "Same body, just `[]string{"install"}` instead of `[]string{"init-dev-config"}`" framing implies verbatim-port shape preservation.
- **W2-FF10** — D5 acceptance (PLAN.md:151) should replace the raw `bytes.Contains` substring check with line-iteration (`strings.Split` + per-line equality) OR pre-pend a synthetic `\n` to handle the first-line-only case.

---

### 3. Summary

**Verdict: PASS-with-three-CONFIRMED-low-severity-findings + one CONFIRMED-MEDIUM finding (W2-FF9).**

| Family | Status | Findings |
| --- | --- | --- |
| A1 — Concurrency / blocked_by | EXHAUSTED | W2-FF7 (REFUTED) |
| A2 — Contract mismatch | EXHAUSTED | W2-FF6 (low CONFIRMED), W2-FF8 (REFUTED), W2-FF9 (medium CONFIRMED) |
| A3 — Hidden coupling / corner cases | EXHAUSTED | W2-FF10 (low CONFIRMED), Laslig-title NIT (REFUTED — round-1 fix sound) |
| A4 — YAGNI / scope-creep | EXHAUSTED | TEST-NAME CONTRACT formalization is NOT YAGNI (load-bearing safety valve) |
| A5 — Shipped-but-not-wired | EXHAUSTED | D2-removed verified configmerge does NOT exist in repo; both rounds correct to skip |
| A6 — Atomicity | EXHAUSTED | D5 post-D2-removal within budget; D7.5 multi-pin acceptance within budget |
| Stale-prose / re-graph | EXHAUSTED | _BLOCKERS.toml chain acyclic; cross-droplet refs current |

**Recommended action before W2 builders fire:** orchestrator addresses W2-FF6, W2-FF9, W2-FF10 inline (three single-bullet edits). All other findings are advisory.

**Round-2 close:** PASS — round-2 reshape is sound. Three small acceptance-bullet sharpenings (one MEDIUM, two low) close the residual gaps without re-graphing.

---

### 4. Hylla Feedback

`None — Hylla answered everything needed.` Hylla queries verified `func run` did not return useful results from keyword search (the symbol is too generic — Hylla returned domain-package functions instead of `cmd/till/main.go:run`); fell back to direct `Read` on `cmd/till/main.go:1-120` to confirm signature. This is a search-relevance ranking issue, not a missing-coverage issue: the symbol IS indexed (the codebase has thousands of `run`-prefixed symbols), but the keyword-rank surfaces unrelated higher-scoring matches. Suggestion: add an `id_search_mode: "exact_full_id"` example to the tool docstring for cases where the user knows the fully-qualified symbol path (`github.com/evanmschultz/tillsyn/cmd/till/run`). Not load-bearing for this review.

The `internal/config/configmerge` non-existence verification was done via `Bash ls` on the directory rather than Hylla, because Hylla wouldn't index a non-existent package; this is the correct tool choice (you cannot prove non-existence via a positive index).

No blocking Hylla miss to report.
