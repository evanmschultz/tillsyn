# _BLOCKERS.toml — workflow/drop_4c_6/DROP_4c.6.W2_TILL_INIT/
# Immediate-children sibling blocker ledger.
# Mirrors inline `Blocked by:` bullets in PLAN.md; PLAN.md is truth.
# Created 2026-05-09 by L2 W2 sub-planner (round 1).
# Updated 2026-05-09 by L2 W2 sub-planner (round 2 — applied W2-FF1 D3 split + W2-FF4 D2 removal).

# D1 — vendor fsatomic. NEW package; no blockers.
[[blockers]]
node = "4c.6.W2.D1"
blocked_by = []
reason = "first vendor droplet; creates internal/vendor/ + VENDOR_SOURCE.md"

# D2 — REMOVED in round 2 per W2-FF4. configmerge was vestigial:
#       - copyAgentsTOML is a copy, not a merge
#       - ensureGitignore is a single-line idempotent append (hand-written sufficient)
#       Future drop can vendor configmerge if section-merging TOML behavior is needed.
#       (Entry kept commented to preserve audit trail.)
# [[blockers]]
# node = "4c.6.W2.D2"
# blocked_by = ["4c.6.W2.D1"]
# reason = "shared file internal/vendor/VENDOR_SOURCE.md (D1 creates, D2 appends)"

# D3a — init_cmd.go skeleton + register in main.go + help.go entry. NEW file in cmd/till; no blockers.
# Round-2 W2-FF1 split: round-1 D3 split into D3a (skeleton + register + help) and D3b (JSON parser).
[[blockers]]
node = "4c.6.W2.D3a"
blocked_by = []
reason = "skeleton droplet; first edit to cmd/till/init_cmd.go (NEW file); first add to cmd/till/main.go rootCmd.AddCommand line; first add to cmd/till/help.go commandHelpSpecs"

# D3b — JSON-payload parser + group-validation + table-test. cmd/till/init_cmd.go-only.
# Round-2 W2-FF1 NEW: split out from round-1 D3 to keep D3a's surface to skeleton + register.
[[blockers]]
node = "4c.6.W2.D3b"
blocked_by = ["4c.6.W2.D3a"]
reason = "same-file lock cmd/till/init_cmd.go (D3a created it; D3b modifies it to add JSON parser body)"

# D4 — runInitTUI bubbletea walk. Same-file lock on cmd/till/init_cmd.go.
# Round-2 W2-FF1 update: blocked_by changed from D3 → D3b (D3b is the most recent init_cmd.go writer).
[[blockers]]
node = "4c.6.W2.D4"
blocked_by = ["4c.6.W2.D3b"]
reason = "same-file lock cmd/till/init_cmd.go (D3b last edited; transitively waits for D3a too)"

# D5 — file-copy pipeline + .gitignore. Imports fsatomic; same-file lock on init_cmd.go.
# Round-2 W2-FF4 update: D2 dropped from blocker list; configmerge no longer vendored.
[[blockers]]
node = "4c.6.W2.D5"
blocked_by = ["4c.6.W2.D1", "4c.6.W2.D4"]
reason = "imports internal/vendor/fsatomic (D1); same-file lock cmd/till/init_cmd.go (D4 last edited); W2-FF4 round-2 removed configmerge dep so D2 dropped"

# D6 — .mcp.json optional registration. Same-file lock on init_cmd.go.
[[blockers]]
node = "4c.6.W2.D6"
blocked_by = ["4c.6.W2.D5"]
reason = "same-file lock cmd/till/init_cmd.go (D5 last edited)"

# D7 — project-DB record + Laslig success. Same-file lock on init_cmd.go.
[[blockers]]
node = "4c.6.W2.D7"
blocked_by = ["4c.6.W2.D6"]
reason = "same-file lock cmd/till/init_cmd.go (D6 last edited)"

# D7.5 — till install CLI command (NEW per OQ#3 verification). NEW file install_cmd.go; shares cmd/till/main.go + cmd/till/help.go with D3a.
# Round-2 W2-FF1 update: blocked_by changed from D3 → D3a (D3a owns the main.go + help.go edits; D3b is init_cmd.go-only).
[[blockers]]
node = "4c.6.W2.D7.5"
blocked_by = ["4c.6.W2.D3a"]
reason = "shares cmd/till/main.go AND cmd/till/help.go file-locks with D3a (D3a added initCmd to rootCmd.AddCommand + help table; D7.5 amends both to add installCmd); D3a lands first per L1 directive"

# D8 — remove init-dev-config from main.go + help.go + main_test.go. Shares cmd/till/main.go + help.go with D3a; behavior-removal premise depends on D7.5.
# Round-2 W2-FF1 update: blocked_by changed from D3 → D3a (D3a is the round-1 D3 main.go-touching half).
# Round-2 W2-FF2 cross-reference: D8 pre-flight bullet hard-codes test names TestRunInstall_CreatesDebugConfig + TestRunInstall_UpdatesExistingConfig; D7.5 acceptance pins the same names.
[[blockers]]
node = "4c.6.W2.D8"
blocked_by = ["4c.6.W2.D3a", "4c.6.W2.D7.5"]
reason = "shares cmd/till/main.go + cmd/till/help.go file-locks with D3a (L1 directive: D3a first since D8 only removes); OQ#3 verification finding: till install does NOT exist today, so D7.5 must add it before D8 removes init-dev-config (otherwise behavior regression on dev-config-creation); D8 pre-flight hard-codes TestRunInstall_* test names per W2-FF2 round-2"
