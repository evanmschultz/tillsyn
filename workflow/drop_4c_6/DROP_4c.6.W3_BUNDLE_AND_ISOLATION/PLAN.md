# DROP_4c.6.W3 — BUNDLE_AND_ISOLATION

**State:** planning
**Blocked by:** 4c.6.W1.D1 (W3's resolver consumes the embedded `till-<group>/<name>.md` files placeholder-shipped by W1.D1); 4c.6.W0 (W3's frontmatter strip wiring calls `config.StripFrontmatterKeys`, exposed by W0.D4).
**Paths (expected):** `internal/app/dispatcher/cli_adapter.go` (MODIFY — `BindingResolved` struct adds `SystemPromptTemplatePath` field at the existing struct definition site, line 102 per ROUND-2 HF4), `internal/app/dispatcher/cli_adapter_test.go` (MODIFY — `BindingResolved` zero-value + populated-value tests covering the new field), `internal/app/dispatcher/binding_resolved.go` (MODIFY — `ResolveBinding` populates `SystemPromptTemplatePath` from `templates.AgentBinding.SystemPromptTemplatePath`), `internal/app/dispatcher/binding_resolved_test.go` (MODIFY — `ResolveBinding` table tests append new field assertion), `internal/app/dispatcher/cli_claude/render/render.go` (MODIFY — `assembleAgentFileBody` swap from F.7.3b stub to 3-tier resolver + frontmatter strip wiring + post-render validator wiring at `Render`'s exit; doc-comment correction at lines 307-319), `internal/app/dispatcher/cli_claude/render/render_test.go` (MODIFY — append D2 resolver tests, D3 frontmatter-strip tests, D5 validator failure-path tests + minimal sentinel-style "bundle body length > stub-threshold" assertion), `internal/app/dispatcher/cli_claude/env.go` (MODIFY — extend closed POSIX baseline with defense-in-depth env-var literal pairs `CLAUDE_CODE_DISABLE_BACKGROUND_TASKS=1`, `CLAUDE_CODE_FORK_SUBAGENT=0`, `DISABLE_AUTOUPDATER=1`, `DISABLE_TELEMETRY=1`), `internal/app/dispatcher/cli_claude/adapter_test.go` (MODIFY — `TestEnvCarriesIsolationEnvVars` asserts `cmd.Env` carries the four defense-in-depth vars).
**Packages (expected):** `internal/app/dispatcher` (D1 — `BindingResolved` struct + `ResolveBinding` populator), `internal/app/dispatcher/cli_claude/render` (D2 + D3 + D5 + D6 — `assembleAgentFileBody` rewrite, frontmatter-strip wiring, post-render validator + sentinel test, doc-comment correction), `internal/app/dispatcher/cli_claude` (D4 — closed-baseline env extension).
**PLAN.md ref:** `workflow/drop_4c_6/PLAN.md` → 4c.6.W3 sub-plan container row (lines 134-153)
**Workflow:** `workflow/example/drops/WORKFLOW.md`
**Cascade concept:** `AGENT_CASCADE_DESIGN.md`
**Started:** 2026-05-09
**Closed:** —

## Scope

Land the five `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` changes D.1-D.5 plus the cross-cutting `SystemPromptTemplatePath` end-to-end plumbing and the doc-comment correction at `render.go:307-319` per `SKETCH.md` § 18 + § 26.W3.

**The five isolation-fix changes**:

- **(D.1)** Ship the FULL agent body via `//go:embed` plus 3-tier resolution priority — `<project>/.tillsyn/agents/<name>.md` → `~/.tillsyn/agents/<group>/<name>.md` → embedded `till-<group>/<name>.md` — replacing today's F.7.3b stub at `render.go:assembleAgentFileBody` (lines 340-364, body sentence at line 360 reads "Behavior loaded from the canonical ... template at the system-installed plugin path" — this is the FACTUALLY-WRONG-UNDER-`--bare` claim called out in `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § A.9 + § C.5).
- **(D.2)** Render-time frontmatter `model:` + `tools:` strip when `agents.toml` has the corresponding key set, per `SKETCH.md` § 4.4. The strip helper is exposed by W0.D4 (`config.StripFrontmatterKeys(frontmatter, stripModel, stripTools)` at `internal/config/frontmatter.go:89` — verified via Read).
- **(D.3)** Defense-in-depth env vars `CLAUDE_CODE_DISABLE_BACKGROUND_TASKS=1`, `CLAUDE_CODE_FORK_SUBAGENT=0`, `DISABLE_AUTOUPDATER=1`, `DISABLE_TELEMETRY=1` injected unconditionally at the closed POSIX baseline in `internal/app/dispatcher/cli_claude/env.go` (today's baseline at lines 37-58 carries name-only entries that resolve via `os.LookupEnv`; the new defense-in-depth set carries literal `name=value` pairs so they're injected even when the orchestrator process has them unset).
- **(D.4)** Post-render validator wired into the render path at `render.Render`'s exit (NOT shipped as a dangling exported helper — see Acceptance contract on D5; ROUND-2 HF8 wiring contract per `feedback_tillsyn_enforces_templates.md`). Errors loud on empty body, F.7.3b stub-shape body, or missing required `name` / `description` frontmatter.
- **(D.5)** Doc-comment correction at `render.go:307-319` ONLY (the `renderAgentFile` block currently describing the F.7.3b stub design — lines 311-313 read "The full canonical templates at ~/.claude/agents/<name>.md remain the source of truth for behavior — they are loaded by claude from the system-installed plugin path (Path B per memory §1)" — `--bare` collapses Path B per `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § A.9). **`SPAWN_PIPELINE.md:24-31` rewrite is OUT OF SCOPE for W3** — sole owner is W6.D4 per ROUND-2 HF3.

**The plumbing**: propagate `SystemPromptTemplatePath` through `dispatcher.BindingResolved` (struct definition site `internal/app/dispatcher/cli_adapter.go:102` per ROUND-2 HF4 — verified via Read; the existing struct body is at lines 102-178 with field declarations from line 106 onward) so that D2's resolver in `render.go:assembleAgentFileBody` has the field to read.

**Sentinel-style integration test scaffolding** lives here too (the FULL `TestSpawnDoesNotInheritUserAgentDefinition` sentinel-injection-into-Path-B suite is Drop 4c.8 W4-D's job per the L1 plan); D5 ships a minimal "bundle body length > stub-threshold AND F.7.3b stub-key-phrase absent AND frontmatter delimiters present" assertion in `render_test.go`.

**Sketch QA verdict locked**: Plan-QA on the L1 W3 sub-plan container reached PASS at round 2 with HF3/HF4/HF8 fixes applied to the L1 PLAN.md. This L2 PLAN.md translates the L1 wave into atomic droplets — NOT a re-validation of the wave-level Specify. Per `SKETCH.md` § 26.W3 inheritance, every droplet below carries a Specify block scoped to the droplet's delta.

## Per-Wave Source-of-Truth

This sub-plan inherits from `SKETCH.md` § 26.W3 + `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` D.1-D.5. Droplet-level Specify blocks INHERIT from those wave-level blocks, scope-narrow to the droplet, and inline only the droplet-specific delta.

## Planner

### Decomposition shape — six atomic droplets across three packages, mostly serialized on `internal/app/dispatcher/cli_claude/render` package compile

The six droplets fall into three package-disjoint groups:

1. **`internal/app/dispatcher` package** — D1 (`SystemPromptTemplatePath` plumbing through `BindingResolved` + `ResolveBinding`). Edits `cli_adapter.go` + `binding_resolved.go` + their `_test.go` siblings.
2. **`internal/app/dispatcher/cli_claude/render` package** — D2 (3-tier resolver), D3 (frontmatter strip wiring), D5 (post-render validator + sentinel test), D6 (doc-comment correction). All four edit `render.go` and `render_test.go` — same compile unit, MUST serialize per `~/.claude/agents/go-planning-agent.md` § "Cascade Design — Atomic Droplets + Parallelization (HARD RULES)": "Siblings sharing a `packages` entry (even with disjoint paths within that package) → `blocked_by`."
3. **`internal/app/dispatcher/cli_claude` package** — D4 (defense-in-depth env vars in `env.go`). Edits `env.go` + `adapter_test.go`. File-disjoint from D2/D3/D5/D6 (parent package, not child); package-disjoint too (`cli_claude` vs `cli_claude/render`). Parallels D1.

Cross-group dependencies:

- **D2 reads `binding.SystemPromptTemplatePath`** — D2 `blocked_by` D1.
- **D3 mutates the body D2 produces** — D3 `blocked_by` D2.
- **D5 validates the body D2 + D3 produce** — D5 `blocked_by` D2, D3.
- **D6 edits the doc-comment block adjacent to (and inside the function-doc-comment range of) the `assembleAgentFileBody` function D2 rewrites** — D6 `blocked_by` D2, D3, D5. Even though D6 is doc-only, it edits the same `render.go` file (same package compile lock); serializing D6 last avoids any chance of D2/D3/D5's function-body edits clobbering D6's adjacent doc-comment edit.
- **D4 is fully parallel** — different package (`cli_claude` vs `cli_claude/render` vs `dispatcher`), different file (`env.go` vs `cli_adapter.go` / `render.go`).

The render-package serial chain therefore reads:

```
W3.D1 ──▶ W3.D2 ──▶ W3.D3 ──▶ W3.D5 ──▶ W3.D6
              (D4 parallel — own package, own file)
```

Tests are co-located with each droplet's production file. D1 appends to `cli_adapter_test.go` + `binding_resolved_test.go`; D2/D3/D5 append to `render_test.go`; D6 is doc-only (no test changes); D4 appends to `adapter_test.go`.

### Droplet 4c.6.W3.D1 — Plumb `SystemPromptTemplatePath` through `BindingResolved` + `ResolveBinding`

- **State:** done
- **Kind:** `build` (atomic droplet; `Irreducible: true`)
- **Paths:** `internal/app/dispatcher/cli_adapter.go` (MODIFY — append `SystemPromptTemplatePath string` field to the `BindingResolved` struct that begins at line 102 per ROUND-2 HF4 verified Read; field placement at the end of the struct, adjacent to the existing string-typed `AgentName` field for shape symmetry — both are non-pointer strings since `""` is the meaningful "absent / use embedded default" sentinel and there's no `explicit-empty-vs-absent` discrimination needed for a relative file path), `internal/app/dispatcher/cli_adapter_test.go` (MODIFY — extend any existing `BindingResolved` zero-value + populated-value table tests to cover the new field; if no such test exists, append `TestBindingResolvedSystemPromptTemplatePath` covering both states), `internal/app/dispatcher/binding_resolved.go` (MODIFY — `ResolveBinding` reads `templates.AgentBinding.SystemPromptTemplatePath` per `internal/templates/schema.go:556` per `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § D.1.b cite — sub-planner verifies via LSP `gopls workspace_symbol SystemPromptTemplatePath` before authoring; AGENT_ARCHITECTURE_TRUTH.md §2.3 confirms this field exists with validator at `load.go:1031-1055`), `internal/app/dispatcher/binding_resolved_test.go` (MODIFY — append a test case to whatever `ResolveBinding` table-test exists asserting the resolved field equals the `templates.AgentBinding.SystemPromptTemplatePath` source value).
- **Packages:** `internal/app/dispatcher`.
- **Acceptance:**
  - `dispatcher.BindingResolved` (struct definition at `internal/app/dispatcher/cli_adapter.go:102`, declaration body lines 102-178 per ROUND-2 HF4 verified Read) gains a non-pointer `SystemPromptTemplatePath string` field with a Go doc-comment explaining: (a) the field carries the per-binding relative path expressing the canonical agent body source, (b) empty string is the "use embedded default" sentinel (NOT a special "explicit empty" value), (c) the resolver in `render.assembleAgentFileBody` consults this field at the second tier of its 3-tier resolution (project `.tillsyn/agents/<basename>` → user `~/.tillsyn/agents/<group>/<basename>` → embedded `till-<group>/<name>.md`).
  - `ResolveBinding` (`binding_resolved.go`) populates the new field from the source `templates.AgentBinding.SystemPromptTemplatePath`. Whatever the source value is — empty string or non-empty path — passes through verbatim; resolver does NOT validate path existence here (validation is W0.5's job at template-load time per the L1 plan; runtime existence check is D2's resolver-tier-walk job).
  - `BindingResolved` zero-value (in the `cli_adapter_test.go` shape-test) carries `SystemPromptTemplatePath: ""`. Populated-value test asserts the field round-trips through struct construction and field-access cleanly.
  - `ResolveBinding` table test (`binding_resolved_test.go`) covers two new cases: (a) source `templates.AgentBinding.SystemPromptTemplatePath = ""` → resolved `SystemPromptTemplatePath = ""`; (b) source non-empty (e.g. `"till-go/go-builder-agent.md"`) → resolved equal to source.
  - No existing test in `cli_adapter_test.go` or `binding_resolved_test.go` regresses — the new field is additive.
  - `mage test-pkg ./internal/app/dispatcher` passes; `mage test-func ./internal/app/dispatcher TestResolveBinding` (or whatever the existing test name is — sub-planner verifies via Read before authoring) passes including the new cases.
- **Blocked by:** —
- **Specify (droplet-scope; inherits `SKETCH.md` § 26.W3 + `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § D.1):**
  - **Objective:** Plumb the `SystemPromptTemplatePath` field end-to-end through the dispatcher binding layer so D2's render-time resolver has a concrete field to read. Single struct field + single resolver assignment + tests; no behavior change at the spawn layer (D2 is the consumer that flips the F.7.3b stub).
  - **AcceptanceCriteria:** see Acceptance bullets above.
  - **ValidationPlan:** `mage test-pkg ./internal/app/dispatcher`; `mage test-func ./internal/app/dispatcher TestBindingResolved...`; `mage test-func ./internal/app/dispatcher TestResolveBinding...`; `mage ci` green.
  - **RiskNotes:**
    - HF4-verified location: `BindingResolved` lives at `internal/app/dispatcher/cli_adapter.go:102` (NOT `binding_resolved.go` — the historical filename mismatch is the trap ROUND-2 HF4 caught). Sub-planner Reads the file before authoring to confirm line stays correct on rebase.
    - Source field on the templates side: `templates.AgentBinding.SystemPromptTemplatePath` per `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § D.1.b cite of `schema.go:556` and `load.go:1031-1055`. Sub-planner verifies via LSP `workspace_symbol SystemPromptTemplatePath` before authoring; Hylla missed on this symbol per `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § Hylla Feedback bullet 1 — fall back to LSP / Read directly.
    - Pointer vs non-pointer field type: chose non-pointer `string` (matches `AgentName string` at line 106 — the existing precedent for "no absent vs explicit-empty distinction needed"). Pointer-typed fields in `BindingResolved` (`Model *string`, `Effort *string`, `MaxTries *int`, etc. — see lines 121-178) are the "explicit zero distinct from absent" cases per the F.7.17 L9 lock at lines 96-101; `SystemPromptTemplatePath` does NOT need that discrimination — empty path means "use embedded default."
    - The resolver does not pre-validate path existence: that's D2's tier-walk concern. Validating here would couple D1 to filesystem state and would make D1's tests need a fixture, which is over-engineering for a plumbing-only droplet.
  - **ContextBlocks:**
    - `reference` (high): `BindingResolved` struct begins at `internal/app/dispatcher/cli_adapter.go:102` per ROUND-2 HF4 verified Read; sub-planner reads the surrounding doc-comment lines 76-101 + struct body lines 102-178 to place the new field consistent with existing field placement order (string-typed fields first, then pointer-typed for absent-vs-explicit-zero discrimination).
    - `reference` (high): `templates.AgentBinding.SystemPromptTemplatePath` source field per `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § D.1.b — `schema.go:556` + `load.go:1031-1055` per AGENT_ARCHITECTURE_TRUTH.md § 2.3.
    - `decision` (normal): non-pointer `string` field type; empty string = "use embedded default" sentinel.
    - `constraint` (high): D1 adds NO behavior change at the spawn layer — D2 is the consumer that flips the F.7.3b stub. D1's only observable side-effect is the new field on `BindingResolved`.
    - `warning` (normal): if Hylla returns zero hits on `SystemPromptTemplatePath` (per the `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § Hylla Feedback miss), fall back to LSP `workspace_symbol` immediately rather than re-trying multiple Hylla queries — this is a known Hylla blind spot for field-declaration tokens.
  - **KindPayload:** `{"changes":[{"file":"internal/app/dispatcher/cli_adapter.go","symbol":"BindingResolved","action":"modify","shape_hint":"append SystemPromptTemplatePath string field at end of struct (line 178+); doc-comment explains 3-tier resolver consumer + empty-string sentinel"},{"file":"internal/app/dispatcher/cli_adapter_test.go","symbol":"TestBindingResolvedSystemPromptTemplatePath","action":"add_or_extend","shape_hint":"zero-value + populated-value coverage"},{"file":"internal/app/dispatcher/binding_resolved.go","symbol":"ResolveBinding","action":"modify","shape_hint":"populate SystemPromptTemplatePath from templates.AgentBinding.SystemPromptTemplatePath verbatim — no validation"},{"file":"internal/app/dispatcher/binding_resolved_test.go","symbol":"TestResolveBinding","action":"extend","shape_hint":"two new table cases: empty source, non-empty source"}]}`

### Droplet 4c.6.W3.D2 — 3-tier agent-body resolver in `render.assembleAgentFileBody`

- **State:** done
- **Kind:** `build` (atomic droplet; `Irreducible: true`)
- **Paths:** `internal/app/dispatcher/cli_claude/render/render.go` (MODIFY — replace the F.7.3b stub at `assembleAgentFileBody` lines 340-364 with a 3-tier resolver: project `<project.RepoPrimaryWorktree>/.tillsyn/agents/<basename>` → user `~/.tillsyn/agents/<group>/<basename>` → embedded `builtin/agents/till-<group>/<name>.md` consumed via the EXISTING `templates.DefaultTemplateFS` at `internal/templates/embed.go:104` — ROUND-2 W3-FF1 fix: render.go MUST NOT declare its own `//go:embed builtin/agents` directive because Go forbids parent-traversal in embed patterns and `internal/templates/builtin/agents/` is not a child of the render package directory; render.go imports `github.com/evanmschultz/tillsyn/internal/templates` and reads via `fs.ReadFile(templates.DefaultTemplateFS, "builtin/agents/<group>/<basename>")`; pass `project domain.Project` through `renderAgentFile` so the resolver can read the project worktree path), `internal/app/dispatcher/cli_claude/render/render_test.go` (MODIFY — append `TestAssembleAgentFileBody_EmbeddedDefault`, `TestAssembleAgentFileBody_UserOverride`, `TestAssembleAgentFileBody_ProjectOverride` exercising the 3-tier priority order via tempdir fixtures), and ship a tiny extension to the existing render call wiring at `render.go:160-163` (`renderAgentFile(bundle, binding)` call site) to thread `project` through — a 1-line signature change at the function-call layer.
- **Packages:** `internal/app/dispatcher/cli_claude/render`.
- **Acceptance:**
  - `assembleAgentFileBody` accepts `project domain.Project` (in addition to `binding dispatcher.BindingResolved`) and returns `(body string, err error)` — error-returning because the 3-tier resolver may fail (e.g., embedded FS is missing the requested file when the agent name doesn't match any shipped default). Callers handle the error via the existing rollback path in `Render` (line 144 `rollback := newRenderRollback(...)`).
  - 3-tier resolver order, top-priority wins: (1) **project tier** — read `<project.RepoPrimaryWorktree>/.tillsyn/agents/<basename>` where `<basename>` is the file-portion of `binding.SystemPromptTemplatePath` if non-empty, else `<binding.AgentName>.md`. If the file exists and is readable, use its contents. (2) **user tier** — read `<user-home>/.tillsyn/agents/<group>/<basename>` where `<group>` is derived from `binding.SystemPromptTemplatePath` per the LOCKED rule below. (3) **embedded tier with cross-group fallback (LOCKED — ROUND-3 W3-FF7)** — read from `templates.DefaultTemplateFS` (declared at `internal/templates/embed.go:104`) via a 2-step lookup ladder: first attempt `fs.ReadFile(templates.DefaultTemplateFS, "builtin/agents/<group>/<basename>")` where `<group>` is the resolved group from the LOCKED derivation rule; if that returns `fs.ErrNotExist`, fall back to `fs.ReadFile(templates.DefaultTemplateFS, "builtin/agents/till-gen/<basename>")` (cross-group fallback to till-gen — the canonical "shared agents" group). If the till-gen fallback also misses, return a wrapped `ErrAgentBodyNotFound` sentinel. **Rationale (W3-FF7)**: `till-go.toml`'s coordination-kind bindings (`[agent_bindings.{closeout,refinement,discussion,human-verify}]` at lines 591-659) ALL set `agent_name = "orchestrator-managed"` with empty `system_prompt_template_path`. Under the empty-path-defaults-to-till-go rule (W3-FF5 LOCKED), the embedded-tier lookup resolves to `builtin/agents/till-go/orchestrator-managed.md` — a path that DOES NOT EXIST (verified via Read of agent dir contents: `till-gen/orchestrator-managed.md` at 940 bytes is the SOLE copy; `till-go/` contains `builder-agent.md`, `closeout-agent.md`, `commit-message-agent.md`, `go-builder-agent.md`, `go-planning-agent.md`, `go-qa-falsification-agent.md`, `go-qa-proof-agent.md`, `go-research-agent.md`, `planning-agent.md`, `qa-falsification-agent.md`, `qa-proof-agent.md`, `research-agent.md` — NO `orchestrator-managed.md`; same for `till-gdd/`). Without the cross-group fallback, render aborts on every coordination-kind spawn (closeout / refinement / discussion / human-verify). The cross-group ladder lets shared agents live in `till-gen/` and be reachable from any group binding — matching the dogfood reality that `orchestrator-managed.md` is canonically per-tillsyn (not per-language). **Defensive scope (W3-FF7 falsification mitigation)**: cross-group fallback ONLY descends into `till-gen` (the canonical shared group), NOT into `till-go` or `till-gdd` from `till-gen` (no symmetric fallback). Bare-filename match — no path traversal in `<basename>`. Cross-group fallback firing emits a debug-level log `"render: agent body not found in <group>/, falling back to till-gen/"` so legitimate config typos are diagnosable without aborting the spawn. Sub-planner verifies the package's existing logging idiom before authoring (charmbracelet/log per the project tech stack); if no logger threaded into the resolver, log via the package's existing log seam or omit log per existing-idiom-fit.
  - **`<group>` derivation rule (LOCKED — ROUND-2 W3-FF5)**: `<group>` MUST be derived as `<group> = path.Dir(binding.SystemPromptTemplatePath)` when `binding.SystemPromptTemplatePath` is non-empty (use `path.Dir`, NOT `filepath.Dir`, because embed.FS paths are always slash-separated regardless of host OS), and `<group> = "till-go"` (dogfood default) when `binding.SystemPromptTemplatePath` is empty. The resolved `binding.SystemPromptTemplatePath` form is `till-<group>/<name>.md` (e.g. `"till-go/builder-agent.md"` → `<group> = "till-go"`, `<basename> = "builder-agent.md"`). This eliminates the divergent-sub-planner-interpretation risk flagged by ROUND-1 plan-QA-falsification Finding 1.5. Adopters wanting `till-gen` / `till-gdd` MUST set `binding.SystemPromptTemplatePath` explicitly in their template; the empty-path fallback to `till-go` is the documented dogfood-only behavior, not a multi-language default. If `path.Dir(binding.SystemPromptTemplatePath)` returns `"."` (i.e. the path has no slash separator), the resolver treats it as malformed and falls back to `till-go` with a logged warning — sub-planner verifies the package's existing logging idiom before authoring.
  - The returned `body` string is the FULL agent file body (frontmatter + body text) — D3 layers frontmatter strip ON TOP of D2's output; D2 emits the resolved content verbatim.
  - The doc-comment on `assembleAgentFileBody` documents the 3-tier resolver order, the empty-string sentinel for `binding.SystemPromptTemplatePath`, and the embedded-FS fallback (now sourced from `templates.DefaultTemplateFS`, NOT a render-package-local embed.FS). Doc-comment correction at the function's existing comment block lines 335-339 ("assembleAgentFileBody builds the agent stub body") rewords away from "stub" — D2 ships substantive content, not a stub.
  - **NO `//go:embed` directive in `render.go`** (ROUND-2 W3-FF1). The render package consumes `templates.DefaultTemplateFS` via the import path `github.com/evanmschultz/tillsyn/internal/templates`. Verify no import cycle: `internal/templates/embed.go` imports only `embed` + `errors` + `fmt` + `slices` + `strings` (stdlib leafs), so importing `templates` from `render` is acyclic. Sub-planner re-confirms via Read of `internal/templates/embed.go` import block before authoring.
  - Test `TestAssembleAgentFileBody_EmbeddedDefault` constructs a `BindingResolved` with `SystemPromptTemplatePath = ""`; calls `assembleAgentFileBody(project, binding)` with a `project` whose `RepoPrimaryWorktree` is a tempdir with NO `.tillsyn/agents/` content; asserts the returned body matches the embedded placeholder for the agent name.
  - Test `TestAssembleAgentFileBody_UserOverride` writes `<tempdir-home>/.tillsyn/agents/till-go/go-builder-agent.md` with sentinel content `SENTINEL_USER_TIER`, sets `os.UserHomeDir` to point to `<tempdir-home>` (via `t.Setenv("HOME", tempdir)`), constructs a project tempdir with NO `.tillsyn/agents/`, calls the resolver; asserts returned body contains `SENTINEL_USER_TIER`.
  - Test `TestAssembleAgentFileBody_ProjectOverride` writes BOTH the user-tier sentinel AND a `<project>/.tillsyn/agents/go-builder-agent.md` with sentinel content `SENTINEL_PROJECT_TIER`; asserts returned body contains `SENTINEL_PROJECT_TIER` (project tier wins over user tier per priority order).
  - Test `TestAssembleAgentFileBody_CrossGroupFallbackToTillGen` (LOCKED — ROUND-3 W3-FF7) constructs a `BindingResolved` with `AgentName = "orchestrator-managed"` and `SystemPromptTemplatePath = ""` (the coordination-kind binding shape from `till-go.toml`); calls `assembleAgentFileBody` with a project tempdir + user tempdir BOTH empty (no per-tier override files); asserts returned body matches the embedded `internal/templates/builtin/agents/till-gen/orchestrator-managed.md` placeholder content (the cross-group fallback path fires because `till-go/orchestrator-managed.md` does not exist in the embedded FS). Companion negative test `TestAssembleAgentFileBody_CrossGroupFallbackMissesBothGroups` constructs an `AgentName = "nonexistent-agent"` with empty path and BOTH empty user/project tiers; asserts returned error wraps `ErrAgentBodyNotFound` (both `till-go/nonexistent-agent.md` AND `till-gen/nonexistent-agent.md` miss).
  - `renderAgentFile` (the caller of `assembleAgentFileBody` at `render.go:326`) passes `project` through; signature changes from `renderAgentFile(bundle, binding)` to `renderAgentFile(bundle, project, binding)`. The single call site at `render.go:160-163` adapts.
  - `mage test-pkg ./internal/app/dispatcher/cli_claude/render` passes; `mage ci` green.
- **Blocked by:** 4c.6.W3.D1 (D2 reads `binding.SystemPromptTemplatePath` plumbed by D1 — without D1 the field doesn't exist on `BindingResolved`); 4c.6.W1.D1 transitively via the L1 plan's `Blocked by: 4c.6.W1.D1` (the embedded-FS tier reads from `internal/templates/builtin/agents/till-<group>/` directories that W1.D1 creates with placeholder content).
- **Specify (droplet-scope; inherits `SKETCH.md` § 26.W3 + `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § D.1):**
  - **Objective:** Replace today's F.7.3b body sentence ("Behavior loaded from the canonical ... template at the system-installed plugin path" — `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § C.5 calls this FACTUALLY WRONG under `--bare`) with substantive agent content sourced via 3-tier resolution. This is the load-bearing fix that makes spawned subagents actually carry behavior definition.
  - **AcceptanceCriteria:** see Acceptance bullets above.
  - **ValidationPlan:** `mage test-pkg ./internal/app/dispatcher/cli_claude/render`; `mage test-func ./internal/app/dispatcher/cli_claude/render TestAssembleAgentFileBody_EmbeddedDefault` and `_UserOverride` and `_ProjectOverride`; `mage ci` green.
  - **RiskNotes:**
    - W1.D1 ships PLACEHOLDER content at `internal/templates/builtin/agents/till-<group>/*.md` per the L1 plan's "this drop ships PLACEHOLDER prompts only ... so the embed FS + path resolution can land without the prompt-authoring effort blocking" — D2 wires the resolver against placeholder bytes; substantive prompt content lands in Drop 4c.8 W4-A/B/C/D. D2 tests assert "non-empty body containing the placeholder marker," NOT "body matches a specific known prompt."
    - Project tier path traversal hardening: the project tier reads `<project.RepoPrimaryWorktree>/.tillsyn/agents/<basename>` — `<basename>` MUST be filesystem-sanitized (no `..`, no absolute paths, no path separators) to prevent escape from `.tillsyn/agents/`. Same defensive shape as `Render`'s existing `binding.AgentName` path-separator check at `render.go:139-142`. Sub-planner reuses or generalizes that check.
    - User-tier `os.UserHomeDir` resolution: tests set `t.Setenv("HOME", tempdir)` to redirect the resolver. Production behavior on Windows / non-POSIX systems may need additional handling; out-of-scope for D2 (Tillsyn is POSIX-only today per `cli_claude/env.go` baseline).
    - **Embedded FS seam (LOCKED — ROUND-2 W3-FF1)**: render.go does NOT declare its own `//go:embed builtin/agents` directive. Go forbids parent-traversal in embed patterns and `internal/templates/builtin/agents/` is not a child of the render package directory; a `render.go`-local directive would yield `pattern builtin/agents: no matching files found` at compile time. The correct seam is the EXISTING `templates.DefaultTemplateFS` at `internal/templates/embed.go:104` (lines 72-100 already include all 21 W1.D1 placeholders + 6 legacy go-* placeholders). Sub-planner imports `github.com/evanmschultz/tillsyn/internal/templates` from render.go and reads agent bodies via `fs.ReadFile(templates.DefaultTemplateFS, "builtin/agents/<group>/<basename>")`.
    - **`<group>` derivation (LOCKED — ROUND-2 W3-FF5)**: `<group> = path.Dir(binding.SystemPromptTemplatePath)` when non-empty; `<group> = "till-go"` when empty. Use `path.Dir` (slash-aware), NOT `filepath.Dir` (OS-aware) — embed.FS is always slash-pathed. This collapses the divergent-interpretation risk flagged by ROUND-1 falsification 1.5.
    - **Cross-group embedded-tier fallback (LOCKED — ROUND-3 W3-FF7)**: when the primary embedded-tier lookup at `builtin/agents/<group>/<basename>` returns `fs.ErrNotExist`, the resolver falls back to `builtin/agents/till-gen/<basename>` (cross-group to till-gen — the canonical "shared agents" group). Without this fallback, every coordination-kind spawn (closeout / refinement / discussion / human-verify) aborts because `till-go.toml` binds those kinds to `agent_name = "orchestrator-managed"` with empty `system_prompt_template_path`, and `orchestrator-managed.md` exists ONLY in `till-gen/` (verified via Read of `internal/templates/builtin/agents/{till-gen,till-go,till-gdd}/`). Cross-group fallback ONLY descends into till-gen (no symmetric fallback from till-gen → till-go); bare-filename match (no path traversal). Logs a debug message when fallback fires for diagnosability of legitimate typos.
  - **ContextBlocks:**
    - `reference` (high): `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § D.1.c (chosen "embedded default + per-project override" model). Sub-planner re-reads § D.1.c before authoring to confirm the recommended shape.
    - `reference` (high): existing `assembleAgentFileBody` at `render.go:340-364` is the rewrite target. Existing function-doc-comment at lines 335-339 needs updating — D2 owns the function-doc-comment update (the broader resolve-comment block at lines 307-319 is D6's scope per HF3).
    - `reference` (high): existing render-rollback machinery (`renderRollback` at `render.go:188-204`) handles partial-failure cleanup; D2's new error path integrates here.
    - `constraint` (high): D2 emits FULL agent body verbatim (frontmatter + body text). D3 layers frontmatter strip on top — D2 does NOT strip; that's D3's contract.
    - `constraint` (high): file-path traversal defense — `<basename>` derived from `binding.SystemPromptTemplatePath` MUST be sanitized to prevent escape from `.tillsyn/agents/`.
    - `decision` (LOAD-BEARING — ROUND-2 W3-FF5): `<group> = path.Dir(binding.SystemPromptTemplatePath)` when non-empty; `<group> = "till-go"` when empty. Slash-aware `path.Dir`, not OS-aware `filepath.Dir`.
    - `decision` (LOAD-BEARING — ROUND-3 W3-FF7): embedded-tier cross-group fallback ladder — primary `builtin/agents/<group>/<basename>` → fallback `builtin/agents/till-gen/<basename>` on `fs.ErrNotExist`. Required for coordination-kind spawns (closeout/refinement/discussion/human-verify) which bind `orchestrator-managed.md` (sole copy in till-gen). One-way fallback (till-gen is the only fallback target; till-gen does NOT fall back to other groups).
    - `decision` (LOAD-BEARING — ROUND-2 W3-FF1): embedded-tier seam is `templates.DefaultTemplateFS`. NO render-package-local `//go:embed` directive.
    - `constraint` (high — ROUND-2 W3-FF1): import path `github.com/evanmschultz/tillsyn/internal/templates` is acyclic (templates leaf-imports stdlib only); D2 introduces this import in render.go.
    - `warning` (normal): W1.D1 ships placeholders ONLY; D2 tests assert "non-empty body containing placeholder marker," not specific prompt content.
    - `constraint` (normal): NO new `ErrAgentBodyNotFound` symbol — declared in same KindPayload below; package-level sentinel returned when 3-tier resolver exhausts all tiers (ROUND-1 plan-QA-proof Finding 1.7 fix).
  - **KindPayload:** `{"changes":[{"file":"internal/app/dispatcher/cli_claude/render/render.go","symbol":"assembleAgentFileBody, renderAgentFile, ErrAgentBodyNotFound","action":"modify","shape_hint":"replace stub with 3-tier resolver consuming templates.DefaultTemplateFS via fs.ReadFile (NO render-package-local //go:embed); add new import github.com/evanmschultz/tillsyn/internal/templates; declare package-level ErrAgentBodyNotFound sentinel; renderAgentFile gains project param; sanitize binding.SystemPromptTemplatePath basename; <group> = path.Dir(binding.SystemPromptTemplatePath) || \"till-go\"; embedded-tier cross-group fallback ladder per W3-FF7: primary builtin/agents/<group>/<basename> → fallback builtin/agents/till-gen/<basename> on fs.ErrNotExist; debug-log on fallback fire"},{"file":"internal/app/dispatcher/cli_claude/render/render_test.go","symbol":"TestAssembleAgentFileBody_EmbeddedDefault, _UserOverride, _ProjectOverride, _CrossGroupFallbackToTillGen, _CrossGroupFallbackMissesBothGroups","action":"add","shape_hint":"tempdir + t.Setenv(HOME) sentinel-content tests; assert priority order; W3-FF7 cross-group fallback positive + negative coverage (orchestrator-managed AgentName resolves via till-gen fallback; nonexistent agent name returns ErrAgentBodyNotFound after both groups miss)"}]}`

### Droplet 4c.6.W3.D3 — Frontmatter strip wiring at render time

- **State:** done
- **Kind:** `build` (atomic droplet; `Irreducible: true`)
- **Paths:** `internal/app/dispatcher/cli_claude/render/render.go` (MODIFY — `assembleAgentFileBody` calls `config.StripFrontmatterKeys(frontmatter, stripModel, stripTools)` after D2's body resolve; computes `stripModel` from `binding.Model != nil && *binding.Model != ""` (ROUND-2 W3-FF2) and sets `stripTools = true` ALWAYS (ROUND-3 W3-FF12 fix — tool-gating keys `tools:` / `allowedTools:` / `disallowedTools:` are template-time only; runtime per-spawn injection is the sole authoritative source, so strip MUST be unconditional to prevent a future-placeholder-author leaving stale `allowedTools:` in disk frontmatter that would survive into the rendered file) per `SKETCH.md` § 4.4 + `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § D.2; needs to split the resolved body into (frontmatter, body) at the leading + trailing `---\n` delimiters before calling the helper, then re-concatenate after strip), `internal/app/dispatcher/cli_claude/render/render_test.go` (MODIFY — append `TestAssembleAgentFileBody_FrontmatterStripModelOnAgentsTOMLSet`, `TestAssembleAgentFileBody_FrontmatterStripToolsOnAgentsTOMLSet`, `TestAssembleAgentFileBody_FrontmatterPreservedWhenAgentsTOMLAbsent` exercising the strip-condition matrix).
- **Packages:** `internal/app/dispatcher/cli_claude/render`.
- **Acceptance:**
  - `assembleAgentFileBody` (post-D2) splits the resolved body at the leading `---\n` and trailing `---\n` delimiters; passes the inner frontmatter string to `config.StripFrontmatterKeys(frontmatter, stripModel, stripTools)`; **re-injects per-spawn runtime tool-gating frontmatter (LOCKED — ROUND-3 W3-PF1)** by appending `allowedTools: <comma-joined binding.ToolsAllowed>` and `disallowedTools: <comma-joined binding.ToolsDisallowed>` lines to the stripped frontmatter when those binding fields are non-empty; re-concatenates the post-strip-and-inject frontmatter (wrapped in `---\n...---\n`) with the post-frontmatter body. **Ordering**: read disk → strip template-time fields (`tools:` / `model:` / `allowedTools:` / `disallowedTools:` per W0.D4 strip universe) → inject runtime per-spawn fields (`allowedTools:` / `disallowedTools:` from binding) → emit. Empty `binding.ToolsAllowed` / `binding.ToolsDisallowed` skip injection (no `allowedTools:` line emitted). **Why this addition is LOAD-BEARING**: pre-W3, `assembleAgentFileBody` was an F.7.3b stub that built the entire body in code, including the per-spawn `allowedTools:` / `disallowedTools:` lines (verified via Read of `render_test.go:331-364` which asserts substrings `"allowedTools: Read, Grep"` and `"disallowedTools: WebFetch, Bash(curl *)"` in the rendered file). Post-D2, the body comes from disk/embedded FS with only `name:` + `description:` frontmatter — no per-spawn tool-gating injection. Without D3 re-injecting, the existing `TestRenderAgentFileFrontmatter` test deterministically fails (rendered file would lack `allowedTools:` / `disallowedTools:` lines). The strip layer (W3-PF1 ordering: strip first) handles the case where embedded MDs accidentally carry stale `allowedTools:` / `disallowedTools:` keys — those are template-time fields and MUST be replaced by runtime values, not merged.
  - `stripModel` is `binding.Model != nil && *binding.Model != ""` (LOCKED — ROUND-2 W3-FF2). Background: `ResolveBinding` (`binding_resolved.go:143-172`) routes `Model` through `resolveStringPtr`, which on the no-override path returns `&v` where `v := rawValue` (lines 170-171); net, `BindingResolved.Model` is NEVER nil after resolve. A bare `binding.Model != nil` predicate would be ALWAYS-TRUE under the current resolver and would strip `model:` on every render regardless of `agents.toml` content — exactly the bug ROUND-1 falsification Finding 1.2 caught. The empty-string-pointer-target predicate `*binding.Model != ""` correctly discriminates "agents.toml set this" from "rawBinding default of empty string promoted to pointer." Empty-string `model:` in `agents.toml` is treated as "not set" — a user-error case settled in QA, not a runtime semantic.
  - `stripTools = true` ALWAYS (LOCKED — ROUND-3 W3-FF12 fix). Tool-gating frontmatter keys (`tools:` / `allowedTools:` / `disallowedTools:`) are template-time only — runtime per-spawn injection (`binding.ToolsAllowed` / `binding.ToolsDisallowed`) is the sole authoritative source. **Why unconditional strip**: the round-2 conditional predicate `len(binding.ToolsAllowed) > 0 || len(binding.ToolsDisallowed) > 0` would short-circuit `StripFrontmatterKeys` (via the no-op path at `frontmatter.go:90-93`) when both binding slices are empty, allowing any stale `allowedTools:` line a future placeholder author accidentally lands in disk frontmatter to survive into the rendered file — deterministically failing `TestRenderAgentFileWithoutToolGating` at `render_test.go:366-401` which asserts ABSENCE of those keys. Always-strip + conditional-inject (per the W3-PF1 ordering below) is the correct pipeline: strip removes any stale disk tool-gating keys regardless of binding state; injection adds runtime values back when the binding has them. Empty `binding.ToolsAllowed` / `binding.ToolsDisallowed` skip the inject step, leaving NO tool-gating frontmatter in the rendered file — matches the `TestRenderAgentFileWithoutToolGating` contract.
  - Both flags false → no strip, embedded frontmatter passes through verbatim per `config.StripFrontmatterKeys`'s documented "no-op short-circuit" path at `frontmatter.go:90-93` (verified Read).
  - Body sourced from D2's resolver flowing through D3 unchanged when no strip applies (frontmatter + body byte-for-byte preserved).
  - Body sourced from D2's resolver with frontmatter `model:` line stripped when `stripModel` true (verified by test fixture with embedded `model: opus` in frontmatter + `binding.Model = &"sonnet"`).
  - Body sourced from D2's resolver with frontmatter `tools:` / `allowedTools:` / `disallowedTools:` lines all stripped when `stripTools` true (per `frontmatter.go:51` `frontmatterToolsKeys = []string{"tools", "allowedTools", "disallowedTools"}` — verified Read).
  - **Existing-test preservation contract (LOCKED — ROUND-3 W3-PF1)**: `TestRenderAgentFileFrontmatter` at `internal/app/dispatcher/cli_claude/render/render_test.go:331-364` MUST stay green post-D3. The test asserts (lines 352-358) the rendered file contains `"---\n"` + `"name: " + binding.AgentName` + `"description: "` + `"allowedTools: Read, Grep"` + `"disallowedTools: WebFetch, Bash(curl *)"`. Pre-W3 these substrings came from the stub builder; post-D3 they come from the strip-then-inject pipeline (`name:` + `description:` survive from the disk-sourced body's frontmatter; `allowedTools:` + `disallowedTools:` are runtime-injected by D3 from `binding.ToolsAllowed` / `binding.ToolsDisallowed`). Sub-planner verifies via test-fixture inspection that the W1.D1 placeholder for `go-builder-agent` (or whichever AgentName the test uses) carries `name:` + `description:` frontmatter — verified via Read of `internal/templates/builtin/agents/till-go/builder-agent.md` lines 1-4 (`name: builder-agent` + `description: PLACEHOLDER — Go+mage-tuned builder agent...`). NOTE: today's `fixtureBinding()` test helper uses `AgentName = "go-builder-agent"` (per render_test.go:113, :144, :335, :370 verified Read); the till-go placeholder is named `builder-agent.md` not `go-builder-agent.md` — sub-planner resolves this fixture-name mismatch per W3-FF10 below.
  - **Empty binding.ToolsAllowed / ToolsDisallowed skip injection**: when both are nil/empty, D3 emits NO `allowedTools:` / `disallowedTools:` line (mirrors the existing `TestRenderAgentFileWithoutToolGating` at `render_test.go:366-401` contract — verified Read shows the test asserts `!strings.Contains(str, "allowedTools:")` and `!strings.Contains(str, "disallowedTools:")` when binding has no tool gates). This test ALSO MUST stay green post-D3.
  - Top-level keys only — `config.StripFrontmatterKeys` walks `root.Content` (the root MappingNode); nested `model:` / `tools:` keys are NOT touched per `frontmatter.go:30-32` doc-comment + the existing W0.D4 implementation. D3's tests confirm this is preserved.
  - Frontmatter delimiter handling: `assembleAgentFileBody` MUST locate the leading `---\n` and trailing `---\n`. If either is absent, D3 leaves the body unchanged AND emits a warning log via the package's existing logging idiom (sub-planner verifies if package has logging; if not, returns a wrapped error per the W3 fail-loud discipline). Decision rationale: malformed agent files (missing frontmatter delimiters) are caught by D5's post-render validator — D3 doesn't need to double-fail here, but it MUST not silently corrupt body bytes.
  - Test `TestAssembleAgentFileBody_FrontmatterStripModelOnAgentsTOMLSet`: embedded fixture frontmatter contains `model: opus`; `binding.Model = ptr("sonnet")`; assert returned body's frontmatter no longer contains `model:` line; assert post-frontmatter body bytes preserved.
  - Test `TestAssembleAgentFileBody_FrontmatterStripToolsOnAgentsTOMLSet`: embedded fixture frontmatter contains `tools: Read, Bash` AND `allowedTools: Read`; `binding.ToolsAllowed = []string{"Read"}`; assert returned body's frontmatter no longer contains `tools:`, `allowedTools:`, or `disallowedTools:` lines.
  - Test `TestAssembleAgentFileBody_FrontmatterPreservedWhenAgentsTOMLAbsent`: embedded fixture frontmatter contains `model: opus` AND `tools: Read, Bash`; `binding.Model = ptr("")` (the resolver's "absent" representation per W3-FF2) AND `binding.ToolsAllowed = nil` AND `binding.ToolsDisallowed = nil`; assert returned body byte-for-byte equal to embedded fixture (no-op short-circuit honored). NOTE: a parallel `binding.Model = nil` test case is also valid coverage but not the load-bearing path under the current resolver; sub-planner adds it as a defensive table case.
  - `mage test-pkg ./internal/app/dispatcher/cli_claude/render` passes; `mage ci` green.
- **Blocked by:** 4c.6.W3.D2 (D3 mutates the body D2 produces via the resolver; same `assembleAgentFileBody` function; same `render.go` file; same package compile).
- **Specify (droplet-scope; inherits `SKETCH.md` § 26.W3 + `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § D.2):**
  - **Objective:** Make `agents.toml` the AUTHORITATIVE runtime source for `model:` / `tools:` per `SKETCH.md` § 4.4 by stripping the corresponding embedded-frontmatter keys at render time when `agents.toml` set them. Without strip, dev-machine-local edits to embedded MD content could pin a model that conflicts with `agents.toml`.
  - **AcceptanceCriteria:** see Acceptance bullets above.
  - **ValidationPlan:** `mage test-pkg ./internal/app/dispatcher/cli_claude/render`; `mage test-func ./internal/app/dispatcher/cli_claude/render TestAssembleAgentFileBody_FrontmatterStripModelOnAgentsTOMLSet`; same for `_StripTools` and `_PreservedWhenAbsent`; `mage ci` green.
  - **RiskNotes:**
    - `config.StripFrontmatterKeys` already handles top-level-only stripping per W0.D4 contract — D3 does NOT re-implement; it WIRES. Verified at `internal/config/frontmatter.go:89` (Read).
    - Frontmatter delimiter parsing: split-on-`---\n` is brittle if the embedded MD has a non-standard delimiter (e.g., `---` without trailing newline). Sub-planner verifies W1.D1's placeholder MDs use canonical `---\n...---\n` shape; if not, normalize at fixture-creation time.
    - `stripModel = binding.Model != nil && *binding.Model != ""` because `ResolveBinding` always populates `Model` via `resolveStringPtr` (which returns `&v` for the no-override path at `binding_resolved.go:170-171`); a bare `!= nil` predicate is ALWAYS-TRUE and would mis-strip `model:` on every render. The empty-string-target check correctly discriminates the "agents.toml SET this" runtime contract. Verified in `binding_resolved.go:143-172` per ROUND-2 W3-FF2 fix.
    - **F.7.17 L9 semantic for `model = ""` (LOCKED — ROUND-3 W3-FF9 Option B)**: F.7.17 L9 (verified via Read of `internal/app/dispatcher/cli_adapter.go:96-101 + :121-124`) states "Pointer-typed fields distinguish 'absent' (the lower-priority layer is authoritative or no value was specified) from 'explicit zero' per F.7.17 locked decision L9. ... `Model *string` ... nil means no override; the adapter falls back to its CLI's default model." The doc-comment for `Model *string` does NOT explicitly call out `Model = ""` (i.e. `*Model == ""` with non-nil pointer) as deliberate-meaningful-zero — F.7.17 L9 names `nil` as the "absent" sentinel and any non-nil value as "override." HOWEVER, falsification round-2 raised the concern that an adopter setting `model = ""` in `agents.toml` deliberately could be interpreted as deliberate-meaningful-zero ("explicitly use no model override"). The round-2 strip predicate `*binding.Model != ""` treats this case as "not set" (no strip), which is a SEMANTIC CHOICE: `model = ""` in `agents.toml` is treated as USER-ERROR. **Rationale (W3-FF9 Option B chosen)**: an explicit empty-string `model:` in `agents.toml` means "use whatever is rendered into the agent .md," which equals "no override" — a degenerate case with no useful semantic distinct from omitting the key. This matches the round-2 LOCKED predicate. **Forward dep**: a W0/W0.5 validator warning flags `model = ""` (or `model_template_path = ""`) in `agents.toml` as suspicious — the warning lands as part of the W0/W0.5 validateAgentBindingNames pass per `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § D.1.b. Cross-reference: `internal/templates/load.go:1031-1055` is the AgentBinding validator entry point per AGENT_ARCHITECTURE_TRUTH.md § 2.3. Sub-planner notes this forward dep in the round-3 plan-QA verdict for W4 / W0.5 visibility. NOT a D1 scope expansion (Options A and C from the W3-FF9 finding are explicitly REJECTED — Option A would re-introduce the W3-FF2 bug; Option C would expand D1 scope to add `Model` companion `bool` field which violates the "D1 is plumbing-only" constraint). Round-2 D3 strip predicate stands.
    - Frontmatter-absent edge case: a malformed embedded MD with no `---` delimiters returns from `assembleAgentFileBody` with body unchanged (no strip applied). D5's validator catches this case via the "frontmatter delimiters present" signal.
    - **Strip-then-inject ordering (LOCKED — ROUND-3 W3-PF1)**: D3 MUST process the frontmatter in this exact order: read disk → strip template-time fields (`tools:` / `model:` / `allowedTools:` / `disallowedTools:` per the strip universe) → inject runtime per-spawn fields (`allowedTools:` / `disallowedTools:` from binding when non-empty) → emit. Inverting the order would either (a) merge stale embedded `allowedTools:` with runtime values (wrong) or (b) strip the freshly-injected runtime values (wrong). Sub-planner verifies via the `TestRenderAgentFileFrontmatter` test pass post-D3.
    - **Pre-W3 baseline observation (W3-PF1)**: pre-W3 `assembleAgentFileBody` built the agent body in code (see `render_test.go:331-364` asserting `allowedTools: Read, Grep` substring presence). Post-D2 the body comes from disk; post-D3 the strip-then-inject pipeline restores the runtime tool-gating frontmatter that the existing test contract requires. Without D3's runtime injection, the test suite has a deterministic regression. ROUND-3 W3-PF1 caught this gap before plan-QA round-3 sign-off.
  - **ContextBlocks:**
    - `reference` (high): `internal/config/frontmatter.go:89` — `StripFrontmatterKeys(frontmatter, stripModel, stripTools) (string, error)` is the W0.D4-shipped helper D3 calls into. Sub-planner re-reads its doc-comment lines 58-88 before authoring to confirm signature + semantics.
    - `reference` (high): `frontmatter.go:51` — `frontmatterToolsKeys = []string{"tools", "allowedTools", "disallowedTools"}` defines the strip universe for `stripTools=true`.
    - `reference` (normal): `SKETCH.md` § 4.4 strip semantics; `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § D.2 + § F implementation sketch.
    - `decision` (LOAD-BEARING — ROUND-2 W3-FF2 + ROUND-3 W3-FF12): `stripModel = binding.Model != nil && *binding.Model != ""` (the pointer-empty-target check is required because `ResolveBinding` always returns non-nil `Model` per `resolveStringPtr` at `binding_resolved.go:170-171` always returning `&v`); `stripTools = true` ALWAYS (ROUND-3 W3-FF12: tool-gating keys are template-time only; runtime per-spawn injection is the sole authoritative source; unconditional strip prevents stale disk tool-gating frontmatter from surviving when binding has empty tool gates, matching `TestRenderAgentFileWithoutToolGating` contract at `render_test.go:366-401`).
    - `decision` (LOAD-BEARING — ROUND-3 W3-PF1): D3 strip-then-inject pipeline. After `config.StripFrontmatterKeys` strips template-time fields, D3 RE-INJECTS per-spawn runtime fields `allowedTools:` and `disallowedTools:` from `binding.ToolsAllowed` / `binding.ToolsDisallowed` when non-empty. Empty binding tool-gate slices skip injection (mirroring `TestRenderAgentFileWithoutToolGating` contract). The strip-then-inject sequence is mandatory ordering — inverting it either leaks stale embedded values OR strips fresh runtime values.
    - `constraint` (LOAD-BEARING — ROUND-3 W3-PF1): `TestRenderAgentFileFrontmatter` at `render_test.go:331-364` AND `TestRenderAgentFileWithoutToolGating` at `render_test.go:366-401` MUST both stay green post-D3. The first asserts runtime-injected `allowedTools:` / `disallowedTools:` substrings; the second asserts those lines are ABSENT when binding has no tool gates. D3's strip-then-inject pipeline satisfies both contracts because (a) injection only fires when binding tool-gate slices are non-empty, (b) strip removes any stale embedded tool-gate frontmatter that would otherwise survive into the rendered file.
    - `constraint` (high): top-level keys only — D3's tests assert nested `model:` keys are NOT touched.
    - `warning` (normal): malformed embedded MD without `---` delimiters returns body unchanged (no strip); D5 catches at validator layer.
  - **KindPayload:** `{"changes":[{"file":"internal/app/dispatcher/cli_claude/render/render.go","symbol":"assembleAgentFileBody","action":"modify","shape_hint":"split body at ---\\n delimiters; call config.StripFrontmatterKeys with computed flags; THEN inject per-spawn runtime allowedTools / disallowedTools from binding when non-empty (W3-PF1 strip-then-inject ordering); re-concatenate"},{"file":"internal/app/dispatcher/cli_claude/render/render_test.go","symbol":"TestAssembleAgentFileBody_FrontmatterStripModel..., _StripTools..., _PreservedWhenAbsent, TestRenderAgentFileFrontmatter (existing — preserved per W3-PF1), TestRenderAgentFileWithoutToolGating (existing — preserved per W3-PF1)","action":"add+preserve","shape_hint":"three new table cases covering strip-on / strip-off / mixed flag matrix; existing TestRenderAgentFileFrontmatter at render_test.go:331-364 + TestRenderAgentFileWithoutToolGating at :366-401 MUST stay green post-D3 — D3 implementation preserves both contracts via strip-then-inject pipeline"}]}`

### Droplet 4c.6.W3.D4 — Defense-in-depth env vars in `cli_claude/env.go`

- **State:** done
- **Kind:** `build` (atomic droplet; `Irreducible: true`)
- **Paths:** `internal/app/dispatcher/cli_claude/env.go` (MODIFY — extend `closedBaselineEnvNames` slice at lines 37-58 with a new alongside data structure, `defenseInDepthEnvLiterals`, that carries `(name, value)` pairs and is unconditionally written into the assembled `cmd.Env` regardless of `os.LookupEnv` result; OR add a new `assembleDefenseInDepthEnv()` helper called inside `assembleEnv` that injects the four pairs into `emitted` map after the `binding.Env` resolution loop and before the closed-baseline resolution loop, per `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § D.3 — sub-planner picks the cleaner shape; preferred: separate slice + injection-loop right after the `binding.Env` loop at line 95-101 so the four literals are part of the closed-baseline universe but distinguishable in code review), `internal/app/dispatcher/cli_claude/adapter_test.go` (MODIFY — append `TestEnvCarriesDefenseInDepthEnvVars` asserting `cmd.Env` carries `CLAUDE_CODE_DISABLE_BACKGROUND_TASKS=1`, `CLAUDE_CODE_FORK_SUBAGENT=0`, `DISABLE_AUTOUPDATER=1`, `DISABLE_TELEMETRY=1` after `BuildCommand` runs).
- **Packages:** `internal/app/dispatcher/cli_claude`.
- **Acceptance:**
  - `assembleEnv` (`env.go:76`) emits four literal `name=value` pairs in `cmd.Env` unconditionally — per `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § D.3 these are defense-in-depth, NOT the primary isolation gate (`--bare` + `--strict-mcp-config` + `--setting-sources ""` are the primary gates per § A.9):
    - `CLAUDE_CODE_DISABLE_BACKGROUND_TASKS=1` — prevents the spawned subagent from forking background subagents.
    - `CLAUDE_CODE_FORK_SUBAGENT=0` — explicit zero for fork-mode toggle.
    - `DISABLE_AUTOUPDATER=1` — prevents spawned `claude` from running its own update logic mid-spawn.
    - `DISABLE_TELEMETRY=1` — privacy + reproducibility.
  - The four literals are written into the `emitted` map BEFORE the closed-baseline resolution loop (so they are part of the emitted set when the slice-builder phase runs at lines 119-145).
  - **Override semantics**: per `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § D.3 RISK clause — make the literals overridable by `binding.Env` (binding wins). Implementation: process `binding.Env` FIRST (existing loop at lines 95-101, which already runs first); then defense-in-depth literals SECOND with `if _, alreadySet := emitted[name]; !alreadySet` check (mirroring the closed-baseline-after-binding precedence at lines 108-111); then closed-baseline THIRD. Net: binding > defense-in-depth > closed-baseline.
  - The four literals appear in the assembled `out` slice at deterministic positions (sub-planner picks an ordering; preferred: defense-in-depth literals immediately after the closed-baseline names in the emitted-slice order, since the existing slice-builder at lines 125-132 walks `closedBaselineEnvNames` first; defense-in-depth appended via a parallel walk of the new slice).
  - `ErrMissingRequiredEnv` semantics unchanged — defense-in-depth literals don't have an "orchestrator-must-set" failure mode; they're literal-valued.
  - Test `TestEnvCarriesDefenseInDepthEnvVars` (`adapter_test.go`) constructs a minimal `BindingResolved`, calls `BuildCommand` (or whichever path produces `cmd.Env`), asserts the four expected `name=value` strings are present in `cmd.Env`. Use `slices.Contains(cmd.Env, "CLAUDE_CODE_DISABLE_BACKGROUND_TASKS=1")` per existing test idiom (sub-planner verifies via Read).
  - Test `TestEnvDefenseInDepthOverridableByBindingEnv` constructs a `BindingResolved` with `Env: []string{"DISABLE_TELEMETRY"}` and `os.Setenv("DISABLE_TELEMETRY", "0")`; asserts emitted env carries `DISABLE_TELEMETRY=0` (binding's resolved value wins over defense-in-depth literal).
  - Existing tests in `env_test.go` and `adapter_test.go` (whichever tests env shape) MUST continue to pass — sub-planner Reads them before authoring to confirm assertions against pre-D4 `cmd.Env` shapes account for the four new literals (e.g., a `len(cmd.Env) == N` assertion needs +4 adjustment).
  - `mage test-pkg ./internal/app/dispatcher/cli_claude` passes; `mage ci` green.
- **Blocked by:** —
- **Specify (droplet-scope; inherits `SKETCH.md` § 26.W3 + `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § D.3):**
  - **Objective:** Close defense-in-depth gaps where future Claude Code versions might add a new auto-discovery path that `--bare` doesn't cover yet. Not the primary isolation gate — the primary gates (`--bare` + `--strict-mcp-config` + `--setting-sources ""`) are already in place per `argv.go:51-124`.
  - **AcceptanceCriteria:** see Acceptance bullets above.
  - **ValidationPlan:** `mage test-pkg ./internal/app/dispatcher/cli_claude`; `mage test-func ./internal/app/dispatcher/cli_claude TestEnvCarriesDefenseInDepthEnvVars`; same for `TestEnvDefenseInDepthOverridableByBindingEnv`; `mage ci` green.
  - **RiskNotes:**
    - Future `claude` flag may want one of the four as a non-default value. Mitigation (per § D.3 RISK clause): defense-in-depth literals are overridable by `binding.Env` — binding wins. This is symmetric to the existing `binding.Env > closed-baseline` rule at `env.go:108-111`. Sub-planner Reads that block before authoring to mirror the precedence pattern.
    - Existing tests asserting exact `cmd.Env` length or full-slice content WILL break with +4 entries. Sub-planner Reads `adapter_test.go` test cases AND `env_test.go` (if it exists — the directory listing shows `adapter_test.go` but no `env_test.go`; sub-planner verifies via Read) BEFORE authoring D4 production code so test updates ride alongside the production change.
    - Determinism: the four literals must be added to the emitted set in a deterministic order so test assertions on full-slice content are stable. Use a declared `defenseInDepthEnvLiterals []struct{ Name, Value string }` slice; the slice-builder walks it in declaration order.
    - File-only edit, no adapter wiring: D4 stays inside `env.go` per the L1 directive. The existing `BuildCommand` call site that consumes `assembleEnv` at `adapter.go` (sub-planner verifies via Read) does not need re-wiring since the function signature is unchanged.
  - **ContextBlocks:**
    - `reference` (high): `internal/app/dispatcher/cli_claude/env.go:37-58` (closed-baseline declaration); lines 76-147 (`assembleEnv` body); lines 95-101 (`binding.Env` loop — runs FIRST and is the precedence reference); lines 108-111 (`alreadySet` skip pattern — the precedence-mirror sub-planner copies for defense-in-depth).
    - `reference` (high): `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § D.3 (the 5 env-var rationale + RISK clause on overridability).
    - `decision` (normal): defense-in-depth literals declared as a named `[]struct{Name, Value string}` slice for determinism + diffability.
    - `constraint` (high): precedence is `binding.Env > defense-in-depth > closed-baseline`. Mirror the existing pattern at lines 108-111.
    - `warning` (normal): existing `cmd.Env` length-or-content assertions need +4 adjustment; sub-planner audits before authoring.
  - **KindPayload:** `{"changes":[{"file":"internal/app/dispatcher/cli_claude/env.go","symbol":"defenseInDepthEnvLiterals, assembleEnv","action":"modify","shape_hint":"add named slice of {Name, Value} pairs; inject into emitted map after binding.Env loop and before closed-baseline loop; respect alreadySet skip pattern"},{"file":"internal/app/dispatcher/cli_claude/adapter_test.go","symbol":"TestEnvCarriesDefenseInDepthEnvVars, TestEnvDefenseInDepthOverridableByBindingEnv","action":"add","shape_hint":"slices.Contains assertions against cmd.Env"}]}`

### Droplet 4c.6.W3.D5 — Post-render validator wired at `Render`'s exit + sentinel test

- **State:** done
- **Kind:** `build` (atomic droplet; `Irreducible: true`)
- **Paths:** `internal/app/dispatcher/cli_claude/render/render.go` (MODIFY — add `validateBundle(bundle dispatcher.Bundle, item domain.ActionItem, binding dispatcher.BindingResolved) error` package-private function; **call it from `Render` BEFORE `return promptBody, nil` at line 178** — this is the LOAD-BEARING wiring contract per ROUND-2 HF8; `Render` returns the validator's wrapped error if it fails, with rollback running first via the existing `rollback.run()` pattern at lines 144-176), `internal/app/dispatcher/cli_claude/render/render_test.go` (MODIFY — append `TestRenderValidatorFailsOnEmptyAgentBody`, `TestRenderValidatorFailsOnStubShapeAgentBody`, `TestRenderValidatorFailsOnMissingFrontmatterDelimiters`, `TestRenderValidatorPassesOnSubstantiveBody` exercising the 3-signal stub-detection signature; PLUS the minimal "bundle body length > stub-threshold" sentinel-style assertion — combined with the other two signals — per the L1 plan's "Minimal sentinel-style integration test" acceptance bullet).
- **Packages:** `internal/app/dispatcher/cli_claude/render`.
- **Acceptance:**
  - `validateBundle` is invoked at `Render`'s exit, BEFORE `return promptBody, nil` at line 178. **The validator MUST be wired — not shipped as a dangling exported helper that no caller invokes** per ROUND-2 HF8 + `feedback_tillsyn_enforces_templates.md` shipped-but-not-wired anti-pattern.
  - `Render`'s wiring shape: after the existing `renderSettings` call at line 173-176, before `return promptBody, nil` at line 178, add `if err := validateBundle(bundle, item, binding); err != nil { rollback.run(); return "", fmt.Errorf("render: validate bundle: %w", err) }`. Rollback runs on validator failure exactly as on any other render-step failure.
  - `validateBundle` reads the rendered `<bundle.Paths.Root>/plugin/agents/<binding.AgentName>.md` from disk (the file `renderAgentFile` just wrote at line 332) and applies the **3-signal stub-detection signature** (OQ#4 resolution — settled at this droplet's authoring per the L1 plan):
    - **Signal A — body length threshold**: bytes after the closing `---\n` delimiter exceed **200 chars** (calibration: today's F.7.3b stub body at line 360-362 is ~120 chars; W1.D1 placeholder content target is > 500 chars — 200 leaves margin both directions). Validator fails when body is shorter than 200 chars.
    - **Signal B — frontmatter delimiters present**: opening `---\n` AND closing `---\n` both present, AND the inner frontmatter contains BOTH `name:` AND `description:` keys (parsed via simple line-prefix match — yaml.v3 dependency is reserved for W0.D4's strip helper; D5 does NOT re-import it). Validator fails when either delimiter is missing OR either required key is absent.
    - **Signal C — positive role-section header present (LOCKED — ROUND-2 W3-FF3 + ROUND-3 W3-FF6)**: body contains AT LEAST ONE of the canonical role-section header markers any legitimate body — placeholder OR substantive — carries: `"# PLACEHOLDER"` (the W1.D1 scaffolding-placeholder marker — verified via Read of `internal/templates/builtin/agents/till-go/builder-agent.md` line 6, `till-gen/orchestrator-managed.md` line 6, `till-gdd/qa-proof-agent.md` line 6: ALL 27 W1.D1 placeholders carry the literal `# PLACEHOLDER — substantive content lands in Drop 4c.8 W4` heading) OR `"# Section 0"` (the canonical Section-0 SEMI-FORMAL REASONING header per `SEMI-FORMAL-REASONING.md` — the marker post-Drop-4c.8-W4 substantive prompts will carry) OR `"## Role"` (the role-prose convention substantive prompts may also carry). The 3-marker disjunction is REQUIRED because ROUND-3 W3-FF6 caught that the round-2 2-marker form (`"# Section 0"` OR `"## Role"`) FAILS-CLOSED on every W1.D1 placeholder — the moment D2+D5 land together a build-break occurs because all 27 placeholder bodies use the `# PLACEHOLDER` heading. Adding `# PLACEHOLDER` as a third valid marker validates today's placeholders AND post-Drop-4c.8-W4 substantive prompts. Redesigned (ROUND-2 W3-FF3) from ROUND-1's hardcoded blacklist of one F.7.3b stub literal because a negative-blacklist Signal C silently fails-open if a future drop reintroduces a stub with different wording. Validator fails when NONE of the three positive markers is present in the body.
  - Combination: `validateBundle` returns a non-nil error if ANY signal fails (logical OR over failure conditions; equivalently, all three must hold for pass).
  - Returned error is wrapped with `ErrInvalidAgentBody` (new package-level sentinel `var ErrInvalidAgentBody = errors.New("render: invalid agent body in rendered bundle")`); error message identifies the specific signal that failed (e.g., `"render: validate bundle: %w: body length 87 < 200"` or `"... missing closing frontmatter delimiter"` or `"... contains F.7.3b stub key phrase"`).
  - Test `TestRenderValidatorFailsOnEmptyAgentBody`: write a deliberately-empty `<bundle>/plugin/agents/<name>.md` (or stub `assembleAgentFileBody` to return empty body via injection — sub-planner picks the cleaner path); call `Render`; assert returned error is non-nil AND `errors.Is(err, ErrInvalidAgentBody)` succeeds AND the bundle root has been cleaned up by rollback (no `<bundle>/plugin/` subtree remains).
  - Test `TestRenderValidatorFailsOnStubShapeAgentBody`: arrange `assembleAgentFileBody` (or a fixture overriding D2's resolver) to return today's exact F.7.3b stub body text (`"Tillsyn-spawned subagent stub. Behavior loaded from the canonical ..."`). Call `Render`; assert error is non-nil + `errors.Is(err, ErrInvalidAgentBody)`. Under the W3-FF3 + W3-FF6 redesigned Signal C, this body fails Signal C (no `"# PLACEHOLDER"` AND no `"# Section 0"` AND no `"## Role"` marker) AND Signal A (length below 200). Error message mentions whichever signal's check fires first per the validator's explicit order; sub-planner picks an order and tests for that specific message wording.
  - Test `TestRenderValidatorFailsOnMissingFrontmatterDelimiters`: arrange a body of `"some body without frontmatter at all\n"` (length > 200 to isolate Signal B). Assert error is non-nil + `errors.Is(err, ErrInvalidAgentBody)` + error mentions Signal B (missing delimiters).
  - Test `TestRenderValidatorPassesOnSubstantiveBody`: arrange a fixture body with full frontmatter + body length > 200 + no stub key phrase. Call `Render`; assert no error + `<bundle>/plugin/agents/<name>.md` was written successfully.
  - **Minimal sentinel-style integration test** (per L1 plan acceptance bullet "Minimal sentinel-style integration test: assert spawned `<bundle>/plugin/agents/<name>.md` body length > stub-threshold"): the four tests above already cover the body-length signal; sub-planner explicitly names ONE of them (preferred: `TestRenderValidatorPassesOnSubstantiveBody`) the "sentinel" gate by adding a test-doc-comment cross-reference to `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § E.2.2 "Sentinel-injection isolation test" — NOT the full sentinel-injection test scaffolding (Drop 4c.8 W4-D's job per the L1 plan), but a documented stand-in.
  - **Existing happy-path test fixture migration (LOCKED — ROUND-3 W3-FF10)**: existing `render_test.go` tests at lines 113, 144, 335, 370 (verified via Read: `TestRenderAgentFileFrontmatter`, `TestRenderAgentFileWithoutToolGating`, and `fixtureBinding()` helper sites) use `AgentName = "go-builder-agent"`. Post-D2, the resolver looks up `binding.SystemPromptTemplatePath` first (empty per default `agents.toml`), then defaults `<group> = "till-go"`, then resolves `<basename> = "<binding.AgentName>.md"` = `"go-builder-agent.md"`. The till-go embed group has BOTH `builder-agent.md` (W1.D1 placeholder, 385 bytes — verified Read) AND `go-builder-agent.md` (W1.D1 legacy placeholder, 609 bytes — verified Bash ls). Both carry `# PLACEHOLDER — substantive content lands in Drop 4c.8 W4` heading + ~200+ char body, satisfying Signal A and Signal C of the post-D5 validator. **Test-fixture mutation requirements (none required for W3 ship)**: the existing test fixture `AgentName = "go-builder-agent"` resolves cleanly via the embedded-tier lookup to `till-go/go-builder-agent.md` placeholder (verified ls byte-count 609 > 200, frontmatter `name: go-builder-agent` + `description: ...` present at lines 1-4 of that file per Read). NO test-fixture mutation required. Sub-planner verifies at build time via `mage test-pkg ./internal/app/dispatcher/cli_claude/render` that the existing tests pass against the post-D2 placeholder body. Forward note: when W5.D3 renames the legacy `go-builder-agent.md` → other (per L1 PLAN.md W5 scope), test fixtures may need re-pointing; that re-pointing rides on W5's wave-end sub-planner pass, not D5's scope.
  - `mage test-pkg ./internal/app/dispatcher/cli_claude/render` passes; `mage ci` green.
- **Blocked by:** 4c.6.W3.D2 (D5 validates the body D2 produces; same `render.go` file; same package compile); 4c.6.W3.D3 (D5 also validates the post-D3 body shape — the post-strip frontmatter must STILL contain `name:` + `description:` per Signal B, so D5's tests run AFTER D3's strip semantics are stable; same package).
- **Specify (droplet-scope; inherits `SKETCH.md` § 26.W3 + `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § D.4 + § E):**
  - **Objective:** Catch render-pipeline regressions where a future change leaves the bundle stub thin. Today's tests cover frontmatter shape but not body substance; without D5's guard, a future `assembleAgentFileBody` change could re-introduce the empty-body bug silently per `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § D.4.
  - **AcceptanceCriteria:** see Acceptance bullets above.
  - **ValidationPlan:** `mage test-pkg ./internal/app/dispatcher/cli_claude/render`; `mage test-func ./internal/app/dispatcher/cli_claude/render TestRenderValidator...`; `mage ci` green.
  - **RiskNotes:**
    - **HF8 wiring is load-bearing**: validator MUST be invoked from `Render`'s exit path. Sub-planner verifies via the test `TestRenderValidatorFailsOnEmptyAgentBody` that mutating the body to empty causes `Render()` (not just `validateBundle()` standalone) to return the wrapped sentinel — this is the test that proves the validator is wired, not dangling. Without this assertion, HF8 is unmet.
    - **OQ#4 stub-detection signature** (settled here, ROUND-2 W3-FF3 + W3-FF4 + ROUND-3 W3-FF6 redesign): 3-signal AND (length > 200 AND delimiters+required-keys present AND positive-role-section-header present). Calibration rationale: today's F.7.3b stub body is 115-140 chars (varies by agent-name length per ROUND-1 plan-QA-proof Finding 1.9); W1.D1 placeholder target is > 500 chars; threshold of 200 leaves a 60-85 char margin above stub and >300 char margin below placeholder. **W3-FF4 calibration consistency**: under AND semantics a short legitimate body (< 200 chars) fails Signal A regardless of Signal C — the prior ROUND-1 RiskNote claim that "Signal C mitigates short-body false positives on Signal A" was logically incoherent and is REMOVED. The ACCEPTED constraint is: every legitimate body — placeholder OR substantive — MUST exceed 200 chars. **Signal A 200-char floor forward-dependency note (ROUND-3 W3-FF8)**: D5's 200-char threshold is enforceable today against the W1.D1 placeholders (sub-planner verified the till-go/builder-agent.md placeholder body is ~250 chars including the heading + body paragraph; verify all 27 placeholders clear 200 at D5 build-time as part of acceptance). Forward, when Drop 4c.8 W4-A/B/C/D authors substantive prompt content, the W4 prompt-authoring contract MUST honor `body_len > 200` as a hard constraint — this is a forward-only honor-system claim with NO enforceable seam in W3 itself. PROPAGATION TARGET: Drop 4c.8 W4. EXPLICIT FORWARD DEP: W4 sub-planner verifies all 7 agent prompt bodies clear the 200-char floor at authoring time AND the W4 PLAN.md acceptance bullets include "all authored prompts MUST satisfy len(body_after_frontmatter) > 200." Accept as documented forward dep; not blocking W3 ship. Documented in `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § E.2.5 + propagated to the W4 prompt-authoring contract via this RiskNote. **ROUND-3 W3-FF6 Signal C 3-marker disjunction**: Signal C accepts `"# PLACEHOLDER"` OR `"# Section 0"` OR `"## Role"` — 3-marker disjunction REQUIRED to validate W1.D1 placeholders today (all 27 use `# PLACEHOLDER` heading per Read verification on till-go/builder-agent.md, till-gen/orchestrator-managed.md, till-gdd/qa-proof-agent.md). 2-marker round-2 form would have build-broken the moment D2+D5 land together. Short-but-legitimate is impossible by construction; placeholder bodies are explicitly verified > 200 at D5 build-time.
    - Body length is measured AFTER frontmatter; a body that's all-frontmatter + 5-char body-text would fail Signal A as designed.
    - Test for Signal B (missing delimiters) needs a body length > 200 to isolate the signal; otherwise Signal A also fires and the test asserts on the wrong signal. Sub-planner constructs the fixture deliberately for orthogonality.
    - File reading at validator time: `validateBundle` reads from `<bundle.Paths.Root>/plugin/agents/<binding.AgentName>.md` — the file written by `renderAgentFile` at `render.go:332`. If `renderAgentFile` failed earlier, the file doesn't exist; validator returns its own missing-file error wrapped with `ErrInvalidAgentBody`. Sub-planner verifies the error path doesn't mask the upstream `renderAgentFile` failure (test).
    - Brittleness mitigation per `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § D.4 RISK: the 3-signal AND keeps the validator strict-but-not-fragile. Future tightening (e.g., regex against expected role strings) is explicitly out of scope per the L1 plan's "FULL sentinel-injection-into-Path-B / system-CLAUDE.md test suite ships in Drop 4c.8 W4-D."
  - **ContextBlocks:**
    - `reference` (high): `internal/app/dispatcher/cli_claude/render/render.go:125-179` (`Render` body — D5 wires at line 177-178 boundary). Sub-planner Reads to confirm exact insertion point post-D2/D3.
    - `reference` (high): `internal/app/dispatcher/cli_claude/render/render.go:188-204` (`renderRollback` machinery — D5's failure path uses `rollback.run()` exactly as the other render-step failures do).
    - `reference` (high): `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § D.4 (validator design) + § E.2.5 (test pattern reference) + § E.2.2 (sentinel-injection note — NOT in scope here; D5 is the minimal stand-in).
    - `decision` (LOAD-BEARING — settled at this droplet's authoring per OQ#4 + ROUND-2 W3-FF3 + W3-FF4 + ROUND-3 W3-FF6 redesign): 3-signal AND stub-detection — body length > 200 AND frontmatter delimiters + required keys present AND POSITIVE role-section header present (`"# PLACEHOLDER"` OR `"# Section 0"` OR `"## Role"`). The 3-marker disjunction (W3-FF6) validates today's W1.D1 placeholder set (all 27 use `# PLACEHOLDER` heading per Read-verified evidence) AND post-Drop-4c.8-W4 substantive prompts (`# Section 0` / `## Role`). The 2-marker round-2 form would have build-broken on every placeholder — W3-FF6 flagged this as a HIGH finding. Negative-blacklist Signal C (the F.7.3b stub phrase check) was REPLACED with the positive-presence check (W3-FF3) because a hardcoded blacklist silently fails-open when future drops use different stub wording. The 200-char floor is documented as an ACCEPTED constraint with explicit Drop 4c.8 W4 forward dep (W3-FF8), eliminating the AND-semantics calibration incoherence ROUND-1 plan-QA-falsification 1.4 caught.
    - `constraint` (LOAD-BEARING per HF8): validator MUST be invoked from `Render`'s exit path; the test that proves this is `TestRenderValidatorFailsOnEmptyAgentBody` — it asserts on `Render()`'s return value, not on `validateBundle()` standalone. Without this test, HF8 is unmet and the validator is shipped-but-not-wired.
    - `warning` (normal): brittleness — 3-signal AND is the documented mitigation; future tightening goes to Drop 4c.8 W4-D.
  - **KindPayload:** `{"changes":[{"file":"internal/app/dispatcher/cli_claude/render/render.go","symbol":"validateBundle, ErrInvalidAgentBody, Render","action":"modify","shape_hint":"add validateBundle private function with 3-signal AND stub-detection (Signal A: body length > 200; Signal B: frontmatter delimiters + name + description present; Signal C POSITIVE 3-marker disjunction: body contains \"# PLACEHOLDER\" OR \"# Section 0\" OR \"## Role\" — W3-FF3 redesign from negative-blacklist + W3-FF6 add of \"# PLACEHOLDER\" to validate today's W1.D1 placeholders); declare ErrInvalidAgentBody package-level sentinel; wire validateBundle call into Render's exit path before line 178 return"},{"file":"internal/app/dispatcher/cli_claude/render/render_test.go","symbol":"TestRenderValidatorFailsOnEmptyAgentBody, _OnStubShapeAgentBody, _OnMissingFrontmatterDelimiters, _PassesOnSubstantiveBody, TestRenderAgentFileFrontmatter (existing; preserved + augmented per W3-PF1)","action":"add+preserve","shape_hint":"each new test asserts on Render() return (not validateBundle standalone) to prove HF8 wiring; rollback verification on failure paths; substantive-body fixture includes a \"# PLACEHOLDER\" / \"# Section 0\" / \"## Role\" marker so Signal C passes; existing TestRenderAgentFileFrontmatter at render_test.go:331-364 MUST stay green post-D2/D3 — D3 acceptance contract per W3-PF1 explicitly enumerates this test as preserved-green"}]}`

### Droplet 4c.6.W3.D6 — Doc-comment correction at `render.go:307-319`

- **State:** todo
- **Kind:** `build` (atomic droplet; `Irreducible: true`)
- **Paths:** `internal/app/dispatcher/cli_claude/render/render.go` (MODIFY — rewrite the doc-comment block at lines 307-319 ONLY; the block currently describes the F.7.3b stub-design + claims `~/.claude/agents/<name>.md` is the source of truth via Path B; under `--bare` Path B is collapsed per `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § A.9 — the doc-comment must reflect the post-D2-D3 reality).
- **Packages:** `internal/app/dispatcher/cli_claude/render`.
- **Acceptance:**
  - The doc-comment block at `render.go:307-319` is rewritten to reflect post-D2 / post-D3 reality:
    - REMOVE the claim "The full canonical templates at ~/.claude/agents/<name>.md remain the source of truth for behavior — they are loaded by claude from the system-installed plugin path (Path B per memory §1), not from the per-spawn plugin (Path A)" (lines 311-314).
    - REPLACE with a doc-comment that accurately describes the 3-tier resolution priority order shipped by D2: project `<project>/.tillsyn/agents/<basename>` → user `~/.tillsyn/agents/<group>/<basename>` → embedded `till-<group>/<name>.md`, AND notes that under `--bare` (which Tillsyn always passes per `argv.go:67-91`) the bundle's plugin/agents subtree is the SOLE source — Path B is collapsed per `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § A.9.
    - PRESERVE the two-layer-tool-gating prose at lines 316-319 (frontmatter `disallowedTools` mirrors `binding.ToolsDisallowed` per memory §5; settings.json permissions are the authoritative gate). That information remains accurate post-W3.
    - PRESERVE the "Future evolution" block at lines 321-325 if `SystemPromptTemplatePath` is now consumed by D2 — actually, that block becomes OBSOLETE post-W3 (D2 implements exactly what the block predicted). Sub-planner UPDATES the block to a breadcrumb explicitly enumerating the three landings (LOCKED — ROUND-3 W3-FF11): "(1) Drop 4c F.7.2 landed the `templates.AgentBinding.SystemPromptTemplatePath` schema field at `internal/templates/schema.go:556` with validator at `load.go:1031-1055`; (2) Drop 4c.6 W3.D1 wired the field through `dispatcher.BindingResolved.SystemPromptTemplatePath` in `cli_adapter.go` + populator in `binding_resolved.go`; (3) Drop 4c.6 W3.D2 implemented the 3-tier render-time resolver in `assembleAgentFileBody` consuming `templates.DefaultTemplateFS` with cross-group till-gen fallback. Comment retained as architectural-history breadcrumb until the next refactor." The collapsed form (round-2: "F.7.2 + 4c.6 W3.D2 landed the field-and-resolver-wired version") elided the W3.D1 plumbing landing and obscured the historical sequence — W3-FF11 expansion makes the three discrete commits diagnosable from doc-comment alone.
  - The function-doc-comment on `assembleAgentFileBody` at lines 335-339 is OWNED BY D2 (D2 ships the substantive content; D2 updates the comment as part of its own work). D6 explicitly does NOT touch lines 335-339 — sub-planner asserts via Read post-D2 that those lines are already updated.
  - **`SPAWN_PIPELINE.md:24-31` is OUT OF SCOPE for D6** — sole owner is W6.D4 per ROUND-2 HF3 + L1 plan acceptance bullet "`SPAWN_PIPELINE.md:24-31` rewrite is OUT OF SCOPE for W3 — sole owner is W6.D4 (ROUND-2 HF3 — duplicate-ownership cut)". D6's commit / change set MUST NOT touch `SPAWN_PIPELINE.md`. Sub-planner verifies this in the build-QA pass.
  - The body sentence at line 360 (today's `b.WriteString("Tillsyn-spawned subagent stub. Behavior loaded from the canonical ...")`) is a BODY string, not a doc-comment — that line is OWNED BY D2's stub-replacement, NOT by D6. D6 explicitly does NOT touch line 360.
  - No production-code change in D6 — doc-comment ONLY. No new tests, no test edits.
  - `mage test-pkg ./internal/app/dispatcher/cli_claude/render` passes (no test changes; tests should still all pass since no production behavior changed); `mage ci` green.
  - `git diff` against the merge base for D6 alone shows ONLY changes to `render.go` lines 307-319 (and possibly 321-325 for the Future-evolution block update). No other files touched.
- **Blocked by:** 4c.6.W3.D2, 4c.6.W3.D3, 4c.6.W3.D5 (D6 edits the same `render.go` file as D2/D3/D5; same package compile; same function-block-adjacent doc-comment range — even though D6 is doc-only, serializing it last guarantees D2/D3/D5's function-body edits don't clobber D6's adjacent doc-comment edit, AND lets D6 author the corrected comment with full knowledge of the final post-D2-D3-D5 code shape).
- **Specify (droplet-scope; inherits `SKETCH.md` § 26.W3 + `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § D.5):**
  - **Objective:** Kill the misleading doc-comment that's the original cause of dev confusion (the comment claimed `~/.claude/agents/<name>.md` is "the source of truth for behavior" — `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § C.5 calls this FACTUALLY WRONG under `--bare`). Doc-only edit in a single block; isolated from production-code changes shipped by D2-D5.
  - **AcceptanceCriteria:** see Acceptance bullets above.
  - **ValidationPlan:** `mage test-pkg ./internal/app/dispatcher/cli_claude/render`; `git diff <merge-base>..HEAD -- internal/app/dispatcher/cli_claude/render/render.go` shows only line-307-319 (and possibly 321-325) changes; `git diff <merge-base>..HEAD -- workflow/ SPAWN_PIPELINE.md CLI_ADAPTER_AUTHORING.md` shows zero `SPAWN_PIPELINE.md` hits (HF3 verification); `mage ci` green.
  - **RiskNotes:**
    - **HF3 boundary**: `SPAWN_PIPELINE.md:24-31` is W6.D4's responsibility; D6 must NOT edit that file. Sub-planner verifies in the diff post-build that `SPAWN_PIPELINE.md` is untouched. If a builder accidentally edits it, build-QA-falsification catches and routes back per the L1 plan's HF3 fix.
    - The function-doc-comment for `assembleAgentFileBody` at lines 335-339 is D2's territory (D2 ships the substantive replacement; D2 updates the comment in lockstep). D6 does NOT touch it. Sub-planner reads post-D2 to confirm before authoring D6.
    - The body string at line 360 ("Tillsyn-spawned subagent stub. Behavior loaded from the canonical ...") is D2's territory (it's the actual stub body D2 replaces). D6 does NOT touch it.
    - Doc-only droplet has no test coverage by design — no behavior change. `mage ci` is green by transitivity from D5's test pass.
    - Future-evolution block at lines 321-325 (LOCKED — ROUND-3 W3-FF11): that block predicts exactly what D2 ships ("System-prompt template path read from binding.SystemPromptTemplatePath"). D6's wording for that block enumerates the three discrete landings: (1) Drop 4c F.7.2 schema field; (2) Drop 4c.6 W3.D1 BindingResolved plumbing; (3) Drop 4c.6 W3.D2 render-time 3-tier resolver. Round-2 collapsed form ("F.7.2 + 4c.6 W3.D2 landed the field-and-resolver-wired version") elided the W3.D1 plumbing landing — the expanded form keeps the historical sequence diagnosable from doc-comment alone. Sub-planner picks the exact wording per Tillsyn's doc-comment style; the three-landing structure is required.
  - **ContextBlocks:**
    - `reference` (high): `internal/app/dispatcher/cli_claude/render/render.go:307-319` (the doc-comment block to rewrite); sub-planner Reads post-D2-D3-D5 to confirm exact line numbers haven't shifted.
    - `reference` (high): `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` § D.5 (the recommended replacement wording); § A.9 + § C.5 (the factual basis for the rewrite).
    - `constraint` (LOAD-BEARING per HF3): D6 MUST NOT edit `SPAWN_PIPELINE.md`. W6.D4 is the sole owner of that file's doc-comment fix.
    - `constraint` (high): D6 is doc-only; no production code changes; no test changes.
    - `decision` (LOCKED — ROUND-3 W3-FF11): Future-evolution block at lines 321-325 retained as architectural-history breadcrumb with three-landing-enumerated wording: (1) Drop 4c F.7.2 schema field; (2) Drop 4c.6 W3.D1 BindingResolved plumbing; (3) Drop 4c.6 W3.D2 render-time resolver. The 3-landing structure is required — the round-2 collapsed form elided the W3.D1 plumbing step.
    - `warning` (normal): if `git diff` post-build shows ANY non-doc-comment change in D6's commit, build-QA-falsification flags as scope creep.
  - **KindPayload:** `{"changes":[{"file":"internal/app/dispatcher/cli_claude/render/render.go","symbol":"renderAgentFile (doc-comment lines 307-319) + Future-evolution block (lines 321-325)","action":"modify","shape_hint":"rewrite doc-comment to reflect 3-tier resolver + --bare collapses Path B; preserve two-layer tool-gating prose; update Future-evolution block per W3-FF11 to enumerate three discrete landings — (1) Drop 4c F.7.2 schema field, (2) Drop 4c.6 W3.D1 BindingResolved plumbing, (3) Drop 4c.6 W3.D2 render-time resolver — round-2 collapsed form elided W3.D1; expanded form keeps historical sequence diagnosable from doc-comment alone"}]}`

## Round-3 Accepted NITs (W3-FF13, W3-FF14, W3-FF15)

The round-3 plan-QA-falsification pass raised three NIT-severity findings that the orchestrator ACCEPTED as documented (no plan amendment) per the falsification agent's own recommendation that "W3-FF13/14/15 negotiable as accepted NITs." Recorded here for audit-trail completeness:

- **W3-FF13 [NIT — Signal C unanchored substring match]**: D5 Signal C uses `strings.Contains` semantics on the 3-marker disjunction (`"# PLACEHOLDER"` OR `"# Section 0"` OR `"## Role"`). A 220+ char stub body that QUOTES one of these markers inside backticks or as prose would pass Signal C falsely. **Accepted because**: the marker-quoted-in-prose stub scenario is contrived (it requires deliberately crafting a stub body that quotes the Section 0 / Role / PLACEHOLDER convention). Signal A (length floor) + Signal B (frontmatter intact) catch the common-case stubs. D5 builder has discretion to upgrade to line-anchored matching (`strings.HasPrefix(line, ...)`) if implementation reads cleaner. Post-build refinement candidate.
- **W3-FF14 [NIT — `## Role` marker has no forward-pinned use case]**: of the 3-marker disjunction, `# PLACEHOLDER` validates W1.D1 placeholders today, `# Section 0` validates Drop 4c.8 W4 substantive prompts (per `SEMI-FORMAL-REASONING.md`), and `## Role` is speculative-future-flexibility with no W4 PLAN.md anchor. **Accepted because**: the third marker is forward-flex hedge with negligible runtime cost (one extra substring scan); dropping it later if no use case emerges is trivial; keeping it now allows W4 prompt-authoring to use a `## Role` heading without a Signal C false-fail.
- **W3-FF15 [NIT — W3-FF9 forward-dep on W0/W0.5 validator warning unenforced]**: round-3 W3-FF9 Option B documents a forward dep on `validateAgentBindingNames` at `internal/templates/load.go:1031-1055` adding a warning for `model = ""` user-error case, but W0/W0.5 are SHIPPED drops — no future drop is contracted to add the warning. **Accepted because**: the empty-string-model case is a degenerate user-error that the round-2 strip predicate `*binding.Model != ""` already handles correctly at runtime (silent passthrough = "use whatever is rendered into the agent .md," which equals "no override"). Logged as a refinement candidate for a future drop's W0/W0.5 validator backfill; not a W3 blocker.

