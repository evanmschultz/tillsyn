# Plan-QA-Proof Round 2 — Drop 4c.6

**Reviewer:** plan-QA-proof subagent (Round 2)
**Authored:** 2026-05-09
**Plan under review:** `workflow/drop_4c_6/PLAN.md` (revised at commit `95ebe58` — "docs(drop-4c.6): planner round 2 apply qa findings")
**Round-1 verdict files (durable, NOT re-reviewed):** `workflow/drop_4c_6/PLAN_QA_PROOF.md` (Round 1, 6 findings), `workflow/drop_4c_6/PLAN_QA_FALSIFICATION.md` (Round 1, 7 counterexamples + 1 borderline)
**Sketch source-of-truth:** `workflow/drop_4c_6/SKETCH.md` v2.8.4 POST-QA FINAL (LOCKED, NOT re-QA'd).
**Scope:** Half A — verify Round-1 findings actually fixed in revised PLAN.md + `_BLOCKERS.toml`. Half B — fresh 5-axis proof pass on the revised plan to catch any defects introduced by the round-2 edits.

## 1. Round-1 Findings Verification

- 1.1 **HF1 (parallelization-graph, high) — W1.D1 ↔ W5.D1 missing blocker on `internal/templates/embed.go`.** **FIXED.** PLAN.md line 178 now declares `Blocked by: 4c.6.W1.D1 (ROUND-2 HF1 — both edit internal/templates/embed.go + embed_test.go AND share internal/templates package compile/test unit; W1.D1 lands the embed-list extension first ... W5.D1 then rebases its rename onto the post-W1.D1 directive. Transitively chains to 4c.6.W0.5 via W1.D1's blocker).` Evidence pointer: PLAN.md line 178 + `_BLOCKERS.toml` lines 42-45 (matching `[[blockers]]` row with `node = "4c.6.W5.D1"`, `blocked_by = ["4c.6.W1.D1"]`). The `_BLOCKERS.toml` reason-string is consistent with PLAN.md's inline bullet — no drift. Wave A list at PLAN.md line 357-359 correctly excludes W1.D1 + W5.D1 (the comment at `_BLOCKERS.toml` line 20-25 explicitly calls out the Wave-A demotion). Cycle check on the new edge: W0.5 (no blockers) → W1.D1 → W5.D1 → … no cycle introduced.

- 1.2 **HF2 (parallelization-graph, high) — `internal/templates` package serialization gap.** **FIXED.** The full chain `W0.5 → W1.D1 → W5.D1 → W5.D2 → W5.D3` is now wired end-to-end. Evidence pointer: PLAN.md line 101 (W1.D1 `Blocked by: 4c.6.W0.5 (ROUND-2 HF2 — both edit internal/templates Go package, must serialize on package compile/test unit ...)`); line 178 (W5.D1 → W1.D1, transitively → W0.5); line 209 (W5.D2 `Blocked by: 4c.6.W5.D1 (both droplets edit internal/templates/embed.go + embed_test.go — package-lock chain)`); line 233 (W5.D3 → W5.D1, W5.D2, W1.D1). `_BLOCKERS.toml` lines 27-30 + 42-55 mirror all four edges with consistent reason-strings. Graph summary at PLAN.md lines 355-378 documents the chain explicitly with the line `4c.6.W1.D1 → 4c.6.W0.5 (ROUND-2 HF1+HF2 — internal/templates package serialization head)` and the round-2 implications block at lines 378. Wave A list (line 372) correctly removes W1.D1 from Wave-A heads. No package-compile race remains for `internal/templates`.

- 1.3 **HF3 (multi-level-decomposition, medium) — W3 ↔ W6.D4 duplicate ownership of `SPAWN_PIPELINE.md:24-31`.** **FIXED** (cut option chosen: W6.D4 owns SPAWN_PIPELINE.md; W3 stops at `render.go:307-319`). Evidence pointers (5 sites, all consistent):
  - PLAN.md line 5 (paths line): `SPAWN_PIPELINE.md (existing, doc-only updates — W6.D4 sole owner), CLI_ADAPTER_AUTHORING.md (existing, doc-only updates — W6.D4 sole owner)` — explicit ownership annotation.
  - PLAN.md line 141 (W3 Scope): `(D.5) doc-comment corrections at render.go:307-319 ... ONLY; **SPAWN_PIPELINE.md:24-31 rewrite is W6.D4's sole responsibility** (ROUND-2 HF3).`
  - PLAN.md line 148 (W3 Acceptance): `Doc-comment corrections at render.go:307-319 (Go-source doc-comment adjacent to the resolver work) reflect ... §D.5 ... **SPAWN_PIPELINE.md:24-31 rewrite is OUT OF SCOPE for W3** — sole owner is W6.D4 (ROUND-2 HF3 — duplicate-ownership cut; W6.D4's Blocked by: 4c.6.W3 ordering is justified by "doc reflects W3-shipped reality," not "doc finishes what W3 started").`
  - PLAN.md line 153 (W3 L2 sub-planner directive D6): `D6 doc-comment corrections at render.go:307-319 ONLY (Go-source side; SPAWN_PIPELINE.md:24-31 is W6.D4's sole responsibility per ROUND-2 HF3 — strip from D6 scope).`
  - PLAN.md line 318 (W6.D4 Acceptance): `SPAWN_PIPELINE.md:24-31 — the prior "two paths" framing — rewritten to reflect --bare-collapsed isolation per RESEARCH/ISOLATION_ENFORCEMENT_FIX.md §D.5.` (W6.D4 retains sole ownership.)
  - `_BLOCKERS.toml` line 65 reason-string: `ROUND-2 HF3 — W6.D4 is SOLE OWNER of SPAWN_PIPELINE.md:24-31 + CLI_ADAPTER_AUTHORING.md doc-comment corrections (W3's previously-claimed SPAWN_PIPELINE.md scope was stripped to eliminate duplicate ownership).`
  No remaining duplicate-claim text anywhere in PLAN.md or `_BLOCKERS.toml`.

- 1.4 **HF4 (parallelization-graph, medium — also Round-1 falsification 1.7 high) — `cli_adapter.go` (correct file for `BindingResolved` struct definition) missing from W3 path list.** **FIXED.** Evidence pointers:
  - PLAN.md line 5 (drop-level paths): `internal/app/dispatcher/cli_adapter.go (BindingResolved struct definition site — ROUND-2 HF4 verified), internal/app/dispatcher/cli_adapter_test.go (BindingResolved struct-construction tests)`.
  - PLAN.md line 141 (W3 Scope): `propagate SystemPromptTemplatePath through BindingResolved (struct definition site is internal/app/dispatcher/cli_adapter.go:102, NOT binding_resolved.go — ROUND-2 HF4 verified; W3 paths therefore include cli_adapter.go + cli_adapter_test.go).`
  - PLAN.md line 153 (W3 L2 sub-planner directive D1): `D1 propagate SystemPromptTemplatePath through BindingResolved — NOTE: BindingResolved struct lives at internal/app/dispatcher/cli_adapter.go:102, NOT binding_resolved.go (ROUND-2 HF4 verified via git grep -n 'type BindingResolved' internal/); D1 paths therefore include internal/app/dispatcher/cli_adapter.go + internal/app/dispatcher/cli_adapter_test.go alongside binding_resolved.go / binding_resolved_test.go.`
  Verified independently via `git grep -n "type BindingResolved" internal/` against current HEAD `08e3507`: returns ONLY `internal/app/dispatcher/cli_adapter.go:102:type BindingResolved struct {`. Planner's claim is correct. The L2 W3 sub-planner now sees the right file in its inherited paths union.

- 1.5 **HF5 (shipped-but-not-wired, low) — W3 post-render validator wiring implicit.** **FIXED.** Evidence pointer: PLAN.md line 147 (W3 Acceptance bullet): `Post-render validator is **wired into the render path at render.Render's exit** (or whichever render call site produces the bundle) — NOT shipped as a separate exported helper that no caller invokes (ROUND-2 HF8 — wiring contract sharpened to prevent shipped-but-not-wired anti-pattern per feedback_tillsyn_enforces_templates.md). The wired validator errors out if <bundle>/plugin/agents/<name>.md is empty, is the OLD F.7.3b stub shape, or is missing name / description frontmatter; minimal sentinel-style assertion in render_test.go exercises the failure path.` PLAN.md line 153 (L2 sub-planner directive D5) carries the same wiring contract: `the validator MUST be wired into render.Render's exit path (or whichever render call site produces the bundle), NOT shipped as an unwired exported helper, so every spawned bundle is validated before the spawn proceeds`. The wiring is now a testable acceptance bullet, not implicit. (Note: planner re-numbered the finding as "HF8" in inline annotations — same fix, different label for round-2 audit-trail purposes; the durable Round-1 reference is still `HF5` per PLAN_QA_PROOF.md.) Open Question #4 disposition (line 423) confirms the L2 sub-planner gets to settle the concrete signal-thresholds (body length + frontmatter presence + stub-key-phrase absence) — the L1 contract pins the wiring-vs-helper question.

- 1.6 **HF6 (specify-block-well-formedness, low) — W6.D2 "Plan Down Build Up section LEADS" not testable.** **FIXED.** Evidence pointer: PLAN.md line 278 (W6.D2 Acceptance bullet): `The first ## H2 section after the H1 title is ## Plan Down, Build Up per SKETCH.md § 26.W6 ContextBlocks constraint(high) — testable via doc inspection / awk-or-equivalent first-H2 check (ROUND-2 HF9 — pinned to grep-able assertion to replace the un-testable "leads the doc" phrasing).` This is now a grep-able / `awk`-able assertion (`awk '/^## /{print; exit}'` would return `## Plan Down, Build Up` against the shipped doc). Plan-QA on the eventual builder output has an objective acceptance to grade against. The sibling concern (line 277 — placeholder structure for OTHER `##` sections) remains as a list of named sections rather than fully ordered, but the "leads" specific concern flagged in HF6 is sharply addressed. (Same note as HF5: planner re-numbered as "HF9" in inline annotations — same fix.)

## 2. Fresh Findings

Fresh 5-axis proof pass on the revised plan looking for defects introduced by the Round-2 edits.

- 2.1 [Axis: atomic-decomposition] — exhausted, no fresh finding. Each direct droplet (W1.D1, W5.D1, W5.D2, W5.D3, W6.D1, W6.D2, W6.D3, W6.D4, W6.D5) carries its own `Specify` block with Objective + AcceptanceCriteria + ValidationPlan + RiskNotes + ContextBlocks + KindPayload. W1.D1's ~24-file footprint (21 placeholders + agents.example.toml + 2 embed files) was Round-1 FALSIFICATION 1.8 (borderline / low / mechanical-bundle defensibility argument applies) — Round-2 didn't address it; that's the round-2 falsification-side's call, not a fresh proof finding.

- 2.2 [Axis: parallelization-graph] — exhausted, no fresh finding. The new total order on `internal/templates` (W0.5 → W1.D1 → W5.D1 → W5.D2 → W5.D3) is acyclic, fully reflected in BOTH PLAN.md inline `Blocked by:` bullets AND `_BLOCKERS.toml` `[[blockers]]` rows, with consistent reason-strings. The newly added `internal/app/dispatcher/cli_adapter.go` + `cli_adapter_test.go` paths to W3 do NOT collide with any other droplet — `internal/app/dispatcher` package is W3-exclusive in this drop (verified by walking every other droplet's `Paths:` line; no other touches that package). The W6.D5 "Wave E" annotation at line 376 correctly notes that W6.D5 is technically launchable as soon as Wave B closes (since W6.D2 and W6.D3 are Wave A and likely complete by then) — the planner accurately documents the conservatism of placing it in Wave E for graphical clarity. No new graph defect introduced by round-2 edits.

- 2.3 [Axis: specify-block-well-formedness] — exhausted, no fresh finding. The HF5-fix bullet at line 147 is a single concrete acceptance claim with multiple testable sub-clauses (wired into `render.Render`'s exit; errors on empty/stub/missing-frontmatter; sentinel-style assertion in `render_test.go` exercises the failure path). The HF6-fix bullet at line 278 is a `grep`-able assertion. The HF4-fix paths line at line 5 carries inline annotation (`BindingResolved struct definition site — ROUND-2 HF4 verified`) that directly cites the verification method. All other Specify blocks (W1.D1, W5.D1, W5.D2, W5.D3, W6.D1, W6.D2, W6.D3, W6.D4, W6.D5) are unchanged from Round 1 except where round-2 edits touched them, and Round-1 proof did not flag them.

- 2.4 [Axis: multi-level-decomposition] — exhausted, no fresh finding. Round-2 correctly cut SPAWN_PIPELINE.md ownership from W3 in 4 mutually-consistent locations (paths line, Scope, Acceptance, L2 directive D6). The L2 W3 sub-planner directive line 153 D6 explicitly says `render.go:307-319 ONLY (Go-source side; SPAWN_PIPELINE.md:24-31 is W6.D4's sole responsibility per ROUND-2 HF3 — strip from D6 scope)`. No remaining duplicate-claim text anywhere. The L2 directives for W2 (line 132) and W3 (line 153) appropriately delegate sub-droplet decomposition to the L2 planner without over-specifying — the planner-down rule is honored.

- 2.5 [Axis: shipped-but-not-wired] — exhausted, no fresh finding. The new W5.D1 + W5.D2 caller-audit lists at lines 168-176 + 195-201 were verified against current HEAD via `git grep -c "default-go.toml" cmd/ internal/` and `git grep -c "default-generic.toml" cmd/ internal/`:
  - `internal/app/template_service.go` — ZERO refs for both names (correctly REMOVED from caller-audit list per ROUND-2 HF6 disposition).
  - `internal/app/auto_generate_steward.go` — ZERO refs for both names (correctly REMOVED).
  - `internal/adapters/server/common/mcp_surface.go:906` — 1 ref to `default-go.toml` (correctly INCLUDED at line 172).
  - `internal/adapters/server/common/mcp_surface.go:908` — 1 ref to `default-generic.toml` (correctly INCLUDED in W5.D2 path list).
  - `internal/app/auto_generate_steward_test.go:18` — 1 ref to BOTH names (correctly INCLUDED in W5.D1 + W5.D2 caller-audit lists).
  - `internal/app/service_test.go` — 7 hits including the load-bearing `filepath.Join("..", "templates", "builtin", "default-go.toml")` literal at :6534 (correctly INCLUDED with the line-number callout at line 170; spec also correctly identifies :6537 as an error-message string and lines 6525, 6529, 6551, 6552, 6713 as forward-looking comments).
  - `internal/app/service.go:383` and :384 — both correctly INCLUDED (line 169 for default-go, line 196 for default-generic).
  All caller-audit claims match `git grep` output against current HEAD. The HF5 wiring contract for the post-render validator (line 147) explicitly forbids the unwired-helper anti-pattern. No new shipped-but-not-wired risk introduced by round-2 edits.

## 3. Missing Evidence

- 3.1 **Round-1 missing-evidence routes 2.1-2.4 from PLAN_QA_PROOF.md remain routed to the L2 sub-planners** — these were already documented as L2-deferred decisions and Round-2 didn't touch them. Specifically: 2.1 (W2 `cmd/till/help_test.go` test scope) routed to L2 W2 sub-planner; 2.2 (W3 sentinel-style integration test file location) routed to L2 W3 sub-planner; 2.3 (W0 frontmatter strip helper package home) routed to L2 W0 sub-planner; 2.4 (W0.5 validator ordering) routed to L2 W0.5 sub-planner. These are out-of-scope for this Round-2 review (L2 deliberately settles them). No fresh missing-evidence introduced by Round-2 edits.

## 4. Summary

**Verdict: pass**
**Round-1 findings fixed: 6/6 (HF1 high, HF2 high, HF3 medium, HF4 medium, HF5 low, HF6 low — ALL FIXED with explicit evidence pointers).**
**Fresh findings: 0**

All six Round-1 findings are addressed in revised PLAN.md + `_BLOCKERS.toml` with consistent multi-site annotations. The new `internal/templates` serialization chain (W0.5 → W1.D1 → W5.D1 → W5.D2 → W5.D3) is acyclic, mirrored in both PLAN.md and `_BLOCKERS.toml`, and Wave-A heads correctly demoted. The HF3 SPAWN_PIPELINE.md ownership cut is reflected in 5 mutually-consistent locations. The HF4 `cli_adapter.go` path additions are independently verified against current HEAD via `git grep -n "type BindingResolved"`. The HF6 caller-audit list regeneration matches `git grep -c` exactly across all 5 sites — `template_service.go` + `auto_generate_steward.go` correctly removed (zero refs), `mcp_surface.go` correctly added (1 ref), `service_test.go:6534` correctly identified as load-bearing path literal. Fresh 5-axis proof pass finds no new defects introduced by Round-2 edits. The plan is ready for build-phase entry pending parallel falsification-side Round-2 verdict.

## 5. Hylla Feedback

N/A — review touched non-Go artifacts only (PLAN.md, `_BLOCKERS.toml`, SKETCH.md, Round-1 verdict files). Hylla is Go-only today; no Hylla queries were made or required. Verification queries against committed Go source went through `git grep` (allowed-list Bash) on current HEAD `08e3507` for: `type BindingResolved` (single result confirming HF4 file location), `default-go.toml` caller-audit count (matched plan claims at 5 sites), `default-generic.toml` caller-audit count (matched plan claims at 4 sites). No fallback miss to log.
