# Plan-QA-Falsification Round 1 — Drop 4c.6

**Verdict:** fail

**Round:** 1

**Plan under attack:** `workflow/drop_4c_6/PLAN.md` (411 lines, authored 2026-05-09 by `go-planning-agent`).

**Sketch source-of-truth (locked, NOT under attack):** `workflow/drop_4c_6/SKETCH.md` v2.8.4 POST-QA FINAL.

## 1. Findings

- 1.1 [Family: concurrency-blocked_by] [severity: high] **W1.D1 ↔ W5.D1 share `internal/templates/embed.go` + `internal/templates/embed_test.go` without `blocked_by`.** Both droplets are declared Wave A heads (no blockers — see PLAN.md graph summary lines 345-346). W1.D1 paths (line 93) and W5.D1 paths (line 161) BOTH list `internal/templates/embed.go` and `internal/templates/embed_test.go`. W1.D1 extends the `//go:embed` directive list; W5.D1 rewrites the same directive (rename `default-go.toml` → `till-go.toml`). Per `WORKFLOW.md` Phase 1 step 5 + Phase 2 step 1: "Droplets sharing a package MUST have explicit `blocked_by` between them" — file-level conflict is even sharper. Repro: dispatch both droplets in parallel from drop start; both edits race the same `//go:embed` line in `embed.go:34`. Fix hint: add explicit `blocked_by: 4c.6.W1.D1` to W5.D1 (W5.D1 should land AFTER W1.D1 because W5.D1's caller-audit fixture references `internal/templates/embed.go` lines 16-23 + 34 + 136-138 + 178 — those line offsets shift once W1.D1 extends the embed list). Mirror the new entry in `_BLOCKERS.toml`.

- 1.2 [Family: concurrency-blocked_by] [severity: high] **W1.D1 ↔ W5.D2 share `internal/templates/embed.go` + `internal/templates/embed_test.go` without transitive `blocked_by` from W1.D1.** W5.D2 paths (line 187) include `internal/templates/embed.go` and `internal/templates/embed_test.go`. W5.D2's only blocker is W5.D1 (line 195). Since W5.D1 itself has no blocker on W1.D1 (counterexample 1.1), W5.D2 has no transitive ordering vs W1.D1. Repro: with current PLAN.md, W5.D2 can dispatch as soon as W5.D1 closes, even if W1.D1 is still building — both then race on `embed.go`. Fix hint: once 1.1 is fixed by adding `blocked_by: 4c.6.W1.D1` to W5.D1, W5.D2's transitive chain via W5.D1 satisfies this. (Alternative: also add direct `blocked_by: 4c.6.W1.D1` to W5.D2 for defence-in-depth.)

- 1.3 [Family: concurrency-blocked_by] [severity: high] **L1 sibling package-overlap on `internal/templates` without blocked_by chain.** PLAN.md line 6 declares `internal/templates` as an expected package. W0.5 (sub-plan, edits `load.go` per line 76), W1.D1 (edits `embed.go` per line 93), W5.D1 (edits `embed.go` per line 161), W5.D2 (edits `embed.go` per line 187), W5.D3 (edits `till-go.toml` + `till-gen.toml` + agent .md placeholders per line 211) ALL touch the `internal/templates` package. Per CLAUDE.md "File- and package-level blocking" + "Package-level locking exists because a single Go package shares one compile — editing different files in the same package still breaks the other agent's test run." Current `blocked_by` chain only links W5.D1→W5.D2 and W5.D1+W5.D2+W1.D1→W5.D3; W0.5 and W1.D1 and W5.D1 are mutually parallel. Repro: dispatch W0.5's L2 droplet (edits `load.go`) in parallel with W1.D1 (edits `embed.go` + `embed_test.go`) — `mage test-pkg ./internal/templates` will fail intermittently for one or both because they share one compile unit + test set. Fix hint: serialize all `internal/templates`-touching siblings via a chain (e.g. W0.5 → W1.D1 → W5.D1 → W5.D2 → W5.D3) OR carve out per-file locks if WORKFLOW.md's mechanism supports it (today it does not — package-lock is the unit). Alternative: add explicit `blocked_by` between the relevant pairs and document why parallel execution is unsafe in PLAN.md's "Sub-plan vs direct-droplet ratio" section.

- 1.4 [Family: concurrency-blocked_by] [severity: medium] **W3 acceptance + W6.D4 acceptance both ship corrections to `SPAWN_PIPELINE.md:24-31`; scope overlap is ambiguous.** W3 acceptance bullet 5 (line 148): "Doc-comment corrections at `render.go:307-319` + `SPAWN_PIPELINE.md:24-31` reflect `--bare`-collapsed isolation per `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` §D.5." W6.D4 acceptance (line 304): "`SPAWN_PIPELINE.md:24-31` — the prior 'two paths' framing — rewritten to reflect `--bare`-collapsed isolation per `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` §D.5." Same lines, same corrected framing, two different droplets. W6.D4 IS `blocked_by W3` so the file-lock is chained, but the SCOPE-overlap means either (a) W3.D6 ships the rewrite and W6.D4's first acceptance bullet is dead-code (only `CLI_ADAPTER_AUTHORING.md` addition is genuine W6.D4 work), or (b) W3 stops at code-side `render.go:307-319` only and W6.D4 owns `SPAWN_PIPELINE.md:24-31` exclusively — but the plan's W3 acceptance text explicitly claims SPAWN_PIPELINE.md correction. Repro: read W3 acceptance line 148 + W6.D4 acceptance line 304 — they overlap textually. Fix hint: split the contracts unambiguously — recommended: (a) strip `SPAWN_PIPELINE.md:24-31` from W3 acceptance bullet 5 (W3 owns code-side `render.go:307-319` only); (b) leave W6.D4 as the sole owner of `SPAWN_PIPELINE.md:24-31` + `CLI_ADAPTER_AUTHORING.md` rewrite. This is the cleaner cut because W6.D4's `blocked_by W3` is justified by "doc reflects W3-shipped reality" — not "doc finishes what W3 started."

- 1.5 [Family: contract-mismatch] [severity: medium] **W5.D1 acceptance internally contradicts on `git grep "default-go.toml"` outcome.** PLAN.md lines 167-168: bullet 4 says `git grep "default-go.toml"` returns zero hits in `cmd/`, `internal/`, top-level Go files; bullet 5 says "historical references in droplet doc-comments (Drop 4c.5 F.2.1 mentions) retained verbatim because they describe past state." If historical doc-comments are retained, `git grep "default-go.toml"` will NOT return zero hits in `cmd/` + `internal/` because those historical comments live in `internal/templates/embed.go` (lines 16-23 confirmed via Read), `internal/templates/load.go` (3 hits), `internal/templates/load_test.go` (2 hits), and likely the caller-audit doc-comments too. Repro: run `git grep "default-go.toml" cmd/ internal/` post-droplet on the proposed final state with retained historical comments — non-zero hits. Verified today: `git grep -c "default-go.toml" cmd/ internal/` reports `internal/templates/load.go:3`, `internal/templates/load_test.go:2`, `internal/templates/embed.go:5` — the 5 in `embed.go` includes 3 historical doc-comment references at lines 16-23 that the plan explicitly says to retain. Fix hint: weaken bullet 4 to "`git grep "default-go.toml"` returns ONLY historical-rename-comment hits (in droplet doc-comments) — no live string-literal references in switch cases, embed directives, or BuiltinTemplateNames literals." Or strengthen bullet 5 to require all historical references be migrated to a single rename-history doc-comment block (and remove from individual files).

- 1.6 [Family: hidden-coupling] [severity: medium] **W5.D1 caller-audit list is both over-claimed (1 file with no refs) AND incomplete (5 files with refs, omitted).** PLAN.md line 5 + line 161 list the 4 caller-audit files as `internal/app/auto_generate_steward.go`, `internal/app/service.go`, `internal/app/template_service.go`, `internal/adapters/server/mcpapi/extended_tools.go`. Verified today via `git grep -c "default-go.toml" cmd/ internal/`:
  - `internal/app/template_service.go` — **ZERO references** (over-claimed).
  - `internal/adapters/server/common/mcp_surface.go:906` — 1 hit, **NOT in plan's caller-audit list**.
  - `internal/app/auto_generate_steward_test.go:18` — 1 hit (test file), **NOT in plan's list** (plan only lists the non-test counterpart).
  - `internal/app/service_test.go` — 7 hits including a literal path string at line 6534 (`filepath.Join("..", "templates", "builtin", "default-go.toml")`), **NOT in plan's list**.
  - `internal/templates/load.go` — 3 hits in doc-comments.
  - `internal/templates/load_test.go` — 2 hits in doc-comments.
  Of these, `internal/app/service_test.go:6534` is load-bearing (filesystem-path literal — will break at test run if not updated). Repro: run the rename per plan, run `mage test-pkg ./internal/app` — `service_test.go:6537` errors with "read default-go.toml: no such file." Fix hint: replace `internal/app/template_service.go` with `internal/adapters/server/common/mcp_surface.go` in the caller-audit list; explicitly add `internal/app/service_test.go` to the W5.D1 paths field (line 5 + line 161); add `internal/app/auto_generate_steward_test.go` for doc-comment update; classify `internal/templates/load.go` + `load_test.go` doc-comments as historical (retained per the rule) or forward-looking (updated). Audit MUST happen before W5.D1 builder spawns or the rename ships broken.

- 1.7 [Family: hidden-coupling] [severity: high] **W3's path list omits `internal/app/dispatcher/cli_adapter.go`, where the `BindingResolved` struct is actually defined.** W3 acceptance bullet 1 (line 143): "`dispatcher.BindingResolved.SystemPromptTemplatePath` field exists and is populated by the resolver." W3 paths line 5 + line 141 list `internal/app/dispatcher/binding_resolved.go` + `binding_resolved_test.go`. Verified today: `git grep -n "type BindingResolved" internal/` returns ONLY `internal/app/dispatcher/cli_adapter.go:102:type BindingResolved struct` — and `binding_resolved.go:16` carries an explicit cross-reference: "Type definitions: internal/app/dispatcher/cli_adapter.go (F.7.17.2)." Adding `SystemPromptTemplatePath string` to `BindingResolved` requires editing `cli_adapter.go` (struct definition) and likely `cli_adapter_test.go` (struct-construction tests). Repro: the W3 sub-planner spawns against the L1 path list, sees `binding_resolved.go` + `binding_resolved_test.go` only, and either (a) cannot add the field at all (struct is in another file), or (b) declares the file outside the path list illegally per the post-Drop-1 paths-discipline rule. Fix hint: extend W3 paths to include `internal/app/dispatcher/cli_adapter.go` and `internal/app/dispatcher/cli_adapter_test.go`. Update PLAN.md line 5 + W3 sub-plan-container `Scope` paragraph (line 141) accordingly. Update `_BLOCKERS.toml` if any other droplet shares `cli_adapter.go` (none currently do — but plan-QA-falsification cannot guarantee that without re-walking all paths).

- 1.8 [Family: atomicity] [severity: low] **W1.D1 file-count footprint (~24 files) materially exceeds CLAUDE.md's "≤3 files" droplet ceiling.** W1.D1 paths (line 93): 21 NEW placeholder agent .md files + 1 NEW `agents.example.toml` + modifications to `internal/templates/embed.go` and `internal/templates/embed_test.go` = ~24 files. CLAUDE.md "Package Map" + planning-agent.md "Atomic Droplet Sizing" specify ≤3 files as a soft ceiling for typical droplets. The mechanical-similarity argument (line 41: "Placeholder agent files + `agents.example.toml` shipping is one mechanical embed-extension droplet; no semantic logic") is defensible — each placeholder body is uniform, and a single builder can complete the work cleanly — but the footprint is ~8x the ceiling. NOT a hard CONFIRMED FAIL because the planner explicitly justified the bundle in the L1 reasoning table; flagged for plan-QA discussion. Repro: count files in W1.D1 paths field — 24 entries. Fix hint: optional split into W1.D1.a (ship 21 placeholder .md files + uniform-body fixture pattern) + W1.D1.b (extend `//go:embed` list + extend `embed_test.go`) — the embed-list extension is the ONE semantic Go-source change; the .md files are pure data. Splitting also makes 1.1's blocker-chain cleaner: the embed.go-touching half of W1.D1 chains with W5.D1, while the placeholder-shipping half can run parallel with W5.D1 (since they only touch .md files in a NEW dir). If the planner chooses to keep W1.D1 unified, document the mechanical-bundle exception explicitly in PLAN.md per the planning-agent's atomicity rules.

## 2. Counterexamples (CONFIRMED summary)

Counterexamples 1.1, 1.2, 1.3, 1.4, 1.5, 1.6, 1.7 are CONFIRMED counterexamples with concrete repro. Counterexample 1.8 is borderline — flagged as `low-severity` per the noise-vs-CONFIRMED rule (mechanical-bundle defensibility argument is genuine).

**Cycle check:** walked every `blocked_by` edge — no cycles found. Wave-A heads (W0, W0.5, W1.D1, W5.D1, W6.D2, W6.D3) all reach termination through Wave B/C/D successors. REFUTED.

**`_BLOCKERS.toml` / PLAN.md drift:** every `[[blockers]]` row in `_BLOCKERS.toml` matches an inline `Blocked by:` bullet in PLAN.md and vice-versa. Wave-A heads are correctly absent from `_BLOCKERS.toml`. REFUTED.

## 3. Summary

**Verdict:** **fail** (7 CONFIRMED counterexamples + 1 low-severity borderline).

**Rationale:** the plan accurately mirrors the SKETCH wave decomposition and routes most known-deferred concerns to the Open-Questions section. However, Wave-A's parallelization claim collides with three concrete file/package overlaps (W1.D1 ↔ W5.D1 + W5.D2 + W0.5 all touch `internal/templates` without `blocked_by`), which `WORKFLOW.md` Phase 2 step 1 calls out as the canonical falsification vector. The W3 path list omits the file (`cli_adapter.go`) where the struct under modification actually lives. W5.D1's caller-audit list both over-claims one file and silently omits five with live references — including `service_test.go:6534`'s filesystem-path literal that will break the test at run. Three counterexamples are **high-severity blockers** (1.1, 1.3, 1.7); four are **medium-severity** drift / contract clarifications (1.2, 1.4, 1.5, 1.6); one is **low-severity** atomicity advisory (1.8). The plan is salvageable with targeted fixes: re-wire the `internal/templates` chain, expand the W3 + W5.D1 path lists, disambiguate W3 vs W6.D4 SPAWN_PIPELINE.md scope. Re-spawn planner for round 2 with these findings.

| Family | Result |
| --- | --- |
| A1 concurrency-blocked_by | **CONFIRMED** (1.1 high, 1.2 high, 1.3 high, 1.4 medium) |
| A2 contract-mismatch | **CONFIRMED** (1.5 medium) |
| A3 hidden-coupling | **CONFIRMED** (1.6 medium, 1.7 high) |
| A4 yagni-scope-creep | EXHAUSTED, no counterexample found — every droplet maps to a sketch §25.1-§25.3 wave or Open-Questions deferral. |
| A5 shipped-but-not-wired | EXHAUSTED, no counterexample found — W0.5 known-wired-set deferral is properly Open-Questioned (Open Question #1, line 405); W5.D3 schema-level field-removal deferral routed to Drop 4c.7+ (Open Question #5); W3 sentinel-injection scaffolding minimal-viable per scope. |
| A6 atomicity | borderline (1.8 low — mechanical-bundle defensibility argument applies) |
| A7 prompt-injection | EXHAUSTED, no counterexample found — dormant pre-team-feature per `feedback_prompt_injection_team.md`. |

## 4. Hylla Feedback

- **Query 1**: `hylla_search_keyword` against `SystemPromptTemplatePath` / `BuiltinTemplateNames` / `runInitDevConfig` on artifact `github.com/evanmschultz/tillsyn@main`.
  - **Missed because**: Hylla returned `enrichment still running for github.com/evanmschultz/tillsyn@main` on subsequent `hylla_search` calls — the artifact's index is mid-enrichment for the current branch, so symbol-keyword lookups returned empty result sets and graph-nav was unavailable.
  - **Worked via**: `git grep` (allowed-list Bash) on the active worktree at `/Users/evanschultz/Documents/Code/hylla/tillsyn/main/` against the same symbols + `Read` on `internal/app/dispatcher/cli_adapter.go`, `binding_resolved.go`, `internal/templates/embed.go`, `SPAWN_PIPELINE.md`.
  - **Suggestion**: when Hylla is mid-enrichment, expose a "stale-OK fallback" mode that returns the last-good snapshot (current snapshot = 5 per the response envelope) rather than 100%-empty results. Today's UX is "indexed-or-empty" with no in-between, which forces fallback to `git grep` + `Read` even when the previous-snapshot answer would have been adequate.
- **Query 2**: ergonomic NIT — `hylla_search` rejected `fields: ["content", "summary"]` with `field must be summary, content, or docstring`. The error's plural-vs-singular split (the parameter name on the schema is `fields` not `field`) is confusing; passing `field: "content"` is the working path. Documented in the schema but the error message uses the singular `field` keyword in the rejection text, which suggests passing `field` instead of `fields` — but `field` is a single-value enum and `fields` is the array form. Actual UX: I tried `fields` → got an error message that talked about `field` (singular) → had to switch to the singular-field shape. Suggestion: clarify the error: "`fields` array entries must be one of: summary, content, docstring" — surface the array vs scalar distinction so callers don't have to guess which key to pass.
