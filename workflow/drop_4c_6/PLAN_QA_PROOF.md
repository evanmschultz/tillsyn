# Plan-QA-Proof Round 1 — Drop 4c.6

**Reviewer:** plan-QA-proof subagent (Round 1)
**Authored:** 2026-05-09
**Plan under review:** `workflow/drop_4c_6/PLAN.md` (411 lines, authored 2026-05-09 by go-planning-agent)
**Sketch source-of-truth:** `workflow/drop_4c_6/SKETCH.md` v2.8.4 POST-QA FINAL §25.1 + §26 (LOCKED — not re-QA'd)
**Scope:** the plan's TRANSLATION of the sketch into atomic droplets + sub-plan containers + `blocked_by` graph; NOT a re-QA of the sketch itself.

## 1. Findings

- 1.1 [Axis: parallelization-graph] [severity: high] **W1.D1 ↔ W5.D1 missing blocker on `internal/templates/embed.go` and `internal/templates/embed_test.go`.** Both droplets edit the same two files (W1.D1 paths line 93: `internal/templates/embed.go`, `internal/templates/embed_test.go`; W5.D1 paths line 161: same files). Both are listed as Wave A parallel-launchable heads (PLAN.md line 358; `_BLOCKERS.toml` lines 14-20 explicitly mark both as having no blocker). Per `CLAUDE.md` § "Blocker Semantics": *"sibling `build` action items sharing a file in `paths` OR a package in `packages` MUST have an explicit `blocked_by` between them."* W1.D1 extends the `//go:embed` directive to include 21 placeholder agent .md files + `agents.example.toml`; W5.D1 modifies the same `//go:embed` directive's `default-go.toml` entry to `till-go.toml` AND mutates `BuiltinTemplateNames` literal + `LoadDefaultTemplateForLanguage` switch in the same file. Two parallel builders editing the same `//go:embed` block + the same exported function literal will merge-conflict — irrespective of which goes first, the second must rebase against the first. → evidence: PLAN.md line 93 (W1.D1 paths), line 161 (W5.D1 paths), line 358 (Wave A parallel claim), `_BLOCKERS.toml` lines 14-20 → fix_hint: add `Blocked by: 4c.6.W1.D1` to W5.D1 (W1.D1 lands the embed-list extension first because W5.D1 is a rename within the existing list shape; W5.D1 then rebases its rename onto the post-W1.D1 directive). Add the matching `[[blockers]]` row to `_BLOCKERS.toml` and update the Wave A list at PLAN.md line 358.

- 1.2 [Axis: parallelization-graph] [severity: high] **W0.5 sub-plan ↔ W1.D1 missing blocker on package `internal/templates`.** W0.5's scope (PLAN.md line 76) edits `internal/templates/load.go` + `internal/templates/load_test.go`; W1.D1 (line 94) edits `internal/templates/embed.go` + `internal/templates/embed_test.go`. Both produce changes in the `internal/templates` Go package — one compile unit. Both are Wave A heads with no blocker between them (PLAN.md line 358; `_BLOCKERS.toml` lines 14-20). Per `CLAUDE.md`: *"a single Go package … shares one compile — editing different files in the same package still breaks the other agent's test run."* W0.5's L2 sub-planner won't see W1.D1 in its scope; the package-level serialization must be wired at L1. The same defect compounds for W0.5 vs W5.D1, W5.D2, W5.D3 — all Wave A or Wave B siblings that touch `internal/templates`. → evidence: PLAN.md lines 76 + 94 (file paths in same package), line 358 (parallel claim), `CLAUDE.md` § "Blocker Semantics" → fix_hint: serialize one direction. Cleanest at L1: `Blocked by: 4c.6.W0.5` on W1.D1, W5.D1, W5.D2, W5.D3 (W0.5's load-time validator changes are the foundation other `internal/templates` work assumes). Alternative: invert (W0.5 blocks on W1.D1) — but then W0.5's L2 sub-plan can't start until W1.D1 closes, which slows the longest-pole sub-plan. Either way: explicit serialization required, and the chosen ordering must be reflected in BOTH PLAN.md inline `Blocked by:` bullets AND `_BLOCKERS.toml`.

- 1.3 [Axis: multi-level-decomposition] [severity: medium] **W3 ↔ W6.D4 duplicate ownership of `SPAWN_PIPELINE.md:24-31` rewrite.** W3 acceptance bullet (PLAN.md line 148): *"Doc-comment corrections at `render.go:307-319` + `SPAWN_PIPELINE.md:24-31` reflect `--bare`-collapsed isolation per `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` §D.5."* W6.D4 acceptance bullet (PLAN.md line 304): *"`SPAWN_PIPELINE.md:24-31` — the prior 'two paths' framing — rewritten to reflect `--bare`-collapsed isolation per `RESEARCH/ISOLATION_ENFORCEMENT_FIX.md` §D.5."* Both droplets claim the same edit, citing the same source. W3's L2 sub-planner directive (line 153) further schedules a `D6 doc-comment corrections at render.go:307-319 + SPAWN_PIPELINE.md:24-31` sub-droplet inside W3. W6.D4 → W3 is declared (so order is fine), but two droplets owning the same edit is a multi-level-decomposition discipline violation: either W3 owns it (and W6.D4 is reduced to `CLI_ADAPTER_AUTHORING.md` only) or W6.D4 owns it (and W3's D.5 sub-droplet drops the SPAWN_PIPELINE.md edit and confines itself to `render.go:307-319`). Currently both claim it — when the second runs, it either re-edits an already-correct file (idempotent but wasteful) or it conflicts with the first (if the first's wording differs from the spec). → evidence: PLAN.md lines 141 + 148 (W3 claim), lines 297-315 (W6.D4 claim), L1 PLAN.md paths line 5 listing `SPAWN_PIPELINE.md (existing, doc-only updates)` under W3 scope → fix_hint: drop `SPAWN_PIPELINE.md:24-31` from W3's acceptance + paths + L2 sub-planner directive D6 (leave `render.go:307-319` doc-comment correction in W3 as it touches Go code adjacent to the resolver work); leave `SPAWN_PIPELINE.md:24-31` + `CLI_ADAPTER_AUTHORING.md` rewrite as W6.D4's sole responsibility (W6.D4 → W3 already correctly orders W6.D4 after W3's render-layer changes ship, so the doc reflects shipped reality). Update `_BLOCKERS.toml` line 50 reason-string to match the trimmed scope.

- 1.4 [Axis: parallelization-graph] [severity: medium] **PLAN.md `paths` list at line 5 names `internal/app/dispatcher/cli_claude/adapter.go` under W3, but the W3 sub-plan body (lines 136-153) does not call out an adapter.go-edit sub-droplet.** Line 5 phrases it as *"`internal/app/dispatcher/cli_claude/adapter.go` (env-var injection wiring)"*. W3's L2 sub-planner directive (line 153) lists D4 as *"defense-in-depth env vars in `cli_claude/env.go`"* — not adapter.go. The discrepancy is small (paths list at line 5 is the L1-level union of every file the drop's children may touch, so it can be a superset of the body), but it matters for the L2 sub-planner: if `adapter.go` IS in scope, W3's D4 should be split into env.go-write + adapter.go-wire-up; if it's NOT in scope, the line-5 entry should be removed. → evidence: PLAN.md line 5 (path entry) vs line 153 (L2 directive without adapter.go) → fix_hint: clarify in W3's Scope (PLAN.md line 141) whether `adapter.go` env-var-injection wiring is W3 work (then expand the L2 directive) or whether line 5's mention is residue from earlier sketch revisions (then remove from the line-5 paths list).

- 1.5 [Axis: shipped-but-not-wired] [severity: low] **W3 acceptance bullet (line 147) describes "post-render validator at `render.Render` (or a new sibling)" but doesn't name a consumer hook.** The validator is shipped in W3 — but a validator with no caller is a shipped-but-not-wired anti-pattern (Drop 3 droplet 3.20). Reading W3 carefully, the validator IS its own caller-of-itself: *"render-time hook that fails loud if `<bundle>/plugin/agents/<name>.md` is empty/stub-shaped/missing required frontmatter"*. So it wires into the render path, not a separate consumer. This is fine but the L1 acceptance text doesn't make the wiring explicit — the L2 sub-planner could read "post-render validator" and ship it as an exported helper without wiring it into the render call site, leaving the same gap. Per `feedback_tillsyn_enforces_templates.md`: *"any 'the system handles X' assumption MUST be wired end-to-end (schema → resolver → consumer → integration test)."* → evidence: PLAN.md line 147 (validator description) vs line 149 (sentinel-style assertion shipping in this wave) → fix_hint: tighten W3 acceptance to: *"Post-render validator wired into `render.Render`'s exit path so every spawned bundle is validated before the spawn proceeds; emits a closed sentinel error if the body fails the empty/stub/missing-frontmatter check; minimal sentinel-style assertion in `render_test.go` exercises the failure path."* The Open-Question #4 (line 408) already flags the stub-shape detection signature ambiguity for L2 to settle — extend OQ #4 to also explicitly call out the wiring-vs-helper ambiguity.

- 1.6 [Axis: specify-block-well-formedness] [severity: low] **W6.D2's "Plan Down, Build Up" lead claim is not testable as authored.** Acceptance line 264: *"'Plan Down, Build Up' section LEADS the doc per `SKETCH.md` § 26.W6 ContextBlocks `constraint`(high)."* "Leads" is asserted but not tested — what makes a section "lead"? First section after the title? First content section after a TOC? The verifiable form would be *"the first H2 section after the document title is `## Plan Down, Build Up`."* Without that sharpening, plan-QA on the next round (or build-QA at the droplet level) has no objective acceptance to grade against; reviewer judgement is the only signal. Per `~/.claude/agents/go-qa-proof-agent.md` Plan-QA-Proof axis 3: *"AcceptanceCriteria bullets are testable (verifiable by code inspection or mage <target>)."* → evidence: PLAN.md line 264 → fix_hint: rewrite the bullet to *"`CASCADE_METHODOLOGY.md` first H2 section after the document title is `## Plan Down, Build Up`; verifiable via doc-inspection."* Same sharpening pattern applies to "skeleton structure" (line 262) — list the H2 sections explicitly and require them in order, instead of leaving "section structure" as judgement.

## 2. Missing Evidence

- 2.1 **W2 sub-plan acceptance (line 122-129) lists `cmd/till/main_test.go` but doesn't name `cmd/till/help_test.go`** — `cmd/till/help.go` is in the L1 paths line 5 + W2 acceptance line 128 ("`cmd/till/help.go` updated accordingly"). Whether `help.go` has a co-located `help_test.go` that needs updating wasn't verifiable from PLAN.md alone — the planner asserts the help-text update without naming a test. This is L2's call (the sub-planner can decide test scope), but plan-QA flagging it here so L2 doesn't overlook the test surface. Routed to: L2 W2 sub-planner.

- 2.2 **W3 acceptance (line 142) lists "minimal sentinel-style integration test"** but the validation plan (line 150) only cites `mage test-pkg ./internal/app/dispatcher/cli_claude/...` — it doesn't say where the new test file lives or whether it's a new file or an append to an existing test. The L2 sub-planner directive (line 153) doesn't pin this either. Routed to: L2 W3 sub-planner.

- 2.3 **W0 sub-plan acceptance (line 60-64) names a "render-time frontmatter strip helper" but does NOT name the package the helper lives in.** Line 58 says "Render-time frontmatter `model:` / `tools:` strip helper exposed for W3 to call into" — but is this helper in `internal/config` (W0's new package), `internal/templates`, or `internal/app/dispatcher/cli_claude/render`? L2 W3 sub-planner directive line 153 says "D3 frontmatter strip wiring (calls into W0's helper)" — calls into where? This affects which package owns the helper's export and which package the import lives in. Routed to: L2 W0 sub-planner — needs to pick a package and document it before W3's L2 starts.

- 2.4 **W0.5 acceptance (line 76) describes 6 validators but doesn't pin the order they must run in.** L2 sub-planner directive line 85 proposes an order ("kind-enum + agent_name first... then cycles, then recursion-depth, then `blocked_by` acyclicity, then claim-vs-impl last"). But this is a suggestion, not a constraint — and validator ordering matters for error-message clarity (a malformed-kind error shouldn't masquerade as a cycle error). Routed to: L2 W0.5 sub-planner — the validators are sequential at load time; pin the order explicitly.

## 3. Summary

**Verdict: fail**
**Finding count: 6 (2 high, 3 medium-low, 1 low) + 4 missing-evidence routes**

The plan TRANSLATES the sketch's §25.1 wave breakdown faithfully — sub-plan vs direct-droplet split is justified, L1-contract acceptance bullets are present on every sub-plan container, all 9 direct droplets carry full Specify blocks (Objective + AcceptanceCriteria + ValidationPlan + RiskNotes + ContextBlocks + KindPayload), the multi-level-decomposition discipline is honored (planner did NOT plan all the way down), and the shipped-but-not-wired check correctly documents the W0.5 known-wired-set Open Question. PLAN.md ↔ `_BLOCKERS.toml` are in sync for every declared blocker.

**Failures cluster on the parallelization-graph axis.** Two high-severity missing blockers — W1.D1 ↔ W5.D1 sharing `internal/templates/embed.go` and W0.5 sub-plan ↔ W1.D1/W5.D{1,2,3} sharing the `internal/templates` Go package — would let parallel builders trip over each other's compile + test cycles. The medium W3 ↔ W6.D4 duplication on `SPAWN_PIPELINE.md:24-31` is a real ownership ambiguity that needs resolution before either L2 or W6.D4's builder spawn. The remaining findings (paths-list residue, validator wiring text, untestable doc-structure bullet) are smaller but worth fixing before plan-acceptance.

Recommend: route findings to Phase 3 dev discussion; planner re-spawn writes Round 2; rerun plan-QA.

## 4. Hylla Feedback

N/A — review touched non-Go artifacts only (PLAN.md, `_BLOCKERS.toml`, SKETCH.md, WORKFLOW.md, CLAUDE.md). Hylla is Go-only today; no Hylla queries were made or required.
