# Droplet 4a.17 — PACKAGE-LEVEL LOCK MANAGER — QA Proof Review

## Verdict

**PASS** — every acceptance item is backed by code+test evidence; all five required tests fire green individually under `mage testFunc` (-race, count=1); `mage ci` is clean (2125 tests, 1 pre-existing skip, dispatcher package coverage 90.0% above the 70% floor); doc-comment makes the two-independent-maps claim explicit and the runtime independence test (`TestPackageLockIndependentFromFileLock`) pins it.

One nit (NOT a fail): per-file coverage on `locks_package.go` is asserted at 100% by the worklog but mage's coverage gate (`magefile.go:291–331`) reports only package-level percentages — there is no per-file `-coverprofile` artifact emitted by `mage ci`. Static branch-by-branch analysis (below, item 4) confirms every statement in `locks_package.go` is reachable from the 9-test set, so 100% is provable by inspection; it is not directly verified by a runtime profile. Filing as a nit because per-package 90.0% with explicit per-statement branch coverage in inspection meets the acceptance intent.

## Acceptance Items

### 1. `packageLockManager` mirroring `fileLockManager` shape: `Acquire` + `Release` signatures

**PROVEN.**

- `internal/app/dispatcher/locks_package.go:97` — `func (m *packageLockManager) Acquire(actionItemID string, packages []string) (acquired []string, conflicts map[string]string, err error)`.
- `internal/app/dispatcher/locks_package.go:142` — `func (m *packageLockManager) Release(actionItemID string)`.
- `internal/app/dispatcher/locks_file.go:82` — reference `Acquire(actionItemID string, paths []string) (acquired []string, conflicts map[string]string, err error)`.
- `internal/app/dispatcher/locks_file.go:127` — reference `Release(actionItemID string)`.
- Diff is exclusively `paths`→`packages` rename and doc text. Identical control flow: empty-input early return, double `mu.Lock()/defer Unlock()`, lazy nil-init for both maps, partial-acquire loop, idempotent same-holder branch (`taken && holder != actionItemID` is the conflict guard, mirroring `locks_file.go:101`), `acquired = make([]string, 0, len(packages))` pre-allocation.

### 2. In-process `sync.Mutex` + `map[string]string` (package → holder). NO SQLite

**PROVEN.**

- `internal/app/dispatcher/locks_package.go:3` — `import "sync"` is the only import. No `database/sql`, no `modernc.org/sqlite`, no SQLite-tracking helpers.
- `internal/app/dispatcher/locks_package.go:51` — `mu sync.Mutex`.
- `internal/app/dispatcher/locks_package.go:55` — `holders map[string]string` (package → holding action-item ID).
- `internal/app/dispatcher/locks_package.go:60` — `itemPackages map[string]map[string]struct{}` (derived index for O(holder-set) Release; not state, just an index).
- Doc comment `internal/app/dispatcher/locks_package.go:24–31` calls out the deferred SQLite-mirror decision as a Drop 4b concern with the API kept small for a non-breaking add-on.

### 3. Five named tests present and verified green

**PROVEN.** All five fired green individually via `mage testFunc ./internal/app/dispatcher <name>` with `-race -count=1`:

| # | Test                                                | File:line                                             | mage testFunc verdict   |
|---|-----------------------------------------------------|-------------------------------------------------------|-------------------------|
| 1 | `TestPackageLockAcquireSinglePackageSucceeds`       | `internal/app/dispatcher/locks_package_test.go:12`    | PASS (1.23s, -race)     |
| 2 | `TestPackageLockAcquireSamePackageTwiceConflicts`   | `internal/app/dispatcher/locks_package_test.go:32`    | PASS (1.25s, -race)     |
| 3 | `TestPackageLockReleaseFreesAllPackagesHeldByItem`  | `internal/app/dispatcher/locks_package_test.go:59`    | PASS (1.20s, -race)     |
| 4 | `TestPackageLockAcquirePartialConflictReturnsConflicts` | `internal/app/dispatcher/locks_package_test.go:86`| PASS (1.24s, -race)     |
| 5 | `TestPackageLockConcurrentAcquireRaceFree`          | `internal/app/dispatcher/locks_package_test.go:127`   | PASS (1.21s, -race)     |

All five are name-verified AND runtime-verified-green. `mage testPkg ./internal/app/dispatcher` reports 95 tests / 95 passed total in the dispatcher package (4 supporting tests beyond the 5 required: `TestPackageLockPackagesAreOpaque`, `TestPackageLockEmptyInputsAreNoOps`, `TestPackageLockZeroValueIsUsable`, `TestPackageLockIndependentFromFileLock`).

### 4. 100% coverage on `locks_package.go`

**PROVEN-BY-INSPECTION (nit: not directly runtime-verified).**

`mage ci` reports `internal/app/dispatcher` at **90.0%** (above the 70% floor), but the gate (`magefile.go:291–331`) measures per-package percent only — no `-coverprofile` is captured, so per-file 100% cannot be asserted from runtime output alone.

Static branch-by-branch coverage of every statement in `locks_package.go`:

| Statement / branch                                                                  | File:line                                                                  | Test that exercises it                                                                                  |
|-------------------------------------------------------------------------------------|----------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------|
| `newPackageLockManager` constructor (both maps allocated)                           | `locks_package.go:68–73`                                                   | every happy-path test                                                                                   |
| `Acquire` empty-input early return (`len(packages) == 0`)                           | `locks_package.go:99–101`                                                  | `TestPackageLockEmptyInputsAreNoOps:237,248`                                                            |
| `Acquire` lazy nil-init `m.holders`                                                 | `locks_package.go:106–108`                                                 | `TestPackageLockZeroValueIsUsable:286` (zero-value `var mgr packageLockManager`)                        |
| `Acquire` lazy nil-init `m.itemPackages`                                            | `locks_package.go:109–111`                                                 | `TestPackageLockZeroValueIsUsable:286`                                                                  |
| `Acquire` conflict branch (`taken && holder != actionItemID`)                       | `locks_package.go:115–119`                                                 | `TestPackageLockAcquireSamePackageTwiceConflicts:41`, `TestPackageLockAcquirePartialConflictReturnsConflicts:95` |
| `Acquire` happy-path / same-holder idempotent insert                                | `locks_package.go:120–129`                                                 | every happy-path test; idempotent branch in `TestPackageLockEmptyInputsAreNoOps:264`, `TestPackageLockAcquirePartialConflictReturnsConflicts:110` |
| `Acquire` first-use of `m.itemPackages[actionItemID]` (creates inner set)           | `locks_package.go:123–127`                                                 | every happy-path test                                                                                   |
| `Release` unknown ID no-op                                                          | `locks_package.go:146–149`                                                 | `TestPackageLockEmptyInputsAreNoOps:258` (`mgr.Release("never-acquired")`)                              |
| `Release` multi-package delete loop                                                 | `locks_package.go:150–153`                                                 | `TestPackageLockReleaseFreesAllPackagesHeldByItem:68`, `TestPackageLockConcurrentAcquireRaceFree:188`   |

Every statement is reachable by at least one test. There is no dead code in `locks_package.go`.

### 5. Doc-comment calls out file-lock + package-lock are TWO INDEPENDENT MAPS

**PROVEN.**

- `internal/app/dispatcher/locks_package.go:14–22` — package-doc paragraph: *"Independence from fileLockManager: this manager and fileLockManager (locks_file.go) are TWO INDEPENDENT MAPS. A path lock on `internal/app/foo.go` and a package lock on `internal/app` do NOT collide inside either manager."* The phrase *TWO INDEPENDENT MAPS* is present verbatim (line 15). Cross-locking ownership is explicitly assigned to walker (4a.18) and conflict detector (4a.20).
- Runtime teeth: `TestPackageLockIndependentFromFileLock` (`locks_package_test.go:315–338`) exercises the claim — `fileMgr.Acquire("item-1", []string{"internal/app"})` does NOT prevent `pkgMgr.Acquire("item-2", []string{"internal/app"})` from succeeding with `acquired=["internal/app"]` and zero conflicts.

### 6. NO premature `lockManager[K]` generic

**PROVEN.**

- `internal/app/dispatcher/locks_package.go:50` — `type packageLockManager struct {` (concrete type, no `[K any]` or `[K comparable]` type parameter list).
- `internal/app/dispatcher/locks_file.go:35` — `type fileLockManager struct {` (concrete type, distinct from `packageLockManager`).
- Doc-comment `locks_package.go:27–31` explicitly defends the two-type choice: *"Package-lock and file-lock are NOT collapsed into a single generic `lockManager[K]` because their planned Drop 4b evolutions diverge: package-lock is expected to grow per-Go-package resolution via `go list -json`, while file-lock will stay opaque-string. Premature generic lands as YAGNI."* This matches WAVE_2_PLAN.md:151 verbatim intent.

## Mage Gate

- `mage testPkg ./internal/app/dispatcher` — 95 tests / 95 passed / 0 failed / 0 skipped. PASS.
- Each of the 5 required tests via `mage testFunc ./internal/app/dispatcher <name>` — PASS individually under `-race -count=1`.
- `mage ci` — 2125 tests / 2124 passed / 1 pre-existing skipped (`TestStewardIntegrationDropOrchSupersedeRejected` — not in scope for this droplet) / 0 failed. Dispatcher package coverage 90.0% (above 70% floor). Format check clean. Build succeeded.
- `mage formatCheck` — clean.
- No `mage install` invocation by the builder per worklog and source diff (verified by file-list contents — only the two new files in the dispatcher directory, no toolchain changes).

## File Citations

- `internal/app/dispatcher/locks_package.go` (155 LOC, NEW)
- `internal/app/dispatcher/locks_package_test.go` (339 LOC, NEW)
- `internal/app/dispatcher/locks_file.go` (140 LOC, reference baseline committed `e3302a3`)
- `internal/app/dispatcher/locks_file_test.go` (321 LOC; provides shared `equalStringSlices` helper at lines 307–321)
- `workflow/drop_4a/4a.17_BUILDER_WORKLOG.md`
- `workflow/drop_4a/PLAN.md:262–268`
- `workflow/drop_4a/WAVE_2_PLAN.md:134–153`

## Hylla Feedback

One miss recorded:

- **Query**: `hylla_search_keyword` on `equalStringSlices` (default `test_mode=hide_tests`).
- **Missed because**: default `hide_tests` behavior excluded `_test.go` results, returning 0 hits even though the helper IS in the indexed graph as a test-file private symbol.
- **Worked via**: rerunning `hylla_search_keyword` with `test_mode=include_tests` returned `internal/adapters/storage/sqlite/equalStringSlices` (a different, name-collision symbol in the sqlite repo); then `Read` of `internal/app/dispatcher/locks_file_test.go:307–321` confirmed the dispatcher-local helper.
- **Suggestion**: when a search hits zero results in `hide_tests` mode, the response could include a one-line hint like `"hint": "0 results in hide_tests mode; 2 results exist in include_tests"`. Cheap nudge, prevents the silent-miss footgun where a real symbol lives in `_test.go` and the default mode hides it. Alternative: surface the helper as part of the dispatcher package node so siblings show in `hylla_node_full` regardless of test-mode filter.

No other Hylla queries needed for this droplet — the file shape was a near-verbatim mirror of an already-committed reference (`locks_file.go`, `e3302a3`), and direct `Read` of both files plus the worklog plus PLAN.md / WAVE_2_PLAN.md sections gave full verification coverage.
