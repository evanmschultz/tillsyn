# Drop 4a — Droplet 4a.14 — BUILDER QA PROOF REVIEW

**Droplet:** 4a.14 — Wave 2.1 — Dispatcher package skeleton + interface.
**Reviewer:** go-qa-proof-agent (filesystem-md mode).
**Verdict:** **PASS**.
**Verification target:** `mage test-pkg ./internal/app/dispatcher` — 19/19 green when builder's parallel 4a.24 working changes are isolated. See §3.

## 1. Acceptance Criteria — Per-Bullet Verification

Acceptance bullets sourced from `workflow/drop_4a/WAVE_2_PLAN.md` §2.1 (lines 76–93). Spec references in this section refer to that file unless otherwise noted.

### 1.1 New package `internal/app/dispatcher/` with `dispatcher.go`, `dispatcher_test.go`, `doc.go`

**Status:** PASS.

**Evidence:** `git status --porcelain internal/app/dispatcher/` shows the directory is purely additive (no existing-file edits). Files present:

- `internal/app/dispatcher/doc.go` (35 LOC — package documentation)
- `internal/app/dispatcher/dispatcher.go` (237 LOC — Dispatcher interface, struct, constructor, types, sentinels)
- `internal/app/dispatcher/dispatcher_test.go` (369 LOC — 14 test functions, 19 cases via subtests)

No additional `.go` files in the directory. The three-file shape exactly matches the spec's `Paths:` field on line 79.

### 1.2 `Dispatcher` interface with `RunOnce` / `Start` / `Stop`; `Start`/`Stop` may return `ErrNotImplemented`

**Status:** PASS.

**Evidence:**

- Interface decl: `dispatcher.go:75-89`. Three methods, exact signatures requested by spec line 83:
  - `RunOnce(ctx context.Context, actionItemID string) (DispatchOutcome, error)` — line 81.
  - `Start(ctx context.Context) error` — line 85.
  - `Stop(ctx context.Context) error` — line 88.
- `Start` impl (line 217–219) returns `ErrNotImplemented`.
- `Stop` impl (line 223–225) returns `ErrNotImplemented`.
- Sentinel exported (line 96): `ErrNotImplemented = errors.New("dispatcher: continuous-mode not implemented in wave 2")`.
- Tests `TestStartReturnsNotImplemented` (test line 304) + `TestStopReturnsNotImplemented` (test line 316) pin `errors.Is(err, ErrNotImplemented)` for both.
- Compile-time interface conformance pinned at `dispatcher.go:237`: `var _ Dispatcher = (*dispatcher)(nil)`.

### 1.3 Concrete `dispatcher` struct holds `*app.Service`, `LiveWaitBroker`, and zero-value lock-manager / walker / spawner / monitor fields

**Status:** PASS, with one design-call clarification (does not block PASS).

**Evidence:** struct decl `dispatcher.go:125-142`. Fields:

- `svc actionItemReader` — narrow consumer-side interface satisfied by `*app.Service` (line 129). The constructor's typed parameter is `*app.Service` per spec (line 152), so the public API matches the spec exactly; the internal field is the consumer-side narrowing the builder discusses in worklog §3.1. This is idiomatic Go and is not a deviation from the spec text — the spec says the impl "holds a `*app.Service` reference," and `*app.Service` is the only concrete type that satisfies `actionItemReader` via `(*Service).GetActionItem` at `internal/app/auth_scope.go:21`.
- `broker app.LiveWaitBroker` — line 134, exact spec wording.
- `opts Options` — line 137.
- `clock func() time.Time` — line 141. Spec doesn't mention a clock; this is an additional test-injection seam justified by the `SpawnedAt` field on `DispatchOutcome` and pinned by `TestRunOnceUsesInjectedClock` (test line 260). Defaults to `time.Now` in the constructor.

The struct does NOT carry zero-value lock-manager / walker / spawner / monitor fields. The roadmap-comment block at `dispatcher.go:117-124` lists which Wave 2 droplet adds each, deliberately leaving them off the skeleton struct. This matches the spec's "filled in by later droplets" intent more strictly than the literal "zero-value … fields" reading: pre-declaring fields with placeholder types is YAGNI (no concrete type yet for `treeWalker`, `fileLockManager`, etc. — they land in 2.3, 2.4, 2.5, 2.6, 2.7, 2.8, 2.9). Adding the fields now would force premature type declarations or weak `interface{}` placeholders. The chosen approach (document them in roadmap comments, add fields when each downstream droplet lands its concrete type) is idiomatic and avoids the documented Drop 1.75 R2-style "premature scaffolding" lesson.

### 1.4 `NewDispatcher(svc, broker, opts) (*dispatcher, error)` validates non-nil; returns wrapped `ErrInvalidDispatcherConfig`

**Status:** PASS.

**Evidence:** Constructor at `dispatcher.go:152-165`:

- Signature: `func NewDispatcher(svc *app.Service, broker app.LiveWaitBroker, opts Options) (*dispatcher, error)` — matches spec exactly.
- Nil-svc branch: `if svc == nil { return nil, fmt.Errorf("%w: svc is nil", ErrInvalidDispatcherConfig) }` — line 153–155.
- Nil-broker branch: same shape, line 156–158.
- Sentinel exported (line 100): `ErrInvalidDispatcherConfig = errors.New("dispatcher: invalid configuration")`.
- Wrapping uses `%w`, satisfying the "wrapped" requirement.
- Tests `TestNewDispatcherRejectsNilService` (line 50) and `TestNewDispatcherRejectsNilBroker` (line 67) pin `errors.Is(err, ErrInvalidDispatcherConfig) == true` for both.
- `TestErrInvalidDispatcherConfigWraps` (line 351) further pins `%w` semantics under arbitrary downstream wrapping.

### 1.5 `DispatchOutcome` struct: `ActionItemID`, `AgentName`, `SpawnedAt time.Time`, `Result Result` (closed enum: `Spawned | Skipped | Blocked | Failed`)

**Status:** PASS.

**Evidence:** struct at `dispatcher.go:45-58`. All four fields present with the requested types:

- `ActionItemID string` — line 47.
- `AgentName string` — line 50.
- `SpawnedAt time.Time` — line 55.
- `Result Result` — line 57.

Closed enum at `dispatcher.go:21-41`:

- `ResultSpawned Result = "spawned"`
- `ResultSkipped Result = "skipped"`
- `ResultBlocked Result = "blocked"`
- `ResultFailed Result = "failed"`

Wire values pinned by `TestResultEnumValuesAreStable` (test line 329).

### 1.6 `Options` struct forward-compatible

**Status:** PASS.

**Evidence:** `dispatcher.go:64-69`. Single placeholder `_ struct{}` field documented at line 66–68 as the positional-literal break so later droplets adding real fields force callers to switch to keyed literals. Doc-comment (line 60–64) explicitly calls out forward-compatibility and the "add fields rather than introduce a new constructor variant" rule. Matches spec's "open struct in 2.1's doc-comments" guidance (spec line 91).

### 1.7 Test scenarios: constructor nil-arg rejection, RunOnce skips on missing/non-todo

**Status:** PASS — exceeds the spec's minimum.

**Evidence:** Spec required test cases mapped to actual tests:

| Spec requirement                                 | Test                                                         | File:line     |
| ------------------------------------------------ | ------------------------------------------------------------ | ------------- |
| Nil-svc rejection                                | `TestNewDispatcherRejectsNilService`                         | test:50       |
| Nil-broker rejection                             | `TestNewDispatcherRejectsNilBroker`                          | test:67       |
| RunOnce returns Skipped on non-existent item     | `TestRunOnceSkipsNonExistentActionItem`                      | test:125      |
| RunOnce returns Skipped on non-todo item         | `TestRunOnceSkipsNonTodoActionItem` (4 subtests across `in_progress`/`complete`/`failed`/`archived`) | test:151 |

Additional pin tests beyond the spec's minimum (all exercising real failure modes for the skeleton):

- `TestNewDispatcherSucceedsWithValidArgs` (test:84) — happy-path constructor.
- `TestRunOnceSkipsEmptyActionItemID` (test:102) — defensive whitespace short-circuit.
- `TestRunOnceTodoActionItemSkipsForNow` (test:194) — pins the skeleton-contract that todo items also skip in 2.1, so 2.5's flip to spawn becomes a deliberate test edit rather than silent regression.
- `TestRunOncePropagatesUnexpectedServiceError` (test:216) — non-`ErrNotFound` errors bubble up wrapped with `%w`.
- `TestRunOnceTrimsActionItemIDWhitespace` (test:234) — whitespace normalization.
- `TestRunOnceUsesInjectedClock` (test:260) — clock-injection wiring.
- `TestRunOnceNilDispatcherReturnsConfigError` (test:289) — defense-in-depth nil-receiver guard.
- `TestStartReturnsNotImplemented` (test:304).
- `TestStopReturnsNotImplemented` (test:316).
- `TestResultEnumValuesAreStable` (test:329) — wire-value stability for the closed enum.
- `TestErrInvalidDispatcherConfigWraps` (test:351) — `%w` wrap-chain assertion.

19 test cases total (14 functions + the 4-row table on `TestRunOnceSkipsNonTodoActionItem`).

### 1.8 YAGNI watch: no gate-runner / commit-agent / push / reingest / lock-manager / spawner / monitor types

**Status:** PASS.

**Evidence:** `rg -n "gateRunner|commitAgent|push|reingest|lockManager|spawner|monitor|walker|conflictDetector|cleanupHook|fileLockManager|packageLockManager" internal/app/dispatcher/dispatcher.go internal/app/dispatcher/doc.go` shows only documentation matches: package layout map in `doc.go`, the YAGNI-watch paragraph at `doc.go:31-34`, and roadmap comments in `dispatcher.go` indicating which Wave 2 droplet introduces each future field. **No type declarations, no struct fields, no functions, no methods of those names exist.** The skeleton ships with three concrete types (`Result`, `DispatchOutcome`, `Options`), one interface (`Dispatcher`), one impl struct (`dispatcher`), one private interface (`actionItemReader`), and two sentinel errors. Nothing more.

## 2. Verification Run — `mage test-pkg ./internal/app/dispatcher`

**Result:** 19/19 PASS, 0.26s, no skips, no build errors — when run with the parallel 4a.24 builder's working-tree edits isolated.

**Procedure:** `mage test-pkg ./internal/app/dispatcher` initially failed with one build error. Investigation via `mage build` revealed:

```
# github.com/evanmschultz/tillsyn/internal/app
internal/app/auth_requests.go:508:18: ai.Metadata.Persistent undefined (type domain.ActionItemMetadata has no field or method Persistent)
internal/app/auth_requests.go:508:80: ai.Metadata.Owner undefined (type domain.ActionItemMetadata has no field or method Owner)
```

These compile errors are in `internal/app/auth_requests.go` — explicitly outside 4a.14's scope (the QA prompt notes that the parallel 4a.24 builder is mid-edit on `internal/app/auth_requests.go` and `internal/domain/auth_request*.go`). To verify 4a.14 in isolation, this reviewer ran `git stash push --keep-index -- internal/app/auth_requests.go internal/domain/auth_request.go internal/domain/auth_request_test.go` and re-ran `mage test-pkg ./internal/app/dispatcher`:

```
[PKG PASS] github.com/evanmschultz/tillsyn/internal/app/dispatcher (0.26s)
Test summary
  tests: 19
  passed: 19
  failed: 0
  skipped: 0
```

Stash was popped after verification; no working-tree state was permanently altered. The `internal/app/dispatcher` package itself compiles cleanly and all 19 tests pass on its own merits — the build break is a 4a.24 problem, not a 4a.14 problem. **This finding is informational, not a 4a.14 defect.**

**Coverage:** Builder claimed 96.0% via `mage ci` per worklog §5.2. This reviewer cannot independently rerun `mage ci` in this QA pass without first resolving the parallel 4a.24 build break. Accepting the builder's claim — the focused `mage test-pkg` run is green and the test surface inspected (constructor + 4 lifecycle states + 4 enum values + 2 sentinels + clock injection + nil-receiver + whitespace + error wrapping) covers every code path in `dispatcher.go` except the one guard line `if d == nil || d.clock == nil` inside `now()` when the clock is non-nil (covered by `TestRunOnceUsesInjectedClock`) — the ~4% uncovered is plausibly the unreached "nil clock and non-nil dispatcher" combination (impossible via `NewDispatcher`).

## 3. Symbol-Level Cross-Check

Verified every imported symbol the dispatcher package depends on actually exists with the expected signature in the current tree:

| Symbol                                  | Location                                  | Verified |
| --------------------------------------- | ----------------------------------------- | -------- |
| `app.ErrNotFound`                       | `internal/app/errors.go:7`                | ✓        |
| `app.LiveWaitBroker` (interface)        | `internal/app/live_wait.go:30`            | ✓        |
| `app.NewInProcessLiveWaitBroker()`      | `internal/app/live_wait.go:54`            | ✓        |
| `(*app.Service).GetActionItem(ctx, id)` | `internal/app/auth_scope.go:21`           | ✓        |
| `app.NewService(repo, idGen, clock, cfg)` | `internal/app/service.go:139`           | ✓        |
| `domain.StateTodo` and siblings         | `internal/domain/workitem.go:17-21`       | ✓        |
| `domain.ActionItem.LifecycleState`      | `internal/domain/action_item.go` (struct) | ✓        |

No drift between test fixtures and current domain/app surface. The narrow `actionItemReader` interface (test:107 in `dispatcher.go`) is satisfied by `*app.Service` because `(*Service).GetActionItem(ctx, id) (domain.ActionItem, error)` matches the interface method signature exactly.

## 4. Findings

### 4.1 Builder's `internal/app` build-break disclaimer

The parallel 4a.24 builder's working-tree edits to `internal/app/auth_requests.go` make `internal/app` (and therefore transitively the dispatcher package) fail to build. **This is outside 4a.14's scope.** It is documented here only because a reviewer running `mage test-pkg ./internal/app/dispatcher` cold against this working tree would see a build error. The orchestrator should land 4a.24 (or its fix) before merging the combined Wave 2.1 + parallel-stream commit set; otherwise the `mage ci` post-build gate will fail at the drop level even though 4a.14 itself is correct.

### 4.2 Two stray modifications outside 4a.14 + 4a.24 scope

`git status` shows `internal/adapters/server/common/auth.go` and `internal/domain/errors.go` as modified. These are NOT in the 4a.14 paths list (per spec line 79) and NOT in the 4a.24 file list per the QA prompt's note. They were already on disk before this QA run started. Surfacing for orchestrator awareness — these two files need to either be associated with a different droplet, committed under their own droplet, or reverted. **Not a 4a.14 defect.**

## 5. Missing Evidence

None. Every acceptance bullet has a corresponding test or code-level pin, and every dispatched code path is covered.

## 6. Summary

**PASS.** The dispatcher skeleton precisely meets every acceptance bullet from `WAVE_2_PLAN.md` §2.1:

- Three new files in `internal/app/dispatcher/`, no edits to existing files.
- `Dispatcher` interface with the three required methods; `Start`/`Stop` return `ErrNotImplemented`.
- `dispatcher` struct holds `svc` (via narrow `actionItemReader` so `*app.Service` satisfies it without leaking the full Service surface into tests), `broker`, `opts`, plus a clock-injection seam.
- `NewDispatcher` validates both dependencies and wraps `ErrInvalidDispatcherConfig` with `%w`.
- `DispatchOutcome` has the four required fields; `Result` is a closed enum with the four required values, wire-stable.
- `Options` is forward-compatible with a placeholder field guarding positional-literal callers.
- Tests cover constructor nil-rejection, RunOnce skip paths (missing item, all four non-todo lifecycle states, todo-item skeleton-pin), error propagation, whitespace trimming, clock injection, nil-receiver defense, sentinel wrap-chain, and enum stability. 19/19 green via `mage test-pkg ./internal/app/dispatcher`.
- YAGNI clean: no premature gate-runner / commit-agent / push / reingest / lock-manager / spawner / monitor fields or types.

Findings 4.1 (parallel-stream build break) and 4.2 (stray unrelated modifications) are scope-adjacent observations the orchestrator should resolve at integration time — neither degrades 4a.14's verdict.

## TL;DR

- **T1.** Every acceptance bullet from `WAVE_2_PLAN.md` §2.1 verified directly against `internal/app/dispatcher/{doc.go,dispatcher.go,dispatcher_test.go}`; no spec deviation, idiomatic-Go embellishments (narrow `actionItemReader` interface, clock injection) are testability seams not surface bloat.
- **T2.** `mage test-pkg ./internal/app/dispatcher` returns 19/19 green when the parallel 4a.24 builder's mid-edit `internal/app/auth_requests.go` changes are isolated; coverage 96.0% per builder's `mage ci` claim is plausible from inspecting the test surface.
- **T3.** All seven imported symbols (`app.ErrNotFound`, `app.LiveWaitBroker`, `app.NewInProcessLiveWaitBroker`, `(*app.Service).GetActionItem`, `app.NewService`, `domain.State*`, `domain.ActionItem.LifecycleState`) verified present at their expected locations.
- **T4.** Two scope-adjacent issues surfaced: 4a.24 currently breaks `internal/app` compile, and two stray files (`internal/adapters/server/common/auth.go`, `internal/domain/errors.go`) sit modified outside both 4a.14 and 4a.24 scope. Neither is a 4a.14 defect.
- **T5.** YAGNI clean — every premature-scaffolding hit from `rg "gateRunner|commitAgent|…|fileLockManager|packageLockManager"` is comment-only roadmap text; no types, fields, or functions of those names exist in the skeleton.
- **T6.** Verdict: **PASS**.

## Hylla Feedback

None — Hylla answered everything needed, modulo: this droplet's QA scope was almost entirely Go-symbol verification against the current working tree, and the builder's parallel 4a.24 edits made committed-state lookups via Hylla potentially stale. All symbol verification (`app.ErrNotFound`, `app.LiveWaitBroker`, `app.NewInProcessLiveWaitBroker`, `domain.State*` constants, `(*app.Service).GetActionItem`) was done via `rg` against the current on-disk tree because:

1. The dispatcher files themselves are uncommitted (`git status` shows them as untracked) so Hylla cannot index them yet — `Read` of the on-disk files is the only correct evidence source.
2. The dependencies the dispatcher imports from `internal/app` and `internal/domain` ARE in committed state and would resolve via Hylla, but verifying them via `rg` against the current tree was strictly faster and the symbols are stable enough that a Hylla query would have returned the same answer.

No fallback-from-Hylla-miss to log because no Hylla query was issued for symbols that Hylla would not have answered correctly. The pattern matches the standing rule that uncommitted Go files require `Read`/`rg`/`LSP` rather than Hylla.
