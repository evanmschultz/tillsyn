# Droplet 4a.22 — TERMINAL-STATE CLEANUP — QA Proof Review (Round 1)

**Verdict:** PASS

**One-line summary:** Every acceptance item is backed by code + tests; all four named tests run green individually under `mage testFunc` and the full package is 105/105 under `mage testPkg`.

**Mode:** filesystem-MD only. Hylla returned zero hits for `cleanupHook OnTerminalState` (snapshot 5 predates this droplet's commit); filesystem `Read` covered the gap as expected for fresh post-ingest code.

---

## Per-Acceptance-Item Verdicts

### A1 — `cleanupHook` with `OnTerminalState(ctx, item)` invoked on transition to complete/failed/archived — PROVEN

- **Cite:** `internal/app/dispatcher/cleanup.go:83-113` defines `cleanupHook` struct.
- **Cite:** `internal/app/dispatcher/cleanup.go:193` declares `func (c *cleanupHook) OnTerminalState(_ context.Context, item domain.ActionItem) error`.
- **Cite:** Doc-comment `cleanup.go:167-173` states: "invoked when an action item transitions to StateComplete, StateFailed, or StateArchived; the caller … is responsible for filtering on lifecycle state — OnTerminalState itself does NOT inspect item.LifecycleState because the contract is 'treat archive as terminal too' and the pipeline is identical across all three terminal states." Matches WAVE_2_PLAN.md §2.9 acceptance #1.
- **Cite:** Tests exercise all three terminal states: `cleanup_test.go:67` (`StateComplete`), `cleanup_test.go:184` (`StateArchived`), `cleanup_test.go:242` (`StateFailed`).

### A2 — Cleanup actions in order: file-lock release → package-lock release → auth-bundle revoke stub → process-monitor unsubscribe — PROVEN

- **Cite:** `cleanup.go:212-230` shows the fixed pipeline order:
  1. `releaseFileLocks(item.ID)` — line 213-217
  2. `releasePackageLocks(item.ID)` — line 218-222
  3. `revokeAuthBundle(item.ID)` — line 223-227
  4. `unsubscribeMonitor(item.ID)` — line 228-230
- **Cite:** Doc-comment `cleanup.go:189` reiterates: "The pipeline order is fixed: file-lock → package-lock → auth-revoke → monitor-unsub."
- **Cite:** Production wiring `newCleanupHook` (`cleanup.go:132-157`) binds the four steps to the lock managers + the auth-revoke stub + the monitor unsubscriber respectively.

### A3 — Idempotent (second call no-op) — PROVEN

- **Cite:** `cleanup.go:201-210` — under `c.mu`, the cleanup hook checks `c.cleaned[item.ID]` and short-circuits before any per-step closure fires.
- **Cite:** `cleaned map[string]struct{}` field declared at `cleanup.go:112`; mutex at `cleanup.go:106`.
- **Cite:** Doc-comment `cleanup.go:180-183` documents the contract.
- **Test:** `TestCleanupIsIdempotent` (`cleanup_test.go:107-153`) calls `OnTerminalState` twice with the same item.ID and asserts each closure ran exactly once via four `atomic.Int32` counters. Verified green individually via `mage testFunc ./internal/app/dispatcher TestCleanupIsIdempotent`.

### A4 — Errors aggregated via `errors.Join`; ALL steps attempted regardless of individual failures — PROVEN

- **Cite:** `cleanup.go:212-231` — every `if c.releaseX != nil { if err := …; err != nil { errs = append(errs, err) } }` block runs unconditionally. The aggregation `return errors.Join(errs...)` is `cleanup.go:231`.
- **Cite:** Doc-comment `cleanup.go:185-189` documents the contract: "every step is attempted regardless of individual failures. errors.Join folds non-nil per-step errors into one return value."
- **Test:** `TestCleanupContinuesPastIndividualFailure` (`cleanup_test.go:214-270`) injects three distinct errors, asserts each subsequent step still runs (`pkgCalled` / `authCalled` / `monitorCalled` `atomic.Bool` flags), and asserts `errors.Is` unwraps every constituent. Verified green individually via `mage testFunc ./internal/app/dispatcher TestCleanupContinuesPastIndividualFailure`.
- **Subtle correctness check:** the `unsubscribeMonitor` step is fire-and-forget (no error appended) — documented at `cleanup.go:64-66` + `cleanup.go:187-188`. This is intentional per the `monitorUnsubscriber` interface contract, not a gap. `TestCleanupContinuesPastIndividualFailure` still asserts `monitorCalled` is true after the three preceding steps all error — confirms the step is reached even when prior steps fail.

### A5 — Four named tests present and green — PROVEN

| Required test name | Defined at | Verified-green per-test? |
|---|---|---|
| `TestCleanupReleasesFileAndPackageLocks` | `cleanup_test.go:46` | YES — `mage testFunc ./internal/app/dispatcher TestCleanupReleasesFileAndPackageLocks` → 1/1 pass |
| `TestCleanupIsIdempotent` | `cleanup_test.go:107` | YES — `mage testFunc ./internal/app/dispatcher TestCleanupIsIdempotent` → 1/1 pass |
| `TestCleanupOnArchivedAlsoFires` | `cleanup_test.go:159` | YES — `mage testFunc ./internal/app/dispatcher TestCleanupOnArchivedAlsoFires` → 1/1 pass |
| `TestCleanupContinuesPastIndividualFailure` | `cleanup_test.go:214` | YES — `mage testFunc ./internal/app/dispatcher TestCleanupContinuesPastIndividualFailure` → 1/1 pass |

Plus full-package run: `mage testPkg ./internal/app/dispatcher` → 105/105 pass (0.00s reported, sub-second).

Three additional guard tests are also present (not required by acceptance, but valuable):
- `TestCleanupEmptyActionItemIDIsNoop` (`cleanup_test.go:278`) — pins the empty-ID short-circuit.
- `TestNewCleanupHookValidatesDependencies` (`cleanup_test.go:306`) — pins constructor non-nil-dep guards.
- `TestRevokeAuthBundleStubReturnsNil` (`cleanup_test.go:347`) — pins Wave-3 seam contract.

### A6 — Auth-bundle revoke stub doc-cross-references Wave 3 / Drop 4c Theme F.7 spawn redesign — PROVEN

- **Cite:** Package doc-comment `cleanup.go:20-25` — "Auth-bundle revoke — STUB in Wave 2; the real revoke lands in Wave 3 of Drop 4a … See Drop 4c Theme F.7 for the eventual wiring; see also spawn.go's AuthBundle stub for the matching seam on the spawn side."
- **Cite:** `revokeAuthBundle` field doc-comment `cleanup.go:93-97` — "Wave 2 wires this to a no-op stub … Wave 3 of Drop 4a replaces the stub with a till.auth_request(revoke) call."
- **Cite:** `newCleanupHook` doc-comment `cleanup.go:118-120` — "Wave 3 swaps it for the real revoke without touching the call site."
- **Cite:** `revokeAuthBundleStub` doc-comment `cleanup.go:234-244` — "Wave 3 fills this in: the Drop 4a Wave 3 spawn-redesign work (per Drop 4c Theme F.7) replaces this stub with a till.auth_request(operation=revoke) call against the action item's session and lease, surfacing the revoke error through the cleanupHook's errors.Join aggregation. Until then the dev cleaning up after a manual-trigger CLI run revokes manually via `till auth_request revoke` — see WAVE_2_PLAN.md §2.9 mitigation paragraph."
- **Cite:** Stub body `cleanup.go:245-248`: `func revokeAuthBundleStub(_ string) error { // Wave 3 fills this in. return nil }` — matches the `// Wave 3 fills this in` literal required by WAVE_2_PLAN.md §2.9.

---

## Files Verified

- `/Users/evanschultz/Documents/Code/hylla/tillsyn/main/internal/app/dispatcher/cleanup.go` (249 lines)
- `/Users/evanschultz/Documents/Code/hylla/tillsyn/main/internal/app/dispatcher/cleanup_test.go` (363 lines, 7 test functions)
- `/Users/evanschultz/Documents/Code/hylla/tillsyn/main/workflow/drop_4a/4a.22_BUILDER_WORKLOG.md`

No source files outside `internal/app/dispatcher/cleanup{,_test}.go` were touched (consistent with builder worklog §"Files Touched" + the PLAN.md row 4a.22 "(NEW)" constraint + the dispatcher.go-untouched discipline that defers wiring to droplet 4a.23).

---

## Test Run Evidence

```
$ mage testPkg ./internal/app/dispatcher
[RUNNING] Running go test ./internal/app/dispatcher
[SUCCESS] Test stream detected
[PKG PASS] github.com/evanmschultz/tillsyn/internal/app/dispatcher (0.00s)
Test summary
  tests: 105
  passed: 105
  failed: 0

$ mage testFunc ./internal/app/dispatcher TestCleanupReleasesFileAndPackageLocks  → 1/1 pass
$ mage testFunc ./internal/app/dispatcher TestCleanupIsIdempotent                  → 1/1 pass
$ mage testFunc ./internal/app/dispatcher TestCleanupOnArchivedAlsoFires           → 1/1 pass
$ mage testFunc ./internal/app/dispatcher TestCleanupContinuesPastIndividualFailure → 1/1 pass
```

Builder worklog also records `mage ci` → 2147/2148 with one unrelated pre-existing skip (`TestStewardIntegrationDropOrchSupersedeRejected` in `internal/adapters/server/mcpapi`); dispatcher coverage 90.6% (floor 70%).

---

## Notes / Observations (Non-Blocking)

1. **Function-typed seams over interface seams** — builder design choice §1 in worklog. Lifts `Release(actionItemID string)` (no-error today) into `func(actionItemID string) error` closures. Justified because `TestCleanupContinuesPastIndividualFailure` requires injectable error returns, and the lift positions Drop 4b's SQLite-mirror `Release` to surface persistence failures without breaking the cleanup contract. Consistent with the package's existing wrap shape.
2. **`monitorUnsubscriber` interface** — defined in cleanup.go (`cleanup.go:67-69`) for forward-compat with droplet 4a.23 wiring. No production type satisfies it today; tests inject `&stubMonitorUnsubscriber{}`. Matches builder worklog §5 design choice.
3. **Empty-ID short-circuit** (`cleanup.go:197-199`) — defensive guard that prevents accidentally releasing the empty-string keyhole in lock managers (which IS a valid holder ID per `locks_file.go:71-73` opacity contract). Not in acceptance criteria but is a sound additional guard, tested at `cleanup_test.go:278-300`.
4. **Idempotency-set growth** — `cleaned` map grows unbounded. Documented in `cleanup.go:42-48` + builder worklog §3 with Drop 4b LRU/arena resolution path. Acceptable for Wave 2 manual-trigger CLI process lifecycle.

---

## Hylla Feedback

| Field | Value |
|---|---|
| Query | `hylla_search_keyword(query="cleanupHook OnTerminalState", artifact_ref="github.com/evanmschultz/tillsyn@main", fields=["content"])` |
| Result | Zero hits (snapshot 5 reported by Hylla; cleanup.go is brand-new in this droplet, not yet in latest ingest) |
| Missed because | Stale ingest — expected pattern for fresh post-droplet code per project rule "Hylla is stale for those files until reingest." Drop-end ingest will pick this up. |
| Worked via | Filesystem `Read` on `cleanup.go` + `cleanup_test.go` directly. |
| Suggestion | None — this is the documented ingest-lag pattern, not a Hylla search-quality issue. |

No structural Hylla miss to flag.

---

## Definition of Done — Met

- [x] PROOF MD written at `workflow/drop_4a/4a.22_BUILDER_QA_PROOF.md`.
- [x] Per-acceptance-item PROVEN/GAP/WRONG with file:line cites.
- [x] Per-test verified-green via `mage testFunc` (not name-only).
- [x] Hylla Feedback section present.

**Verdict: PASS.**
