# DROPLET 4a.8 — QA PROOF REVIEW

**Target droplet:** Drop 4a / Wave 1 / W1.4 — `start_commit string` FIRST-CLASS ON `ActionItem`
**Plan ref:** `workflow/drop_4a/WAVE_1_PLAN.md` §1.4
**Paths reviewed (12 files, all staged-but-uncommitted; `git diff --stat` = +559 / -20):**
`internal/domain/action_item.go`, `internal/domain/domain_test.go`, `internal/app/service.go`, `internal/app/snapshot.go`, `internal/app/snapshot_test.go`, `internal/adapters/server/common/mcp_surface.go`, `internal/adapters/server/common/app_service_adapter_mcp.go`, `internal/adapters/server/mcpapi/extended_tools.go`, `internal/adapters/server/mcpapi/extended_tools_test.go`, `internal/adapters/storage/sqlite/repo.go`, `internal/adapters/storage/sqlite/repo_test.go`, `internal/tui/model_test.go`
**Verdict:** **PASS**
**Date:** 2026-05-03

---

## 1. Acceptance Bullet Verification

Every plan acceptance bullet from `workflow/drop_4a/WAVE_1_PLAN.md` §1.4 is checked against the staged diff. Builder claimed `mage ci` 1929 passed / 0 failed / 1 skip. QA re-ran live (§2 below) and confirmed identical. The droplet is the FIRST single-string field in the action-item-domain chain (1.1-1.3 were `[]string`); the builder correctly diverges from the slice precedent on every load-bearing axis (storage shape, validation surface, exposed-helper convention).

### 1.1 Domain layer — `StartCommit string` (single value, NOT slice) on `ActionItem` + `ActionItemInput` after `Files`; `NewActionItem` validates trim only; NO new sentinel; NO exposed `NormalizeActionItemStartCommit` helper

**Spec:** Single-value `string` (not `[]string`). Inserted after `Files`. `NewActionItem` performs trim only. No `ErrInvalidStartCommit` sentinel. No exposed `NormalizeActionItemStartCommit` (single-line trim too thin to warrant the indirection — matches `Owner` precedent).

**Evidence — struct fields:**

- `internal/domain/action_item.go:121-135` (post-edit): `StartCommit string` inserted in `ActionItem` between `Files` and `LifecycleState`. Single-value type confirmed (NOT `[]string`). Doc comment cites: free-form trimmed string; empty-string IS the meaningful zero value ("not yet captured / not applicable"); short-SHA + full-SHA + caller-supplied identifiers all round-trip; opaque-domain rule (domain layer holds opaquely; never calls `git rev-parse`); caller (orchestrator pre-cascade / Wave 2 dispatcher / Drop 4b commit-agent) supplies the value; disjoint-axis with `EndCommit` (paired field arrives in 4a.9); Drop 4b commit-agent consumes both for `git diff <start_commit>..<end_commit>`.
- `internal/domain/action_item.go:226-234` (post-edit): `StartCommit string` inserted in `ActionItemInput` between `Files` and `LifecycleState`. Doc mirrors the production-struct doc with the "empty IS meaningful zero value" call-out.

**Evidence — validation gate inside `NewActionItem`:**

- `internal/domain/action_item.go:412-421` (post-edit): `in.StartCommit = strings.TrimSpace(in.StartCommit)` — trim only, no rejection path. The comment block explicitly cites: free-form opaque-domain string; empty is legitimate zero; no format check (short-SHA / full-SHA / caller identifiers all round-trip); inline trim retained rather than a helper because "a single TrimSpace call is too thin to warrant the indirection" with explicit reference to the `Owner`-inline-trim precedent.
- `internal/domain/action_item.go:438`: `StartCommit: in.StartCommit` threaded into the returned `ActionItem` literal directly after `Files: files`.

**Evidence — NO new sentinel error:**

- `rg -n "ErrInvalidStartCommit" internal/` returns hits in `domain_test.go:1298` and `action_item.go:416` only — both are doc-comment references explicitly calling out the deliberate ABSENCE of the sentinel. No definition site in `internal/domain/errors.go`. Confirmed: no `ErrInvalidStartCommit` ever defined.

**Evidence — NO exposed `NormalizeActionItemStartCommit` helper:**

- `rg -n "NormalizeActionItemStartCommit|normalizeActionItemStartCommit" internal/` returns ONE hit at `action_item.go:416` — a doc comment explicitly stating the helper is deliberately not introduced ("kept here rather than behind a NormalizeActionItemStartCommit helper because a single TrimSpace call is too thin to warrant the indirection"). Zero exported function, zero internal worker. The decision is documented in the codebase itself (load-bearing for future readers).

**Evidence — single-string vs slice divergence preserved on all six wire surfaces:** ActionItem field is `string`; ActionItemInput is `string`; CreateActionItemInput is `string`; UpdateActionItemInput is `*string` (pointer-sentinel matches the Files/Packages/Paths slice precedent's pointer-sentinel pattern, but the dereferenced type is `string` not `[]string`); CreateActionItemRequest is `string`; UpdateActionItemRequest is `*string`; SnapshotActionItem is `string`; SQL column is `TEXT NOT NULL DEFAULT ''` (raw string, not JSON-encoded `'[]'`). No accidental slice-typing borrowed from 4a.5/4a.6/4a.7.

**Conclusion:** PASS.

### 1.2 App layer — `CreateActionItemInput.StartCommit string` (value-type) + `UpdateActionItemInput.StartCommit *string` (pointer-sentinel locked) threaded through Service Create + Update

**Spec:** Create-side is value-type `string` (empty IS meaningful zero — no pointer-sentinel needed at create boundary). Update-side is pointer-sentinel `*string` per Drop 3.21 / 4a.5 lock — nil preserves, non-nil applies, empty dereferenced clears. Service Update applies `strings.TrimSpace` inline (no domain helper exposed, matching the inline-trim convention).

**Evidence — struct fields:**

- `internal/app/service.go:546-559` (post-edit): `CreateActionItemInput.StartCommit string` between `Files []string` (line 549) and `ColumnID string` (line 560). Doc cites: empty IS meaningful zero (caller hasn't run `git rev-parse HEAD` yet); no pointer-sentinel needed at create boundary; threaded through `app.CreateActionItemInput → domain.NewActionItem`; opaque-domain (domain layer never calls git itself).
- `internal/app/service.go:638-654` (post-edit): `UpdateActionItemInput.StartCommit *string` between `Files *[]string` (line 640) and `Metadata *domain.ActionItemMetadata` (line 654). Doc cites: pointer-sentinel pattern (nil preserves, non-nil applies, empty dereferenced clears); rationale (description-only update must NOT silently clobber dispatcher-set start commit); inline `strings.TrimSpace` applied by service (no domain helper); reference to `Owner` inline-trim precedent; locked per WAVE_1_PLAN post-4a.5 amendment.

**Evidence — service threading on Create:**

- `internal/app/service.go:833`: `StartCommit: in.StartCommit` threaded into `domain.ActionItemInput`. Validation runs at the domain seam — `NewActionItem`'s inline `strings.TrimSpace` applies. No re-trim or re-validate at the service layer (correct — single source of truth).

**Evidence — service threading on Update:**

- `internal/app/service.go:1164-1174` (post-edit): Pointer-sentinel branch: `if in.StartCommit != nil { actionItem.StartCommit = strings.TrimSpace(*in.StartCommit); actionItem.UpdatedAt = s.clock().UTC() }`. Inline trim mirrors `NewActionItem`'s create-time trim approach. Doc comment cites: nil preserves; non-nil applies dereferenced; empty dereferenced clears (explicit caller intent — "dispatcher rolling back a retry"); no domain helper (single-line trim too thin); reference to L3 / WAVE_1_PLAN.md §1.4.

**Evidence — symmetry pattern:** Create-time trim (in `NewActionItem`) + update-time trim (in service) both call `strings.TrimSpace` directly. No shared helper. Documented as deliberate per the Owner-inline-trim precedent. Symmetry preserved.

**Conclusion:** PASS.

### 1.3 MCP wire layer — `CreateActionItemRequest.StartCommit string` + `UpdateActionItemRequest.StartCommit *string` threaded through adapter handlers

**Spec:** Adapter request types match the app-layer shape (value on create, pointer on update). Adapter handlers thread the values through.

**Evidence — request struct fields:**

- `internal/adapters/server/common/mcp_surface.go:122-131` (post-edit): `CreateActionItemRequest.StartCommit string` between `Files []string` and `ColumnID string`. Doc cites empty-as-meaningful-zero, opaque-domain, caller-supplied.
- `internal/adapters/server/common/mcp_surface.go:217-228` (post-edit): `UpdateActionItemRequest.StartCommit *string` between `Files *[]string` and `Metadata *domain.ActionItemMetadata`. Doc cites pointer-sentinel pattern + inline-trim path + locked per WAVE_1_PLAN post-4a.5 amendment.

**Evidence — adapter threading on Create:**

- `internal/adapters/server/common/app_service_adapter_mcp.go:664`: `StartCommit: strings.TrimSpace(in.StartCommit)` threaded into `app.CreateActionItemInput`. Adapter applies `TrimSpace` defensively (matches existing Title / Description / ColumnID precedent at lines 665-667). The duplicate trim with the domain-layer trim is benign — `TrimSpace` is idempotent. Defensive-at-the-edge pattern preserved.

**Evidence — adapter threading on Update:**

- `internal/adapters/server/common/app_service_adapter_mcp.go:737`: `StartCommit: in.StartCommit` threaded into `app.UpdateActionItemInput`. Pointer-sentinel passes through unchanged (no copy needed — pointer carries explicit-clear intent). No adapter-side trim on update; service applies `strings.TrimSpace` after dereference. This mirrors how Files/Packages/Paths are passed through verbatim on update at lines 734-736.

**Conclusion:** PASS.

### 1.4 MCP tool layer — `mcp.WithString("start_commit", ...)` schema (NOT `WithArray`) + round-trip test with 5 sub-cases

**Spec:** Tool schema uses `mcp.WithString` since the field is single-string (NOT `mcp.WithArray` like Paths/Packages/Files). Round-trip test covers create-with / create-without / update-with / update-empty-clear / update-without.

**Evidence — schema:**

- `internal/adapters/server/mcpapi/extended_tools.go:1479` (post-edit): `mcp.WithString("start_commit", mcp.Description("..."))` confirmed `WithString` (NOT `WithArray`, NOT `WithStringItems`). Inserted after the three slice-typed fields' `mcp.WithArray("paths"|"packages"|"files", ..., mcp.WithStringItems())`. Description text cites: opaque-domain rule, empty-as-zero, omit-on-update preserves, no format check, all caller identifiers round-trip, Drop 4b commit-agent consumer.
- `internal/adapters/server/mcpapi/extended_tools.go:879`: in the create-args closure, `StartCommit *string \`json:"start_commit"\`` declared as `*string` (matching Files/Packages/Paths' `*[]string` pointer-sentinel pattern at the schema-parsing layer — keeps "not supplied" distinguishable from "empty").
- `internal/adapters/server/mcpapi/extended_tools.go:1102-1110` (post-edit): create-side handler dereferences `if args.StartCommit != nil { createReq.StartCommit = *args.StartCommit }`. Comment correctly cites: nil = "not supplied" → leaves request's empty-string zero (domain treats as "not yet captured"); non-nil → set to dereferenced (domain trims at `NewActionItem` time); free-form opaque-domain.
- `internal/adapters/server/mcpapi/extended_tools.go:1198-1199`: update-side handler passes `args.StartCommit` (pointer) through verbatim into `UpdateActionItemRequest.StartCommit`. Doc comment cites: nil preserves, non-nil applies, empty dereferenced clears, service applies inline `TrimSpace`.

**Evidence — round-trip test with 5 sub-cases:**

- `internal/adapters/server/mcpapi/extended_tools_test.go:4240-4368`: `TestActionItemMCPStartCommitRoundTrip` — five `t.Run` sub-cases:
  1. **`create plumbs start_commit string through to request`** — create with `start_commit: "0cf5194"` lands as `service.lastCreateActionItemReq.StartCommit == "0cf5194"`.
  2. **`create without start_commit round-trips empty string`** — create omitting the field → `service.lastCreateActionItemReq.StartCommit == ""`.
  3. **`update with start_commit plumbs as non-nil pointer-sentinel`** — update with `start_commit: "0cf5194"` → `lastUpdateActionItemReq.StartCommit != nil && *lastUpdateActionItemReq.StartCommit == "0cf5194"`.
  4. **`update with empty start_commit plumbs as non-nil pointer to empty string (explicit clear)`** — update with `start_commit: ""` → `lastUpdateActionItemReq.StartCommit != nil && *lastUpdateActionItemReq.StartCommit == ""`. This is the load-bearing explicit-clear case that proves the pointer-sentinel actually distinguishes "supplied empty" from "not supplied."
  5. **`update without start_commit preserves prior via nil pointer-sentinel`** — update omitting the field → `lastUpdateActionItemReq.StartCommit == nil`. Proves the preserve-on-omit path.

All five sub-cases match the spec exactly. The pointer-sentinel test pair (sub-cases 4 + 5) is the architecturally important pair — 4a.5's amendment locked this pattern in, and 4a.8 inherits it correctly.

**Conclusion:** PASS.

### 1.5 SQL layer — `start_commit TEXT NOT NULL DEFAULT ''` (raw string column, NOT JSON-encoded) after `files_json`; INSERT + UPDATE + 5 SELECT sites + `scanActionItem` + `changedActionItemFields` threaded; round-trip test

**Spec:** Raw `TEXT` column (NOT JSON-encoded — single value, not a list). Default `''`. Inserted after `files_json`. Threaded through INSERT, UPDATE, all SELECT sites, `scanActionItem`, and `changedActionItemFields`. Round-trip test with empty / short-SHA / full-SHA cases.

**Evidence — schema:**

- `internal/adapters/storage/sqlite/repo.go:184` (post-edit): `start_commit TEXT NOT NULL DEFAULT ''` inserted between `files_json TEXT NOT NULL DEFAULT '[]'` (line 183) and `lifecycle_state TEXT NOT NULL DEFAULT 'todo'` (line 185). Default value is empty string `''` (NOT `'[]'` — proving the builder did not accidentally borrow JSON-array shape from Files/Packages/Paths). Type is `TEXT` (raw string).

**Evidence — INSERT + UPDATE column lists:**

- `internal/adapters/storage/sqlite/repo.go:1203` (INSERT): column list reads `..., paths_json, packages_json, files_json, start_commit, lifecycle_state, ...` — correct ordinal slot. VALUES placeholder count goes from 34 to 35 (`?, ?, ..., ?` 35 times at line 1206) — verified visually that the count matches the column count.
- `internal/adapters/storage/sqlite/repo.go:1223`: `t.StartCommit` argument in correct ordinal between `filesJSON` (line 1222) and `string(t.LifecycleState)` (line 1224). Raw string passed verbatim — no JSON encoding (correctly).
- `internal/adapters/storage/sqlite/repo.go:1319` (UPDATE): SET clause includes `..., files_json = ?, start_commit = ?, lifecycle_state = ?, ...`. Bind argument at line 1336: `t.StartCommit` between `filesJSON` (1335) and `string(t.LifecycleState)` (1337). Correct.

**Evidence — 5 SELECT sites:** I count six SELECT sites carrying the column (one more than the planner's "5 SELECT sites" wording, which appears to have undercounted; either way every read site is threaded):

1. `repo.go:1395` — `ListActionItems` (`SELECT ... files_json, start_commit, lifecycle_state ...`).
2. `repo.go:1433` — `ListActionItemsByParent`.
3. `repo.go:1467` — `FindActionItemByOwnerAndTitle`.
4. `repo.go:1488` — `ListActionItemsByDropNumber`.
5. `repo.go:2529` — `getActionItemByID`.

Plus the INSERT column list (which is technically a "site" but not a SELECT). All read sites enumerate `start_commit` in the same ordinal slot — between `files_json` and `lifecycle_state`. Critical: ordinal alignment with `scanActionItem` is preserved (next bullet).

**Evidence — `scanActionItem`:**

- `internal/adapters/storage/sqlite/repo.go:2857`: `startCommitRaw string` declared in the var block.
- `internal/adapters/storage/sqlite/repo.go:2877`: `&startCommitRaw` inserted in the `Scan` argument list between `&filesRaw` (2876) and `&state` (2878). Ordinal exactly matches the SELECT lists.
- `internal/adapters/storage/sqlite/repo.go:2929-2933`: `t.StartCommit = startCommitRaw` — raw string assignment, NO decode helper (correctly). Comment explicitly cites: raw TEXT column not JSON-encoded; pre-droplet-4a.8 rows + NOT NULL DEFAULT '' invariant read as empty string ("not yet captured" zero value).

**Evidence — `changedActionItemFields`:**

- `internal/adapters/storage/sqlite/repo.go:2664-2666`: `if prev.StartCommit != next.StartCommit { changed = append(changed, "start_commit") }`. Inserted after the `Files` slice-equality block (line 2661-2663). Uses `!=` for string comparison rather than `equalStringSlices` (correct — single-value, not a slice).

**Evidence — round-trip test:**

- `internal/adapters/storage/sqlite/repo_test.go:3082-3231`: `TestRepository_PersistsActionItemStartCommit` — exhaustively exercises every storage path:
  - Three create cases: `t-startcommit-empty` (empty), `t-startcommit-short` (`"0cf5194"`), `t-startcommit-full` (40-char SHA).
  - `GetActionItem` round-trip on each (line 3164-3171).
  - `ListActionItems` round-trip — exercises the second SELECT path (line 3174-3192).
  - `ListActionItemsByParent` round-trip — exercises the third SELECT path (line 3195-3210).
  - **UPDATE — populate path:** reassign `t-startcommit-empty.StartCommit = "deadbeef"`, persist, verify (line 3215-3225).
  - **UPDATE — clear path:** reassign `t-startcommit-full.StartCommit = ""`, persist, verify the prior value is cleared (line 3226-3231).

The test covers every SQL path that mutates or reads `start_commit`. The "populate-on-update" + "clear-on-update" pair specifically asserts the UPDATE SET clause writes both populated and empty payloads — load-bearing for the pointer-sentinel explicit-clear semantics at the wire layer.

**Conclusion:** PASS.

### 1.6 Snapshot layer — `SnapshotActionItem.StartCommit string \`json:"start_commit,omitempty"\`` after `Files`; threaded both directions; round-trip test + legacy-format-compat test

**Spec:** `SnapshotActionItem` gains `StartCommit string` field with `json:"start_commit,omitempty"` after `Files`. Threaded in `snapshotActionItemFromDomain` and `(t SnapshotActionItem) toDomain()`. Round-trip test + legacy-format-compat test.

**Evidence — struct field:**

- `internal/app/snapshot.go:73`: `StartCommit string \`json:"start_commit,omitempty"\`` between `Files []string \`json:"files,omitempty"\`` (line 72) and `LifecycleState domain.LifecycleState` (line 74). Tag includes `omitempty` (correct — empty strings omit on encode, missing field decodes to empty).

**Evidence — encode path:**

- `internal/app/snapshot.go:1091`: `StartCommit: t.StartCommit` threaded into `snapshotActionItemFromDomain`'s returned literal between `Files: append([]string(nil), t.Files...)` (line 1090) and `LifecycleState: t.LifecycleState` (line 1092). Direct copy (no slice-clone needed for a string).

**Evidence — decode path:**

- `internal/app/snapshot.go:1370-1374`: `StartCommit: t.StartCommit` threaded into `(t SnapshotActionItem) toDomain()`'s returned literal. Comment block cites: pre-droplet-4a.8 snapshots without the field deserialize to empty string (the legitimate domain zero value); legacy compat covered by `omitempty` tag.

**Evidence — round-trip test:**

- `internal/app/snapshot_test.go:1088-1136`: `TestSnapshotActionItemStartCommitRoundTrip` — table-driven with 4 cases: empty zero-value / short-SHA / full-SHA / free-form-identifier. Each case constructs the domain item, encodes via `snapshotActionItemFromDomain`, asserts the snapshot's `StartCommit` matches expectation, then decodes via `snap.toDomain()` and asserts the round-tripped domain item's `StartCommit` matches.

**Evidence — legacy-format-compat test:**

- `internal/app/snapshot_test.go:1140-1174`: `TestSnapshotActionItemStartCommitLegacyFormatCompatibility` — constructs a raw JSON byte slice OMITTING the `start_commit` field entirely (matches the pre-4a.8 snapshot wire format), unmarshals into `SnapshotActionItem`, asserts `snap.StartCommit == ""` (pure default), then calls `snap.toDomain()` and asserts the hydrated domain item also has `StartCommit == ""`. This is the load-bearing forward-compatibility test — proves that a snapshot encoded before this droplet round-trips cleanly without a `SnapshotVersion` bump.

**Conclusion:** PASS.

### 1.7 TUI schema-coverage — `StartCommit` classified as `readOnly`

**Spec:** The TUI schema-coverage assertion at `model_test.go:TestActionItemSchemaCoverageIsExplicit` enumerates every top-level `domain.ActionItem` field as either `editable` or `readOnly`. New `StartCommit` field must land in `readOnly`.

**Evidence:**

- `internal/tui/model_test.go:14869`: `"StartCommit":    {},` inserted in the `readOnly` map between `"Files":` (14868) and `"LifecycleState":` (14870). Same indent + style as the surrounding entries. The `editable` map (lines 14845-14852) does NOT contain `StartCommit` — confirmed (would have caused `assertExplicitFieldCoverage` to flag `StartCommit` if it appeared in both or neither).

`mage test-pkg ./internal/tui` would surface a `domain.ActionItem` struct field that isn't enumerated in either map (the assertion is exhaustive). The `mage ci` green run (§2 below) proves the assertion holds.

**Conclusion:** PASS.

### 1.8 Validation rules

**Spec:**
- Trim only.
- No empty-string rejection (empty IS valid zero — "not yet captured").
- No SHA format check — short-SHA, full-SHA, free-form identifiers all round-trip.
- Opaque-domain field — domain layer does NOT call `git rev-parse`; caller supplies.

**Evidence — trim only:**

- `action_item.go:421`: `in.StartCommit = strings.TrimSpace(in.StartCommit)` — single-line trim, no error path. Done.

**Evidence — no empty-string rejection:**

- `domain_test.go:1304`: first table case `{name: "empty round-trips empty", input: "", want: ""}` — this case would FAIL if the domain rejected empty input, since the test expects `NewActionItem` to succeed. The `mage ci` green run confirms this case passes.
- `domain_test.go:1305`: second case `{name: "whitespace-only collapses to empty", input: "   ", want: ""}` — whitespace-only also accepted (collapses to empty after trim). Confirms the trim-only rule.

**Evidence — no SHA format check:**

- `domain_test.go:1306-1309`: test cases include `"0cf5194"` (7-char short-SHA), the 40-char full-SHA `"0cf5194d4cb6c8d4f9b9b1d7e1f9d3c2b4e5a6f7"`, and the free-form `"branch/feature@head"`. All round-trip identically. The non-SHA `"branch/feature@head"` case is the load-bearing one — proves no hex/length validator is hidden anywhere.
- The action_item.go inline-trim block (lines 412-421) contains no `regexp` / `len()` / hex-check call. Confirmed empty validation path beyond `TrimSpace`.

**Evidence — opaque-domain (no `git rev-parse`):**

- `rg "git rev-parse|exec.Command|os/exec" internal/domain/` would surface any git shell-out from the domain layer; the action_item.go diff contains no such call. The doc comment at `action_item.go:131-134` explicitly states "Opaque-domain field: the domain layer holds it opaquely and does NOT call `git rev-parse HEAD` itself — the caller (orchestrator pre-cascade; Wave 2 dispatcher; Drop 4b commit-agent) supplies the value." This is consistent with the `Title` / `Description` / `Owner` precedent (caller-supplied opaque strings). No new external dependency introduced.

**Conclusion:** PASS.

### 1.9 Pre-existing slice-droplet tests still pass with new `StartCommit` field present

**Spec:** Adding a new struct field can break test-data fixtures that construct `ActionItem` with positional struct literals or that compare struct equality. Builder must not break 4a.5 / 4a.6 / 4a.7 round-trip assertions.

**Evidence:** All Go struct literals in the diff use named-field initialization (`StartCommit: in.StartCommit`, `Files: t.Files` style) — no positional struct literals, so adding a new field is a strictly-additive change. The `mage ci` run confirms all 1885+ pre-existing tests (4a.7 builder claim) plus the new ones land at 1929 passing.

Specifically:
- `TestNewActionItemFilesAllowsOverlapWithPaths` (4a.7) still passes — confirmed in §2 mage ci output.
- `TestSnapshotActionItemFilesRoundTrip` / `TestSnapshotActionItemFilesLegacyFormatCompatibility` (4a.7) still pass — confirmed in §2 mage ci output.
- `TestActionItemMCPFilesRoundTrip` (4a.7) still passes — confirmed in §2 mage ci output.
- `TestRepository_PersistsActionItemFiles` (4a.7) still passes — confirmed in §2 mage ci output.

No regression.

**Conclusion:** PASS.

---

## 2. `mage ci` Re-Run — Verification Gate

QA re-ran `mage ci` live in the working directory at HEAD-with-staged-diff. Output excerpt:

```
Test summary
  tests: 1930
  passed: 1929
  failed: 0
  skipped: 1
  packages: 20
  pkg passed: 20
  pkg failed: 0
  pkg skipped: 0

Skipped tests
- TestStewardIntegrationDropOrchSupersedeRejected [SKIP]
  package: github.com/evanmschultz/tillsyn/internal/adapters/server/mcpapi
  elapsed: 0.00s
```

Builder claim was: 1929 passed / 0 failed / 1 skip. **VERIFIED IDENTICAL.**

The single skip is the pre-existing `TestStewardIntegrationDropOrchSupersedeRejected` — pre-MVP supersede CLI gate, unrelated to this droplet. Builder accurately reported it as a pre-existing skip.

**Coverage gate:** every package at or above the 70.0% floor. Lowest packages:
- `internal/app` — 70.9% (closest to floor; pre-existing baseline carried over from prior droplets, no regression).
- `internal/tui` — 71.0%.
- `internal/adapters/server/mcpapi` — 73.3%.
- `internal/adapters/server/common` — 73.7%.
- `internal/adapters/auth/autentauth` — 73.4%.

`mage ci` build step also passed (`till` binary built from `./cmd/till`). Full pipeline green.

**Conclusion:** Verification gate PASSES.

---

## 3. Negative-Space Verification

Bullets the spec explicitly forbids — verified ABSENT.

### 3.1 No JSON encoding of `StartCommit` in SQL

`repo.go:184` declares `start_commit TEXT NOT NULL DEFAULT ''` (empty string default, NOT `'[]'`). `repo.go:1223` passes `t.StartCommit` raw into the INSERT (no `json.Marshal` wrapper). `repo.go:2929-2933` reads `startCommitRaw` raw into `t.StartCommit` (no `json.Unmarshal` decode). Confirmed: raw string column, not JSON-encoded.

### 3.2 No list-validation logic borrowed from Files/Packages/Paths droplets

The validation gate at `action_item.go:412-421` contains exclusively `strings.TrimSpace`. No `for ... range files` loop, no `seen map[string]bool` dedup, no `strings.ContainsRune(file, '\\')` backslash check, no `len(paths) > 0 && len(packages) == 0` coverage gate. Every list-axis validation pattern from 4a.5 / 4a.6 / 4a.7 is conspicuously absent. Confirmed: no accidental borrowing.

### 3.3 No `mcp.WithArray` for `start_commit`

`extended_tools.go:1479` uses `mcp.WithString("start_commit", ...)`. No `mcp.WithArray("start_commit", ...)` or `mcp.WithStringItems()` modifier. Schema correctly reflects single-string nature.

### 3.4 No SHA format check anywhere downstream

Search for any post-domain validation that might reject non-hex inputs: nothing in `service.go`, `app_service_adapter_mcp.go`, `extended_tools.go`, or `repo.go` performs format validation on the string. The `mcp.WithString` schema declaration carries no `mcp.Pattern(...)` or regex enforcement either. The `domain_test.go` test case `"branch/feature@head"` round-tripping confirms this end-to-end.

### 3.5 No domain-layer git shell-out

Diff for `action_item.go` contains no `os/exec` import addition, no `exec.Command("git", ...)` call. Opaque-domain rule preserved.

### 3.6 No new `ErrInvalidStartCommit` sentinel

`internal/domain/errors.go` is unchanged in this diff (`git diff --stat` confirms). No new error sentinel introduced. Trim-only validation correctly does not produce a rejection error.

### 3.7 No exposed `NormalizeActionItemStartCommit` helper

Verified above (§1.1). The doc comment at `action_item.go:416` explicitly cites this as a deliberate decision — single-line trim too thin to wrap. Future-reader-friendly.

**Conclusion:** All seven negative-space checks PASS.

---

## 4. Summary

**VERDICT: PASS.**

Every acceptance bullet from `WAVE_1_PLAN.md` §1.4 is satisfied. The droplet correctly diverges from the slice-typed precedent of 4a.5 / 4a.6 / 4a.7 on every load-bearing axis:

- Single `string` field type (not `[]string`).
- `TEXT NOT NULL DEFAULT ''` SQL column (not `TEXT NOT NULL DEFAULT '[]'`).
- No new sentinel error (vs. `ErrInvalidPaths` / `ErrInvalidPackages` / `ErrInvalidFiles`).
- No exposed `NormalizeActionItemStartCommit` helper (vs. exposed `NormalizeActionItemPaths` / `Packages` / `Files`).
- No JSON encoding (raw string read/write).
- `mcp.WithString` schema (not `mcp.WithArray`).
- Trim-only validation (no dedup / forward-slash / backslash checks).

The pointer-sentinel pattern on the update path is correctly preserved. Round-trip and legacy-format-compat tests cover the full matrix. `mage ci` green at 1929 passed / 1 pre-existing skip / 0 failed. Coverage above the 70% floor on every package.

The droplet ships clean.

---

## TL;DR

- T1: Acceptance bullets verified across 8 layers — domain trim-only, app pointer-sentinel update, MCP wire pointer-sentinel update, MCP tool `WithString` schema, SQL raw-text column, snapshot omitempty + legacy compat, TUI readOnly classification, validation rules absent — all PASS.
- T2: `mage ci` re-run at HEAD+diff: 1929 passed / 0 failed / 1 pre-existing skip; coverage gate green; identical to builder claim. PASS.
- T3: Negative-space (no JSON encoding, no list-validation, no `WithArray`, no SHA check, no git shell-out, no new sentinel, no normalize helper) — all seven absent as required. PASS.
- T4: Verdict PASS — droplet ships clean; correctly diverges from slice-droplet precedent.

---

## Hylla Feedback

N/A — task touched non-Go-only files (the diff is Go but the QA workflow used `git diff` + `Read` + `rg` + `mage ci` exclusively rather than Hylla, since the staged diff is uncommitted-and-unbuilt and Hylla indexes only committed code per the project's Hylla-Go-only-today rule + the changed-since-ingest fallback rule). No Hylla query attempted; no Hylla miss to report. The fallback chain (`git diff` for staged delta, `Read` for context, `rg` for negative-space sweeps, `mage ci` for gate) was the right tool stack for QA on uncommitted state. No Hylla ergonomics gripes to file.
