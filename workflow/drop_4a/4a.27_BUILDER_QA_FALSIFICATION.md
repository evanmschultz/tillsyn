# Droplet 4a.27 — QA Falsification (Round 1)

## Verdict

**PASS-WITH-NIT.** Tests pin the W3.4 acceptance behavior with strong assertions; case (d) integration genuinely exercises the metadata-driven cross-subtree exception via the live ancestry walk. Three non-blocking NITs surfaced: a refinement-routing gap (worklog claim vs memory file), a dead helper (`unitStewardSession`), and a spec-vs-implementation deviation in case (e) (stubbed vs spec-prescribed real-toggle drive).

## Attack Matrix

### A1 — Stub-vs-integration discipline: do (a)/(b)/(c)/(e) actually exercise `case "approve"` mapping?

- **Hypothesis.** Cases use `stubAuthRequestService` — could the stub bypass the JSON-RPC `case "approve"` switch entirely (e.g., the stub plus the test fixture short-circuit before line 321 of `handler.go`).
- **Evidence.** `internal/adapters/server/mcpapi/handler.go:321-370` — the `case "approve"` block validates required args, calls `authRequests.ApproveAuthRequest(ctx, ...)` (line 353), routes errors through `toolResultFromError`. The stub `stubAuthRequestService.ApproveAuthRequest` (`handler_test.go:212-218`) is the `authRequests` interface implementation; the test wires it via `NewHandler(Config{}, capture, nil)`. The JSON-RPC payload `{"operation": "approve", ...}` (built by `approveArgs`, line 2444-2453) flows through the dispatch switch at handler.go:321.
- **Verdict.** **MITIGATED.** Cases (a)/(b)/(c)/(e) exercise the full wire-format path — `tools/call` → `case "auth_request"` → `case "approve"` → required-arg validation → adapter call → `mapToolError` (on err) or `mcp.NewToolResultJSON(record)` (on ok). The stub records `lastApprove = req` (line 213) BEFORE branching on `approveErr`, so case (c)'s `lastApprove.ActingSessionID` assertion (line 2617-2619) confirms the request reached the stub.

### A2 — Case (a) `approving_principal_id` exact-match vs non-empty

- **Hypothesis.** Test asserts only `non-empty` on audit fields → could pass for wrong reason if any string lands.
- **Evidence.** `handler_test.go:2498-2506` — three exact-match assertions:
  - `requestRaw["approving_principal_id"].(string); got != orch.principalID` (orch.principalID = `"ORCH_INSTANCE_W34"`).
  - `requestRaw["approving_agent_instance_id"].(string); got != orch.agentInstanceID` (= `"AGENT_INSTANCE_W34_ORCH"`).
  - `requestRaw["approving_lease_token"].(string); got != orch.leaseToken` (= `"LEASE_TOKEN_W34_ORCH"`).
  - Plus stub round-trip assertions on `lastApprove.{ActingSessionID, ActingSessionSecret, AgentInstanceID, LeaseToken}` (lines 2511-2525).
- **Verdict.** **MITIGATED.** Exact-match. Strong audit-field discrimination — only `omitempty`-elided value is empty string, and that would FAIL the assertions. ✓

### A3 — Case (b) error-text discrimination from case (c)

- **Hypothesis.** Case (b) error fragment could overlap case (c) fragment so a (c)-style error trips (b)'s assertion.
- **Evidence.**
  - Case (b) sentinel (`auth_requests.go:449`): `"orch cannot approve another orchestrator's auth request: %w"` wrapping `ErrAuthorizationDenied`.
  - Case (c) sentinel (`auth_requests.go:520`): `"cross-orch approval requires steward approver (request requested_by %q, approver %q): %w"` wrapping `ErrAuthorizationDenied`.
  - Test (b) asserts `Contains "orch cannot approve another orchestrator"` (line 2565) — the substring NEVER appears in case (c)'s error.
  - Test (c) asserts `Contains "cross-orch approval requires steward approver"` (line 2612) — the substring NEVER appears in case (b)'s error.
  - Both also assert `HasPrefix "auth_denied:"` because both wrap `ErrAuthorizationDenied`.
- **Verdict.** **MITIGATED.** Strong cross-discrimination; neither test passes for the other's wrapped sentinel. ✓

### A4 — Case (c) `requested_by_actor` semantics: real flow vs synthetic flow

- **Hypothesis.** Plan spec line 137 demands the test verify "regardless of orch_A's path-encompasses status" — does the unit case actually exercise the `requested_by_actor != approver.principal_id` rejection, or is it a stubbed simulacrum?
- **Evidence.** Case (c) at `handler_test.go:2582-2620` is **stub-only**. The stub returns `approveErr` directly — the `RequestedByActor != approverPrincipalID` semantics are NOT tested at the MCP layer here. This is **explicit, documented**: worklog "Integration vs Unit Split" §1 (line 25): "the unit cases here verify the JSON-RPC handler's mapping from the gate's wrapped errors to MCP `isError=true` responses with the correct error-text fragments". The actual `requested_by_actor` semantics are tested at the service layer (`internal/app/auth_requests_test.go:1112+ stewardCrossSubtreeFixture`).
- **Verdict.** **ACCEPTED.** Documented unit/integration split; service-layer tests own the gate-logic contract. The MCP layer test owns the wire-format error propagation. Worklog flags this explicitly.

### A5 — Case (d) integration: does it exercise metadata-driven persistent-parent detection vs STEWARD-principal-type bypass?

- **Hypothesis.** STEWARD principal_type alone could short-circuit the cross-subtree exception, never reaching the `requireStewardPersistentAncestor` ancestry walk.
- **Evidence.** Service-layer gate (`internal/app/auth_requests.go:496-525`):
  1. Path-encompasses check (line 496) — STEWARD's project-scope path encompasses request's `project/p-w34-d/branch/PERSISTENT_STEWARD_W34_D`. ✓ pass.
  2. Cross-orch trigger (line 509): `requestedBy="OTHER_DROP_ORCH_W34" != approverPrincipalID="STEWARD"` → enters cross-orch branch.
  3. STEWARD-principal-type gate (line 519): `session.AuthRequestPrincipalType == "steward"` → passes. **NOT a bypass — gates onto step 4.**
  4. **Ancestry walk** (line 522): `requireStewardPersistentAncestor(requestPath)`. Walk loads action_item `PERSISTENT_STEWARD_W34_D` (the request's `BranchID`); checks `ai.Persistent && strings.EqualFold(ai.Owner, "STEWARD")`. Both true (fixture sets them at handler_steward_integration_test.go:730-731). Returns nil → approve proceeds.
  - **Counterfactual proof of necessity**: If the persistent-ancestor record were not created, `s.repo.GetActionItem` would return `ErrNotFound` (line 556-558), exit the loop, and return the rejection at line 566. Therefore the test cannot pass without the metadata-driven ancestry walk firing.
- **Verdict.** **MITIGATED.** Case (d) genuinely exercises the metadata-driven detection mechanism. Strong integration test. ✓

### A6 — Case (e) `ErrOrchSelfApprovalDisabled` discrimination

- **Hypothesis.** Test asserts only `isError=true` → could pass for any error including (b) or (c) wrapping.
- **Evidence.** `handler_test.go:2661-2667`:
  - `text := toolResultText(...)` then `Contains "orch self-approval disabled by project metadata"` (the `domain.ErrOrchSelfApprovalDisabled` sentinel string).
  - `Contains "opted out of orch self-approval"` (the wrap fragment from `auth_requests.go:443`).
  - Neither substring appears in (b)'s `"orch cannot approve another orchestrator"` or (c)'s `"cross-orch approval requires steward approver"`.
  - Stub error: `fmt.Errorf("project %q has opted out of orch self-approval: %w", "p1", domain.ErrOrchSelfApprovalDisabled)` (line 2646).
- **Verdict.** **MITIGATED.** Sharp sentinel discrimination on text fragments. ✓

### A7 — `ErrOrchSelfApprovalDisabled` mapping refinement: real bug or test fragility?

- **Hypothesis.** Worklog flags the `mapToolError` switch has no case for `ErrOrchSelfApprovalDisabled` → response surfaces with `internal_error:` prefix instead of a sharper code. Is the test fragile (assertion pinned to wrong-prefix observation) or is it a real refinement-grade defect?
- **Evidence.**
  - `handler.go:892-966 mapToolError` switch: no case matches `domain.ErrOrchSelfApprovalDisabled`. Falls to `default: "internal_error: " + err.Error()` (line 960-965).
  - Test (line 2661-2667) asserts on **message text fragments only**, NOT on prefix — the test is correctly written to NOT pin behavior to a wrong-code observation. If a future drop adds the case, the test still passes; the prefix-related observation is acknowledged in the test docstring (line 2629-2635).
  - Worklog claim (line 53): "Refinement candidate (added to project_drop_4a_refinements_raised memory)".
  - **Memory check:** `~/.claude/projects/-Users-evanschultz-Documents-Code-hylla-tillsyn/memory/project_drop_4a_refinements_raised.md` carries R1–R11 inclusive. **No R12 entry for the `ErrOrchSelfApprovalDisabled` mapping.** The worklog's claim of "added to memory" is unsubstantiated as of inspection.
- **Verdict.** **CONFIRMED NIT (refinement-routing gap).** The bug is real but routed correctly to the test docstring + worklog narrative; the routing-to-memory step is missing. Refinement-grade — does not block droplet completion, but breaks the audit chain the worklog claims. **Recommend: append R12 to `project_drop_4a_refinements_raised.md` capturing the `mapToolError` mapping refinement before drop closes.** Test itself is well-constructed.

### A8 — Tool schema test: negative-existence assertion shape correctness

- **Hypothesis.** Schema test could mis-shape the negative-existence probe (e.g., probe `properties` map without also probing `operation` enum values; or use a `Contains` substring that gives false negatives).
- **Evidence.** `handler_test.go:2678-2748`:
  - Resolves `authSchema["properties"]` map; iterates `briefForbidden` (4 keys); asserts `_, exists := properties[key]; exists` is false (line 2723-2725). Direct map-key existence — exact match. ✓
  - Resolves `operation` enum via `schemaPropertyEnumStrings(t, authSchema, "operation")`; asserts `slices.Contains(enums, "approve")` is true (line 2732-2734); iterates enums × forbidden cross-product, asserts no value matches a forbidden (line 2735-2741). ✓
  - Asserts five documented args (`request_id`, `acting_session_id`, `acting_session_secret`, `agent_instance_id`, `lease_token`) all exist in `properties` (line 2744-2748). ✓
- **Verdict.** **MITIGATED.** Three-pronged: properties-map-no-leak, operation-enum-no-leak, documented-args-present. Defense in depth. ✓

### A9 — Helper `unitStewardSession` consistency with 4a.24 metadata key

- **Hypothesis.** `unitStewardSession` writes a stub-only tuple with `principalID="STEWARD"`. The 4a.24 chain plumbs `auth_request_principal_type` metadata key through `IssueSession → mapSessionView → AuthSession.AuthRequestPrincipalType → gate`. Drift would silently break case (d).
- **Evidence.** Case (d) does NOT use `unitStewardSession` — it builds the STEWARD session via `auth.IssueSession(ctx, autentauth.IssueSessionInput{PrincipalType: "steward", ...})` (handler_steward_integration_test.go:744-757). Service writes `metadata["auth_request_principal_type"] = "steward"` at autentauth/service.go:228; reads back at line 773 and 1438. Gate reads `session.AuthRequestPrincipalType` at auth_requests.go:519. **Full chain consistent.**
- **Verdict.** **MITIGATED.** Plumbing chain verified end-to-end. ✓

### A10 — Concurrent fixture state: shared DB, race risk

- **Hypothesis.** Helpers reuse a DB across tests → race / shared state. Or a `t.Parallel()` test corrupts another's DB.
- **Evidence.**
  - Integration test: `repo, err := sqlite.Open(filepath.Join(t.TempDir(), "tillsyn.db"))` at handler_steward_integration_test.go:684 — fresh per-test DB. ✓
  - Unit cases (a)/(b)/(c)/(e) call `t.Parallel()` (lines 2461, 2537, 2583, 2637); schema test (line 2679) too. Each constructs an isolated `stubAuthRequestService` + `httptest.NewServer`. No shared mutable state between unit tests.
  - Integration test (line 833) does NOT call `t.Parallel()`. Per-test `t.TempDir()` would make parallel safe, but the integration test runs serially. **Minor missed opportunity** — not a bug.
- **Verdict.** **MITIGATED with NIT.** Fresh-DB-per-test + isolated stubs eliminate concurrent-state races. NIT: integration test could call `t.Parallel()` but doesn't.

### A11 — `unitStewardSession` is dead code

- **Hypothesis.** Helper is defined but never called.
- **Evidence.**
  - `unitStewardSession` defined at `handler_test.go:2387-2395`.
  - Referenced only in docstrings (lines 2352, 2361, 2384). Never invoked.
  - Docstring claims: "Used by the case-(e) toggle test — case (d) integration uses real-session helpers" (line 2384-2386). But case (e) at line 2636-2668 uses `unitOrchSession(t, "B")` (line 2639), NOT `unitStewardSession`.
- **Verdict.** **CONFIRMED NIT (dead code).** Non-blocking. Either delete or wire case (e) to `unitStewardSession` to match the docstring intent. The wire-format tests don't depend on which tuple is used since the stub doesn't validate; either works.

### A12 — Case (e) implementation deviates from spec

- **Hypothesis.** Plan spec at WAVE_3_PLAN.md:139 says: "Set project metadata `OrchSelfApprovalEnabled = *false` via `till.project(operation=update)`. Repeat case (a) setup." Implementation stubs `approveErr` directly without touching project metadata.
- **Evidence.** Test at line 2636-2668 does NOT call `till.project(operation=update)`; injects the wrapped sentinel via `approveErr` (line 2646). Worklog "Integration vs Unit Split" justifies stubbing for wire-format coverage; the actual toggle behavior is exercised by service-layer tests (`internal/app/auth_requests_test.go`).
- **Verdict.** **ACCEPTED with NIT.** Documented departure from spec wording — the wire-format coverage justification holds, and the gate-logic contract is owned by service-layer tests. Spec-vs-implementation drift is intentional and worklogged. NIT-grade — could be sharpened to spec-faithful by promoting (e) to integration-style alongside (d), but not required for droplet completion.

### A13 — `unitOrchSession` unused parameter `branchID`

- **Hypothesis.** Helper takes `branchID` parameter that's never used in the function body.
- **Evidence.** `handler_test.go:2373-2382` — `_ = branchID // path is encoded in the request, not the tuple` (line 2374). Parameter exists for symmetry with the spec's signature shape.
- **Verdict.** **ACCEPTED.** Documented intentional unused-parameter pattern. Could be removed but harmless.

### A14 — Sentinel string drift: domain `ErrOrchSelfApprovalDisabled` text

- **Hypothesis.** Test asserts `Contains "orch self-approval disabled by project metadata"` — if the domain sentinel's text changes, test breaks silently.
- **Evidence.** Test stub error format (line 2646): `"project %q has opted out of orch self-approval: %w"` wrapping `domain.ErrOrchSelfApprovalDisabled`. Whatever the sentinel's text, `errors.Unwrap → err.Error()` recursive flatten produces `"project \"p1\" has opted out of orch self-approval: <sentinel-text>"`. The test's first assertion `Contains "orch self-approval disabled by project metadata"` is the sentinel text contract — drift in domain layer would break this test loudly. Pinning behavior, not coincidental match.
- **Verdict.** **MITIGATED.** Sharp pin on the domain sentinel text. ✓

## Summary

- 14 attacks attempted; 9 **MITIGATED**, 2 **ACCEPTED** (documented intentional choices), 3 **CONFIRMED NIT** — none blocking droplet completion.
- Strong audit-field exact-match assertions in case (a) and case (d). Case (d) integration genuinely exercises the metadata-driven persistent-parent ancestry walk (counterfactual proof: removing the persistent ancestor record would force test failure via `requireStewardPersistentAncestor` → `ErrNotFound` → reject path). Sentinel discrimination across (b)/(c)/(e) is sharp — no cross-assertion overlap. Schema test probes both `properties` map and `operation` enum (defense in depth). Helper consistency with 4a.24's `auth_request_principal_type` metadata key plumbing verified end-to-end. Two unit/integration-split deviations from spec are documented and justified at the worklog level (cases (c) and (e) stub wire-format coverage; gate logic owned by service-layer tests).

## Confirmed NITs (non-blocking; route to drop closeout)

- **NIT-1 (refinement-routing gap).** Worklog claims `mapToolError` `ErrOrchSelfApprovalDisabled` mapping refinement was "added to project_drop_4a_refinements_raised memory" (worklog line 53). Memory file inspection shows R1–R11 inclusive; no R12 entry for this refinement. **Recommend: append R12 capturing the mapping gap before drop 4a closes** so the audit chain matches the worklog narrative.
- **NIT-2 (dead helper).** `unitStewardSession` (`handler_test.go:2387-2395`) is defined but never called. Docstring claims case-(e) usage; case (e) actually uses `unitOrchSession`. Either delete the helper or wire case (e) to use it.
- **NIT-3 (case-(e) spec-vs-implementation deviation).** Plan spec (WAVE_3_PLAN.md:139) prescribes driving the toggle via `till.project(operation=update)`; implementation stubs `approveErr`. Justified by the worklog's unit/integration split, but a future hardening pass could promote case (e) to integration-style alongside case (d) for spec-faithful coverage.

## Hylla Feedback

- **Query**: `hylla_search_keyword` for the new test surface (`TestAuthRequestApprove`, `unitOrchSession`, `stewardApproveFixture`).
  **Missed because**: Hylla snapshot (5) is pre-Wave-3 — the 4a.27 commit is not yet ingested. Reingest is drop-end only.
  **Worked via**: `Read` against the working tree, `grep -n` via `/usr/bin/grep` for cross-references between test files, handler.go, auth_requests.go, autentauth/service.go.
  **Suggestion**: Same systemic gripe as 4a.27 builder's Hylla feedback — mid-drop "soft reingest" cadence (after each wave / non-trivial cluster) would keep subsequent droplets in the same drop searchable. Not a 4a.27 blocker.

- **Ergonomic note**: As builder noted, `hylla_search_keyword` default `test_mode=hide_tests` masks the `TestAuthRequestApprove*` surface even when present in a current snapshot. For QA-falsification work specifically, the desired-default inverts (we are explicitly probing test bodies).
