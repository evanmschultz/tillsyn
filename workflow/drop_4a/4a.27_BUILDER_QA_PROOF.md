# Droplet 4a.27 — QA Proof Round 1

**Verdict:** PASS-WITH-NIT — every acceptance item PROVEN; one minor schema-test scope nit (the existing 4a.24 sibling test already covers the brief's vocabulary, but the worklog itself documents the deliberate sibling rather than mutation, so this is observed not failed).

**Method:** read both test files, executed `mage test-pkg ./internal/adapters/server/mcpapi` (170 pass / 1 pre-existing skip), executed each of the 6 W3.4 tests individually via `mage testFunc` to confirm verified-green not name-only.

---

## 1. Test Fixture Helpers (Acceptance Item 1) — PROVEN

W3.4 brief asks for `setupOrchSession`, `setupStewardSession`, `createPendingSubagentRequest` "(or equivalent shape)". Builder implemented an equivalent-shape split keyed to the unit/integration mode boundary.

| Helper                              | Mode             | File:Line                                                                                         | Purpose                                                                                                    |
| ----------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| `orchSessionTuple` (struct)         | shared           | `internal/adapters/server/mcpapi/handler_test.go:2362-2368`                                       | Five-field tuple (sessionID/secret, agentInstanceID, leaseToken, principalID) reused across unit + integration. |
| `unitOrchSession`                   | unit             | `handler_test.go:2373-2382`                                                                       | Deterministic drop-orch tuple — stub records, doesn't validate.                                            |
| `unitStewardSession`                | unit             | `handler_test.go:2387-2395`                                                                       | Deterministic STEWARD tuple — used by case (e).                                                            |
| `stubPendingSubagentRequest`        | unit             | `handler_test.go:2401-2424`                                                                       | Returns `common.AuthRequestRecord` shaped like a pending subagent request, branchID encoded into path.     |
| `approvedAuthRequestRecord`         | unit             | `handler_test.go:2430-2439`                                                                       | Turns one pending record into approved-state with audit-trail fields populated. Powers case (a)'s stub.    |
| `approveArgs`                       | shared           | `handler_test.go:2444-2453`                                                                       | JSON-RPC argument map for one approve call — pinned to the documented vocabulary only.                     |
| `stewardApproveFixture` + factory   | integration      | `handler_steward_integration_test.go:664-824`                                                     | Real-stack equivalent: sqlite + autent + adapter + httptest + STEWARD-typed session + persistent ancestor. |

Brief shape match — equivalent shape per the brief's "(or equivalent shape)" allowance:

- `setupOrchSession` ↔ `unitOrchSession` (unit) + `stewardApproveFixture` (integration).
- `setupStewardSession` ↔ `unitStewardSession` (unit) + `stewardApproveFixture` STEWARD-typed session minting.
- `createPendingSubagentRequest` ↔ `stubPendingSubagentRequest` (unit) + `svc.CreateAuthRequest` call inside `newStewardApproveFixture` (integration, `handler_steward_integration_test.go:779-793`).

The split is explicitly justified in the worklog (4a.27_BUILDER_WORKLOG.md:31-34) — unit-mode helpers are stub-only value tuples; integration-mode helpers run the real stack. Acceptable equivalent shape.

## 2. Five Named Test Cases (Acceptance Item 2) — PROVEN, all verified-green

| Case | Required Test Name                                                              | Located At                                                                                       | Verified Green                                |
| ---- | ------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ | --------------------------------------------- |
| (a)  | `TestAuthRequestApproveOrchInSubtreeApprovesNonOrchSucceeds`                    | `handler_test.go:2460-2526`                                                                      | `mage testFunc` PASS, 1.42s, race detector on |
| (b)  | `TestAuthRequestApproveOrchOfOrchestratorRejected`                              | `handler_test.go:2536-2568`                                                                      | `mage testFunc` PASS, 1.42s                   |
| (c)  | `TestAuthRequestApproveCrossOrchSubtreeRejected`                                | `handler_test.go:2582-2620`                                                                      | `mage testFunc` PASS, 1.42s                   |
| (d)  | `TestAuthRequestApproveStewardCrossSubtreeSucceedsForPersistentParent`          | `handler_steward_integration_test.go:833-905`                                                    | `mage testFunc` PASS, 7.98s (real stack)      |
| (e)  | `TestAuthRequestApproveProjectToggleDisabledRejected`                           | `handler_test.go:2636-2668`                                                                      | `mage testFunc` PASS, 1.41s                   |

### Per-case proof of acceptance criteria

**Case (a)** (`handler_test.go:2460-2526`):

- Drop-orch tuple at branch B (`unitOrchSession(t, "B")`, line 2463) approves a pending qa-proof subagent at the same branch (line 2464). PROVEN.
- Asserts `state == "approved"` (line 2495-2497). PROVEN.
- Asserts non-empty issued session secret (line 2507-2509). PROVEN.
- Asserts audit fields all populated: `approving_principal_id` (line 2498-2500), `approving_agent_instance_id` (line 2501-2503), `approving_lease_token` (line 2504-2506). PROVEN.
- Cross-checks stub's `lastApprove` records the orch's tuple intact (lines 2511-2525). EXTRA-MITIGATION — verifies handler→service tuple round-trip, not strictly required by acceptance.

**Case (b)** (`handler_test.go:2536-2568`):

- Orch-A approves another orchestrator → rejected. Stub returns `fmt.Errorf("orch cannot approve another orchestrator's auth request: %w", common.ErrAuthorizationDenied)` at line 2546 — wraps the real service-layer sentinel from `auth_requests.go:449`. PROVEN.
- Asserts `isError == true` (line 2558-2560). PROVEN.
- Asserts `auth_denied:` prefix on error text (line 2562-2564). PROVEN.
- Asserts role-mismatch fragment "orch cannot approve another orchestrator" (line 2565-2567). PROVEN.

**Case (c)** (`handler_test.go:2582-2620`):

- Orch-A approves orch-B's subagent → rejected. Stub returns `fmt.Errorf("cross-orch approval requires steward approver (request requested_by %q, approver %q): %w", "ORCH_B_W34", orchA.principalID, common.ErrAuthorizationDenied)` at line 2593 — matches `auth_requests.go:520` cross-orch sentinel. PROVEN.
- Asserts `isError == true` + `auth_denied:` prefix + cross-orch fragment (lines 2605-2614). PROVEN.
- Comment block (lines 2578-2581) explicitly notes the rejection fires regardless of path-encompassing status because it's the `requested_by_actor != approver.principal_id` branch — falsification-attack-1 mitigation explicitly pinned. PROVEN.
- Sanity check: stub's `lastApprove` recorded approver tuple (lines 2617-2619), confirming the request reached the service layer before rejection. EXTRA-MITIGATION.

**Case (d)** (`handler_steward_integration_test.go:833-905`):

- STEWARD session approves under persistent parent → success. Setup uses real `auth.IssueSession` with `PrincipalType: "steward"` (line 744-757), persistent ancestor created via `domain.NewActionItemForTest(...{Persistent: true, Owner: "STEWARD"}...)` (lines 723-731). PROVEN.
- Asserts `state == "approved"` (line 849-851), audit fields populated (lines 852-860), session secret non-empty (lines 861-864). PROVEN.
- DB round-trip: `adapter.GetAuthRequest(ctx, fixture.pendingReq.ID)` and asserts persisted state + persisted audit fields (lines 870-888). EXTRA-MITIGATION — proves 4a.26 column persistence + cross-subtree exception path landed audit values.
- JSON-Marshal surface check: encoded record contains `"approving_principal_id":"STEWARD"` etc. (lines 890-904). EXTRA-MITIGATION — proves omitempty doesn't elide populated values.
- **Metadata-driven detection (no hardcoded IDs)**: persistentID is computed inside the fixture (`"PERSISTENT_STEWARD_W34_D"`, line 722) and the live gate's `requireStewardPersistentAncestor` walk reads `metadata.Persistent + metadata.Owner` (auth_requests.go:561 case-insensitive). The test does NOT add the persistent ID to any allowlist — the gate finds it via metadata only. PROVEN.

**Case (e)** (`handler_test.go:2636-2668`):

- Project toggle disabled → rejected with `ErrOrchSelfApprovalDisabled` sentinel. Stub returns `fmt.Errorf("project %q has opted out of orch self-approval: %w", "p1", domain.ErrOrchSelfApprovalDisabled)` at line 2646. PROVEN that the sentinel is wrapped in the error chain.
- Asserts `isError == true` (lines 2658-2660). PROVEN.
- Asserts sentinel message text "orch self-approval disabled by project metadata" (lines 2662-2664) AND wrap fragment "opted out of orch self-approval" (lines 2665-2667). PROVEN.
- **NIT (refinement, NOT a failure)**: the test asserts message text rather than `errors.Is(err, ErrOrchSelfApprovalDisabled)` — but it's an MCP wire-format test (response body inspection), not a Go-error inspection point. The sentinel is wrapped server-side and unwrappable on the server; the wire response surfaces text only. The worklog (lines 51-53) explicitly raises this as a refinement: the handler's `mapToolError` should map `ErrOrchSelfApprovalDisabled` to a sharper code (e.g. `auth_disabled` or extend `auth_denied`). Logged as a Drop 4a refinement. ACCEPTABLE for 4a.27.

## 3. Tool Schema Assertion (Acceptance Item 3) — PROVEN

`TestAuthRequestToolSchemaApproveAcceptsOnlyDocumentedArgs` at `handler_test.go:2678-2749`.

Brief-mandated forbidden vocabulary asserted at lines 2716-2721:

```
"auto_approve_threshold",
"max_per_hour",
"auto_approve_roles",
"dev_required_roles",
```

- Properties-map probe rejects each (lines 2722-2726). PROVEN.
- Operation-enum probe ALSO rejects each (lines 2731-2741) — defense-in-depth against an enum-only drift route. PROVEN.
- Operation enum must include `"approve"` (lines 2731-2734). PROVEN.
- Five documented args must remain present: `request_id`, `acting_session_id`, `acting_session_secret`, `agent_instance_id`, `lease_token` (lines 2744-2748). PROVEN.

**Sibling-test note (NIT, not a failure)**: the worklog (lines 38-42) explicitly states the existing `TestAuthRequestApproveToolSchemaHasNoConfigurabilityKnobs` (added in 4a.24) covers an overlapping forbidden vocabulary including aliases (`max_approvals_per_hour`, `rate_limit`); 4a.27's new test pins the brief's literal vocabulary. Two tests with overlapping forbidden lists is intentional (drift in either direction trips a test). ACCEPTABLE.

## 4. Integration vs Unit Split (Acceptance Item 4) — PROVEN

- Unit-style: (a)/(b)/(c)/(e) all in `handler_test.go` driven over `httptest.NewServer(handler)` with stub `stubAuthRequestService` (handler_test.go:2467, 2540, 2586, 2640). PROVEN.
- Integration-style: (d) lives in `handler_steward_integration_test.go` with real `sqlite.Open` + `autentauth.NewSharedDB` + `app.NewService` + `servercommon.NewAppServiceAdapter` + `httptest.NewServer(handler)` (`handler_steward_integration_test.go:684-803`). PROVEN.
- All six tests live in the `mcpapi` package per the brief — no test leakage to other packages. PROVEN.

## 5. Coverage (Acceptance Item 5) — PROVEN at package scope

Worklog states (line 75): "`internal/adapters/server/mcpapi` at 73.5% (was ~73.4% pre-droplet; the integration test exercises the live approve path so the marginal cover-up offsets test-only LOC bloat)." `mage ci` enforces 70% coverage as a hard failure (per project CLAUDE.md "Build Verification" §3). Package coverage above threshold ⇒ implicitly clears block-level threshold for the case "approve" handler block (since handlers without coverage would drag the package below 70%).

Block-level coverage for the `case "approve"` block specifically — six tests exercise:
- Success path with audit fields populated (case a, case d).
- Three distinct rejection paths via wrapped service-layer errors (cases b, c, e).
- Schema introspection via `tools/list` (schema test).
- DB round-trip from approved state (case d).

All six paths execute through the handler's `case "approve"` switch arm. PROVEN sufficient at the granularity `mage ci` enforces (70% line on the package).

**NOT-EXECUTED**: `mage ci` not re-run by QA-proof in this round (worklog states `mage ci` was clean post-droplet). I executed `mage test-pkg ./internal/adapters/server/mcpapi` only; that's the W3.4 verification target per the brief (line 142). PASS.

## 6. Hylla Feedback

- **Query**: I did not query Hylla for this proof review. Acceptance items asked for direct file:line cites + verified-green test runs; both came from `Read` + `mage testFunc` against the working tree (which is post-Wave-3 commits, beyond the current Hylla snapshot per worklog feedback).
- **Worked via**: `Read` on `handler_test.go:2355-2750` and `handler_steward_integration_test.go:660-906`; `Grep` for test-name + helper-name discovery; `mage testFunc` per-test execution.
- **Suggestion**: matches builder worklog observation — drop-orch reingest is end-of-drop only; mid-drop W3.x commits are invisible to Hylla until close. A soft-reingest cadence per wave or per non-trivial commit cluster would unblock Hylla-first review on intra-drop droplets like 4a.27.

## 7. Definition of Done — VERDICT

**PASS-WITH-NIT.**

- All 5 acceptance items PROVEN.
- 5 named test cases + 1 schema-shape test all verified-green via individual `mage testFunc` runs (not name-only).
- 0 GAP / 0 WRONG findings.
- 1 NIT (refinement, already logged by builder): `mapToolError` should grow an `ErrOrchSelfApprovalDisabled` case for sharper error-code observability symmetry with the `auth_denied` path. Out-of-scope for 4a.27.

Artifact path: `workflow/drop_4a/4a.27_BUILDER_QA_PROOF.md`.
