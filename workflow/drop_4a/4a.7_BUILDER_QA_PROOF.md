# DROPLET 4a.7 — QA PROOF REVIEW

**Target droplet:** Drop 4a / Wave 1 / W1.3 — `files []string` FIRST-CLASS ON `ActionItem`
**Plan ref:** `workflow/drop_4a/WAVE_1_PLAN.md` §1.3
**Paths reviewed:** `internal/domain/action_item.go`, `internal/domain/errors.go`, `internal/domain/domain_test.go`, `internal/app/service.go`, `internal/app/snapshot.go`, `internal/app/snapshot_test.go`, `internal/adapters/server/common/mcp_surface.go`, `internal/adapters/server/common/app_service_adapter_mcp.go`, `internal/adapters/server/mcpapi/extended_tools.go`, `internal/adapters/server/mcpapi/extended_tools_test.go`, `internal/adapters/storage/sqlite/repo.go`, `internal/adapters/storage/sqlite/repo_test.go`, `internal/tui/model_test.go` — all staged-but-uncommitted (`git diff --stat` = 13 files, +729 / -14)
**Verdict:** **PASS**
**Date:** 2026-05-03

---

## 1. Acceptance Bullet Verification

Every plan acceptance bullet from `workflow/drop_4a/WAVE_1_PLAN.md` §1.3 is checked against the staged diff. Builder claimed `mage ci` green at 1885+ tests with per-package totals (199 domain / 325 app / 76 sqlite / 149 common / 137 mcpapi); QA re-ran live and confirmed identical (see §2 below).

### 1.1 Domain layer — `Files []string` first-class on `ActionItem` + `ActionItemInput`; `NewActionItem` validates trim+dedup+forward-slash-only; `ErrInvalidFiles` sentinel; disjoint-axis rule; exposed `NormalizeActionItemFiles`

**Spec:** `ActionItem` and `ActionItemInput` both gain `Files []string` after `Packages`. `NewActionItem` validates trim, dedup, forward-slash-only (backslash rejected), returning new `ErrInvalidFiles` sentinel on whitespace-only / empty / backslash-bearing entries. **Disjoint-axis rule:** NO Paths/Files cross-check — overlap is permitted. `domain.NormalizeActionItemFiles` exposed for shared create/update validation.

**Evidence — struct fields:**

- `internal/domain/action_item.go:104-120` — `Files []string` inserted in `ActionItem` between `Packages` (line 104, post-merge) and `LifecycleState` (line 121). Doc comment names trim+dedup, forward-slash convention (`git ls-files`-shaped), the disjoint-axis-with-Paths rule (read attention vs write intent — overlap permitted), and the deferred path-exists check (consumer = Drop 4.5 file-viewer).
- `internal/domain/action_item.go:202-211` — `Files []string` inserted in `ActionItemInput` between `Packages` (line 202) and `LifecycleState` (line 212). Doc comment mirrors the production-struct doc, with the "empty slice is meaningful zero value" call-out.

**Evidence — sentinel:**

- `internal/domain/errors.go:32` — `ErrInvalidFiles = errors.New("invalid files")` inserted in the closed-vocabulary error block directly after `ErrInvalidPackages` (line 31) and before `ErrInvalidLifecycleState` (line 33). Naming matches the existing sibling pattern.

**Evidence — validation gate inside `NewActionItem`:**

- `internal/domain/action_item.go:380-386` — `files, err := normalizeActionItemFiles(in.Files); if err != nil { return ActionItem{}, err }`. Inserted after the Packages coverage gate. Comment explicitly cites the disjoint-axis rule and the absence of a cross-axis coverage check.
- `internal/domain/action_item.go:404` — `Files: files` threaded into the returned `ActionItem` literal directly after `Packages: packages` (line 403).

**Evidence — normalization helper `normalizeActionItemFiles` and exposed `NormalizeActionItemFiles`:**

- `internal/domain/action_item.go:734-754` — internal worker. Empty input (`len == 0`) returns `nil, nil` (zero value). Loop trims each entry; empty/whitespace-only entries reject with `ErrInvalidFiles`; backslash-bearing entries reject with `ErrInvalidFiles` via `strings.ContainsRune(file, '\\')`; dedupes via `seen` map; preserves insertion order. NO format check beyond backslash-rejection (matches plan).
- `internal/domain/action_item.go:725-732` — exposed `NormalizeActionItemFiles(files []string) ([]string, error)` is a thin wrapper around `normalizeActionItemFiles`. Doc comment (lines 706-727) explicitly cites the disjoint-axis rule, the deferred path-exists check, and the canonical consumer (Drop 4.5 file-viewer pane).

**Evidence — disjoint-axis NOT cross-checked:** `NewActionItem` performs ONLY `normalizeActionItemFiles` on `Files`. There is no `len(paths) > 0 && len(files) == ?` gate analogous to the Paths/Packages coverage rule. Confirmed by inspection of the entire `NewActionItem` body in the diff — the only cross-axis gate is the existing Paths/Packages coverage one (line 372-378), and that gate references `paths` and `packages` only, never `files`. No phantom mutual-exclusivity check, no overlap rejection.

**Conclusion:** PASS.

### 1.2 App layer — `CreateActionItemInput.Files []string` (value-type) + `UpdateActionItemInput.Files *[]string` (pointer-sentinel) threaded through service

**Spec:** Create-side is value-type `[]string` (empty slice is meaningful zero). Update-side is pointer-sentinel `*[]string` (nil preserves; non-nil replaces, with empty dereferenced slice clearing). NO cross-axis coverage re-check vs Paths.

**Evidence — struct fields:**

- `internal/app/service.go:540-549` — `CreateActionItemInput.Files []string` between `Packages []string` (line 539) and `ColumnID` (line 550). Doc comment cites disjoint-axis-with-Paths rule and "no pointer-sentinel needed" at the create boundary.
- `internal/app/service.go:621-630` — `UpdateActionItemInput.Files *[]string` between `Packages *[]string` (line 620) and `Metadata *domain.ActionItemMetadata` (line 631). Doc comment cites the pointer-sentinel rationale, the routing through `domain.NormalizeActionItemFiles`, and the disjoint-axis rule (no cross-axis coverage check on update).

**Evidence — service threading on Create:**

- `internal/app/service.go:809` — `Service.CreateActionItem` threads `Files: in.Files` through into `domain.ActionItemInput`. Validation runs at the domain layer — `NewActionItem` calls `normalizeActionItemFiles`, so an invalid `Files` rejects with `ErrInvalidFiles` at the domain seam.

**Evidence — service threading on Update:**

- `internal/app/service.go:1126-1136` — Pointer-sentinel branch: `if in.Files != nil { normalized, err := domain.NormalizeActionItemFiles(*in.Files); ...; actionItem.Files = normalized; actionItem.UpdatedAt = s.clock().UTC() }`. Routes through the exposed normalizer so create/update normalization stay symmetric.
- **No coverage re-check inserted** — confirmed by inspection: the only post-apply guard near this section is the pre-existing Paths/Packages coverage check at `service.go:1123-1125`, which references only `actionItem.Paths` and `actionItem.Packages`. No `if len(actionItem.Paths) > 0 && len(actionItem.Files) ...` gate exists. Disjoint-axis rule preserved on the update path.

**Evidence — exposed normalizer reused, not reimplemented:** `domain.NormalizeActionItemFiles` is the only path through which update-time files normalize. No local trim loop in the service. Same symmetry pattern as 4a.5/4a.6.

**Conclusion:** PASS.

### 1.3 MCP wire layer — `CreateActionItemRequest.Files []string` + `UpdateActionItemRequest.Files *[]string` threaded through adapter handlers

**Spec:** Adapter request types match the app-layer shape (value on create, pointer on update). Adapter handlers thread the values through to `app.CreateActionItemInput` / `app.UpdateActionItemInput`.

**Evidence — request struct fields:**

- `internal/adapters/server/common/mcp_surface.go:112-122` — `CreateActionItemRequest.Files []string` between `Packages []string` (line 112) and `ColumnID` (line 123). Doc comment cites disjoint-axis rule and threads through to `app.CreateActionItemInput → domain.NewActionItem`.
- `internal/adapters/server/common/mcp_surface.go:200-208` — `UpdateActionItemRequest.Files *[]string` between `Packages *[]string` (line 199) and `Metadata *domain.ActionItemMetadata` (line 209). Doc comment cites pointer-sentinel rationale + routing through `domain.NormalizeActionItemFiles` + disjoint-axis (no cross-axis check).

**Evidence — adapter threading on Create:**

- `internal/adapters/server/common/app_service_adapter_mcp.go:663` — `Files: append([]string(nil), in.Files...)` threaded into `app.CreateActionItemInput`. The defensive copy via `append([]string(nil), ...)` matches the `Paths` (line 661) and `Packages` (line 662) precedent — prevents the adapter input from sharing backing array mutation with the service-layer struct.

**Evidence — adapter threading on Update:**

- `internal/adapters/server/common/app_service_adapter_mcp.go:735` — `Files: in.Files` threaded into `app.UpdateActionItemInput`. Pointer-sentinel passes through unchanged (no copy needed since the pointer carries explicit-clear intent).

**Conclusion:** PASS.

### 1.4 MCP tool layer — `mcp.WithArray("files", ...)` schema + 5-sub-case round-trip test `TestActionItemMCPFilesRoundTrip`

**Spec:** Tool definition gains `mcp.WithArray("files", ...)` with `mcp.WithStringItems()`; round-trip test asserts JSON-RPC → adapter request hop preserves the field across 5 sub-cases.

**Evidence — schema:**

- `internal/adapters/server/mcpapi/extended_tools.go:1461` — `mcp.WithArray("files", mcp.Description("..."), mcp.WithStringItems())` registered on the `till.action_item` tool definition. Description text is comprehensive: cites Paths-vs-Files distinction (write-scope vs read attention), forward-slash convention, trim/dedup behavior, disjoint-axis rule (overlap permitted), the path-exists deferral to Drop 4.5, and the "domain primitive" provenance line. `mcp.WithStringItems()` enforces array-of-strings (not array-of-anything) at the schema layer — verifies the QA Falsification attack vector "schema actually array-of-anything" is mitigated.

**Evidence — request parsing:**

- `internal/adapters/server/mcpapi/extended_tools.go:878` — anonymous args struct gains `Files *[]string \`json:"files"\`` between `Packages` (line 877) and `ColumnID` (line 879). Pointer keeps the "field absent" vs "field present empty" distinction explicit at the JSON layer (mirrors the create side's pointer-sentinel parsing pattern).
- `internal/adapters/server/mcpapi/extended_tools.go:1093-1100` — Create branch: `if args.Files != nil { createReq.Files = append([]string(nil), (*args.Files)...) }`. Defensive copy mirrors Paths/Packages handling.
- `internal/adapters/server/mcpapi/extended_tools.go:1176-1182` — Update branch: `Files: args.Files` passed straight through (pointer-sentinel preserved end-to-end so explicit-clear via `[]any{}` reaches the service).

**Evidence — round-trip test:**

- `internal/adapters/server/mcpapi/extended_tools_test.go:4101-4239` — `TestActionItemMCPFilesRoundTrip` exercises 5 sub-cases via `t.Run`:
  1. **`create plumbs files slice through to request`** (lines 4128-4151) — JSON-RPC create with `"files": []any{"docs/A.md", "docs/B.md"}` → asserts `service.lastCreateActionItemReq.Files == ["docs/A.md", "docs/B.md"]`. Order preserved via index-by-index assertion.
  2. **`create without files round-trips empty slice`** (lines 4153-4170) — JSON-RPC create omits `files` entirely → asserts `len(service.lastCreateActionItemReq.Files) == 0`. Verifies value-type zero behaviour.
  3. **`update with files plumbs as non-nil pointer-sentinel`** (lines 4172-4193) — JSON-RPC update with `"files": []any{"docs/A.md"}` → asserts `service.lastUpdateActionItemReq.Files != nil && *service.lastUpdateActionItemReq.Files == ["docs/A.md"]`.
  4. **`update with empty files array plumbs as non-nil pointer to empty slice (explicit clear)`** (lines 4195-4214) — JSON-RPC update with `"files": []any{}` → asserts `service.lastUpdateActionItemReq.Files != nil && len(*service.lastUpdateActionItemReq.Files) == 0`. Verifies explicit-clear semantics survive the pointer-sentinel hop.
  5. **`update without files preserves prior via nil pointer-sentinel`** (lines 4216-4238) — JSON-RPC update omits `files` entirely → asserts `service.lastUpdateActionItemReq.Files == nil`. Verifies preserve semantics.

**Conclusion:** PASS — exactly 5 sub-cases, every pointer-sentinel edge covered.

### 1.5 SQL layer — `files_json TEXT NOT NULL DEFAULT '[]'` after `packages_json`; encode/decode helpers; INSERT + UPDATE + 5 SELECT sites + `scanActionItem` + `changedActionItemFields` threaded; round-trip test

**Spec:** schema column added in the documented position; encode/decode helpers parallel `packages_json` precedent; every CRUD path that touches `action_items` threads the column; `changedActionItemFields` reports `"files"` on diff; round-trip test covers create + get + list + list-by-parent + update.

**Evidence — schema column:**

- `internal/adapters/storage/sqlite/repo.go:183` — `files_json TEXT NOT NULL DEFAULT '[]'` added in the `CREATE TABLE action_items` block directly after `packages_json TEXT NOT NULL DEFAULT '[]'` (line 182) and before `lifecycle_state TEXT NOT NULL DEFAULT 'todo'` (line 184). Position matches plan.

**Evidence — encode/decode helpers:**

- `internal/adapters/storage/sqlite/repo.go:3339-3372` — `encodeActionItemFilesJSON` + `decodeActionItemFilesJSON`. Encoder returns `"[]"` on nil/empty (preserves NOT NULL DEFAULT '[]' invariant). Decoder treats empty string and `"null"` as zero-value nil (legacy/coerced rows compat). Both wrap `json.Marshal`/`json.Unmarshal` with `fmt.Errorf("...: %w", err)` — error wrapping discipline matches `paths_json`/`packages_json` precedent.

**Evidence — INSERT path:**

- `internal/adapters/storage/sqlite/repo.go:1180-1183` — `filesJSON, err := encodeActionItemFilesJSON(t.Files)` invoked alongside `pathsJSON` and `packagesJSON` in `CreateActionItem`.
- `internal/adapters/storage/sqlite/repo.go:1202` — INSERT column list adds `files_json` between `packages_json` and `lifecycle_state`.
- `internal/adapters/storage/sqlite/repo.go:1205` — VALUES placeholder count increases by 1 (33 → 34 placeholders); column-ordinal alignment preserved.
- `internal/adapters/storage/sqlite/repo.go:1221` — `filesJSON` parameter inserted between `packagesJSON` (line 1220) and `string(t.LifecycleState)` (line 1222).

**Evidence — UPDATE path:**

- `internal/adapters/storage/sqlite/repo.go:1290-1293` — `filesJSON, err := encodeActionItemFilesJSON(t.Files)` invoked in `UpdateActionItem`.
- `internal/adapters/storage/sqlite/repo.go:1317` — UPDATE SET clause adds `files_json = ?` between `packages_json = ?` and `lifecycle_state = ?`.
- `internal/adapters/storage/sqlite/repo.go:1333` — `filesJSON` parameter inserted between `packagesJSON` (line 1332) and `string(t.LifecycleState)` (line 1334).

**Evidence — 5 SELECT sites:** the diff's `+ files_json,` adds appear at five distinct SELECT call-sites:

1. `repo.go:1392` — `ListActionItems`.
2. `repo.go:1430` — `ListActionItemsByParent`.
3. `repo.go:1464` — `FindActionItemByOwnerAndTitle`.
4. `repo.go:1485` — `ListActionItemsByDropNumber`.
5. `repo.go:2526` — `getActionItemByID` (free function, used by `GetActionItem`).

Each adds `files_json` to its column list between `packages_json` and `lifecycle_state`. Column-ordinal alignment preserved across every read path.

**Evidence — `scanActionItem`:**

- `internal/adapters/storage/sqlite/repo.go:2850` — `filesRaw string` declared between `packagesRaw` (line 2849) and `state` (line 2851).
- `internal/adapters/storage/sqlite/repo.go:2869` — `&filesRaw` passed to `s.Scan(...)` between `&packagesRaw` (line 2868) and `&state` (line 2870). Ordinal alignment with the SELECT column lists confirmed.
- `internal/adapters/storage/sqlite/repo.go:2916-2920` — decode + assignment: `files, err := decodeActionItemFilesJSON(filesRaw); if err != nil { return ... }; t.Files = files`. Mirrors the Packages decode call directly above (lines 2911-2915).

**Evidence — `changedActionItemFields`:**

- `internal/adapters/storage/sqlite/repo.go:2658-2660` — `if !equalStringSlices(prev.Files, next.Files) { changed = append(changed, "files") }`. Appended after the Packages diff branch (lines 2655-2657) and before Metadata. Label `"files"` matches the JSON tag and the diff convention.

**Evidence — round-trip test:**

- `internal/adapters/storage/sqlite/repo_test.go:2929-3081` — `TestRepository_PersistsActionItemFiles` covers:
  - 3 sub-cases via table loop: empty / single / multi (lines 2965-2969).
  - For each sub-case: `domain.NewActionItem` → `repo.CreateActionItem` → `repo.GetActionItem` (exercises `getActionItemByID` SELECT path).
  - `repo.ListActionItems` (lines 3025-3041) — exercises the second SELECT path.
  - `repo.ListActionItemsByParent` (lines 3043-3060) — exercises the third SELECT path.
  - Update reassign: `t-files-empty.Files` populated → `repo.UpdateActionItem` → reload + assert (lines 3062-3076). Exercises UPDATE SET clause.
  - Update clear: `t-files-multi.Files = nil` → `repo.UpdateActionItem` → reload + assert empty (lines 3078-3094). Exercises UPDATE SET clause for the explicit-clear path.

**Conclusion:** PASS — every CRUD path threaded; round-trip test exercises 3 sub-cases × 4 read paths × 2 update directions.

### 1.6 Snapshot layer — `SnapshotActionItem.Files []string \`json:"files,omitempty"\`` after `Packages`; threaded both directions; round-trip + legacy-format-compat tests

**Spec:** `SnapshotActionItem` field, both encode (`snapshotActionItemFromDomain`) and decode (`SnapshotActionItem.toDomain`) paths threaded; round-trip + pre-4a.7 legacy-format-compat tests covering omitempty.

**Evidence — struct field + JSON tag:**

- `internal/app/snapshot.go:72` — `Files []string \`json:"files,omitempty"\`` between `Packages []string \`json:"packages,omitempty"\`` (line 71) and `LifecycleState` (line 73). Position matches plan; tag spelling matches Paths/Packages precedent.

**Evidence — encode direction:**

- `internal/app/snapshot.go:1089` — `Files: append([]string(nil), t.Files...)` inserted in `snapshotActionItemFromDomain` literal between `Packages` (line 1088) and `LifecycleState` (line 1090). Defensive copy preserves snapshot/domain isolation.

**Evidence — decode direction:**

- `internal/app/snapshot.go:1361-1366` — `Files: append([]string(nil), t.Files...)` inserted in `SnapshotActionItem.toDomain` literal between `Packages` (line 1360, post-merge) and `LifecycleState` (line 1367). Doc comment cites the omitempty fallback for pre-4a.7 snapshots.

**Evidence — round-trip test:**

- `internal/app/snapshot_test.go:985-1057` — `TestSnapshotActionItemFilesRoundTrip` covers 4 sub-cases via table loop:
  1. `nil-zero-value` — Files=nil round-trips as nil.
  2. `single-file` — single entry preserved.
  3. `multi-file-order-preserved` — three entries preserved in insertion order.
  4. `files-overlap-with-paths-allowed` — populated `Paths=[shared]` + `Packages=[covering]` + `Files=[shared]` round-trips without rejection (verifies disjoint-axis rule survives the snapshot hop). Covering Packages supplied so the Paths/Packages coverage invariant doesn't shadow the Files-overlap assertion.
- Each sub-case: `domain.NewActionItemForTest` → `snapshotActionItemFromDomain` → assert snap.Files matches → `snap.toDomain` → assert hydrated Files matches.

**Evidence — legacy-format-compat test:**

- `internal/app/snapshot_test.go:1063-1097` — `TestSnapshotActionItemFilesLegacyFormatCompatibility` constructs a literal pre-4a.7 JSON payload (no `"files"` key) and asserts `json.Unmarshal` deserializes `snap.Files == nil` and `snap.toDomain().Files == nil`. omitempty contract verified — no SnapshotVersion bump required.

**Conclusion:** PASS — both directions threaded; 4 round-trip cases + 1 legacy-format case.

### 1.7 TUI schema-coverage — `Files` classified as `readOnly`

**Spec:** `TestActionItemSchemaCoverageIsExplicit` has `Files` listed in the `readOnly` map so `assertExplicitFieldCoverage` over `domain.ActionItem`'s reflected fields does not drift.

**Evidence:**

- `internal/tui/model_test.go:14868` — `"Files": {},` inserted in the `readOnly` map between `"Packages": {},` (line 14867) and `"LifecycleState": {},` (line 14869). The `editable` map (lines 14845-14852) is unchanged — Title/Description/Priority/DueAt/Labels/Metadata only. Classification matches Paths/Packages precedent (read-only because the TUI does not currently expose paths/packages/files editing flows; future Drop 4.5 file-viewer pane is a read-only consumer).

**Conclusion:** PASS.

### 1.8 Validation rules — trim, dedup, whitespace-only/empty rejection, backslash rejection, disjoint-axis with Paths confirmed

**Spec:** Trim entries; whitespace-only/empty rejected; dedup after trim (silently); backslash rejected; **disjoint-axis confirmed** via a domain test that exercises Paths and Files containing the same path string and verifies BOTH round-trip without rejection.

**Evidence — validation rules in `TestNewActionItemFilesNormalization`:**

- `internal/domain/domain_test.go:1209-1262` — table-driven test with 9 sub-cases:
  - **Trim:** `surrounding whitespace trimmed` case (line 1218) feeds `"  docs/A.md  "` and asserts the trimmed `"docs/A.md"` survives.
  - **Dedup after trim:** `duplicates after trim dedupe` case (line 1219) feeds `["docs/A.md", "docs/A.md", "  docs/A.md  ", "docs/B.md"]` and asserts the result is `["docs/A.md", "docs/B.md"]` (silent dedup, insertion order preserved).
  - **Whitespace-only/empty rejection:** `empty entry rejects` (line 1220, input `["docs/A.md", ""]`) and `whitespace-only entry rejects` (line 1221, input `["   ", "docs/A.md"]`) — both assert `wantErr == ErrInvalidFiles`.
  - **Backslash rejection:** `backslash entry rejects` case (line 1222) feeds `[\`docs\\A.md\`]` (raw-string literal with backslash) and asserts `wantErr == ErrInvalidFiles`.
  - **Empty/zero round-trip:** `nil round-trips empty` and `empty slice round-trips empty` cases (lines 1213-1214) confirm zero-value behaviour.
  - **Order preservation:** `multi file preserves insertion order` case (line 1217) confirms the loop appends in source order.

**Evidence — disjoint-axis confirmed:**

- `internal/domain/domain_test.go:1273-1295` — `TestNewActionItemFilesAllowsOverlapWithPaths` is a dedicated test that:
  1. Sets `shared := "internal/domain/action_item.go"`.
  2. Constructs `ActionItemInput` with `Paths=[shared]` AND `Packages=["internal/domain"]` (covering Packages so the Paths/Packages coverage invariant does not shadow the Files-overlap assertion) AND `Files=[shared]`.
  3. Asserts `NewActionItem` returns no error.
  4. Asserts both `actionItem.Paths == [shared]` and `actionItem.Files == [shared]` (overlap survives both round-trips).
- This is exactly the disjoint-axis assertion the plan calls for ("a domain test exercises Paths and Files containing the same path string and verifies BOTH round-trip without rejection").

**Conclusion:** PASS — every validation rule is asserted by a named sub-case, and the disjoint-axis rule has a dedicated test that performs the overlap assertion exactly as specified.

### 1.9 Verification — `mage ci` green, exact counts confirmed

**Spec:** Builder claimed 1885+ tests passing across 199 domain + 325 app + 76 sqlite + 149 common + 137 mcpapi. Re-run live to confirm exact counts.

**Evidence — `mage ci` (live re-run, 2026-05-03 QA-time):**

- Test summary: **1910 tests, 1909 passed, 0 failed, 1 skipped** across **20 packages**.
- Skipped test: `TestStewardIntegrationDropOrchSupersedeRejected` (pre-existing, unrelated to 4a.7).
- Coverage: every package ≥ 70% floor:
  - `internal/domain` 80.5%
  - `internal/app` 70.9%
  - `internal/adapters/storage/sqlite` 75.3%
  - `internal/adapters/server/common` 73.7%
  - `internal/adapters/server/mcpapi` 73.2%
  - `internal/tui` 71.0%
  - all other packages well above floor (max 100% buildinfo).
- Build: `till` from `./cmd/till` built clean.
- Sources, formatting checks all green.

**Evidence — per-package counts (live re-run, `mage test-pkg`):**

- `internal/domain`: **199 tests passed across 1 package.** Matches builder's claim.
- `internal/app`: **325 tests passed across 1 package.** Matches builder's claim.
- `internal/adapters/storage/sqlite`: **76 tests passed across 1 package.** Matches builder's claim.
- `internal/adapters/server/common`: **149 tests passed across 1 package.** Matches builder's claim.
- `internal/adapters/server/mcpapi`: **138 tests, 137 passed, 1 skipped, 0 failed.** Matches builder's claim (137 passed).

**Conclusion:** PASS — every count matches builder's claim; full `mage ci` green.

---

## 2. Counterexample Sweep (Falsification-style attacks the proof must rebut)

In keeping with the asymmetric proof discipline, here are the counter-attacks I checked while looking at this diff. Each is mitigated by the citations in §1; this section makes the rebuttals explicit so the falsification sibling has a starting set rather than independently re-discovering them.

- **A1 — "Files actually missing `omitempty` JSON tag"** → REBUTTED at `snapshot.go:72` — `\`json:"files,omitempty"\`` confirmed. Legacy-format compat test confirms behaviour end-to-end.
- **A2 — "MCP schema accepts array-of-anything, not array-of-string"** → REBUTTED at `extended_tools.go:1461` — `mcp.WithStringItems()` is part of the `mcp.WithArray("files", ...)` registration, restricting the array element type at the schema layer.
- **A3 — "Disjoint-axis test missing — plan says BOTH must round-trip without rejection"** → REBUTTED at `domain_test.go:1273-1295` — `TestNewActionItemFilesAllowsOverlapWithPaths` is dedicated to this exact assertion. Plan-language match is verbatim.
- **A4 — "`changedActionItemFields` not threaded — `files` won't appear in field-changed lists"** → REBUTTED at `repo.go:2658-2660` — `equalStringSlices` branch added with label `"files"`.
- **A5 — "Phantom Paths/Files mutual-exclusivity check sneaks in"** → REBUTTED by inspection of `NewActionItem` body: only the existing Paths/Packages coverage gate exists; no `files`-and-`paths` cross-axis gate. Service `UpdateActionItem` likewise: post-apply guard at `service.go:1123-1125` references only `Paths`/`Packages`. Disjoint-axis preserved end-to-end.
- **A6 — "TUI schema-coverage test still passes only because `Files` was added to *editable* by mistake"** → REBUTTED at `model_test.go:14868` — `Files` is unambiguously in the `readOnly` map (between `Packages` and `LifecycleState`). The `editable` map at lines 14845-14852 contains only Title/Description/Priority/DueAt/Labels/Metadata; no shadowing.
- **A7 — "5 SELECT sites claimed but only 4 actually threaded"** → REBUTTED — enumerated above as `repo.go:1392`, `:1430`, `:1464`, `:1485`, `:2526`. Five distinct SELECT statements all threaded.
- **A8 — "Builder claimed counts but ran wrong build"** → REBUTTED — live `mage ci` and `mage test-pkg` re-runs at QA time match builder's claim exactly (199 / 325 / 76 / 149 / 137; 1910 total / 1 skip / 0 fail).

All counterexamples mitigated by direct citation. No unresolved attack remains.

---

## 3. Summary

**Verdict: PASS.**

Every plan acceptance bullet from `WAVE_1_PLAN.md` §1.3 is satisfied by the staged diff with citation-grade evidence:

- Domain `Files []string` first-class on both `ActionItem` and `ActionItemInput`, post-`Packages`, validating trim+dedup+forward-slash-only with `ErrInvalidFiles` sentinel and exposed `NormalizeActionItemFiles` helper.
- App layer `CreateActionItemInput.Files []string` (value) + `UpdateActionItemInput.Files *[]string` (pointer-sentinel) threaded through Service Create + Update.
- MCP wire layer `CreateActionItemRequest.Files` (value) + `UpdateActionItemRequest.Files` (pointer) threaded through adapter handlers.
- MCP tool layer registers `mcp.WithArray("files", ..., mcp.WithStringItems())` schema; `TestActionItemMCPFilesRoundTrip` covers 5 sub-cases (create populated, create empty, update populated, update empty-array clear, update absent preserve).
- SQL layer adds `files_json TEXT NOT NULL DEFAULT '[]'` after `packages_json`; encode/decode helpers parallel `packages_json`; INSERT + UPDATE + 5 SELECT sites + `scanActionItem` + `changedActionItemFields` all threaded; round-trip test covers 3 inputs × 4 read paths × 2 update directions.
- Snapshot `SnapshotActionItem.Files []string \`json:"files,omitempty"\`` after `Packages`; both directions threaded; round-trip test (4 sub-cases including overlap-with-Paths) + legacy-format-compat test.
- TUI schema-coverage `Files` classified as `readOnly` in `TestActionItemSchemaCoverageIsExplicit`.
- Disjoint-axis rule confirmed via dedicated `TestNewActionItemFilesAllowsOverlapWithPaths` test; no phantom Paths/Files cross-check exists in the create or update path.
- `mage ci` live re-run green: 1910 tests / 1909 passed / 1 skipped (pre-existing) / 0 failed; coverage ≥ 70% on every package; builder's per-package counts (199/325/76/149/137) all match exactly.

No findings requiring builder fixes. No missing evidence. Droplet 4a.7 satisfies every acceptance bullet and is ready to commit.

---

## TL;DR

- **T1** Every plan §1.3 acceptance bullet (domain field + sentinel + normalizer + disjoint-axis rule, app value/pointer-sentinel split, MCP wire + tool schema with `WithStringItems()`, SQL column + encode/decode + INSERT/UPDATE/5 SELECTs/`scanActionItem`/`changedActionItemFields`, snapshot omitempty + both directions, TUI readOnly classification, validation table for trim/dedup/whitespace/backslash, disjoint-axis dedicated overlap test) is satisfied with citation-grade evidence in the staged 13-file diff.
- **T2** All 8 falsification-style counter-attacks (omitempty tag, schema string-item type, missing disjoint test, `changedActionItemFields` threading, phantom mutual-exclusivity, TUI editable shadow, SELECT-site count, builder count veracity) explicitly rebutted by direct citation; live `mage ci` matches builder's claim exactly.
- **T3** Verdict PASS — droplet 4a.7 is ready to commit. 1910 tests / 1909 passed / 1 pre-existing skip / 0 failed; per-package counts 199/325/76/149/137 confirmed live.

---

## Hylla Feedback

N/A — task touched non-Go-code review only at the file level (the touched files ARE all Go, but per the WAVE_1_PLAN constraint and CLAUDE.md doctrine for filesystem-md-only mode, this proof review used direct `Read` + `git diff` against staged uncommitted changes rather than Hylla queries. Hylla's view of these files is stale by definition until the drop-end re-ingest, so any Hylla query would have been either redundant with the diff or actively misleading. No miss — Hylla was not the right tool for staged-uncommitted review).
