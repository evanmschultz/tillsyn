# DROPLET 4a.5 — QA PROOF REVIEW

**Target droplet:** Drop 4a / Wave 1 / W1.1 — `paths []string` FIRST-CLASS ON `ActionItem`
**Plan ref:** `workflow/drop_4a/WAVE_1_PLAN.md` §1.1
**Paths reviewed:** `internal/domain/action_item.go`, `internal/domain/errors.go`, `internal/domain/domain_test.go`, `internal/app/service.go`, `internal/app/snapshot.go`, `internal/app/snapshot_test.go`, `internal/adapters/server/common/mcp_surface.go`, `internal/adapters/server/common/app_service_adapter_mcp.go`, `internal/adapters/server/mcpapi/extended_tools.go`, `internal/adapters/server/mcpapi/extended_tools_test.go`, `internal/adapters/storage/sqlite/repo.go`, `internal/adapters/storage/sqlite/repo_test.go`, `internal/tui/model_test.go` — all staged-but-uncommitted (`git diff` = +660 / -19 across 13 files)
**Verdict:** **PASS**
**Date:** 2026-05-03

---

## 1. Acceptance Bullet Verification

Every plan acceptance bullet from `workflow/drop_4a/WAVE_1_PLAN.md` §1.1 is checked against the staged diff. Builder's claim of `mage ci` green at 1857/1/0/20pkg/≥70%-cov was re-run live at QA time — confirmed identical (see §2 below).

### 1.1 Domain layer — `Paths []string` first-class on `ActionItem` + `ActionItemInput` + `ErrInvalidPaths` sentinel

**Spec:** `ActionItem` and `ActionItemInput` both gain `Paths []string` after `DevGated`. `NewActionItem` validates trim+dedup+forward-slash-only, returning new `ErrInvalidPaths` sentinel.

**Evidence — struct fields:**

- `internal/domain/action_item.go:91` — `Paths []string` inserted in `ActionItem` between `DevGated bool` (line 81) and `LifecycleState LifecycleState` (line 92). Doc comment (lines 82-90) accurately describes the trim/dedup/forward-slash rules + the deferred path-exists check.
- `internal/domain/action_item.go:163` (post-diff) — `Paths []string` inserted in `ActionItemInput` between `DevGated bool` (line 156) and `LifecycleState LifecycleState` (line 164). Doc comment mirrors the production-struct doc.

**Evidence — sentinel:**

- `internal/domain/errors.go:30` — `ErrInvalidPaths = errors.New("invalid paths")` inserted in the closed-vocabulary error block between `ErrInvalidDropNumber` (line 29) and `ErrInvalidLifecycleState` (line 31). Naming matches the existing sibling pattern (`ErrInvalidPersistent`, `ErrInvalidDevGated`, `ErrInvalidDropNumber`).

**Evidence — validation gate inside `NewActionItem`:**

- `internal/domain/action_item.go:315-318` — `paths, err := normalizeActionItemPaths(in.Paths); if err != nil { return ActionItem{}, err }`. Inserted BEFORE the returned literal, AFTER all other validation gates. Threaded into the literal at line 333 (`Paths: paths,`).

**Evidence — normalization helper `normalizeActionItemPaths` (and exposed `NormalizeActionItemPaths`):**

- `internal/domain/action_item.go:553-580` — internal worker. `if len(paths) == 0 { return nil, nil }` → empty zero-value path. Loop trims each entry; rejects empty/whitespace-only with `ErrInvalidPaths`; rejects backslash-bearing with `ErrInvalidPaths`; dedupes via `seen` map; preserves insertion order via `out` slice with append. Final `return out, nil`.
- `internal/domain/action_item.go:550` — exposed `NormalizeActionItemPaths(paths []string) ([]string, error)` is a thin wrapper around `normalizeActionItemPaths`. Doc comment (lines 537-549) explicitly justifies the export: callers that mutate `ActionItem.Paths` after construction (specifically `Service.UpdateActionItem`) reuse the canonical logic rather than reimplement.

**Conclusion:** PASS.

### 1.2 App layer — `CreateActionItemInput.Paths []string` + `UpdateActionItemInput.Paths *[]string` (pointer-sentinel) threaded through service

**Spec:** Create-side is value-type `[]string` (empty slice is meaningful zero). Update-side is pointer-sentinel `*[]string` (nil preserves existing; non-nil replaces).

**Evidence — struct fields:**

- `internal/app/service.go:531` — `CreateActionItemInput.Paths []string` between `DevGated bool` (line 524) and `ColumnID string` (line 532). Doc comment notes "no pointer-sentinel needed" at the create boundary.
- `internal/app/service.go:592` — `UpdateActionItemInput.Paths *[]string` between `DevGated *bool` (line 584) and `Metadata *domain.ActionItemMetadata` (line 593). Doc comment justifies the pointer-sentinel: a description-only update must not silently clobber a planner-set Paths declaration. Same rationale as `Persistent *bool` / `DevGated *bool` precedent.

**Evidence — service threading:**

- `internal/app/service.go:769` — `Service.CreateActionItem` threads `Paths: in.Paths` through into the `ActionItemInput` passed to `domain.NewActionItem`. Validation runs at the domain layer, so empty/backslash-bearing paths reject with `ErrInvalidPaths` at create time.
- `internal/app/service.go:1052-1059` — `Service.UpdateActionItem` applies the pointer-sentinel: `if in.Paths != nil { normalized, err := domain.NormalizeActionItemPaths(*in.Paths); if err != nil { return domain.ActionItem{}, err }; actionItem.Paths = normalized; actionItem.UpdatedAt = s.clock().UTC() }`. Routes through `domain.NormalizeActionItemPaths` so the same trim/dedup/backslash rules apply at update time as create time.

**Evidence — exposed normalizer reused, not reimplemented:** `domain.NormalizeActionItemPaths` is the only path through which update-time paths normalize. No local trim loop in the service. The export from §1.1 is the load-bearing symmetry.

**Conclusion:** PASS.

### 1.3 MCP wire layer — `CreateActionItemRequest.Paths []string` + `UpdateActionItemRequest.Paths *[]string` threaded through adapter

**Spec:** Wire-layer request shapes mirror app-layer Create/Update split (value-type on create, pointer-sentinel on update). `(*AppServiceAdapter).CreateActionItem` and `.UpdateActionItem` thread the field through.

**Evidence — request shapes:**

- `internal/adapters/server/common/mcp_surface.go:104` — `CreateActionItemRequest.Paths []string` between `DevGated bool` (line 98) and `ColumnID string` (line 105). Doc comment matches app-layer doc.
- `internal/adapters/server/common/mcp_surface.go:171` — `UpdateActionItemRequest.Paths *[]string` between `DevGated *bool` (line 161) and `Metadata *domain.ActionItemMetadata` (line 172). Doc comment justifies the pointer-sentinel by reference to the same "description-only update must not clobber" rationale.

**Evidence — adapter threading:**

- `internal/adapters/server/common/app_service_adapter_mcp.go:661` — `(*AppServiceAdapter).CreateActionItem` threads `Paths: append([]string(nil), in.Paths...)` into `app.CreateActionItemInput`. The `append([]string(nil), in.Paths...)` pattern defensively copies — protects against caller-side mutation of the supplied slice after the call returns. Idiom matches the repo's existing copy-pattern for `Labels` (cross-checked at the same file `:653-657` baseline).
- `internal/adapters/server/common/app_service_adapter_mcp.go:731` — `(*AppServiceAdapter).UpdateActionItem` threads `Paths: in.Paths` (no copy) — appropriate because the pointer is the sentinel; the caller's slice is consumed by `domain.NormalizeActionItemPaths` which produces a fresh allocation. No double-copy.

**Conclusion:** PASS.

### 1.4 MCP tool layer — `mcp.WithArray("paths", ...)` schema + round-trip test in `extended_tools_test.go`

**Spec:** `till.action_item` tool (create + update operations) schema gains `mcp.WithArray("paths", ...)`; round-trip test in `extended_tools_test.go`.

**Evidence — schema attached:**

- `internal/adapters/server/mcpapi/extended_tools.go:1427` — `mcp.WithArray("paths", mcp.Description(...), mcp.WithStringItems())` inserted between `mcp.WithBoolean("dev_gated", ...)` (line 1426) and `mcp.WithString("description", ...)` (line 1428). Description text covers the empty-array semantics on both create and update, the trim/dedup/backslash domain rules, and the Drop 4a Wave 2 lock-manager consumer context.

**Evidence — args struct decode:**

- `internal/adapters/server/mcpapi/extended_tools.go:876` — `Paths *[]string \`json:"paths"\`` field on the per-call args struct. Pointer-typed at the decode boundary so we can distinguish "absent" from "explicit empty" — the JSON-RPC `paths: null` and "field omitted" both decode to nil pointer; `paths: []` decodes to non-nil pointer to empty slice.

**Evidence — create-side application:**

- `internal/adapters/server/mcpapi/extended_tools.go:1077-1082` — `if args.Paths != nil { createReq.Paths = append([]string(nil), (*args.Paths)...) }`. nil pointer leaves zero-value empty slice on the request (matches the value-type create-side semantics). Non-nil pointer copies through.

**Evidence — update-side passthrough:**

- `internal/adapters/server/mcpapi/extended_tools.go:1149` — `Paths: args.Paths` (no dereference). Pointer-sentinel passes through verbatim. nil preserves at service boundary; non-nil empty clears (explicit caller intent); non-nil populated replaces.

**Evidence — round-trip test:**

- `internal/adapters/server/mcpapi/extended_tools_test.go:3821-3958` — `TestActionItemMCPPathsRoundTrip` covers 5 scenarios:
  1. Create with `paths: ["internal/domain/action_item.go", "internal/domain/errors.go"]` → exact slice round-trips into `service.lastCreateActionItemReq.Paths`.
  2. Create with no `paths` field → `service.lastCreateActionItemReq.Paths` is empty (len 0).
  3. Update with `paths: ["a/b.go"]` → `service.lastUpdateActionItemReq.Paths` is non-nil pointer to `["a/b.go"]`.
  4. Update with `paths: []` (explicit empty) → `service.lastUpdateActionItemReq.Paths` is non-nil pointer to empty slice.
  5. Update with NO paths field → `service.lastUpdateActionItemReq.Paths` is nil pointer (preserve sentinel).
- `t.Parallel()` on every subtest. Mirrors the immediately-preceding `TestActionItemMCPOwnerDropNumberPersistentDevGatedRoundTrip` structure.

**Conclusion:** PASS.

### 1.5 SQL layer — `paths_json TEXT NOT NULL DEFAULT '[]'` column + scan/INSERT/UPDATE plumbing + round-trip test

**Spec:** New `paths_json` column appended after `dev_gated`. `scanActionItem` decodes JSON. INSERT + UPDATE encode JSON. Round-trip test in `repo_test.go`.

**Evidence — schema column:**

- `internal/adapters/storage/sqlite/repo.go:181` — `paths_json TEXT NOT NULL DEFAULT '[]',` column added in `CREATE TABLE action_items`, ordered immediately after `dev_gated INTEGER NOT NULL DEFAULT 0,` (line 180) and before `lifecycle_state TEXT NOT NULL DEFAULT 'todo',` (line 182). NOT NULL with `'[]'` default preserves the encode invariant that `decodeActionItemPathsJSON("[]")` always returns nil (zero value).

**Evidence — encode helpers:**

- `internal/adapters/storage/sqlite/repo.go:3236-3253` — `encodeActionItemPathsJSON([]string) (string, error)`. Empty/nil slice → returns `"[]"` (preserves NOT NULL DEFAULT contract). Non-empty → `json.Marshal` with error wrapping (`"encode paths_json: %w"`).
- `internal/adapters/storage/sqlite/repo.go:3255-3266` — `decodeActionItemPathsJSON(string) ([]string, error)`. Trim raw input; empty/`"null"` → return `nil, nil`; otherwise `json.Unmarshal` with error wrapping. Defensive against legacy NULL-coerced reads (though schema `NOT NULL DEFAULT '[]'` rules them out for new rows).

**Evidence — INSERT plumbing:**

- `internal/adapters/storage/sqlite/repo.go:1170-1173` — `pathsJSON, err := encodeActionItemPathsJSON(t.Paths); if err != nil { return err }`.
- `internal/adapters/storage/sqlite/repo.go:1192` — column name `paths_json` added to INSERT column list between `dev_gated` and `lifecycle_state`.
- `internal/adapters/storage/sqlite/repo.go:1195` — placeholder count bumped from 31 to 32 (`?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?` — 32 placeholders, verified count match).
- `internal/adapters/storage/sqlite/repo.go:1209` — bind value `pathsJSON` inserted in argument list immediately after `boolToInt(t.DevGated)` (line 1208) and before `string(t.LifecycleState)` (line 1210). Ordinal alignment matches column-list order.

**Evidence — UPDATE plumbing:**

- `internal/adapters/storage/sqlite/repo.go:1270-1273` — same encode call.
- `internal/adapters/storage/sqlite/repo.go:1297` — `paths_json = ?,` inserted in UPDATE SET clause between `dev_gated = ?,` and `lifecycle_state = ?,`.
- `internal/adapters/storage/sqlite/repo.go:1311` — bind value `pathsJSON` in argument list.

**Evidence — SELECT plumbing (5 read paths):** column name `paths_json` added in identical position (between `dev_gated,` and `lifecycle_state,`) on every read query:

- `ListActionItems`: line 1370.
- `ListActionItemsByParent`: line 1408.
- `FindActionItemByOwnerAndTitle`: line 1442.
- `ListActionItemsByDropNumber`: line 1463.
- `getActionItemByID`: line 2504.

All five paths hand the columns to `scanActionItem` in identical order — column-list ordinal alignment is consistent across all six read sites.

**Evidence — `scanActionItem` decode:**

- `internal/adapters/storage/sqlite/repo.go:2820` — `pathsRaw string` declared in the local-variable scan block.
- `internal/adapters/storage/sqlite/repo.go:2837` — `&pathsRaw` inserted in the `s.Scan(...)` argument list at the correct position (between `&devGatedRaw` and `&state`). Ordinal alignment matches the column-list ordering above.
- `internal/adapters/storage/sqlite/repo.go:2874-2878` — `paths, err := decodeActionItemPathsJSON(pathsRaw); if err != nil { return domain.ActionItem{}, err }; t.Paths = paths`. Decode error propagates to caller.

**Evidence — change-tracking helper updated:**

- `internal/adapters/storage/sqlite/repo.go:2630-2632` — `if !equalStringSlices(prev.Paths, next.Paths) { changed = append(changed, "paths") }`. Inserted in `changedActionItemFields` between the `labels` check and the `metadata` check. `equalStringSlices` (line 2655-2665) handles nil-vs-empty correctly via `len()` comparison.

**Evidence — round-trip test:**

- `internal/adapters/storage/sqlite/repo_test.go:2609-2756` — `TestRepository_PersistsActionItemPaths` covers:
  - Create + Get round-trip across nil / single / multi cases (lines 2640-2671).
  - `ListActionItems` exercise with assertion across all three cases (lines 2673-2691).
  - `ListActionItemsByParent` exercise with assertion across all three cases (lines 2693-2710).
  - Update-side: populate-from-empty (`t-paths-empty` → `["x/y.go", "z.go"]`) followed by reload + assert (lines 2712-2728).
  - Update-side: clear-from-multi (`t-paths-multi` → nil) followed by reload + assert empty (lines 2730-2746).
- `assertPathsEqual` helper (lines 2749-2756) treats nil and len-0 slices as equal — pragmatic helper, not a hidden bug.

**Conclusion:** PASS.

### 1.6 Snapshot layer — `SnapshotActionItem.Paths []string` + bidirectional thread + round-trip + legacy-compat tests

**Spec:** `SnapshotActionItem.Paths []string \`json:"paths,omitempty"\`` after `DevGated`; threaded through `snapshotActionItemFromDomain` + `(t SnapshotActionItem) toDomain()`; round-trip test.

**Evidence — snapshot field:**

- `internal/app/snapshot.go:70` — `Paths []string \`json:"paths,omitempty"\`` between `DevGated bool \`json:"dev_gated,omitempty"\`` (line 69) and `LifecycleState domain.LifecycleState \`json:"lifecycle_state"\`` (line 71). `omitempty` tag preserves backward-compat with pre-droplet-4a.5 snapshots.

**Evidence — domain → snapshot:**

- `internal/app/snapshot.go:1085` — `Paths: append([]string(nil), t.Paths...)` in `snapshotActionItemFromDomain`. Defensive copy preserves caller-mutation safety.

**Evidence — snapshot → domain:**

- `internal/app/snapshot.go:1349-1352` — `(t SnapshotActionItem) toDomain()` threads `Paths: append([]string(nil), t.Paths...)` after the `Owner / DropNumber / Persistent / DevGated` block. Inline doc comment explicitly notes "no fallback — same rationale as the four primitives above. Pre-droplet-4a.5 snapshots without this field deserialize to nil, which is the legitimate domain zero value."

**Evidence — round-trip test:**

- `internal/app/snapshot_test.go:786-844` — `TestSnapshotActionItemPathsRoundTrip` covers nil / single / multi cases. Each case exercises `domain → snapshotActionItemFromDomain → toDomain → assert` with insertion-order-preserved checks.

**Evidence — legacy-format compatibility test:**

- `internal/app/snapshot_test.go:846-877` — `TestSnapshotActionItemPathsLegacyFormatCompatibility` constructs a hand-rolled JSON byte slice with NO `paths` field (mimicking pre-4a.5 snapshots). Deserializes via `json.Unmarshal` → asserts `snap.Paths` is nil → `snap.toDomain()` → asserts `hydrated.Paths` is also nil. This is the load-bearing forward-compat assertion: `omitempty` on the JSON tag is functionally tested.

**Conclusion:** PASS.

### 1.7 TUI schema-coverage — `Paths` classified as `readOnly` in `TestActionItemSchemaCoverageIsExplicit`

**Spec:** TUI schema-coverage test enumerates every `domain.ActionItem` field as either editable or readOnly. New `Paths` field must appear in one bucket.

**Evidence:**

- `internal/tui/model_test.go:14866` — `"Paths": {},` inserted in the `readOnly` map between `"DevGated": {},` (line 14865) and `"LifecycleState": {},` (line 14867). Builder's claim of "readOnly classification" matches.
- `assertExplicitFieldCoverage` (the test's terminal assertion at line 14882, called against `reflect.TypeOf(domain.ActionItem{})`) exhaustively requires every struct field be classified — without this entry, the test would fail with an "unclassified field: Paths" complaint at runtime. The test is the implicit coverage gate.

**Rationale check:** `Paths` as `readOnly` is correct for today. The TUI does not yet ship a paths-editing form; planner-set values come in via MCP. When Drop 4a Wave 2 + Drop 4.5 build the dispatcher's lock-manager and the file-viewer pane, the field stays an MCP-set lock-domain primitive — not a user-editable text field in the kanban TUI. Future drops can promote it to `editable` if a TUI editor is built; today's `readOnly` is a deliberate pin.

**Conclusion:** PASS.

### 1.8 Validation rules verified

**Spec sub-bullets (5 items):**

- (a) **Trim entries (whitespace-only / empty rejected with `ErrInvalidPaths`).**
- (b) **Dedup after trim (silently — matches Labels precedent).**
- (c) **Forward slashes only; backslash rejected with `ErrInvalidPaths`.**
- (d) **Empty slice (nil or len-0) is VALID (zero value).**
- (e) **Path-exists check NOT enforced at domain layer.**

**Evidence — (a) Trim + reject empty:** `normalizeActionItemPaths` line 568-570: `path := strings.TrimSpace(raw); if path == "" { return nil, ErrInvalidPaths }`. Matches spec literally.

**Evidence — (b) Dedup after trim:** lines 575-578: `if _, ok := seen[path]; ok { continue }; seen[path] = struct{}{}; out = append(out, path)`. The `seen` map is keyed on the trimmed value, so dedup is post-trim. The `continue` (not return error) implements "silently".

**Evidence — (c) Backslash rejection:** lines 571-573: `if strings.ContainsRune(path, '\\') { return nil, ErrInvalidPaths }`. Tested at `TestNewActionItemPathsNormalization` cases `"backslash rejects"` (raw `internal\domain\action_item.go`) and `"mixed slashes rejects on first backslash"` (mixed `["a/b.go", "c\\d.go"]`).

**Evidence — (d) Empty slice valid:** `normalizeActionItemPaths` line 564-566: `if len(paths) == 0 { return nil, nil }`. Empty input collapses to nil with no error. Tested at `"nil round-trips empty"` and `"empty slice round-trips empty"` cases.

**Evidence — (e) No path-exists check:** the entire `normalizeActionItemPaths` function (action_item.go:561-579) contains no `os.Stat`, no `filepath.Walk`, no filesystem call. Doc comment at line 547-549 explicitly states "Path-exists is intentionally NOT enforced — paths often refer to files the build droplet will create. Drop 4a Wave 2 lock manager performs runtime validation when locks are acquired." Verified by absence (no false positive could exist if there is no filesystem call to perform).

**Conclusion:** PASS — all five sub-bullets backed by code citation + test case.

### 1.9 Verification gate — `mage ci` re-run at QA time confirms 1857/1/0/20pkg

**Spec:** `mage ci` green; 1857 pass / 1 skip / 0 fail across 20 packages, all ≥ 70% coverage.

**Evidence:** Live `mage ci` re-run at QA time produced (full snapshot in §2 below):

```
Test summary
  tests: 1858
  passed: 1857
  failed: 0
  skipped: 1
  packages: 20
```

Identical to builder's claim. Coverage threshold (`70.0%`) met across all 16 instrumented packages. Build green. The single skipped test is `TestStewardIntegrationDropOrchSupersedeRejected` — long-standing pre-existing skip, unrelated to droplet 4a.5.

**Conclusion:** PASS.

### 1.10 `domain.NormalizeActionItemPaths` exposure soundness check

**Spec (from spawn prompt, beyond plan):** Builder exposed `NormalizeActionItemPaths` (capital-N) to share validation logic between create + update paths. Verify same rules apply at update time as create time.

**Evidence:**

- `internal/domain/action_item.go:550` — `NormalizeActionItemPaths(paths []string) ([]string, error) { return normalizeActionItemPaths(paths) }`. The exposed function is a literal one-line passthrough to the internal worker. Update-time normalization is byte-for-byte identical to create-time normalization — same trim, same dedup, same backslash rejection, same empty-rejects-as-error.
- `internal/app/service.go:1054` — `Service.UpdateActionItem` is the only consumer: `normalized, err := domain.NormalizeActionItemPaths(*in.Paths)`. The worker is shared; code duplication is zero.

The export pattern (`Foo` thin-wraps `foo`) is a standard Go idiom for sharing an unexported helper between in-package callers (here `NewActionItem` calls `normalizeActionItemPaths` directly to avoid the extra wrapper hop, per the doc comment at lines 553-554) and out-of-package callers (`Service.UpdateActionItem` calls `domain.NormalizeActionItemPaths`). No coupling concern.

**Conclusion:** PASS — symmetric validation across create + update.

---

## 2. Mage CI Snapshot

Full re-run at QA time after the staged-but-uncommitted droplet 4a.5 diff was applied to the working tree. Note: dev's `~/.tillsyn/tillsyn.db` was not deleted before this run (delete attempt was sandbox-blocked) — the green result confirms in-memory test repos are not affected by stale on-disk schema. Build verification gate is satisfied by `mage ci` because the test suite uses `OpenInMemory()` (verified in `repo_test.go:2618`), which builds a fresh schema per test run from the migrate path that includes the new `paths_json` column.

```
Sources

[RUNNING] Verifying tracked sources
[SUCCESS] Verified tracked sources

Formatting

[RUNNING] Listing tracked Go files
[SUCCESS] Listed tracked Go files

[RUNNING] Checking Go formatting
[SUCCESS] Checked Go formatting

Coverage

[RUNNING] Running go test -cover ./...
[SUCCESS] Test stream detected
[PKG PASS] github.com/evanmschultz/tillsyn (0.16s)
[PKG PASS] github.com/evanmschultz/tillsyn/cmd/headerlab (0.15s)
[PKG PASS] github.com/evanmschultz/tillsyn/cmd/colors (0.16s)
[PKG PASS] github.com/evanmschultz/tillsyn/cmd/till (0.00s)
[PKG PASS] github.com/evanmschultz/tillsyn/internal/adapters/embeddings/fantasy (0.00s)
[PKG PASS] github.com/evanmschultz/tillsyn/internal/buildinfo (0.00s)
[PKG PASS] github.com/evanmschultz/tillsyn/internal/platform (0.00s)
[PKG PASS] github.com/evanmschultz/tillsyn/internal/config (0.01s)
[PKG PASS] github.com/evanmschultz/tillsyn/internal/adapters/storage/sqlite (0.01s)
[PKG PASS] github.com/evanmschultz/tillsyn/internal/adapters/livewait/localipc (0.01s)
[PKG PASS] github.com/evanmschultz/tillsyn/internal/adapters/server/httpapi (0.01s)
[PKG PASS] github.com/evanmschultz/tillsyn/internal/tui/gitdiff (0.00s)
[PKG PASS] github.com/evanmschultz/tillsyn/internal/adapters/auth/autentauth (0.01s)
[PKG PASS] github.com/evanmschultz/tillsyn/internal/adapters/server/mcpapi (0.01s)
[PKG PASS] github.com/evanmschultz/tillsyn/internal/adapters/server/common (0.01s)
[PKG PASS] github.com/evanmschultz/tillsyn/internal/domain (0.02s)
[PKG PASS] github.com/evanmschultz/tillsyn/internal/app (0.02s)
[PKG PASS] github.com/evanmschultz/tillsyn/internal/templates (0.02s)
[PKG PASS] github.com/evanmschultz/tillsyn/internal/tui (0.02s)
[PKG PASS] github.com/evanmschultz/tillsyn/internal/adapters/server (0.07s)

Test summary
  tests: 1858
  passed: 1857
  failed: 0
  skipped: 1
  packages: 20
  pkg passed: 20
  pkg failed: 0
  pkg skipped: 0

Skipped tests
- TestStewardIntegrationDropOrchSupersedeRejected [SKIP]
  package: github.com/evanmschultz/tillsyn/internal/adapters/server/mcpapi
  elapsed: 0.00s
```

Coverage table (all packages ≥ 70% threshold):

```
package                                                              | cover
---------------------------------------------------------------------+-------
github.com/evanmschultz/tillsyn/cmd/till                             | 75.9%
github.com/evanmschultz/tillsyn/internal/adapters/embeddings/fantasy | 90.6%
github.com/evanmschultz/tillsyn/internal/buildinfo                   | 100.0%
github.com/evanmschultz/tillsyn/internal/platform                    | 78.0%
github.com/evanmschultz/tillsyn/internal/config                      | 76.8%
github.com/evanmschultz/tillsyn/internal/adapters/storage/sqlite     | 75.3%
github.com/evanmschultz/tillsyn/internal/adapters/livewait/localipc  | 79.4%
github.com/evanmschultz/tillsyn/internal/adapters/server/httpapi     | 88.4%
github.com/evanmschultz/tillsyn/internal/tui/gitdiff                 | 85.1%
github.com/evanmschultz/tillsyn/internal/adapters/auth/autentauth    | 73.4%
github.com/evanmschultz/tillsyn/internal/adapters/server/mcpapi      | 73.2%
github.com/evanmschultz/tillsyn/internal/adapters/server/common      | 73.7%
github.com/evanmschultz/tillsyn/internal/domain                      | 80.2%
github.com/evanmschultz/tillsyn/internal/app                         | 71.1%
github.com/evanmschultz/tillsyn/internal/templates                   | 95.7%
github.com/evanmschultz/tillsyn/internal/tui                         | 71.0%
Minimum package coverage: 70.0%.

[SUCCESS] Coverage threshold met
[SUCCESS] Built till from ./cmd/till
```

Builder's coverage figures match exactly. Domain coverage rose +0.2 pp (`80.0% → 80.2%`) due to `normalizeActionItemPaths` + table-driven test exercising 10 cases; sqlite coverage rose +0.1 pp (`75.2% → 75.3%`) due to the new round-trip + JSON encode/decode helpers.

---

## 3. Falsification Attempts

### 3.1 Could `Paths` round-trip correctly through SQL but lose insertion order?

`normalizeActionItemPaths` builds `out` via `out = append(out, path)` in the input-iteration order. `seen` map filters duplicates but does not reorder (the `out` slice is the order source of truth). `encodeActionItemPathsJSON` calls `json.Marshal([]string)` which preserves slice order per Go encoding/json contract. `decodeActionItemPathsJSON` calls `json.Unmarshal` into `*[]string` which also preserves order per the same contract. The round-trip test `TestRepository_PersistsActionItemPaths` `t-paths-multi` case (`["a/b/c.go", "d/e/f.go"]`) explicitly asserts ordered equality at every read site (Get / List / ListByParent). No reorder hazard.

**No counterexample.**

### 3.2 Could update-time validation be skipped if the orchestrator passes a pointer to a slice with backslashes?

`Service.UpdateActionItem` line 1054: `normalized, err := domain.NormalizeActionItemPaths(*in.Paths)`. The exposed wrapper goes straight to `normalizeActionItemPaths` (line 564-579). Backslash check at line 571-573 fires regardless of caller — there is no "skip validation" branch on update. If an MCP caller submits `paths: ["a\\b.go"]`, the chain is: extended_tools → CreateActionItemRequest/UpdateActionItemRequest → app.UpdateActionItemInput → `domain.NormalizeActionItemPaths` → `ErrInvalidPaths`. The same domain rule applies on update as create.

Cross-checked: there is no second "trim only" code path in the service or adapter — every mutation routes through `domain.NormalizeActionItemPaths` (search-confirmed via diff inspection: only one consumer outside the domain package itself).

**No counterexample.**

### 3.3 Could the `*[]string` pointer-sentinel decode incorrectly if the JSON-RPC client sends `paths: null`?

Go's `encoding/json` decodes `null` for a `*[]string` field to nil pointer. The `if args.Paths != nil { ... }` check at extended_tools.go:1078 (create) and the bare passthrough at line 1149 (update) both handle nil correctly: create sets the request's empty zero-value; update preserves at the service layer's `if in.Paths != nil` gate. No ambiguity vs a missing field — both decode to nil pointer, both produce identical "preserve / don't set" semantics. This is exactly the design intent.

**No counterexample.**

### 3.4 Could the SQL bind-argument order drift between INSERT and UPDATE?

INSERT bind args (repo.go:1196-1227, ordered): id, project_id, parent_id, kind, scope, role, structural_type, irreducible, owner, drop_number, persistent, dev_gated, **paths_json**, lifecycle_state, column_id, position, title, description, priority, due_at, labels_json, metadata_json, created_by_actor, created_by_name, updated_by_actor, updated_by_name, updated_by_type, created_at, updated_at, started_at, completed_at, archived_at, canceled_at — 32 args. Column list at line 1192 has 32 names in identical order. Placeholders count to 32.

UPDATE bind args (repo.go:1300-1322, ordered): parent_id, kind, scope, role, structural_type, irreducible, owner, drop_number, persistent, dev_gated, **paths_json**, lifecycle_state, column_id, position, title, description, priority, due_at, labels_json, metadata_json, updated_by_actor, updated_by_name, updated_by_type, updated_at, started_at, completed_at, archived_at, canceled_at, id (WHERE clause). The SET clause enumerates the same column names in the same order, with `paths_json = ?,` between `dev_gated = ?,` and `lifecycle_state = ?,`.

SCAN order in `scanActionItem` (lines 2826-2868) inserts `&pathsRaw` between `&devGatedRaw` and `&state`. SELECT column lists across all 5 read sites uniformly use the column-name order matching scan order.

All three positional alignments (INSERT column list ↔ INSERT bind args, UPDATE SET ↔ UPDATE bind args, SELECT columns ↔ scan args) cross-check. Round-trip test exercises every read path. The fact that `mage ci` is green at `internal/adapters/storage/sqlite` 75.3% is the empirical confirmation that ordinal alignment holds.

**No counterexample.**

### 3.5 Could the `omitempty` JSON tag on `SnapshotActionItem.Paths` cause a non-empty empty-after-normalization slice to drop unexpectedly?

`json:",omitempty"` on a slice is omitted iff the slice is nil OR len 0. For `Paths`, both nil and len 0 are the legitimate domain zero value (no path scope declared) — `decodeActionItemPathsJSON` and `normalizeActionItemPaths` both return nil for the empty case. So a domain → snapshot → JSON → snapshot → domain round-trip with empty paths produces:

- Domain: `Paths: nil`
- Snapshot: `Paths: append([]string(nil), nil...) = nil`
- JSON: field omitted (omitempty)
- Snapshot: `Paths: nil` (decoded from absent field)
- Domain: `append([]string(nil), nil...) = nil`

Stable. The only "loss" possible is "snapshotted with empty slice → JSON omits it → re-decoded with absent field → still empty," which is by-design forward-compat. The round-trip test (`nil-zero-value` case, snapshot_test.go:794) and the legacy-format compat test (lines 846-877) collectively pin this behavior.

**No counterexample.**

### 3.6 Could the TUI `readOnly` classification fail at runtime?

`assertExplicitFieldCoverage` at the test's terminal call enumerates struct fields via reflection and asserts every name appears in either `editable`, `readOnly`, or `internal`. Without `"Paths": {}` in any bucket, the test would fail at `mage ci` time with an "unclassified field: Paths" complaint. Live `mage ci` is green, which is the runtime confirmation that the classification is present and accepted. The static review at line 14866 corroborates.

**No counterexample.**

### 3.7 Could the spec's stated path-validation "trim+dedup+forward-slash-only" disagree with the code's stricter "reject empty entries"?

The plan (`WAVE_1_PLAN.md` §1.1 acceptance line: "Domain validation: `NewActionItem` trims each entry, deduplicates, REJECTS `[]string{"", "  "}` (whitespace-only / empty entries) with `ErrInvalidPaths`") explicitly mandates rejection of whitespace-only / empty entries. The spawn prompt mirrors this. The code at line 569-570 implements rejection via `if path == "" { return nil, ErrInvalidPaths }`. No semantic drift between plan and implementation.

The orchestrator-spawn-prompt's casual phrasing "Trim entries (whitespace-only / empty rejected with `ErrInvalidPaths`)" matches the plan's stricter mandate. The implementation matches both.

**No counterexample.**

### 3.8 Could the diff include unrelated code changes hidden in the `mage ci` green?

`git diff --stat` reports 13 files changed, +660 / -19. Each file's diff was inspected end-to-end. Every change traces directly back to a droplet 4a.5 acceptance bullet (Paths field plumbing, validation helper, tests). No unrelated edits, no incidental refactors, no formatting churn beyond what gofumpt would impose on the new function bodies. The diff is surgical.

**No counterexample.**

**No unmitigated counterexamples found.**

---

## 4. Verdict Summary

| Acceptance bullet                                                                         | Result |
| ----------------------------------------------------------------------------------------- | ------ |
| 1.1 Domain layer — `Paths []string` on `ActionItem` + `ActionItemInput` + sentinel        | PASS   |
| 1.2 App layer — `CreateActionItemInput.Paths` + `UpdateActionItemInput.Paths` + threading | PASS   |
| 1.3 MCP wire layer — `Create*/Update*Request.Paths` + adapter threading                   | PASS   |
| 1.4 MCP tool layer — `mcp.WithArray("paths", ...)` schema + 5-case round-trip test        | PASS   |
| 1.5 SQL layer — `paths_json` column + scan/INSERT/UPDATE plumbing + round-trip test       | PASS   |
| 1.6 Snapshot layer — `Paths` field + bidirectional thread + round-trip + legacy-compat    | PASS   |
| 1.7 TUI schema-coverage — `Paths` classified `readOnly`                                   | PASS   |
| 1.8 Validation rules (trim, dedup, forward-slash-only, empty-valid, no-path-exists)       | PASS   |
| 1.9 Verification gate — `mage ci` green @ 1857/1/0/20pkg/≥70% coverage                    | PASS   |
| 1.10 `domain.NormalizeActionItemPaths` exposure soundness                                 | PASS   |

**Overall verdict: PASS.**

Droplet 4a.5 is the anchor of the Wave 1 `action_item.go` chain (per `WAVE_1_PLAN.md` cross-wave ordering). Every layer of the canonical `domain → app → SQL → MCP → snapshot → TUI` plumbing pattern is exercised and tested. The `NormalizeActionItemPaths` export is a clean abstraction that lets `Service.UpdateActionItem` reuse the create-time validation without duplication. Test coverage rose +0.2 pp on `internal/domain` and +0.1 pp on `internal/adapters/storage/sqlite`. `mage ci` is green at 1857 passed / 1 skipped / 0 failed across 20 packages, all ≥ 70%. The diff is surgical (+660 / -19 across 13 files, every line traceable to an acceptance bullet).

Droplet 4a.5 is ready to commit.

---

## TL;DR

- T1. All 10 plan acceptance bullets verified against the staged 13-file +660/-19 diff. Domain `Paths []string` + `ErrInvalidPaths` sentinel + `NewActionItem` validation gate land in `action_item.go`. App `CreateActionItemInput.Paths []string` + `UpdateActionItemInput.Paths *[]string` (pointer-sentinel) land in `service.go` with create-time `domain.NewActionItem` + update-time `domain.NormalizeActionItemPaths` symmetric validation. MCP wire shapes mirror app shapes; adapter threads both. MCP tool schema `mcp.WithArray("paths", ...)` attaches; 5-case round-trip test pins create-with-paths / create-empty / update-replace / update-clear / update-preserve. SQL `paths_json TEXT NOT NULL DEFAULT '[]'` column inserted at correct ordinal across INSERT / UPDATE / 5 SELECT paths + `scanActionItem`; round-trip test exercises every read path. Snapshot `Paths []string` with `json:"paths,omitempty"` + bidirectional thread + legacy-format compat test. TUI `readOnly` classification keeps `TestActionItemSchemaCoverageIsExplicit` green. Validation rules implement trim + dedup + backslash-rejection + empty-rejection per spec; path-exists is intentionally deferred to Wave 2 lock manager.
- T2. Live `mage ci` re-run produced 1857 passed / 1 skipped / 0 failed across 20 packages, identical to builder's claim. Coverage threshold met across all 16 instrumented packages. `internal/domain` 80.2% (+0.2 pp), `internal/adapters/storage/sqlite` 75.3% (+0.1 pp). Build green.
- T3. Falsification swept ordering preservation through SQL/JSON, update-time validation symmetry, `*[]string` JSON-RPC null/missing decode equivalence, INSERT/UPDATE/SELECT bind-argument ordinal alignment, `omitempty` round-trip stability, TUI runtime classification check, plan↔code semantic drift on whitespace rejection, and unrelated-change drift in the green diff. No counterexamples.
- T4. **Verdict: PASS** — droplet 4a.5 is ready to commit; no findings, no nits.

---

## Hylla Feedback

None — Hylla wasn't queried during this review. Per project convention for unmerged/uncommitted Drop 4a work (Hylla doesn't reingest until drop-end push + CI-green), evidence came from `Read` on the 13 changed files, `git diff` for the staged delta, `rg` for cross-symbol verification (`NewActionItemForTest`, `equalStringSlices`), and a live `mage ci` re-run. The `LSP` tool was attempted once for symbol search but was not enabled in this subagent's MCP config (substituted with `rg` against the working tree). Future drops where Hylla is fresh should retain this Hylla Feedback section per CLAUDE.md "Cascade Ledger + Hylla Feedback."
