# 4a.29 — main/CLAUDE.md DISPATCHER-AWARE SWEEP

**Mode:** orchestrator-direct (MD-only droplet, no subagent per memory rule `feedback_md_update_qa.md`).
**Status:** complete.
**Date:** 2026-05-04.

## Scope

Wave 4.1 surgical sweep of `main/CLAUDE.md` to reflect post-Drop-4a runtime state per WAVE_4_PLAN.md §W4.1.

## Edits Applied (8 total)

1. **§"Cascade Plan" line 20** — appended sentence: `Drop 4a landed the manual-trigger dispatcher (internal/app/dispatcher/ + till dispatcher run); orchestrator-IS-dispatcher remains the documented pre-cascade fallback while dogfooding ramps. Drop 4b adds gate execution + post-build pipeline.`

2. **§"Agent Bindings" line 105-106 wrapper** — appended sentence: `Post-Drop-4a: Wave 2 delivered the dispatcher loop with manual-trigger CLI (till dispatcher run --action-item <id>); automatic dispatch on in_progress transitions lands in Drop 4b.`

3. **§"Post-Build Gates" gate-1 (line 124)** — extended `mage ci` bullet to mention Wave 0 hooks: `Wave 0 of Drop 4a wired mage ci into .githooks/pre-push, so a clean push is itself the smoke check (dev runs mage install-hooks once per fresh clone).`

4. **§"Blocker Semantics" line 133-134** — both bullets rewritten to past-tense canonical:
   - Parent-child bullet: `Always-on invariant — Wave 1 of Drop 4a removed the RequireChildrenComplete policy bit; the rule is unconditional.`
   - blocked_by bullet: `Wave 2 of Drop 4a delivered the dispatcher's lock manager (file + package), and the conflict detector inserts runtime blocked_by on in_progress promotion when sibling locks conflict.`

5. **§"State-Trigger Dispatch" line 139** — added sibling sentence after pre-cascade text: `Drop 4a delivered the manual-trigger dispatcher: till dispatcher run --action-item <id> (cmd/till) reads the same template agent_bindings, acquires file/package locks via the lock manager (internal/app/dispatcher/locks_file.go + locks_package.go), spawns the subagent via claude --agent <name>, and provisions auth via Wave-3's orch-self-approval flow. Per-drop work currently dogfoods both paths until Drop 4b's gate runner ships.`

6. **§"Action-Item Lifecycle (Current HEAD)" lifecycle states list (line 256-262)** — past-tense canonical:
   - Lead sentence: `Four terminal-reachable states: todo, in_progress, complete, failed (Drop 1 landed failed as a real terminal state):`
   - Failure bullet: `transition to failed (real terminal state since Drop 1)` (dropped pre-Drop-1 hedge).
   - Supersede bullet: dropped `Before Drop 1 this doesn't exist` clause.
   - Closing paragraph: `No parent can move to terminal-success if any child is in failed or blocked state — always-on invariant (Wave 1 of Drop 4a removed the RequireChildrenComplete policy bit; the rule is unconditional).`

7. **§"Paths and Packages (Drop-1 Target)" → §"Paths and Packages"** — heading parenthetical dropped; body fully rewritten to first-class language covering all five Wave-1 fields (`paths`, `packages`, `files`, `start_commit`, `end_commit`) + cross-reference to WIKI's atomic-drop-granularity rule.

8. **§"Auth and Leases" line 272-273** — auto-revoke bullet pushed to Drop 4b; new bullet added after for Wave-3 orch-self-approval landing including project-level toggle backstop.

## Verification

- `rg -n "Drop 1 of the cascade plan|arrives in Drop 1|becomes always-on in Drop 1|metadata\.paths|auto-revoke on terminal state is a Drop-1 item|Drop-1 Target" CLAUDE.md` returns zero hits.
- `mage format-check` clean (no Go changes).
- All cross-references intact (PLAN.md, WIKI.md, workflow paths).

## Self-QA

- **No drift introduced** — every rewritten paragraph uses past-tense `landed` / `delivered` / `removed` verbs marking historical state.
- **Pre-cascade dogfood window preserved** — the orchestrator-IS-dispatcher framing survives where the dogfood window genuinely persists (per W4.1 spec: surgical edits, not section retitling).
- **Code-symbol verification** — references to `internal/app/dispatcher/`, `till dispatcher run`, `RequireChildrenComplete` (removed), `OrchSelfApprovalEnabled` are all anchored against committed code from prior Drop 4a droplets.
- **No closeout MD rollups** per pre-MVP rule.

## Hylla Feedback

None — MD-only sweep, Hylla indexes Go only.
