# Droplet 4a.17 — PACKAGE-LEVEL LOCK MANAGER — QA Falsification

## Round 1

**Verdict:** PASS-WITH-NIT
**Summary:** `packageLockManager` is a faithful byte-level mirror of the committed 4a.16 `fileLockManager`. Every counterexample I attempted is either MITIGATED by the existing code/tests or ACCEPTED as documented opaque-string semantics. Two cosmetic nits surface and are flagged for follow-up; neither is a correctness defect, and both have parity with 4a.16's already-merged shape so they belong upstream as a refinement, not a 4a.17 fix-now.

## Attack Matrix

| # | Attack vector | Disposition | Evidence |
|---|---|---|---|
| 1 | Race conditions / map-iterate-while-mutating | **MITIGATED** | `mage testFunc TestPackageLockConcurrentAcquireRaceFree` ran with `-race -count=1`, passed in 1.23s. All read/write happen inside `m.mu.Lock()`/`defer m.mu.Unlock()` (`locks_package.go:103-104`, `:143-144`). Range-over-`owned` in `Release` (line 150) iterates a sub-map that no other goroutine can observe under the held mutex — no concurrent-mutation hazard. |
| 2 | Same-holder idempotent reacquire | **MITIGATED** | `locks_package.go:114-130` — when `holders[pkg]` already equals `actionItemID`, the `taken && holder != actionItemID` guard at line 116 falls through to the write block, the map writes are no-ops, and `acquired = append(acquired, pkg)`. Pinned by `TestPackageLockEmptyInputsAreNoOps` (re-acquire branch, lines 261–273) and `TestPackageLockAcquirePartialConflictReturnsConflicts` (idempotent retry, lines 110–119). |
| 3 | Release-of-unknown-ID / Release after no Acquire | **MITIGATED** | `Release("ghost")` early-returns at `locks_package.go:147-149` when `itemPackages["ghost"]` is absent. No panic, no map mutation. Pinned by `TestPackageLockEmptyInputsAreNoOps` line 258 (`mgr.Release("never-acquired")`). |
| 4 | Partial-acquire atomicity (`Acquire(A, [x, y])` with y taken) | **ACCEPTED** | Documented partial-acquire contract (`locks_package.go:75-83`): free items go to `acquired`, held-by-other items go to `conflicts`. NOT all-or-nothing. Pinned by `TestPackageLockAcquirePartialConflictReturnsConflicts` lines 95–104; the partially-acquired package IS held post-call (line 106-119 runtime sanity check). Same shape as 4a.16. |
| 5 | Empty packages-slice short-circuit | **MITIGATED** | `locks_package.go:99-101` early-returns `(nil, freshConflictsMap, nil)` for `len(packages) == 0`. Pinned by `TestPackageLockEmptyInputsAreNoOps` lines 237–255 (both `nil` slice and `[]string{}` exercised). |
| 6 | Empty `actionItemID` (`Acquire("", [x])`) | **ACCEPTED** | Documented as opaque per `locks_package.go:86-88`: `"an empty actionItemID is treated as opaque (no validation in this droplet)"`. The empty string becomes the holder; subsequent `Release("")` deletes both maps cleanly via the standard path. Same documented behavior as 4a.16. |
| 7 | Returned `conflicts` map aliasing — caller mutate corrupts internal | **MITIGATED** | `locks_package.go:98` allocates `conflicts = make(map[string]string)` fresh per call. The manager never re-references this map after return. Doc comment at line 90-92 promises this explicitly: *"freshly allocated per call. Callers may mutate it freely; the manager retains no reference."* |
| 8 | Two-map bookkeeping divergence (`holders` vs `itemPackages`) | **MITIGATED** | Acquire path: conflict branch (lines 116-119) writes neither map (continue); fall-through branch (lines 122-128) writes both atomically under the single mutex. Release path (lines 142-154): iterates `itemPackages[id]` and deletes `holders[pkg]` for each, then deletes `itemPackages[id]`. No code path mutates one map without the other. No drift possible while the mutex contract holds. |
| 9 | Cross-axis collision: file-lock vs package-lock independence | **MITIGATED** | `TestPackageLockIndependentFromFileLock` (lines 315–338) takes a file-lock on the literal string `"internal/app"` via `fileMgr` and then takes a package-lock on the SAME literal string via `pkgMgr` — observes zero conflicts and full acquired set. Documented at `locks_package.go:14-22`. The doc-comment claim has runtime teeth. |
| 10 | Ghost-package edge: same string is a path AND a package label | **MITIGATED** | Falls out of #9 — the maps are physically separate Go map values on different struct types. The string `"internal/app"` interned identically in both still hashes to two distinct map slots. No case-folding, no trim — verified in `TestPackageLockPackagesAreOpaque` (lines 206–225) for `./internal/app` vs `internal/app`. |
| 11 | LSP `unusedfunc` for `newPackageLockManager` (line 68) — YAGNI? | **ACCEPTED** | Forward-compat-justified by **WAVE_2_PLAN.md §2.9 line 258** (cleanup hook explicitly invokes `packageLockManager.Release(item.ID)`) and **§2.7 conflict detector**. **Mirror parity with 4a.16:** `newFileLockManager` carries the same diagnostic in committed `e3302a3` and is justified by the same downstream consumers. Diagnostic is real, not a defect. The only call site today is the constructor used by tests; the dispatcher struct integration lands in 4a.21 (see `dispatcher.go:119` roadmap). |
| 12 | Same-string duplicate within one Acquire (`Acquire(A, ["x", "x"])`) | **PASS-WITH-NIT** | Loop at `locks_package.go:114-130` processes each input element; the second `"x"` re-enters the same-holder branch and appends `"x"` to `acquired` again. Caller observes `acquired=["x", "x"]`. Doc says "preserves input order for caller-side determinism" — silent on dedup. **Mirror parity with 4a.16** (same shape, same silence). Not a correctness bug — caller asked for duplicates and got them — but worth a doc clarification. NOT a 4a.17-specific blocker; raise as cross-cutting refinement covering both managers. |
| 13 | `equalStringSlices` sorts before compare — order-preservation invariant un-pinned | **PASS-WITH-NIT** | The shared test helper at `locks_file_test.go:307-321` sorts both slices before equality. The doc-comment claim "acquired slice preserves input order" is therefore not actually verified by any test — a future regression that scrambled order would still pass. **Mirror parity with 4a.16** — same hole exists there. Raise as a cross-cutting refinement covering both managers. NOT a 4a.17 blocker. |
| 14 | Test-file coupling — `equalStringSlices` defined only in `locks_file_test.go` | **ACCEPTED** | Same-package test files share helpers by Go convention (both files compile into the same `_test` build). Removing `locks_file_test.go` without the helper would break `locks_package_test.go`, but that's normal Go same-package coupling, not a defect. No refactor warranted. |
| 15 | Mirror fidelity of error-return contract | **MITIGATED** | Both files declare `err` always nil today with identical doc-comment justification (`locks_package.go:94-96`, `locks_file.go:79-81`). Signature reserved for Drop 4b SQLite-mirror failures. Faithful mirror. |
| 16 | Test rigor — do tests pin behavior or pass for the wrong reason? | **MITIGATED** | The 9 tests cover: happy path, conflict, multi-package release, partial conflict + idempotent retry, 32-goroutine race + post-Release recovery, opacity, empty inputs (4 sub-cases), zero-value usability (both nil-map branches), and cross-manager independence. Each test asserts both `acquired` and `conflicts` shape AND the pre-/post-state. The race test asserts exactly-one winner and that all losers see the winner's ID — pins both the mutex AND the conflict-reporting determinism, not just the absence of a data-race-flag hit. |

## Mirror Fidelity vs 4a.16

Diffed `locks_package.go` against `locks_file.go` (committed `e3302a3`) and `locks_package_test.go` against `locks_file_test.go`. Differences are exactly:

- Type rename: `fileLockManager` → `packageLockManager`.
- Constructor rename: `newFileLockManager` → `newPackageLockManager`.
- Field rename: `itemPaths` → `itemPackages`.
- Parameter rename: `paths` → `packages`, `path` → `pkg`.
- Doc-comment expansion: 4a.17 adds the cross-manager-independence paragraph (`locks_package.go:14-22`) and the YAGNI-no-generic paragraph (lines 26-31), both of which are 4a.17-specific by spec (WAVE_2_PLAN §2.4 line 151 mandate). Worklog "Design Choices" §2 confirms.
- One additional supporting test (`TestPackageLockIndependentFromFileLock`) that does not exist in the file-lock side because there is nothing for the file-lock test to reference symmetrically.

No structural divergence. No shape divergence. No undocumented behavioral drift.

## Methodology Discipline

- Hylla queried first (`hylla_search_keyword` for both `packageLockManager` and `newPackageLockManager`) — empty results because these files are not in the latest committed snapshot (snapshot 5; reingest is drop-end-only). Recorded under Hylla Feedback below.
- Fell back to `git grep` for repo-wide reference search — succeeded under sandbox.
- `Read` for file-content inspection of the new files plus the 4a.16 reference.
- `mage testPkg ./internal/app/dispatcher/...` — 95/95 pass.
- `mage testFunc ./internal/app/dispatcher/ TestPackageLockConcurrentAcquireRaceFree` — passed under `-race -count=1`.
- No raw `go test` / `go vet` / `go build` invocations.
- No `mage install` invocations.

## Hylla Feedback

| # | Query | Missed because | Worked via | Suggestion |
|---|---|---|---|---|
| 1 | `hylla_search_keyword(query="packageLockManager", visibility_mode=include_private, test_mode=include_tests)` for `internal/app/dispatcher/` | Files `locks_package.go` + `_test.go` are working-tree-only (untracked, never committed); current snapshot 5 was indexed before this droplet's files existed. Same hit applies to `newPackageLockManager`. | `git grep` from the worktree root surfaced the only consumer reference (`dispatcher.go:119` roadmap doc-comment) plus all PLAN/QA/WAVE doc references. | Acceptable — drop-end reingest fixes this — but a Hylla reply field like `snapshot_age_warning: "X commits behind HEAD"` would help the agent pre-empt fallbacks instead of issuing two empty queries before realizing the snapshot is stale for new files. |

## Routed Unknowns / Cross-Cutting Refinements

These are NOT 4a.17 blockers — they are cross-cutting nits affecting BOTH 4a.16 and 4a.17 by mirror parity. Route as a separate refinement; do not block this droplet's commit.

- **R1.** Doc-comment silence on duplicate-key behavior in `Acquire`. Both `fileLockManager` and `packageLockManager` silently allow `Acquire(A, ["x", "x"])` to return `acquired=["x", "x"]`. Either dedup, or document explicitly that duplicates pass through.
- **R2.** Order-preservation invariant un-pinned. `equalStringSlices` sorts before compare; no test asserts that `acquired` matches input order verbatim. Add an order-pinning assertion to one happy-path test in each file, or change the helper to compare in-place.

## Definition of Done

- Wrote `workflow/drop_4a/4a.17_BUILDER_QA_FALSIFICATION.md` (this file).
- All 16 attack vectors in the matrix labeled CONFIRMED / MITIGATED / ACCEPTED / PASS-WITH-NIT with concrete evidence.
- `mage testPkg ./internal/app/dispatcher/...` green.
- `mage testFunc ... TestPackageLockConcurrentAcquireRaceFree` green under `-race -count=1`.
- No CONFIRMED counterexamples.

## Hand-off

**Verdict:** PASS-WITH-NIT
**Artifact:** `/Users/evanschultz/Documents/Code/hylla/tillsyn/main/workflow/drop_4a/4a.17_BUILDER_QA_FALSIFICATION.md`
**Summary:** Faithful mirror of 4a.16; no falsification holds. Two doc/test nits (R1 duplicate-key silence, R2 order invariant un-pinned) inherited from 4a.16's shape — route upstream as cross-cutting refinement, do not block this droplet.
