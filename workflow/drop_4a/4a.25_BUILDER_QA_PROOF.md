# 4a.25 (Wave 3.2) — PROJECT-METADATA OPT-OUT TOGGLE — Builder QA PROOF

## Round 1 — Filesystem-MD mode

**Verdict:** PASS-WITH-NIT
**Summary:** All 8 acceptance items proven; `mage test-pkg` green for all three target packages; named tests all verified-green via `mage test-func`. Single nit: spec test name `TestProjectMetadataJSONRoundTrip` was implemented as `TestProjectMetadataOrchSelfApprovalEnabledJSONRoundTrip` — more descriptive, content-equivalent, harmless.

### Per-acceptance evidence

| # | Acceptance | Status | Evidence |
| --- | --- | --- | --- |
| 1 | `OrchSelfApprovalEnabled *bool` on `ProjectMetadata` w/ JSON tag `json:"orch_self_approval_enabled,omitempty"`. POINTER not bare bool (FA3 mitigation). | PROVEN | `internal/domain/project.go:121` — field declared as `*bool` with exact JSON tag. Doc-comment block at `:99-118` cites W3.2 + falsification-attack-3 nil-means-enabled rationale. |
| 2 | Helper `(ProjectMetadata).OrchSelfApprovalIsEnabled() bool` — nil → true, *true → true, *false → false. | PROVEN | `internal/domain/project.go:134-139` — exact branch logic: `if m.OrchSelfApprovalEnabled == nil { return true }; return *m.OrchSelfApprovalEnabled`. Doc-comment at `:124-133`. |
| 3 | MCP surface field `OrchSelfApprovalEnabled *bool` on project create/update payloads. | PROVEN (via embedded struct) | `internal/adapters/server/common/mcp_surface.go:63` (`CreateProjectRequest.Metadata domain.ProjectMetadata`) and `:83` (`UpdateProjectRequest.Metadata domain.ProjectMetadata`). Both surfaces embed the domain struct directly, so the new field rides the existing transport with zero source change — see worklog item §"Files touched" para 2. Adapter round-trip test (item 7) drives this end-to-end and asserts the toggle persists across create/update/get. |
| 4 | Project create + update plumbing accepts the new field. | PROVEN | Adapter round-trip test `TestAppServiceAdapterApproveAuthRequestHonorsProjectToggleDisabled` (`internal/adapters/server/common/app_service_adapter_auth_requests_test.go:1131-1261`) flips toggle via `repo.UpdateProject(ctx, project)` and re-reads via `repo.GetProject(ctx, "p1")` — round-trip persistence verified at `:1175-1179`. Existing fixtures unaffected because the field is `omitempty`. |
| 5 | Gate honors toggle BEFORE role-gate. Load project, if disabled, reject with `ErrOrchSelfApprovalDisabled` BEFORE role/path/STEWARD-exception checks. | PROVEN | `internal/app/auth_requests.go:425-445` — Reject 0 sits at the very top of `checkOrchSelfApprovalGate` after only the auth-backend nil-check + `GetAuthRequest`. Reject 1 (orchestrator-role check) doesn't fire until line `:448`. Project-id resolution: `req.ProjectID` first (`:431`), `ParseAuthRequestPath(req.Path)` fallback (`:433-435`), skips `domain.AuthRequestGlobalProjectID` sentinel (`:437`). Wraps `ErrOrchSelfApprovalDisabled` via `%w` at `:443`. |
| 6 | `ErrOrchSelfApprovalDisabled` typed sentinel. | PROVEN | `internal/domain/errors.go:78` — `ErrOrchSelfApprovalDisabled = errors.New("orch self-approval disabled by project metadata")`. Doc comment at `:72-77` distinguishes from `ErrAuthorizationDenied` for observability. |
| 7 | Five named tests (incl STEWARD persistent-parent fixture). | PROVEN (with name nit on test 2) | See §"Named tests" below. |
| 8 | Total backstop: STEWARD cross-subtree path also rejected when toggle disabled (FA10 mitigation). | PROVEN | `internal/app/auth_requests_test.go:1444-1486` — `TestApproveAuthRequestRejectsWhenProjectToggleDisabled/steward_cross_subtree` sub-test uses `newStewardCrossSubtreeFixture(t)` (the W3.1 STEWARD-persistent-parent fixture builder) and asserts STEWARD's cross-subtree approve attempt rejects with `ErrOrchSelfApprovalDisabled`. Verified-green via `mage test-func ./internal/app TestApproveAuthRequestRejectsWhenProjectToggleDisabled` (3/3 sub-tests pass). |

### Named tests

| Spec test name | Implemented as | Status | Verified-green |
| --- | --- | --- | --- |
| `TestProjectMetadataOrchSelfApprovalIsEnabledDefaults` | `TestProjectMetadataOrchSelfApprovalIsEnabledDefaults` (`internal/domain/project_test.go:15-38`) | PROVEN | `mage test-func ./internal/domain` — 4 tests pass (parent + 3 sub-cases nil/`*true`/`*false`). |
| `TestProjectMetadataJSONRoundTrip` | `TestProjectMetadataOrchSelfApprovalEnabledJSONRoundTrip` (`internal/domain/project_test.go:44-102`) — **NIT: name extended for clarity, content matches spec exactly** | PROVEN-WITH-NAME-NIT | `mage test-func ./internal/domain` — 4 tests pass (parent + 3 sub-cases nil-omits / explicit-true-serializes / explicit-false-serializes). Asserts both `omitempty` raw-JSON behavior AND pointer nil-ness preservation across encode→decode. |
| `TestApproveAuthRequestRejectsWhenProjectToggleDisabled` (incl STEWARD-persistent-parent fixture path) | `TestApproveAuthRequestRejectsWhenProjectToggleDisabled` (`internal/app/auth_requests_test.go:1374-1486`) | PROVEN | `mage test-func ./internal/app` — 3 tests pass (parent + `non_steward_orch` + `steward_cross_subtree`). The `non_steward_orch` sub-test also asserts the rejection does NOT also wrap `ErrAuthorizationDenied` (defense-in-depth observability). |
| `TestApproveAuthRequestAllowedWhenProjectToggleNil` | `TestApproveAuthRequestAllowedWhenProjectToggleNil` (`internal/app/auth_requests_test.go:1492-1494`) | PROVEN | `mage test-func ./internal/app` — 1 test passes. Asserts pointer is nil + helper returns true post-load + cascade succeeds + session secret returned. |
| Adapter round-trip test | `TestAppServiceAdapterApproveAuthRequestHonorsProjectToggleDisabled` (`internal/adapters/server/common/app_service_adapter_auth_requests_test.go:1131-1261`) | PROVEN | `mage test-func ./internal/adapters/server/common` — 1 test passes. Drives create→toggle-flip→approve-rejected→toggle-reset→approve-succeeds round-trip, proving the toggle (not unrelated drift) caused the rejection. |

### model_test.go scope-check

Out-of-declared-paths touch is genuinely minimum-touch:

- Diff is +8 lines (1 classification entry `"OrchSelfApprovalEnabled": {}` at `internal/tui/model_test.go:15009` + 7-line doc comment at `:15002-15008`).
- The `assertExplicitFieldCoverage(t, reflect.TypeOf(domain.ProjectMetadata{}), ...)` call at `:15011` enforces every `ProjectMetadata` field is classified. Adding the new field to `ProjectMetadata` mandates the single classification entry — refusing it would break `mage ci`.
- No production-code change. No test-logic change. No alternative tag on the field would have avoided this. **Confirmed minimum-touch.**

### Mage verification

- `mage test-pkg ./internal/domain` — 260/260 pass.
- `mage test-pkg ./internal/app` — 357/357 pass.
- `mage test-pkg ./internal/adapters/server/common` — 159/159 pass.
- All five named tests verified-green via direct `mage test-func` execution (counts above).

### Hylla feedback

None for QA-proof — verification was driven by `git diff` (uncommitted changes) + `Read` for file:line checks + `mage test-func`/`test-pkg` for ground-truth test runs. The committed-code Hylla index was not exercised because the diff was uncommitted; this is the expected workflow per `feedback_hylla_go_code.md` (use `git diff` for changed-since-ingest files).

### Verdict

**PASS-WITH-NIT** — all 8 acceptance items proven, all 5 named tests green, gate ordering correct (Reject 0 before Reject 1), STEWARD backstop verified, model_test.go change minimum-touch. One harmless name nit: spec called for `TestProjectMetadataJSONRoundTrip` but implementation extended the name to `TestProjectMetadataOrchSelfApprovalEnabledJSONRoundTrip` for clarity. Content matches spec exactly. No fix required; flag for orch awareness.
