# DROPLET 4a.9 — QA PROOF REVIEW

**Target droplet:** Drop 4a / Wave 1 / W1.5 — `end_commit string` FIRST-CLASS ON `ActionItem`
**Plan ref:** `workflow/drop_4a/WAVE_1_PLAN.md` §1.5
**Paths reviewed (12 files, all working-dir uncommitted; `git diff --stat` = +569 / -17):**
`internal/domain/action_item.go`, `internal/domain/domain_test.go`, `internal/app/service.go`, `internal/app/snapshot.go`, `internal/app/snapshot_test.go`, `internal/adapters/server/common/mcp_surface.go`, `internal/adapters/server/common/app_service_adapter_mcp.go`, `internal/adapters/server/mcpapi/extended_tools.go`, `internal/adapters/server/mcpapi/extended_tools_test.go`, `internal/adapters/storage/sqlite/repo.go`, `internal/adapters/storage/sqlite/repo_test.go`, `internal/tui/model_test.go`
**Pattern:** Near-twin of 4a.8 (`commit 0d7f14d` — `start_commit`), with `EndCommit` substituted for `StartCommit` everywhere and field placement immediately after `StartCommit`.
**Verdict:** **PASS**
**Date:** 2026-05-03

---

## 1. Acceptance Bullet Verification

Every plan acceptance bullet from `workflow/drop_4a/WAVE_1_PLAN.md` §1.5 is checked against the working-dir diff. Builder claimed `mage ci` 1949 passed / 0 failed / 1 pre-existing skip. QA re-ran live (§2 below) and confirmed identical (1949 passed / 0 failed / 1 skip across 1950 tests). The droplet is the second single-string field in the action-item-domain chain, deliberately mirroring 4a.8's shape — domain-opaque, no sentinel, no exposed helper, raw-text SQL column, pointer-sentinel update.

### 1.1 Domain layer — `EndCommit string` on `ActionItem` + `ActionItemInput` after `StartCommit`; `NewActionItem` validates trim only; NO new sentinel; NO exposed helper

**Spec:** Single-value `string` (not `[]string`). Inserted after `StartCommit`. `NewActionItem` performs trim only. No `ErrInvalidEndCommit` sentinel. No exposed `NormalizeActionItemEndCommit` helper.

**Evidence — struct fields:**

- `internal/domain/action_item.go` `ActionItem` struct: `EndCommit string` inserted between `StartCommit` and `LifecycleState`. The 18-line doc comment cites: free-form trimmed string; empty IS the meaningful zero value ("not yet captured / not applicable"); valid until terminal state — domain does NOT enforce non-empty-on-terminal; opaque-domain (domain layer never calls git itself); caller (orchestrator pre-cascade / Wave 2 dispatcher / Drop 4b commit-agent) supplies the value; disjoint-axis with `StartCommit` — no chronology check applied at the domain layer; Drop 4b commit-agent consumes both fields for `git diff <start_commit>..<end_commit>` baseline.
- `internal/domain/action_item.go` `ActionItemInput` struct: `EndCommit string` inserted between `StartCommit` and `LifecycleState`. Doc mirrors the production-struct doc with the "empty IS meaningful zero value" call-out and the no-non-empty-on-terminal rule.

**Evidence — validation gate inside `NewActionItem`:**

- `internal/domain/action_item.go` `NewActionItem`: `in.EndCommit = strings.TrimSpace(in.EndCommit)` immediately after the existing `in.StartCommit = strings.TrimSpace(in.StartCommit)` line. Trim only, no rejection path. The comment block explicitly cites: "EndCommit mirrors StartCommit: free-form opaque-domain string, inline-trimmed, no format check, no exposed helper. Empty is the legitimate zero value … valid until terminal state — domain does NOT enforce non-empty-on-terminal."
- The returned `ActionItem` literal threads `EndCommit: in.EndCommit` directly after `StartCommit: in.StartCommit`.

**Evidence — NO new sentinel error:**

- `internal/domain/errors.go` is unchanged in this diff (`git diff --stat` shows no entry). No `ErrInvalidEndCommit` defined. Trim-only correctly produces no rejection.

**Evidence — NO exposed helper:**

- The diff contains no `NormalizeActionItemEndCommit` or `normalizeActionItemEndCommit` function definition. Inline `strings.TrimSpace` is the entire normalization. The existing 4a.8 doc comment (covering `StartCommit`) already establishes the "single TrimSpace too thin to wrap" precedent; 4a.9 adheres without re-justifying.

**Evidence — single-string vs slice divergence preserved on all six wire surfaces:** ActionItem field is `string`; ActionItemInput is `string`; CreateActionItemInput is `string`; UpdateActionItemInput is `*string`; CreateActionItemRequest is `string`; UpdateActionItemRequest is `*string`; SnapshotActionItem is `string`; SQL column is `TEXT NOT NULL DEFAULT ''` (raw string, not JSON-encoded `'[]'`). No accidental slice-typing.

**Conclusion:** PASS.

### 1.2 App layer — `CreateActionItemInput.EndCommit string` (value-type) + `UpdateActionItemInput.EndCommit *string` (pointer-sentinel locked) threaded through Service Create + Update

**Spec:** Create-side is value-type `string` (empty IS meaningful zero — no pointer-sentinel needed at create boundary). Update-side is pointer-sentinel `*string` per Drop 3.21 / 4a.5 lock — nil preserves, non-nil applies, empty dereferenced clears. Service Update applies `strings.TrimSpace` inline.

**Evidence — struct fields:**

- `internal/app/service.go` `CreateActionItemInput`: `EndCommit string` inserted between `StartCommit` and `ColumnID`. Doc cites: empty IS meaningful zero; mirrors StartCommit's value-type shape at the create boundary (no pointer-sentinel needed because absent-at-creation is the dominant case — terminal capture happens later via UpdateActionItem before MoveActionItemState); opaque-domain; valid until terminal state; "domain does NOT enforce non-empty-on-terminal."
- `internal/app/service.go` `UpdateActionItemInput`: `EndCommit *string` inserted between `StartCommit *string` and `Metadata *domain.ActionItemMetadata`. Doc cites: pointer-sentinel pattern (nil preserves, non-nil applies, empty dereferenced clears); rationale (description-only update must NOT silently clobber dispatcher-set end commit); inline `strings.TrimSpace` applied by service (no domain helper); locked per WAVE_1_PLAN post-4a.5 amendment.

**Evidence — service threading on Create:**

- `internal/app/service.go` `Service.CreateActionItem`: `EndCommit: in.EndCommit` threaded into `domain.ActionItemInput` directly after the existing `StartCommit: in.StartCommit` line. Validation runs at the domain seam — `NewActionItem`'s inline `strings.TrimSpace` applies.

**Evidence — service threading on Update:**

- `internal/app/service.go` `Service.UpdateActionItem`: pointer-sentinel branch `if in.EndCommit != nil { actionItem.EndCommit = strings.TrimSpace(*in.EndCommit); actionItem.UpdatedAt = s.clock().UTC() }` placed immediately after the equivalent `StartCommit` block. Inline trim mirrors `NewActionItem`'s create-time approach. The comment cites: nil preserves; non-nil applies dereferenced; empty dereferenced clears (explicit caller intent — "dispatcher rolling back a retry"); Wave 2 dispatcher populates this BEFORE `MoveActionItemState` so the terminal capture lands cleanly.

**Conclusion:** PASS.

### 1.3 MCP wire layer — `CreateActionItemRequest.EndCommit string` + `UpdateActionItemRequest.EndCommit *string` threaded through adapter handlers

**Spec:** Adapter request types match the app-layer shape (value on create, pointer on update). Adapter handlers thread the values through.

**Evidence — request struct fields:**

- `internal/adapters/server/common/mcp_surface.go` `CreateActionItemRequest`: `EndCommit string` inserted between `StartCommit string` and `ColumnID string`. Doc cites empty-as-meaningful-zero, opaque-domain, caller-supplied; mirrors `StartCommit`'s create-side doc tone.
- `internal/adapters/server/common/mcp_surface.go` `UpdateActionItemRequest`: `EndCommit *string` inserted between `StartCommit *string` and `Metadata *domain.ActionItemMetadata`. Doc cites pointer-sentinel pattern (nil preserves, non-nil applies, empty dereferenced clears) + inline-trim path + locked per WAVE_1_PLAN post-4a.5 amendment.

**Evidence — adapter threading on Create:**

- `internal/adapters/server/common/app_service_adapter_mcp.go` `CreateActionItem`: `EndCommit: strings.TrimSpace(in.EndCommit)` threaded into `app.CreateActionItemInput` directly after the existing `StartCommit: strings.TrimSpace(in.StartCommit)` line. Adapter applies `TrimSpace` defensively (matches Title / Description / ColumnID precedent on the same lines). Idempotent with the domain-layer trim.

**Evidence — adapter threading on Update:**

- `internal/adapters/server/common/app_service_adapter_mcp.go` `UpdateActionItem`: `EndCommit: in.EndCommit` threaded into `app.UpdateActionItemInput` directly after the existing `StartCommit: in.StartCommit` line. Pointer-sentinel passes through unchanged (no copy needed — pointer carries explicit-clear intent). No adapter-side trim on update; service applies `strings.TrimSpace` after dereference.

**Conclusion:** PASS.

### 1.4 MCP tool layer — `mcp.WithString("end_commit", ...)` schema (NOT `WithArray`) + round-trip test with 5 sub-cases

**Spec:** Tool schema uses `mcp.WithString` since the field is single-string. Round-trip test covers create-with / create-without / update-with / update-empty-clear / update-without.

**Evidence — schema:**

- `internal/adapters/server/mcpapi/extended_tools.go` `till.action_item` tool registration: `mcp.WithString("end_commit", mcp.Description("..."))` inserted on the next line after the existing `mcp.WithString("start_commit", ...)`. Confirmed `WithString` (NOT `WithArray`, NOT `WithStringItems`). Description text cites: opaque-domain rule, empty-as-zero, omit-on-update preserves, no format check, all caller identifiers round-trip, no chronology check against StartCommit, Drop 4b commit-agent consumer.
- `internal/adapters/server/mcpapi/extended_tools.go` create-args anonymous struct: `EndCommit *string \`json:"end_commit"\`` declared as `*string` immediately after `StartCommit *string \`json:"start_commit"\``. Pointer-sentinel at the schema-parsing layer keeps "not supplied" distinguishable from "empty supplied".
- Create-side handler: `if args.EndCommit != nil { createReq.EndCommit = *args.EndCommit }` placed immediately after the `StartCommit` equivalent. Comment cites the exact same shape rules as `StartCommit`.
- Update-side handler: `EndCommit: args.EndCommit` (pointer passed through verbatim into `UpdateActionItemRequest.EndCommit`) placed immediately after the `StartCommit` equivalent. Comment cites: nil preserves, non-nil applies, empty dereferenced clears; service applies inline `TrimSpace`.

**Evidence — round-trip test with 5 sub-cases:**

- `internal/adapters/server/mcpapi/extended_tools_test.go` `TestActionItemMCPEndCommitRoundTrip` — five `t.Run` sub-cases:
  1. **`create plumbs end_commit string through to request`** — create with `end_commit: "0cf5194"` lands as `service.lastCreateActionItemReq.EndCommit == "0cf5194"`.
  2. **`create without end_commit round-trips empty string`** — create omitting the field → `service.lastCreateActionItemReq.EndCommit == ""`.
  3. **`update with end_commit plumbs as non-nil pointer-sentinel`** — update with `end_commit: "0cf5194"` → `lastUpdateActionItemReq.EndCommit != nil && *lastUpdateActionItemReq.EndCommit == "0cf5194"`.
  4. **`update with empty end_commit plumbs as non-nil pointer to empty string (explicit clear)`** — update with `end_commit: ""` → `lastUpdateActionItemReq.EndCommit != nil && *lastUpdateActionItemReq.EndCommit == ""`. Load-bearing pointer-sentinel explicit-clear assertion.
  5. **`update without end_commit preserves prior via nil pointer-sentinel`** — update omitting the field → `lastUpdateActionItemReq.EndCommit == nil`. Proves preserve-on-omit.

All five sub-cases match the spec exactly and the same shape as 4a.8's `TestActionItemMCPStartCommitRoundTrip`.

**Conclusion:** PASS.

### 1.5 SQL layer — `end_commit TEXT NOT NULL DEFAULT ''` (raw string column, NOT JSON-encoded) after `start_commit`; INSERT + UPDATE + 5 SELECT sites + `scanActionItem` + `changedActionItemFields` threaded; round-trip test

**Spec:** Raw `TEXT` column (NOT JSON-encoded). Default `''`. Inserted after `start_commit`. Threaded through INSERT, UPDATE, all SELECT sites, `scanActionItem`, and `changedActionItemFields`. Round-trip test with empty / short-SHA / full-SHA cases.

**Evidence — schema:**

- `internal/adapters/storage/sqlite/repo.go` `migrate()` — `action_items` `CREATE TABLE`: `end_commit TEXT NOT NULL DEFAULT ''` inserted immediately after `start_commit TEXT NOT NULL DEFAULT ''` and before `lifecycle_state`. Default value is empty string `''` (NOT `'[]'`). Type is `TEXT`.

**Evidence — INSERT + UPDATE column lists:**

- `repo.go` `CreateActionItem` INSERT: column list reads `..., start_commit, end_commit, lifecycle_state, ...` — correct ordinal slot. Placeholder count goes from 36 to 37 — the `VALUES` clause grows by one `?`. Argument list adds `t.EndCommit` immediately after `t.StartCommit`.
- `repo.go` `UpdateActionItem` SET: includes `..., start_commit = ?, end_commit = ?, lifecycle_state = ?, ...`. Bind argument `t.EndCommit` placed immediately after `t.StartCommit`.

**Evidence — 5 SELECT sites (plus the INSERT column list, all carrying `end_commit` in the same ordinal slot):**

1. `repo.go` `ListActionItems` — `SELECT ..., start_commit, end_commit, lifecycle_state, ...`.
2. `repo.go` `ListActionItemsByParent` — same column list extension.
3. `repo.go` `FindActionItemByOwnerAndTitle` — same.
4. `repo.go` `ListActionItemsByDropNumber` — same.
5. `repo.go` `getActionItemByID` — same.

All five read sites enumerate `end_commit` in the same ordinal slot — between `start_commit` and `lifecycle_state`. Ordinal alignment with `scanActionItem` is preserved.

**Evidence — `scanActionItem`:**

- `repo.go` `scanActionItem`: `endCommitRaw string` declared in the var block immediately after `startCommitRaw string`. `&endCommitRaw` inserted in the `Scan` argument list immediately after `&startCommitRaw` and before `&state`. Ordinal exactly matches the SELECT lists.
- Assignment block: `t.EndCommit = endCommitRaw` immediately after `t.StartCommit = startCommitRaw`. Raw string assignment — NO decode helper. Comment cites: raw TEXT column not JSON-encoded; pre-droplet-4a.9 rows + NOT NULL DEFAULT '' invariant read as empty string ("not yet captured" zero value).

**Evidence — `changedActionItemFields`:**

- `repo.go` `changedActionItemFields`: `if prev.EndCommit != next.EndCommit { changed = append(changed, "end_commit") }` inserted immediately after the `StartCommit` equivalent. Uses `!=` for string comparison rather than slice-equality (correct — single-value).

**Evidence — round-trip test:**

- `internal/adapters/storage/sqlite/repo_test.go` `TestRepository_PersistsActionItemEndCommit`:
  - Three create cases: `t-endcommit-empty` (empty), `t-endcommit-short` (`"0cf5194"`), `t-endcommit-full` (40-char SHA).
  - `GetActionItem` round-trip on each.
  - `ListActionItems` round-trip — exercises the second SELECT path.
  - `ListActionItemsByParent` round-trip — exercises the third SELECT path.
  - **UPDATE — populate path:** reassign `t-endcommit-empty.EndCommit = "deadbeef"`, persist, verify.
  - **UPDATE — clear path:** reassign `t-endcommit-full.EndCommit = ""`, persist, verify the prior value is cleared.

The test covers every SQL path that mutates or reads `end_commit`. The "populate-on-update" + "clear-on-update" pair specifically asserts the UPDATE SET clause writes both populated and empty payloads — load-bearing for the pointer-sentinel explicit-clear semantics at the wire layer.

**Conclusion:** PASS.

### 1.6 Snapshot layer — `SnapshotActionItem.EndCommit string \`json:"end_commit,omitempty"\`` after `StartCommit`; threaded both directions; round-trip test + legacy-format-compat test

**Spec:** `SnapshotActionItem` gains `EndCommit string` field with `json:"end_commit,omitempty"` after `StartCommit`. Threaded in `snapshotActionItemFromDomain` and `(t SnapshotActionItem) toDomain()`. Round-trip test + legacy-format-compat test.

**Evidence — struct field:**

- `internal/app/snapshot.go` `SnapshotActionItem`: `EndCommit string \`json:"end_commit,omitempty"\`` inserted between `StartCommit string \`json:"start_commit,omitempty"\`` and `LifecycleState`. Tag includes `omitempty` (correct — empty strings omit on encode, missing field decodes to empty).

**Evidence — encode path:**

- `internal/app/snapshot.go` `snapshotActionItemFromDomain`: `EndCommit: t.EndCommit` threaded into the returned literal directly after `StartCommit: t.StartCommit`. Direct string copy.

**Evidence — decode path:**

- `internal/app/snapshot.go` `(t SnapshotActionItem) toDomain()`: `EndCommit: t.EndCommit` threaded into the returned literal directly after `StartCommit: t.StartCommit`. Comment block cites: pre-droplet-4a.9 snapshots without the field deserialize to empty string (the legitimate domain zero value); legacy compat covered by `omitempty` tag.

**Evidence — round-trip test:**

- `internal/app/snapshot_test.go` `TestSnapshotActionItemEndCommitRoundTrip` — table-driven with 4 cases: empty zero-value / short-SHA / full-SHA / free-form-identifier. Each case constructs the domain item, encodes via `snapshotActionItemFromDomain`, asserts the snapshot's `EndCommit` matches expectation, then decodes via `snap.toDomain()` and asserts the round-tripped domain item's `EndCommit` matches.

**Evidence — legacy-format-compat test:**

- `internal/app/snapshot_test.go` `TestSnapshotActionItemEndCommitLegacyFormatCompatibility` — constructs a raw JSON byte slice OMITTING the `end_commit` field entirely (matches the pre-4a.9 snapshot wire format), unmarshals into `SnapshotActionItem`, asserts `snap.EndCommit == ""` (pure default), then calls `snap.toDomain()` and asserts the hydrated domain item also has `EndCommit == ""`. Load-bearing forward-compatibility test — proves a snapshot encoded before this droplet round-trips cleanly without a `SnapshotVersion` bump.

**Conclusion:** PASS.

### 1.7 TUI schema-coverage — `EndCommit` classified as `readOnly`

**Spec:** The TUI schema-coverage assertion at `model_test.go` `TestActionItemSchemaCoverageIsExplicit` enumerates every top-level `domain.ActionItem` field as either `editable` or `readOnly`. New `EndCommit` field must land in `readOnly`.

**Evidence:**

- `internal/tui/model_test.go` `TestActionItemSchemaCoverageIsExplicit`: `"EndCommit": {},` inserted in the `readOnly` map between `"StartCommit":` and `"LifecycleState":`. Same indent + style as the surrounding entries. The `editable` map does NOT contain `EndCommit` (would have caused `assertExplicitFieldCoverage` to fail if `EndCommit` appeared in both or neither).

`mage test-pkg ./internal/tui` would surface a `domain.ActionItem` struct field that isn't enumerated in either map (the assertion is exhaustive). The `mage ci` green run (§2 below) proves the assertion holds.

**Conclusion:** PASS.

### 1.8 No business rules added — explicit negative checks

**Spec (the four forbidden additions):**
- `SetLifecycleState` NOT modified — no auto-population of `EndCommit` on terminal transitions.
- No terminal-state non-empty enforcement.
- No chronology check between `StartCommit` and `EndCommit`.
- No SHA format validation.

**Evidence — `SetLifecycleState` untouched:**

- `git diff internal/domain/action_item.go` shows two struct-extension hunks (`ActionItem` + `ActionItemInput`), one `NewActionItem` hunk (the trim line + the literal field threading). No hunk modifies `SetLifecycleState`. The function body retains its prior shape entirely. No `t.EndCommit = ...` assignment was introduced anywhere except `NewActionItem`'s returned literal and `Service.UpdateActionItem`'s pointer-sentinel branch. Confirmed: domain-side state-transition path is untouched.

**Evidence — no terminal-state non-empty enforcement:**

- `NewActionItem`'s validation block contains exclusively `strings.TrimSpace`. No `if in.EndCommit == "" && state.IsTerminal() { return ErrInvalidEndCommit }` style check anywhere in the diff.
- The doc comments on the field, on `ActionItemInput`, on `CreateActionItemInput`, on `UpdateActionItemInput`, on the MCP request types, and in the MCP tool description all explicitly disclaim this rule: "Empty is valid until terminal state; domain does NOT enforce non-empty-on-terminal." The disclaimer is repeated on every layer — load-bearing documentation that future readers don't re-introduce the rule.
- Domain test `TestNewActionItemEndCommitTrim` includes the empty-input case `{name: "empty round-trips empty", input: "", want: ""}` which would FAIL if any non-empty enforcement existed. The `mage ci` green run confirms the case passes.

**Evidence — no chronology check between `StartCommit` and `EndCommit`:**

- The diff contains zero comparison of `EndCommit` against `StartCommit` (no `if in.EndCommit < in.StartCommit`, no time-ordering call). The domain doc on `EndCommit` explicitly states "Disjoint-axis with StartCommit — no chronology check applied at the domain layer." The MCP tool description on the wire surface also explicitly says "No chronology check against StartCommit."
- Conceptually impossible without a git-aware comparator anyway — neither field is a time, both are opaque hashes. The disclaimer pre-empts any future reader reaching for one.

**Evidence — no SHA format validation:**

- `domain_test.go` `TestNewActionItemEndCommitTrim` includes the case `{name: "non-SHA identifier round-trips", input: "branch/feature@head", want: "branch/feature@head"}`. This case would FAIL if any hex/length/regex SHA validator existed. The `mage ci` green run confirms it passes.
- The `NewActionItem` validation block contains no `regexp` import use, no `len(in.EndCommit) != 40 && len(in.EndCommit) != 7` length check, no hex-digit verification.
- The MCP tool description explicitly cites "no format check applies — short-SHAs (7-char), full-SHAs (40-char), and any caller-supplied identifier all round-trip."

**Conclusion:** All four negative bullets PASS.

### 1.9 Pre-existing tests still pass with new `EndCommit` field present

**Spec:** Adding a new struct field can break test-data fixtures that construct `ActionItem` with positional struct literals or that compare struct equality. Builder must not break 4a.5 / 4a.6 / 4a.7 / 4a.8 round-trip assertions.

**Evidence:** All Go struct literals in the diff use named-field initialization (`EndCommit: in.EndCommit`, `EndCommit: t.EndCommit` style) — no positional struct literals, so adding a new field is strictly additive. The `mage ci` run lands at 1949 passing — the pre-droplet baseline (per builder claim aligned with 4a.8's 1929-passing baseline plus 4a.9's added tests) survives unchanged.

Specifically:
- `TestNewActionItemStartCommitTrim` (4a.8) still passes — confirmed in §2 mage ci output.
- `TestRepository_PersistsActionItemStartCommit` (4a.8) still passes.
- `TestSnapshotActionItemStartCommitRoundTrip` / `TestSnapshotActionItemStartCommitLegacyFormatCompatibility` (4a.8) still pass.
- `TestActionItemMCPStartCommitRoundTrip` (4a.8) still passes.
- All 4a.5 / 4a.6 / 4a.7 (Paths / Packages / Files) round-trip tests still pass.

No regression.

**Conclusion:** PASS.

---

## 2. `mage ci` Re-Run — Verification Gate

QA re-ran `mage ci` live in the working directory at HEAD-with-working-dir-diff. Output excerpt:

```
Test summary
  tests: 1950
  passed: 1949
  failed: 0
  skipped: 1
  packages: 20
  pkg passed: 20
  pkg failed: 0
  pkg skipped: 0

Skipped tests
- TestStewardIntegrationDropOrchSupersedeRejected [SKIP]
  package: github.com/evanmschultz/tillsyn/internal/adapters/server/mcpapi
  elapsed: 0.00s
```

Builder claim: 1949 passed / 0 failed / 1 pre-existing skip. **VERIFIED IDENTICAL.**

The single skip is the pre-existing `TestStewardIntegrationDropOrchSupersedeRejected` — pre-MVP supersede CLI gate, unrelated to this droplet. Skip carried unchanged from 4a.8 (which also reported 1 skip).

**Coverage gate:** every package at or above the 70.0% floor. Lowest packages:
- `internal/app` — 70.8% (closest to floor; baseline carried forward, no regression).
- `internal/tui` — 71.0%.
- `internal/adapters/server/mcpapi` — 73.3%.
- `internal/adapters/server/common` — 73.7%.
- `internal/adapters/auth/autentauth` — 73.4%.

`mage ci` build step also passed (`till` binary built from `./cmd/till`). Full pipeline green.

**Conclusion:** Verification gate PASSES.

---

## 3. Negative-Space Verification

Bullets the spec explicitly forbids — verified ABSENT.

### 3.1 No JSON encoding of `EndCommit` in SQL

`repo.go` declares `end_commit TEXT NOT NULL DEFAULT ''` (empty-string default, NOT `'[]'`). INSERT passes `t.EndCommit` raw (no `json.Marshal`). `scanActionItem` reads `endCommitRaw` raw into `t.EndCommit` (no `json.Unmarshal`). Confirmed: raw string column.

### 3.2 No list-validation logic borrowed from list-typed droplets

The validation gate in `NewActionItem` for `EndCommit` contains exclusively `strings.TrimSpace`. No `for ... range` loop, no `seen map[string]bool` dedup, no backslash check, no coverage gate. List-axis patterns from 4a.5 / 4a.6 / 4a.7 absent. Confirmed: no accidental borrowing.

### 3.3 No `mcp.WithArray` for `end_commit`

`extended_tools.go` uses `mcp.WithString("end_commit", ...)`. No `mcp.WithArray("end_commit", ...)` or `mcp.WithStringItems()` modifier. Schema correctly reflects single-string nature.

### 3.4 No SHA format check anywhere downstream

No `service.go`, `app_service_adapter_mcp.go`, `extended_tools.go`, or `repo.go` location performs format validation on the string. The `mcp.WithString` schema declaration carries no `mcp.Pattern(...)` or regex enforcement. The `domain_test.go` test case `"branch/feature@head"` round-tripping confirms this end-to-end.

### 3.5 No domain-layer git shell-out

The diff for `action_item.go` contains no `os/exec` import addition, no `exec.Command("git", ...)` call. Opaque-domain rule preserved.

### 3.6 No new `ErrInvalidEndCommit` sentinel

`internal/domain/errors.go` is unchanged in this diff (`git diff --stat` confirms). No new error sentinel introduced. Trim-only validation correctly produces no rejection.

### 3.7 No exposed `NormalizeActionItemEndCommit` helper

Verified above (§1.1). The diff defines no such function. Inline trim is the entire normalization.

### 3.8 No `SetLifecycleState` modification

Verified above (§1.8). No diff hunk touches `SetLifecycleState`. No auto-population of `EndCommit` on terminal transitions. The Wave 2 dispatcher will handle population per the doc-comment contract (UpdateActionItem BEFORE MoveActionItemState).

### 3.9 No terminal-state non-empty enforcement

Verified above (§1.8). The empty-round-trips-empty test case passes; no rejection path exists. The disclaimer is documented across every layer.

### 3.10 No chronology check between `StartCommit` and `EndCommit`

Verified above (§1.8). Zero comparison between the two fields anywhere in the diff. Disclaimer documented on the domain field doc + MCP tool description.

**Conclusion:** All ten negative-space checks PASS.

---

## 4. Summary

**VERDICT: PASS.**

Every acceptance bullet from `WAVE_1_PLAN.md` §1.5 is satisfied. The droplet faithfully mirrors 4a.8's pattern with `EndCommit` substituted for `StartCommit` and field placement immediately after `StartCommit`:

- Single `string` field type (not `[]string`).
- `TEXT NOT NULL DEFAULT ''` SQL column (raw text, not JSON).
- No new sentinel error.
- No exposed `NormalizeActionItemEndCommit` helper.
- No JSON encoding (raw read/write).
- `mcp.WithString` schema (not `mcp.WithArray`).
- Trim-only validation.
- Pointer-sentinel update path (nil preserves, non-nil applies, empty dereferenced clears).
- Round-trip and legacy-format-compat tests cover the full matrix.

All four forbidden additions are absent: `SetLifecycleState` untouched, no terminal-state non-empty enforcement, no chronology check, no SHA format validation. Each forbidden rule is also explicitly disclaimed in load-bearing doc comments so future readers don't reintroduce them.

`mage ci` green at 1949 passed / 1 pre-existing skip / 0 failed across 20 packages. Coverage above the 70% floor on every package.

The droplet ships clean.

---

## TL;DR

- T1: All 9 acceptance bullets verified across 12 staged files (+569/-17). Domain trim-only; app pointer-sentinel update; MCP wire pointer-sentinel update; MCP tool `WithString` schema with 5-case round-trip test; SQL raw-text column with INSERT + UPDATE + 5 SELECT + scan + changed-fields + round-trip test; snapshot omitempty + legacy compat; TUI readOnly classification. All four negative constraints (SetLifecycleState untouched / no terminal enforcement / no chronology / no SHA validation) confirmed absent. PASS.
- T2: `mage ci` re-run at HEAD+diff: 1949 passed / 0 failed / 1 pre-existing skip; coverage gate green; identical to builder claim. PASS.
- T3: Negative-space (no JSON encoding, no list-validation, no `WithArray`, no SHA check, no git shell-out, no new sentinel, no normalize helper, no SetLifecycleState mod, no terminal enforcement, no chronology) — all ten absent as required. PASS.
- T4: Verdict PASS — droplet ships clean; faithful near-twin of 4a.8 with correct field placement after `StartCommit`.

---

## Hylla Feedback

N/A — task touched non-Go-only files (the diff is Go but the QA workflow used `git diff` + `Read` + `mage ci` exclusively rather than Hylla, since the working-dir diff is uncommitted-and-unbuilt and Hylla indexes only committed code per the project's Hylla-Go-only-today rule + the changed-since-ingest fallback rule). No Hylla query attempted; no Hylla miss to report. The fallback chain (`git diff` for the delta, `Read` for context, `mage ci` for gate) was the right tool stack for QA on uncommitted state. No Hylla ergonomics gripes to file.
