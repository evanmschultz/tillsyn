# 4a.30 — main/WIKI.md POST-DISPATCHER COHERENCE CHECK

**Mode:** orchestrator-direct (MD-only droplet).
**Status:** complete.
**Date:** 2026-05-04.

## Scope

Wave 4.2 coherence check + edits on `main/WIKI.md` per WAVE_4_PLAN.md §W4.2.

## Edits Applied (3 total)

1. **§"Drop Decomposition Rules — Ordering: Use blocked_by, Not depends_on" bullet 2 (line 149)** — past-tense rewrite for the dispatcher half: `Wave 2 of Drop 4a delivered the dispatcher's lock manager and conflict detector — runtime blocked_by insertion fires on in_progress promotion when sibling locks conflict (file or package).`

2. **§"Build-QA-Commit Loop (Pre-Cascade)" lead sentence (line 170)** — rewritten: `Until the gate runner ships in Drop 4b, the parent orchestrator session OR Drop-4a's manual-trigger dispatcher (till dispatcher run --action-item <id>) runs this loop. Loop body unchanged; the dispatcher merely automates the spawn + lock + auth-provision steps.` Loop steps 1-7 unchanged.

3. **§"Auth Approval Cascade" line 242 — Pre-fix vs post-fix paragraph** — RETIRED. Replaced with: `Capability landing. Wave 3 of Drop 4a (Drop 1.6 absorbed) landed the orch-self-approves-non-orch-subagent capability. Orch-side approval is the canonical path; cross-orch and orch-spawning-orch still route through the dev TUI. Project-level OrchSelfApprovalEnabled = *false toggle is the total backstop (reverts ALL approves under that project — including STEWARD's cross-subtree path — to dev-TUI approval).`

## Verification

- `rg -n "PLAN §19\.1\.6|Pre-fix vs post-fix|metadata\.paths|metadata\.packages|metadata\.files|Until the cascade dispatcher ships" WIKI.md` returns zero hits.
- `mage format-check` clean.
- All cross-references intact.

## Sections Verified But Not Edited

- **§"The Tillsyn Model (Node Types)"** — references `metadata.role` and `metadata.structural_type` correctly; first-class fields not in this section. No drift.
- **§"Cascade Vocabulary"** — canonical glossary owned by drop_3/3.6; pointer-style only. No drift.
- **§"Drop Decomposition Rules — Atomic Drop Granularity"** — already uses first-class `paths` / `packages` language. No edit needed.
- **§"Drop Decomposition Rules — Level-1 Drop Sizing + Parallelism"** — first-class language consistent. No edit.
- **§"Auth Approval Cascade — Approval scope"** numbered list — rules are post-Wave-3 canonical (dev-TUI for cross-orch + orch-spawning-orch); intact.
- **§"Drop Orch Cross-Subtree Exception"** — STEWARD's six persistent level_1 parents reference unchanged.
- **§"Response Shape — Section 0 Semi-Formal Reasoning"** — canonical pointer to SEMI-FORMAL-REASONING.md; unchanged.
- **§"Drop-End Closeout Checklist"** — verified; project-node first-class fields could earn a one-line addition but per-MVP rule "no closeout MD rollups" suggests deferring this expansion. Intact for now.
- **§"Related Files"** — all listed files still exist.

## Self-QA

- **No drift introduced** — every rewritten paragraph aligns with post-Drop-4a runtime state.
- **Single-Canonical-Source Rule honored** — WIKI.md's own rule (line 81-83) preserved; updates land in WIKI's auth section + dispatcher refs only, not in CLAUDE.md duplicates.
- **No retitling of sections.** Surgical edits only.
- **Cross-doc consistency** with CLAUDE.md (4a.29 sweep): both now use the same `Drop 4a Wave 2 / Drop 4a Wave 3 / Drop 4b` vocabulary for dispatcher / auth-cascade / gate-runner respectively.

## Hylla Feedback

None — MD-only sweep, Hylla indexes Go only.
