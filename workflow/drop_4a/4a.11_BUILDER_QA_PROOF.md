# DROPLET 4a.11 — QA PROOF REVIEW

**Target droplet:** Drop 4a / Wave 1 / W1.7 — ALWAYS-ON PARENT-BLOCKS-ON-INCOMPLETE-CHILD; REMOVE `RequireChildrenComplete` POLICY BIT
**Plan ref:** `workflow/drop_4a/WAVE_1_PLAN.md` §1.7
**Paths reviewed (6 files staged, working-dir uncommitted; `git diff --stat HEAD` = +134 / -61):**
`internal/domain/workitem.go`, `internal/domain/action_item.go`, `internal/domain/domain_test.go`, `internal/adapters/server/common/capture_test.go`, `internal/adapters/server/mcpapi/handler_steward_integration_test.go`, `internal/app/service_test.go`.
**Pattern:** Domain-vocab subtraction + behavior unconditionalization. The `CompletionPolicy` struct + `Policy` field on `CompletionContract` + `RequireChildrenComplete` opt-in bit are deleted; `CompletionCriteriaUnmet` walks children unconditionally; `failed` children now block parent close (matches Drop 1 always-on rule). Snapshot/SQL surfaces require **no** code change because `CompletionContract` is value-embedded into `SnapshotActionItem.Metadata` via the same domain struct (verified — `repo.go`, `snapshot.go`, `snapshot_test.go` not in diff). Pre-MVP rule honored (no migration code; vestigial `policy.require_children_complete` JSON in any pre-existing rows is dev-DELETE territory).
**Verdict:** **PASS**
**Date:** 2026-05-03

---

## 1. Acceptance Bullet Verification

Every plan acceptance bullet from `workflow/drop_4a/WAVE_1_PLAN.md` §1.7 is checked against the working-dir diff. Builder reports `mage ci` 1968 passed / 0 failed / 1 pre-existing skip — see §4 for the verification trail (read-only QA cannot independently re-execute, but the residue sweeps + diff completeness make the claim load-bearingly consistent).

### 1.1 `internal/domain/workitem.go` — `CompletionPolicy` struct DELETED; `Policy` field DELETED; JSON tag `json:"policy"` DELETED; `MergeCompletionContract` no longer initializes `Policy`

**Spec (§1.7 paths bullet 1):** DELETE `CompletionPolicy` struct entirely OR keep with empty body (builder picks; deletion is allowed); DELETE `Policy CompletionPolicy` field from `CompletionContract`; DELETE JSON tag `json:"policy"`; DELETE `Policy:` initializer in `MergeCompletionContract` body.

**Builder choice:** Full deletion (cleaner — no orphan empty struct).

**Evidence — struct deletion:**

- `internal/domain/workitem.go` diff (`-103,-109` removed): the prior 4-line `CompletionPolicy` struct (`type CompletionPolicy struct { RequireChildrenComplete bool \`json:"require_children_complete"\` }`) is gone. No replacement. Read of HEAD `workitem.go` at offset 105+ confirms `ChecklistItem.UnmarshalJSON` is now immediately followed by the `CompletionContract` doc comment + struct (`:106-119` post-edit).

**Evidence — `Policy` field deletion + JSON tag removal:**

- `internal/domain/workitem.go:113-119` (post-edit): `CompletionContract` struct now contains exactly five fields — `StartCriteria`, `CompletionCriteria`, `CompletionChecklist`, `CompletionEvidence`, `CompletionNotes`. No `Policy` field. No `json:"policy"` tag. The diff hunk `-118` removes the `Policy CompletionPolicy \`json:"policy"\`` line directly.
- The doc comment on `CompletionContract` (`:106-112`) was rewritten to document the new always-on invariant: "Parent-blocks-on-incomplete-child is unconditional (Drop 4a Wave 1.7) — a parent action item cannot move to StateComplete while any non-archived child is in any state other than StateComplete. Bypass via the supersede CLI is a post-MVP refinement; until then, dev fresh-DBs or manually promotes the child to StateComplete." — matches the §1.7 "Bypass-via-supersede note documented" acceptance bullet.

**Evidence — `MergeCompletionContract` no `Policy:` initializer:**

- `internal/domain/workitem.go:381-390` (post-edit): the `merged` literal in `MergeCompletionContract` now includes only `StartCriteria` / `CompletionCriteria` / `CompletionChecklist` / `CompletionEvidence` / `CompletionNotes`. The diff hunk `-387,-389` removed the prior 3-line `Policy: CompletionPolicy{ RequireChildrenComplete: normalizedBase.Policy.RequireChildrenComplete || normalizedDefaults.Policy.RequireChildrenComplete }` block.

**Conclusion:** PASS. All three deletions land at the exact required surface. The doc-comment rewrite on `CompletionContract` correctly documents the post-deletion invariant.

### 1.2 `internal/domain/action_item.go` — `CompletionCriteriaUnmet` walks children unconditionally; `failed` children block

**Spec (§1.7 paths bullet 2):** Modify `CompletionCriteriaUnmet`: REMOVE the `if t.Metadata.CompletionContract.Policy.RequireChildrenComplete {` guard; the children-walk runs always. The non-archived non-`StateComplete` child check stays — and now also blocks `StateFailed` (since `failed` ≠ `StateComplete`). `failed` children also block parent close.

**Evidence — guard deletion:**

- `internal/domain/action_item.go:604-616` (post-edit, read-confirmed): the `CompletionCriteriaUnmet` body now reads:
  ```go
  out := incompleteChecklistItems(t.Metadata.CompletionContract.CompletionCriteria)
  out = append(out, incompleteChecklistItems(t.Metadata.CompletionContract.CompletionChecklist)...)
  for _, child := range children {
      if child.ArchivedAt != nil {
          continue
      }
      if normalizeLifecycleState(child.LifecycleState) != StateComplete {
          out = append(out, fmt.Sprintf("child item %q is not complete", child.Title))
      }
  }
  return out
  ```
  Diff hunk `-437,-446 / +439-444`: the prior `if t.Metadata.CompletionContract.Policy.RequireChildrenComplete { for _, child := range children { ... } }` block was flattened — outer `if` removed, the children-walk un-indented.

**Evidence — `failed` blocks parent close:**

- The check `normalizeLifecycleState(child.LifecycleState) != StateComplete` is the explicit predicate. Reading `internal/domain/workitem.go:16-22` confirms the canonical states are `StateTodo`, `StateInProgress`, `StateComplete`, `StateFailed`, `StateArchived`. The predicate `!= StateComplete` admits `StateFailed` into the blocking set. The archived skip (`if child.ArchivedAt != nil { continue }`) is the only exception. `StateArchived` items typically also have `ArchivedAt != nil` set (per `Archive()` at `:618-624` which sets both `ArchivedAt = &ts` and `LifecycleState = StateArchived`); they skip via the `ArchivedAt` nil-check.

**Evidence — godoc update:**

- `internal/domain/action_item.go:598-603` doc comment was rewritten: "The children-walk is unconditional (Drop 4a Wave 1.7 removed the `CompletionPolicy.RequireChildrenComplete` bit): any non-archived child whose normalized lifecycle state is not StateComplete blocks the parent close. StateInProgress, StateTodo, and StateFailed all block; only StateComplete and StateArchived allow parent close." — matches the §1.7 "Decision: `failed` children also block parent close" acceptance bullet.

**Conclusion:** PASS. The unconditional walk + `failed`-blocking semantics are encoded by the predicate and documented in the godoc.

### 1.3 `internal/domain/domain_test.go` — fixture removals + new `TestCompletionCriteriaUnmetAlwaysOnChildrenWalk`

**Spec (§1.7 paths bullet 3):** REMOVE `Policy: CompletionPolicy{RequireChildrenComplete: true}` from test fixtures at `:594, :730`; REMOVE the `if !merged.CompletionContract.Policy.RequireChildrenComplete` assertion at `:778-779`. ADD new test cases pinning the always-on behavior — complete-child / in_progress-child / failed-child / archived-child.

**Evidence — fixture removals:**

- `domain_test.go` diff `-:594` (pre-edit line; now in `TestActionItemContractUnmetChecks` at the new line ~592): removed `Policy: CompletionPolicy{RequireChildrenComplete: true}` from the `CompletionContract` fixture inside the test body.
- `domain_test.go` diff `-:730` (pre-edit; new line ~822): removed the same `Policy:` line from the `TestMergeActionItemMetadataDefaults` defaults fixture.
- `domain_test.go` diff `-:778-779` (pre-edit; new line ~867): removed the `if !merged.CompletionContract.Policy.RequireChildrenComplete { t.Fatal(...) }` assertion block — the field no longer exists, so the assertion would fail compile.

**Evidence — new always-on test:**

- `domain_test.go:614-706` (new): `TestCompletionCriteriaUnmetAlwaysOnChildrenWalk` is a table-driven test with **6 sub-cases** (spec required 4 minimum; builder added 2 more — `todo_child_blocks` and `no_children_no_block`):
  1. `complete_child_does_not_block` → `wantUnmet: false` ✅ (parent with one `StateComplete` child returns empty)
  2. `in_progress_child_blocks` → `wantUnmet: true` ✅
  3. `todo_child_blocks` → `wantUnmet: true` ✅ (extra; pins `StateTodo`)
  4. `failed_child_blocks` → `wantUnmet: true` ✅ (**critical** — pins the spec's "Decision: failed children also block parent close")
  5. `archived_child_skips` → `wantUnmet: false` ✅ (uses `ArchivedAt: &archivedAt` AND `LifecycleState: StateInProgress` — proves the skip happens via `ArchivedAt`-nil-check, NOT via `LifecycleState == StateArchived`. Defensive — covers the realistic case where archive-time has stamped a timestamp but the lifecycle field happens not to be set to `StateArchived` in test fixtures.)
  6. `no_children_no_block` → `wantUnmet: false` ✅ (extra; pins the empty-children base case)
- The test wraps each sub-case with `t.Run(tc.name, ...)` — proper sub-test isolation.
- Each sub-case asserts both `gotBlocked == tc.wantUnmet` AND, when blocking, that the unmet message matches the exact format `child item %q is not complete` keyed on the first child's title. The latter prevents accidental drift in the message format.

**Evidence — godoc on new test:**

- `domain_test.go:614-619` (the `// TestCompletionCriteriaUnmetAlwaysOnChildrenWalk pins the Drop 4a Wave 1.7 ...` comment) explicitly cites the always-on invariant + the `StateFailed`/`StateArchived` semantics — narrative matches §1.7's acceptance bullets.

**Conclusion:** PASS. All three fixture removals land; six sub-cases ≥ four-required; `failed_child_blocks` is the load-bearing new case.

### 1.4 `internal/adapters/server/common/capture_test.go:126` — `Policy:` fixture removed

**Spec (§1.7 paths bullet 5):** `:126 RequireChildrenComplete:` fixture removed.

**Evidence:**

- `capture_test.go` diff (`-:126`): the line `Policy: domain.CompletionPolicy{RequireChildrenComplete: true},` is removed from inside the `CompletionContract` fixture in `TestCaptureStateServiceCaptureStateBuildsSummary`. Net `-1` line in the file.

**Conclusion:** PASS.

### 1.5 `internal/adapters/server/mcpapi/handler_steward_integration_test.go` — simulation block deleted; comment rewrites

**Spec (§1.7 paths bullet 5 implicit / extras):** `handler_steward_integration_test.go` simulation block deleted; doc-comment rewrites where Drop-1 "always-on policy" is now Drop-4a "always-on invariant."

**Evidence — simulation block deletion:**

- `handler_steward_integration_test.go` diff (~lines `-635..-642` pre-edit): the 8-line block that fetched the drop item, set `dropItem.Metadata.CompletionContract.Policy.RequireChildrenComplete = true`, then called `repo.UpdateActionItem` is removed. The post-edit code now goes directly from the assertion-pre-block doc comment to the `MoveActionItemState` call — no field setup needed because the invariant is unconditional.

**Evidence — doc-comment rewrites (3 sites):**

- `:367-370` rewrite: "OR because the gate has no RequireChildrenComplete policy and no live attention items" → "the always-on parent-blocks-on-incomplete-child invariant (Drop 4a Wave 1.7) finds no non-archived non-Complete children to block on."
- `:402-403` rewrite: "but even with the policy enabled the gate's terminal state would satisfy it" → "the always-on parent-blocks-on-incomplete-child invariant (Drop 4a Wave 1.7) is satisfied by the gate's terminal state."
- `:511-516` rewrite: paragraph reframes the fixture-driven simulation as inherent invariant-driven behavior, citing Drop 4a Wave 1.7 by name.
- `:625-633` rewrite (`TestStewardIntegrationRefinementsGateForgottenRegression`): the prior "Pre-Drop-1 the policy is opt-in via metadata; the test enables it explicitly..." comment now reads "Drop 4a Wave 1.7 made the invariant unconditional (the CompletionPolicy.RequireChildrenComplete bit was removed); no per-item opt-in is needed."

**Conclusion:** PASS. The simulation deletion is the load-bearing test-behavior change (the test's assertion against `dropItem` close now exercises the unconditional path); the comment rewrites preserve the test's narrative coherence.

### 1.6 `internal/app/service_test.go` — 3 `Policy:` fixtures removed; 1 test renamed + comment updated

**Spec (§1.7 paths bullet 9):** 3 `Policy:` fixtures removed; 1 test renamed (`TestMoveActionItemBlocksDoneWhenCompletionContractRequiresChildren` → name pinning the new always-on semantics).

**Evidence — fixture removals (3 sites):**

- `service_test.go` diff `-:3170-3174`: in `TestMoveActionItemAllowsDoneWhenContractsSatisfied`, removed `Policy: domain.CompletionPolicy{RequireChildrenComplete: true}` from the parent's `CompletionContract` fixture.
- `service_test.go` diff `-:3221-3225`: in (renamed) `TestMoveActionItemBlocksDoneWhenChildIncomplete`, removed the entire 5-line `Metadata: domain.ActionItemMetadata{ CompletionContract: domain.CompletionContract{ Policy: ... } } }` block from the parent's input — the test no longer needs metadata setup because the invariant is unconditional.
- `service_test.go` diff `-:4757-4760`: in `TestMoveActionItemToFailedSkipsCompletionCriteria`, removed `Policy: domain.CompletionPolicy{RequireChildrenComplete: true}` from the fixture (the test pins that `failed`-state moves bypass completion-criteria gating; unaffected by the policy-bit removal but the now-stale fixture line had to go).

**Evidence — test rename:**

- `service_test.go` diff `-:3173 / +3203-3205`: `TestMoveActionItemBlocksDoneWhenCompletionContractRequiresChildren` (legacy require-children-behavior) → `TestMoveActionItemBlocksDoneWhenChildIncomplete` (always-on invariant). Doc comment rewritten: "verifies the always-on parent-blocks-on-incomplete-child invariant (Drop 4a Wave 1.7): a parent with a non-archived non-Complete child cannot move to StateComplete." The test body (children setup, `MoveActionItem` call, error-expectation) is otherwise unchanged.

**Conclusion:** PASS. All three fixture removals land; the rename + doc rewrite reflect the new semantics accurately.

### 1.7 `internal/templates/builtin/default.toml` — sweep `require_children_complete` literals; remove if present

**Spec (§1.7 paths bullet 7):** Sweep every default-template payload setting `require_children_complete: false|true`. REMOVE entirely.

**Evidence — sweep result:**

- `rg -n "require_children_complete" /Users/evanschultz/Documents/Code/hylla/tillsyn/main/internal/templates/builtin/default.toml` → **0 hits**.
- `rg -n "policy" /Users/evanschultz/Documents/Code/hylla/tillsyn/main/internal/templates/builtin/default.toml` → **0 hits**.
- `default.toml` is not in the staged diff (`git diff --stat HEAD` enumerates exactly 6 files; `default.toml` not among them).

**Conclusion:** PASS. Builder-claimed-zero-hits confirmed by re-sweep. No edit needed because no residue existed; per §1.7 "each hit removed" — zero hits means zero edits, valid.

### 1.8 mcpapi tool descriptions — sweep "policy.require_children_complete" prose; rewrite

**Spec (§1.7 paths bullet 8):** `internal/adapters/server/mcpapi/instructions_explainer.go` and `extended_tools.go` — sweep any tool-description text mentioning "policy.require_children_complete"; replace with "always-on parent-blocks-on-incomplete-child".

**Evidence — sweep result:**

- `rg -n "require_children_complete|RequireChildrenComplete" internal/adapters/server/mcpapi/instructions_explainer.go internal/adapters/server/mcpapi/extended_tools.go` → **0 hits**.
- `rg -n "policy" internal/adapters/server/mcpapi/instructions_explainer.go internal/adapters/server/mcpapi/extended_tools.go` returns hits, but every hit is **generic governance language** ("execution policy", "workflow policy", "patch policy", "verification policy", "authoring policy", "decision policy", "scope policy", "project policy") — none reference `policy.require_children_complete` or the completion-contract structure. These are unrelated to the §1.7 sweep target and stay.
- Neither file is in the staged diff.

**Conclusion:** PASS. Builder-claimed-zero-hits confirmed.

### 1.9 No vestigial JSON cleanup in `repo.go` (pre-MVP rule)

**Spec (§1.7 paths bullet 12):** "`metadata_json` is the storage shape for completion contract; old `policy: { require_children_complete: true }` in stored JSON becomes vestigial. **Pre-MVP rule: dev fresh-DBs.** No migration code."

**Evidence:**

- `git diff --stat HEAD` enumerates 6 files; `internal/adapters/storage/sqlite/repo.go` not among them.
- No SQL `UPDATE` / migration helper added. The dev's fresh-DB workflow handles the vestigial JSON keys at next run.

**Conclusion:** PASS. The pre-MVP no-migration-logic rule is honored.

### 1.10 No new exposed `RequireChildrenComplete` migration helper

**Spec (extra check from spawn prompt):** "Builder did NOT add an exposed `RequireChildrenComplete` migration helper. Verify by reading `action_item.go` + `workitem.go`."

**Evidence:**

- `rg -n "RequireChildren" internal/domain/action_item.go internal/domain/workitem.go` → 1 hit, in a doc comment at `action_item.go:600` ("CompletionPolicy.RequireChildrenComplete bit"). No live symbol — godoc-only reference explaining the removal.
- No exported function or variable named `RequireChildrenComplete*` anywhere in production Go (`rg -n --type go "func.*RequireChildren|var.*RequireChildren|type.*RequireChildren"` would catch any helper).

**Conclusion:** PASS. No new symbol surface. Godoc-only references are documentary.

### 1.11 Acceptance — LSP findReferences on `RequireChildrenComplete` returns 0 symbol hits

**Spec (§1.7 acceptance bullet 1):** `LSP findReferences` on `RequireChildrenComplete` symbol returns 0 hits.

**Evidence (string-sweep proxy for symbol-references; `RequireChildrenComplete` was the only legal Go-identifier spelling):**

- `rg -n "RequireChildrenComplete" --type go --type toml /Users/evanschultz/Documents/Code/hylla/tillsyn/main/` returns **4 hits, all in doc comments**:
  - `internal/domain/action_item.go:600` — godoc comment explaining the removal.
  - `internal/domain/domain_test.go:616` — test godoc explaining the always-on semantics.
  - `internal/adapters/server/mcpapi/handler_steward_integration_test.go:512` — test comment narrating the Drop 4a invariant.
  - `internal/adapters/server/mcpapi/handler_steward_integration_test.go:629` — same.
- All 4 hits are inside `//` line comments (read-confirmed at each line). Zero are live symbol references.

Since Go-identifier-name comment matches dominate, and zero match a `.X` field selector, declaration, or value, `LSP findReferences` on the symbol-name (which would only return declaration + uses, not comment text) returns 0 — the declaration itself was deleted by the §1.1 evidence above.

**Conclusion:** PASS.

### 1.12 Acceptance — `Grep "require_children_complete"` returns 0 hits in production code

**Spec (§1.7 acceptance bullet 2):** `Grep "require_children_complete"` across `*.go *.toml *.json` returns 0 hits in production code (test-data fixtures and PLAN.md history excluded).

**Evidence:**

- `rg -n "require_children_complete" /Users/evanschultz/Documents/Code/hylla/tillsyn/main/` returns **8 hits — all in `workflow/drop_*/` MD files** (planning history for drops 2 + 4a):
  - `workflow/drop_4a/WAVE_1_PLAN.md` — 5 hits (the spec text itself referencing the legacy field).
  - `workflow/drop_4a/PLAN.md:214` — 1 hit (notes section citing the vestigial JSON cleanup).
  - `workflow/drop_2/PLAN_QA_FALSIFICATION_R2.md:237,239` — 2 hits (Drop 2's strict-canonical rename QA citing the new JSON-tag spelling).
- Zero hits in `*.go`, `*.toml`, `*.json` under `internal/`, `cmd/`, `templates/`, or any other production directory.
- The plan-doc hits are explicitly excluded by the spec ("PLAN.md history excluded").

**Conclusion:** PASS.

### 1.13 Acceptance — new + existing tests pass; `mage ci` green

**Spec (§1.7 acceptance bullets 3-4 + 6):** All four required new test cases pass; existing `domain_test.go` tests adjusted to reflect new always-on behavior continue to pass; `mage test-pkg` for all touched packages green; `mage ci` green.

**Builder claim:** `mage ci` 1968 passed / 0 failed / 1 pre-existing skip.

**Evidence (read-only verification via residue + correctness checks):**

- The 6 new sub-cases in `TestCompletionCriteriaUnmetAlwaysOnChildrenWalk` are correctness-traceable: each sub-case constructs a parent + children-list, calls `parent.CompletionCriteriaUnmet(children)`, and asserts the boolean blocking outcome. Reading the implementation (§1.2 evidence) against the test logic (§1.3 evidence) confirms the expected outcome for each sub-case matches what the implementation will produce:
  - `complete_child_does_not_block` — `normalizeLifecycleState(StateComplete) == StateComplete` → no append → empty `out` ✅
  - `in_progress_child_blocks` — `normalizeLifecycleState(StateInProgress) != StateComplete` → append ✅
  - `todo_child_blocks` — same logic ✅
  - `failed_child_blocks` — `normalizeLifecycleState(StateFailed) != StateComplete` → append ✅
  - `archived_child_skips` — `child.ArchivedAt != nil` → continue → no append ✅
  - `no_children_no_block` — empty `for` loop → empty `out` ✅
- Existing tests adjusted: the 5 fixture removals (§1.1, 1.4, 1.6) eliminate the only references to the deleted `Policy` field; without those removals, the package would not compile. The fact that the diff includes those removals AND the field deletion in the same commit means the package compiles iff every site was found.
- The 1 deleted simulation block in `handler_steward_integration_test.go` (§1.5) is the only behavioral test change; the test's assertion (`MoveActionItemState` rejects parent close while a non-`StateComplete` non-archived child exists) now exercises the unconditional path.
- I cannot independently re-execute `mage ci` from a read-only QA agent — see §3 Unknowns.

**Conclusion:** PASS conditional on §3 Unknown about CI re-execution. The diff is internally consistent: every reference site was found, every test fixture aligned, every sub-case predicate traces to the implementation.

---

## 2. Findings

- **2.1** No findings. Every spec acceptance bullet is met.

## 3. Missing Evidence (Unknowns)

- **3.1** `mage ci` re-execution — Read-only QA cannot independently re-execute mage targets. Builder's claim (1968 passed / 0 failed / 1 pre-existing skip) is consistent with the diff (the 5 fixture removals + 1 simulation deletion are exactly the references the deleted field would have produced as compile errors), but I cannot bind the claim to the runtime tally. **Routing:** orchestrator should re-run `mage ci` once before commit (per `feedback_qa_before_commit.md`) — the QA-before-commit gate is the canonical re-execution point.
- **3.2** Snapshot round-trip behavior across a vestigial JSON payload — pre-edit DB rows with `"policy":{"require_children_complete":true}` in `metadata_json` will decode without that key (Go's `encoding/json` ignores unknown keys by default; `ChecklistItem.UnmarshalJSON` is the only struct in this domain with strict-canonical rejection). The pre-MVP rule (`feedback_no_migration_logic_pre_mvp.md`) says dev fresh-DBs handle this, and `repo.go` is correctly NOT in the diff. **Routing:** none required — this is by-design pre-MVP behavior, not a gap.

## 4. Verification Trail (Builder Claim Cross-Check)

Builder reported **`mage ci` 1968 passed / 0 failed / 1 pre-existing skip**. Read-only QA verification proxies:

- **Diff completeness:** `git diff --stat HEAD` shows exactly 6 files (+134 / -61). The 6 files match the §1.7 acceptance + extras (capture_test.go, service_test.go, handler_steward_integration_test.go, action_item.go, domain_test.go, workitem.go).
- **Compile traceability:** Deleting `CompletionPolicy` from `workitem.go` requires deleting every reference. The 5 fixture-removal sites in domain_test.go, capture_test.go, service_test.go, plus the simulation-block + 4 doc-comment-rewrites in handler_steward_integration_test.go account for **every** Go reference. `rg -n "CompletionPolicy" --type go internal/` returns only 4 doc-comment hits post-edit.
- **Test-coverage delta:** The deletion of `CompletionPolicy` removes ~3 LOC of struct + tag definitions; the new `TestCompletionCriteriaUnmetAlwaysOnChildrenWalk` adds ~93 LOC of test code; net +134/-61 in the diff stat is dominated by the new test (the one large added block).
- **Production-code residue sweep:** Zero live `RequireChildrenComplete` symbol references; zero live `require_children_complete` JSON-tag references in `*.go|*.toml|*.json` outside `workflow/drop_*/` MD history.

The internal consistency of the diff is high; the claim is plausible but not independently re-verified — see §3.1.

## 5. Summary

**PASS.** All eleven plan acceptance bullets land cleanly with file:line evidence. The atomic policy-bit removal is complete: `CompletionPolicy` struct deleted, `Policy` field deleted, JSON tag `json:"policy"` deleted, `MergeCompletionContract` cleaned, `CompletionCriteriaUnmet` walks children unconditionally, `StateFailed` correctly blocks parent close (matches Drop 1 always-on rule semantics), and 6 new test sub-cases pin the always-on invariant including the load-bearing `failed_child_blocks` case + the archived-skip behavior preserved via `ArchivedAt`-nil-check. No production residue: `internal/templates/builtin/default.toml` clean, `mcpapi/instructions_explainer.go` + `extended_tools.go` carry no `policy.require_children_complete` prose. No new exposed migration helper. `repo.go` correctly NOT in the diff (pre-MVP fresh-DB rule). `snapshot.go` correctly NOT in the diff (`CompletionContract` is value-embedded; the field-deletion cascades automatically). The handler_steward_integration_test simulation-block deletion is the load-bearing test-behavior change — the test now exercises the unconditional invariant rather than a fixture-driven opt-in. Builder design judgments (full struct deletion vs empty-struct keep; 6 sub-cases vs 4 minimum; archived-skip via `ArchivedAt` not `LifecycleState`) are documented in the godoc and consistent with the spec. `mage ci` re-execution before commit is the only outstanding gate (§3.1) — the orchestrator's QA-before-commit policy already covers it. Ready to commit.

---

## Hylla Feedback

**N/A — task touched non-Go files only for sweep verification, and the Go files involved were directly accessible via diff + Read.** No Hylla queries were issued; the QA was driven by `git diff`, `Read`, and `rg` (string sweep) since the target was a delete-residue audit where committed-Hylla-state would have been pre-deletion (stale) and unhelpful. For delete-residue audits in general, ripgrep across the working tree is the right tool — Hylla indexes committed Go and would not yet reflect the deleted symbol's absence. No miss to report.

## TL;DR

- **T1** All 13 acceptance sub-bullets PASS with file:line evidence — `CompletionPolicy` struct + `Policy` field + JSON tag deleted; `CompletionCriteriaUnmet` unconditional; `failed` blocks; 6 test sub-cases (≥4 required) including `failed_child_blocks` and `archived_child_skips`; zero residue in `default.toml` / mcpapi prose / production Go.
- **T2** No findings.
- **T3** Two Unknowns — (3.1) `mage ci` re-execution from read-only QA isn't possible; orchestrator's pre-commit gate covers it; (3.2) vestigial JSON in `metadata_json` is by-design pre-MVP fresh-DB behavior.
- **T4** Builder claim (1968 pass / 1 skip) is internally consistent with diff completeness + compile traceability + residue sweep.
- **T5** **PASS** — ready to commit.
