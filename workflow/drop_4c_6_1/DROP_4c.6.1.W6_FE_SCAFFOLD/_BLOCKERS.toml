# _BLOCKERS.toml — DROP_4c.6.1.W6_FE_SCAFFOLD/
# Immediate-children sibling blocker ledger.
# Mirrors inline Blocked by: bullets in PLAN.md; PLAN.md is truth.
# Round 2: paths updated for R10-D3 Wails-at-repo-root layout (no fe/ subfolder).

[[blockers]]
node = "W6.D2"
blocked_by = ["W6.D1"]
reason = "D2 (app.go at tillsyn repo root) is in the same Go module + package main as D1 (main.go); both carry //go:build wails; package-level compile lock requires serialization"

[[blockers]]
node = "W6.D4"
blocked_by = ["W6.D2", "W6.D3"]
reason = "D4 needs IPC method signatures from D2 to write frontend/src/lib/wails.ts wrapper types; needs Astro + pnpm dev environment + public/ assets from D3"

[[blockers]]
node = "W6.D5"
blocked_by = ["W6.D2", "W6.D3", "W6.D4"]
reason = "D5 adds listActionItems to frontend/src/lib/wails.ts (file created by D4); file-level conflict on shared wails.ts — serialized after D4"

[[blockers]]
node = "W6.D6"
blocked_by = ["W6.D5"]
reason = "D6 adds createActionItem to frontend/src/lib/wails.ts; serialized after D5 to avoid concurrent wails.ts edits"

[[blockers]]
node = "W6.D7"
blocked_by = ["W6.D6"]
reason = "D7 adds runDispatcher to frontend/src/lib/wails.ts; serialized after D6 to avoid concurrent wails.ts edits"

[[blockers]]
node = "W6.D8"
blocked_by = ["W6.D7"]
reason = "D8 adds getAgentsConfig + getTemplateConfig to frontend/src/lib/wails.ts and adds settings nav link to frontend/src/layouts/MainLayout.astro (created by D3, filled by D4); serialized after D7"

[[blockers]]
node = "W6.D9"
blocked_by = ["W6.D3"]
reason = "D9 (vim engine) requires Vitest config + pnpm dev environment from D3; requires frontend/public/stil-baseline.json (copied by D3); no IPC dependency; parallel to D4-D8 chain"
