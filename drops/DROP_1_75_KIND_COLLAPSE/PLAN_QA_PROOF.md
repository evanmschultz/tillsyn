# Plan QA Proof — Round 9

**Verdict:** pass

## Summary

All five Round 8 triage payloads landed correctly in PLAN.md and every pre-existing claim still verifies against the committed tree. Scope bullet 3 on line 30 reads the full `ALTER TABLE projects DROP COLUMN kind.` with no SQLite-equivalent parenthetical. The DEV REMINDER opening (line 272) and Trigger subsection (line 278) both frame the callout as "after Phase 6 Verify completes ... and before Phase 7 Closeout begins" per Round 8 F4. The Unit 1.3 `GetProject` sub-bullet at line 110 correctly cites `repo.go:1398`, which was verified to hold the cited `SELECT id, slug, name, description, kind, metadata_json, created_at, updated_at, archived_at FROM projects WHERE id = ?` literal. Unit 1.6's Paths (line 149) now calls out the `SnapshotVersion` v4→v5 bump at `snapshot.go:16` and the `snapshot_test.go` fixture updates; Acceptance (lines 155–156) encodes the two `rg` gate bullets. The driver citation at line 280 names `github.com/ncruces/go-sqlite3 v0.23.3` (embeds SQLite 3.49.1, verified against `~/go/pkg/mod/github.com/ncruces/go-sqlite3@v0.23.3/embed/README.md:3`) plus the dev CLI path (`sqlite3 --version` = 3.51.0), and correctly does not name `modernc.org/sqlite` as the driver (the indirect v1.46.1 dep at `go.mod:112` is not cited as such).

## Findings

None — all Round 8 triage payloads landed and all prior claims still verify.

## Confirmed Claims

- **Line 30 Scope bullet 3 — full text, no parenthetical.** PLAN.md:30 reads `   - \`ALTER TABLE projects DROP COLUMN kind\`.` Verified via Grep.
- **Line 272 DEV REMINDER opening anchor.** Text reads `After WORKFLOW.md Phase 6 Verify completes (CI green on \`drop/1.75\`) and before Phase 7 Closeout begins — specifically, before the dev executes \`scripts/drops-rewrite.sql\` ...`. The pre-Round-8 framing ("At the start of WORKFLOW.md Phase 6 (Verify)") is absent — verified via `rg 'At the start of WORKFLOW\.md Phase 6' PLAN.md` returns 0.
- **Line 278 Trigger subsection anchor.** Text reads `**Trigger (Round-7 F4 anchor):** surface this callout after Phase 6 Verify completes (CI green on \`drop/1.75\`) and before Phase 7 Closeout begins`. The pre-Round-8 framing ("at the transition into Phase 6 Verify") is absent — verified via `rg 'at the transition into Phase 6' PLAN.md` returns 0.
- **Unit 1.3 `GetProject` sub-bullet present at line 110.** Quotes the full SELECT column list and notes `scanProject` handles the Scan side but each caller holds its own SELECT literal. Target confirmed: `repo.go:1398` in the committed tree contains `SELECT id, slug, name, description, kind, metadata_json, created_at, updated_at, archived_at FROM projects WHERE id = ?` inside `GetProject` at `repo.go:1396`.
- **Unit 1.6 SnapshotVersion v4→v5 bump in Paths (line 149).** Paths now read `also bump SnapshotVersion from tillsyn.snapshot.v4 to tillsyn.snapshot.v5 at :16`. `snapshot_test.go` is listed as a Path with `(update fixtures and any golden test data pinning the tillsyn.snapshot.v4 literal to tillsyn.snapshot.v5)`. Acceptance at line 155 reads `rg 'tillsyn\.snapshot\.v4' drop/1.75/internal/app/ returns 0 matches` and line 156 reads `rg 'tillsyn\.snapshot\.v5' drop/1.75/internal/app/snapshot.go returns exactly 1 match (the const SnapshotVersion line at :16)`. Target confirmed: `snapshot.go:16` currently reads `const SnapshotVersion = "tillsyn.snapshot.v4"`.
- **Line 280 driver citation correct.** Text reads `The Go binary's driver is github.com/ncruces/go-sqlite3 v0.23.3 (embeds SQLite 3.49.1, per ~/go/pkg/mod/github.com/ncruces/go-sqlite3@v0.23.3/embed/README.md)` plus dev CLI path `local sqlite3 CLI (3.51.0)`. Does NOT name `modernc.org/sqlite` as the driver. Verified: `go.mod:65` is `github.com/ncruces/go-sqlite3 v0.23.3` (direct require); `go.mod:112` is `modernc.org/sqlite v1.46.1 // indirect` (not cited as driver in the SQL version context). README at `~/go/pkg/mod/github.com/ncruces/go-sqlite3@v0.23.3/embed/README.md:3` reads `This folder includes an embeddable Wasm build of SQLite 3.49.1 for use with`. Dev CLI at `/usr/bin/sqlite3 --version` = `3.51.0 2025-06-12 ...` — matches the cited version.
- **Unit 1.14 `project_allowed_kinds` assertion intact at line 269.** Text reads `SELECT COUNT(*) FROM project_allowed_kinds WHERE kind_id NOT IN ('project','actionItem') OR kind_id IS NULL returns 0 (per Round-6 F3 Option A — catches any allowlist row pointing at a deleted legacy kind; if the assertion fires, the script aborts cleanly via RAISE(ROLLBACK) and dev handles the residue manually).`.
- **Options A/B/C DEV REMINDER block intact at lines 274–276.** All three options present; Option A marked `(chosen, encoded above)`, B and C marked `(not chosen)`.
- **Memory file exists.** `~/.claude/projects/-Users-evanschultz-Documents-Code-hylla-tillsyn/memory/project_drop_1_75_unit_1_14_f3_decision.md` present, size 2.4k.
- **`snapshot.go:377-378` validation site cited correctly.** Inside `Snapshot.Validate()` at `snapshot.go:375`, line 377 reads `if strings.TrimSpace(s.Version) != SnapshotVersion {` and line 378 reads `return fmt.Errorf("unsupported snapshot version: %q", s.Version)`. This is the strict equality check that justifies the v4→v5 bump (Round 8 rationale held).
- **No residual pre-Round-7 artifacts.** Searched for `projects_old`, "12-step" as a live proposal, `PRAGMA foreign_keys = OFF/ON` as an active wrapper — only hit is line 280's explanatory note stating the old approach was *eliminated*, which is the correct Round 7 F2 framing. Searched `rg 'At the start of WORKFLOW\.md Phase 6|at the transition into Phase 6' PLAN.md` returns 0.

## Hylla Feedback

N/A — task touched non-Go files only (PLAN.md review). The Go code anchors checked (`repo.go:1398`, `snapshot.go:16`, `snapshot.go:377-378`, `go.mod:65`, `go.mod:112`) were verified by direct `Read` at specific line offsets already cited in PLAN.md, which is the correct tool for line-anchor verification rather than Hylla semantic search.
