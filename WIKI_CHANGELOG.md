# Tillsyn — Wiki Changelog

One-liner per slice capturing what landed that changed best practice or project state. Mirrored from the slice's ledger entry during the `SLICE <N> END — LEDGER UPDATE` task. Paired with `WIKI.md` — the wiki is the current snapshot; this changelog is the per-slice trail.

Newest-first ordering. Entry format:

```
## Slice <N> — <Title> (<closed_date>)

- <One-liner bullet — what changed and why it matters>
- <Additional bullets if multiple distinct changes>
```

Keep each bullet under ~150 chars. If more detail is needed, link into `LEDGER.md` for the full slice entry. This file is a scan-able index, not a narrative.

---

<!-- Slice 0 entry gets appended by the orchestrator during the SLICE 0 END — LEDGER UPDATE task, after ingest + ledger update complete. -->
